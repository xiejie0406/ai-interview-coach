# Aden 桌面执行底座验证记录

> 文档类型：Feature 技术验证唯一事实源  
> 版本：0.14.0；文档状态：Completed；创建：2026-09-12；更新：2026-09-13  
> Feature：FEAT-ADEN-001；当前范围：IMP-10 技术封板；IMP-01～10 Completed；Stage 9 用户验收尚未开始  
> owner：Codex 记录技术执行事实；用户验收不在本文代签  
> 批准依据：2026-09-12 用户批准[十阶段实施任务清单](任务清单.md)并要求进入编码实现  
> 限制：仅合成数据和安全本地构建；无真实账号 / Provider / 外部副作用 / 业务数据库 / 发布 / Git 写操作

## 1. 记录规则

每个 EV 只记录实际执行的环境、命令、预期、关键输出、退出码、结果、时间和限制。结果使用 `Pass / Fail / Blocked / NotRun / NotApplicable`；文件存在、历史 `target/out/node_modules` 或子任务口头摘要都不自动算 Pass。

当前工作树在本 Feature 开工前已有大量用户未提交修改和目录迁移。本次只修改任务清单授权范围内的 Aden 路径，所有 Git 写操作保持未授权。

## 2. IMP-01 验证矩阵

| EV | 关联任务 / AC | 验证对象 | 当前结果 | 限制 |
| --- | --- | --- | --- | --- |
| EV-IMP01-ENV | ADEN-TASK-007 | JDK / Maven / Node / npm / Python / uv 与本机数据库条件 | Pass（带限制） | Docker 非项目依赖；MySQL80 未启动 |
| EV-IMP01-JAVA | ADEN-TASK-007 / AC-01 | `ruoyi-aden` 定向测试与 Reactor 装配 | Pass | 不证明数据库 / API |
| EV-IMP01-DESKTOP | ADEN-TASK-007 / AC-09 | `aden-desktop` test / typecheck / bundle | Pass | 不证明窗口 E2E |
| EV-IMP01-RUNNER | ADEN-TASK-005/007 / AC-13 | `aden-runner` lint / type / test / build / CLI | Pass | IMP-01 仅工程基线 |
| EV-IMP01-AGENT | ADEN-TASK-003/005/007 / AC-16 | `aden-agent-runtime` lint / type / test / build / CLI | Pass | Fake、provider-disabled；不证明真实模型 |
| EV-IMP01-CONTRACT | ADEN-TASK-001～005 | OpenAPI / JSON Schema / examples / refs | Pass | current 不发布 Agent Worker API |
| EV-IMP01-CONSUMERS | ADEN-TASK-004/005 | Java / TypeScript / Python 直接消费 current/experimental 契约 | Pass | 显式 mapping，不生成第二套 DTO Schema |
| EV-IMP01-CONFIG | ADEN-TASK-006 | 17 个类型化外部配置键与故障测试值 | Pass | 只冻结名称、类型、范围和默认/测试值 |
| EV-IMP01-DOC | ADEN-TASK-006 | ADR、控制页与文档链接 | Pass | 文档检查不证明代码运行 |

## 3. 实际执行

### EV-IMP01-START — 授权与范围切换

- 时间：2026-09-12（Asia/Shanghai）。
- 动作：记录用户批准；将唯一任务清单切换为 Approved、IMP-01 切换为 InProgress；建立本验证事实源。
- 实际：Stage 7 已开始；IMP-02～IMP-10 仍为 NotStarted。
- 结果：Pass。
- 限制：这是治理与范围事实，不是任何工程构建通过证据。

### EV-IMP01-ENV — 工具链与受控环境

- 时间：2026-09-12 23:40（Asia/Shanghai）。
- 工作目录：仓库根。
- 命令：`java -version`、`mvn -version`、`node --version`、`npm --version`、`python --version`、`uv --version`、`docker --version`、读取 `MySQL80` 服务状态。
- 预期：确认 IMP-01 四工程工具链；不启动服务。
- 实际：Windows 11 amd64；Temurin JDK 21.0.10；Maven 3.9.15；Node 24.15.0；npm 11.12.1；Python 3.12.4；uv 0.11.18。Docker 命令不存在；MySQL80 服务存在但为 Stopped / Automatic，未启动。
- 退出码：组合探针 0；其中 Docker 子命令明确失败为 command not found。
- 结果：Pass（带限制）。
- 限制：JDK 运行时为 21，项目仍按 Java 17 source/target 构建；Docker 缺失不再阻断 MySQL 验证，后续统一使用本机一次性隔离实例且不接业务库。

### EV-IMP01-JAVA — RuoYi Aden Reactor 基线

- 时间：2026-09-12 23:40:37（Asia/Shanghai）。
- 工作目录：`platform-backend/`。
- 命令：`mvn -pl ruoyi-aden -am test`。
- 预期：`ruoyi-common → ruoyi-system → ruoyi-framework → ruoyi-aden` Reactor 可装配，既有领域测试通过。
- 实际：5 个 Reactor 项目均 SUCCESS；`AdenTaskTransitionsTest` 运行 7 项，Failures 0、Errors 0、Skipped 0；BUILD SUCCESS。
- 退出码：0。
- 结果：Pass。
- 限制：只证明既有纯 Java 状态骨架与模块依赖；尚未覆盖 actor/permission、数据库、Spring 容器、API 或迁移。

### EV-IMP01-DESKTOP — Electron 工程基线

- 时间：2026-09-12 23:40～23:41（Asia/Shanghai）。
- 工作目录：`aden-desktop/`。
- 命令：依次执行 `npm test`、`npm run typecheck`、`npm run build`。
- 预期：既有 Vitest、Vue / Node 类型检查和 electron-vite 三入口 bundle 可重复通过。
- 实际：Vitest 1 个文件、5 项测试通过；类型检查退出 0；构建生成并检查 `out/main/index.js`、`out/preload/index.mjs`、`out/renderer/index.html`，三入口均可读。
- 退出码：0。
- 结果：Pass。
- 限制：这是 bundle 基线，不是 Electron 窗口、登录、IPC、SSE 或安装包验收。

### EV-IMP01-RUNNER — 独立 Runner Python 基线

- 时间：2026-09-12 23:40～23:42（Asia/Shanghai）。
- 工作目录：`aden-runner/`。
- 命令：`uv sync --extra test --locked`；随后用锁定环境执行 `ruff check .`、`pyright`、`pytest`、`uv build` 和 `uv run aden-runner`。
- 预期：独立 Python 3.12 发行单元可锁定、静态检查、测试、构建；CLI 明确关闭所有真实执行能力。
- 实际：锁文件解析 20 个包；ruff 通过；pyright 为 0 errors / 0 warnings；pytest 11 passed；sdist 与 wheel 构建成功；CLI 输出 baseline 且 outbound network、UIA、Shell、browser、任意文件、external write 全部 disabled。
- 退出码：0。
- 结果：Pass。
- 限制：仅 IMP-01 工程和本地 Schema 校验基线；Session / claim / lease / fence / receipt 属于 IMP-05。

### EV-IMP01-AGENT — 独立 Agent Python 基线

- 时间：2026-09-12 23:42（Asia/Shanghai）。
- 工作目录：`aden-agent-runtime/`。
- 命令：`uv sync --extra test --locked`；随后用锁定环境执行 `ruff check .`、`pyright`、`pytest`、`uv build` 和 `uv run aden-agent-runtime`。
- 预期：独立 Python 3.12 发行单元和自有 ModelAgentPort / Fake 基线可重复验证，真实 Provider fail closed。
- 实际：锁文件解析 20 个包；ruff 通过；pyright 为 0 errors / 0 warnings；pytest 13 passed；sdist 与 wheel 构建成功；CLI 输出 offline_fake、provider_disabled=true，网络与高权限能力全部 disabled。
- 退出码：0。
- 结果：Pass。
- 限制：Fake 结果不证明模型质量、Provider SDK、AgentJob 服务端链或真实网络；这些仍未启用。

### EV-IMP01-CONTRACT — 唯一机器契约门禁

- 时间：2026-09-13 00:03～00:05（Asia/Shanghai）。
- 工作目录：`contracts/aden/`。
- 命令：`python scripts/validate_contracts.py`；`python scripts/probe_consumers.py`。
- 预期：Schema 是 body/event DTO 唯一机器事实源；OpenAPI 仅以相对引用组合；current / experimental 不串线；正负案例和三语言边界一致。
- 实际：4 份 JSON Schema、2 份 OpenAPI、18 个 operation、717 个引用均解析；生成 6 个临时本地 bundle；26 个合法案例通过、9 个非法案例按预期拒绝；17 个类型化外部配置键及跨键不变量通过。Python 3.12 / Pydantic、Node 24.15 / TypeScript 5.7.3、javac 21 的 wire scalar 与公开命令探针通过。
- 退出码：两条命令均为 0。
- 结果：Pass。
- 限制：临时 bundle 不是新事实源；current 未发布 Agent Worker API；探针不证明数据库或 HTTP 实现。

### EV-IMP01-CONSUMERS — Java / TypeScript / Python 契约消费

- 时间：2026-09-12 23:50～23:57（Asia/Shanghai）。
- 工作目录：`platform-backend/`、`aden-desktop/`、两个 Python 工程。
- 命令：`mvn -pl ruoyi-aden -am test`；`npm test`、`npm run typecheck`、`npm run build`；两个 Python CLI 分别校验 current Runner 与 experimental Agent 的 Schema fragment/example。
- 预期：消费者直接读取 canonical Schema/example；64 位边界无精度损失；内部命令不可作为 Operator 命令；EventEnvelope workspaceId 可读。
- 实际：Java 共 10 项测试通过（新增契约测试 3 项）；Electron/Vitest 共 15 项通过（新增契约测试 10 项），类型检查和三入口 bundle 通过；两个 Python 工程各自对 current/experimental 实例校验退出 0。未新增 Java/TypeScript 依赖。
- 退出码：最终规范命令均为 0。
- 结果：Pass。
- 过程偏差：一次定向 Maven 尝试因 PowerShell 未给带点号的 `-D` 参数加引号，被 Maven 解析为 lifecycle phase；改为带引号参数后通过。该次是命令转义失败，不是测试失败，未改验收标准。
- 限制：消费者使用显式边界 mapping，无持久化生成 DTO；业务 Controller / renderer 页面仍未实现。

### EV-IMP01-DOC — ADR 与文档结构

- 时间：2026-09-13 00:05（Asia/Shanghai）。
- 工作目录：仓库根。
- 命令：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-docs.ps1`，并执行十阶段 / TASK / REQ / AC 结构计数。
- 预期：ADR、控制页、任务清单和验证记录链接有效；只有一个任务事实源；阶段编号与追溯数量不漂移。
- 实际：235 个 Markdown、19 个 HTML 的目录与链接检查通过；恰好 10 个 IMP；72 个 TASK 连续；11 个 REQ、17 个 AC；不存在平行 `实施计划.md`。
- 退出码：0。
- 结果：Pass。
- 限制：只证明文档结构和链接，不证明实现行为。

## 4. IMP-02 验证矩阵

| EV | 关联任务 / AC | 验证对象 | 当前结果 | 限制 |
| --- | --- | --- | --- | --- |
| EV-IMP02-MODULE | ADEN-TASK-008/011 / AC-01、15 | Java 17、四层骨架、17 键配置、UTC / UUID / int64 边界 | Pass | 不证明 MySQL DDL 可执行 |
| EV-IMP02-MIGRATION | ADEN-TASK-009/010 / AC-05、13 | 独立 CLI、只读启动 guard、V1～V4、12 表静态契约与真实执行 | Pass | 仅隔离 MySQL，不代表生产迁移 |
| EV-IMP02-PERMISSION | ADEN-TASK-012 / AC-15 | 15 个 `aden:*` 权限定义、幂等和无默认授权 | Pass（静态） | 未写任何真实 `sys_menu` |
| EV-IMP02-MYSQL | ADEN-TASK-013 / AC-05、13 | MySQL 8 baseline / migrate / validate / checksum / 错库保护 | Pass | 本机一次性 MySQL 8.0.46；Docker 未使用 |
| EV-IMP02-ADMIN | ADEN-TASK-008/009 / AC-01 | `ruoyi-admin` 15 模块组合编译、普通启动禁止自动 migrate | Pass | 未启动应用、数据库或 Redis |

### EV-IMP02-MODULE — 组合根、配置和持久化标量

- 时间：2026-09-13 00:38（Asia/Shanghai）。
- 工作目录：`platform-backend/`。
- 命令：`mvn -pl ruoyi-aden -am test`。
- 预期：`ruoyi-aden` 使用 Java `release=17`；domain 不依赖 Spring / MyBatis / Flyway；17 个 canonical 配置默认值和跨键约束、UTC `DATETIME(6)` 转换、UUID 与 wire int64 边界通过。
- 实际：`ruoyi-aden` 以 `release 17` 编译 21 个主源码；非容器测试 22 项全部通过，包括 domain 纯 Java 扫描、17 个配置默认值 / 失败约束、UTC 微秒 round-trip、UUID 和既有 int64 消费者契约。
- 退出码：0。
- 结果：Pass。
- 限制：JDK 运行时仍为 21；编译目标为 17。单元测试不证明数据库方言。

### EV-IMP02-MIGRATION — CLI、Schema Guard 与静态 DDL

- 时间：2026-09-13 00:29～00:38（Asia/Shanghai）。
- 工作目录：`platform-backend/` 与仓库根。
- 命令：`mvn -pl ruoyi-aden -am test`；读取四个 migration 的表、`workspace_id` 与 `DATETIME(6)` 快照；`mvn -pl ruoyi-aden dependency:tree "-Dincludes=org.flywaydb:*"`。
- 预期：migration location / history / baseline 策略不可外部漂移；普通应用禁用自动 migration；当前业务表精确为 12 张；测试依赖不混用大版本。
- 实际：V1～V4 静态测试检出精确 12 表、12 个 ASCII-binary `workspace_id` 和 44 个真实列定义中的 `DATETIME(6)`；应用配置固定 `spring.flyway.enabled=false`，Aden 默认关闭，local/test 显式绑定 `127.0.0.1`；CLI 只接受 `baseline / migrate / validate` 和固定参数，密码只从指定环境变量读取；precheck 校验数据库名、`sys_menu`、未知 / 无历史 `aden_*` 冲突，guard 额外回读当前 12 表。Flyway 为 11.14.1；按用户决定移除 Aden 模块的 Testcontainers 依赖，MySQL 测试只使用本机一次性实例。
- 退出码：最终命令均为 0。
- 结果：Pass（静态 / 单元）。
- 过程偏差：首次新增测试编译因 `FlywayValidateException` 包名错误失败，修正为 `org.flywaydb.core.api.exception` 后通过；随后识别并消除了 Testcontainers 1.20.6 与 Boot 管理的 core 2.0.5 混用。一次 dependency tree 在仓库根误读旧根 POM 失败，切换到 `platform-backend/` 后通过。这些失败均未改验收标准。
- 限制：静态门本身不证明数据库方言；真实执行结果另见 EV-IMP02-MYSQL。

### EV-IMP02-PERMISSION — 权限增量边界

- 时间：2026-09-13 01:09～01:17（Asia/Shanghai）。
- 工作目录：仓库根。
- 方法：`AdenMigrationStaticContractTest` 读取 `platform-backend/sql/aden-permissions.sql`。
- 预期：只定义已批准权限字符；可重复运行；不创建业务对象，不写 `sys_role_menu`。
- 实际：脚本定义 15 个精确 `aden:*` 权限，按 `perms` 和候选 `menu_id` 双重 `not exists`；测试确认无 `insert into sys_role_menu`、无 workspace seed。
- 结果：Pass（静态）。
- 限制：未连接或写入任何 RuoYi 数据库。

### EV-IMP02-MYSQL — 隔离 MySQL 8 迁移

- 时间：2026-09-13 00:38（Asia/Shanghai）。
- 工作目录：`platform-backend/`。
- 命令：`ruoyi-aden/scripts/run-local-mysql-tests.ps1`；脚本用已安装的 MySQL 8.0.46 可执行文件在模块 `target` 下初始化一次性数据目录，随机 loopback 端口启动后设置进程级测试变量，再运行 `mvn -pl ruoyi-aden -am -Dtest=com.ruoyi.aden.migration.AdenMySqlMigrationTest -Dsurefire.failIfNoSpecifiedTests=false test`。
- 预期：隔离 MySQL 8 导入 `sql/ry_20260417.sql`，显式 baseline 0，执行 V1～V4，重复 migrate 为 0，validate 通过；错库 / 已选择的空库拒绝；checksum 篡改被检出并恢复；回读 12 表、关键索引、44 个 `DATETIME(6)` 与 12 个 ASCII-binary workspace 列。
- 实际：最终实例只监听 `127.0.0.1:12152`，随机库名使用固定 `aden_test_<32hex>` 前缀；RuoYi 基线导入成功，Flyway 在版本 0 上成功执行 4 个 migration 到 v4，第二次 migrate 执行 0 个；validate、错库 / 空库保护、checksum 篡改检测与恢复、13 张 `aden_*` 表（12 业务表 + 1 history）及关键索引回读全部通过。`Tests run: 4, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。测试结束后随机测试库、临时实例及模块 `target` 下数据目录全部清理；`MySQL80` 仍为 Stopped / Automatic，未接触其数据目录。
- 退出码：最终命令 0。
- 结果：Pass。
- 过程偏差：直接启动现有 `MySQL80` 服务因当前权限不足而失败，服务保持停止且未发生数据库写入；随后改用同一已安装版本的一次性实例。首次真实执行发现时间列断言误把 SQL 注释计为列（预期 45、实际 44）；第二次发现复合索引查询按索引列计数；两项均修正为实际列定义和 `seq_in_index = 1` 后通过，DDL 与迁移未因这些测试缺陷修改。
- 限制：证明 MySQL 8.0.46 隔离环境兼容，不代表现有业务库、生产 migration、其他 MySQL 小版本或真实数据验证；Docker 不参与该测试链。

### EV-IMP02-ADMIN — RuoYi 组合编译

- 时间：2026-09-13 00:23（Asia/Shanghai）。
- 工作目录：`platform-backend/`。
- 命令：`mvn -pl ruoyi-admin -am -DskipTests compile`。
- 预期：Aden 的 Flyway、配置与 TypeHandler 能进入 RuoYi 组合根，不破坏其他模块编译。
- 实际：15 个 Reactor 模块全部 SUCCESS，包括 Interview、Fashion、Aden、APS、Quartz、Generator 与 `ruoyi-admin`；BUILD SUCCESS。
- 退出码：0。
- 结果：Pass。
- 限制：这是编译证据，不是应用启动或跨模块运行证据。

## 5. IMP-03 验证矩阵

| EV | 关联任务 / AC | 验证对象 | 当前结果 | 限制 |
| --- | --- | --- | --- | --- |
| EV-IMP03-UNIT | ADEN-TASK-014～018 / AC-02、15 | Workspace 领域、Operator 身份、四层门禁、角色矩阵与事务注解 | Pass | MySQL 类在无测试变量时不运行 |
| EV-IMP03-HTTP | ADEN-TASK-015/017/018 / AC-02 | 401/403/404/400、Origin、CSRF、create/list 与 ErrorEnvelope | Pass | MockMvc 合成身份，不证明真实 Redis 登录态 |
| EV-IMP03-MYSQL | ADEN-TASK-014/016/017 / AC-02、15 | MyBatis round-trip、三写原子回滚、拒绝审计最小化 | Pass | 本机一次性 MySQL 8.0.46；未接业务库 |
| EV-IMP03-ADMIN | ADEN-TASK-015/017 / AC-01 | `ruoyi-admin` 15 模块聚合编译 | Pass | 未启动 RuoYi / Redis |
| EV-IMP03-CLEANUP | ADEN-TASK-018 / AC-13 | 临时 MySQL 进程、数据目录与现有服务状态 | Pass | 仅检查本次宿主环境 |

### EV-IMP03-UNIT / HTTP — Workspace 和安全边界

- 时间：2026-09-13 01:53～02:04（Asia/Shanghai）。
- 工作目录：`platform-backend/`。
- 命令：`mvn -pl ruoyi-aden -am test`；修复 404 fallback 后又执行一次最终全量命令。
- 预期：Workspace 领域和 MyBatis 契约成立；功能权限、有效 membership、角色、资源 workspace 四层门禁不可由 `*:*:*` 绕过；OWNER / OPERATOR / VIEWER 固定矩阵通过；Aden API 返回真实 HTTP 400/401/403/404；POST 不被 CSRF 或 request cache 误拦；任何 Origin（含 preflight）在 Controller 前返回 403 且无通配 CORS 响应头。
- 实际：最终 `Tests run: 55, Failures: 0, Errors: 0, Skipped: 0`，5 个 Reactor 模块均 SUCCESS。其中文件级统计包括 MockMvc 9 项、Workspace guard 13 项、Workspace service 3 项、架构门禁 4 项；MySQL 测试类在没有 `ADEN_TEST_DB_*` 变量时不启动数据库。未知 Aden 路由通过 Aden fallback 返回 ErrorEnvelope，不再产生空 404；服务类已移除 `final`，拒绝审计声明 `REQUIRES_NEW`。
- 退出码：0。
- 结果：Pass。
- 过程偏差：首次加入未知路由测试时发现 Spring 对无 handler 的 404 不会进入限定 Aden 包的 advice，响应体为空；新增 Aden 专用 catch-all Controller 后，精确业务路由优先匹配，未知路由稳定返回 Aden 404。此前两次测试上下文失败分别来自测试替身仍触发真实 JWT `TokenService` 注入和未启用 Web Security，均在测试装配层修复。一次 PowerShell 定向测试命令未给逗号参数加引号而被 shell 解析失败，随后用引号重跑通过。
- 限制：使用合成 `LoginUser`，未启动 Redis 或真实登录；未知资源当前沿用 current 契约已有的 `ADEN_TASK_NOT_FOUND`，未新增契约错误码。

### EV-IMP03-MYSQL — Workspace 持久化、事务与拒绝审计

- 时间：2026-09-13 01:58～02:00（Asia/Shanghai）。
- 工作目录：`platform-backend/ruoyi-aden/`。
- 命令：`./scripts/run-local-mysql-tests.ps1`。脚本以 `platform-backend/pom.xml` 为 Reactor 根，运行 `-Dtest=*MySql*Test`。
- 预期：真实 MySQL 完成迁移门与 Workspace MyBatis round-trip；创建 Workspace、首个 OWNER、`WORKSPACE_CREATED` 审计同事务；审计失败时前两项回滚；拒绝访问审计为平台级、REJECTED、空 details，不受目标 Workspace 外键影响。
- 实际：一次性 MySQL 8.0.46 只监听 `127.0.0.1:13378`；`AdenWorkspaceMySqlTest` 3/3、`AdenMySqlMigrationTest` 4/4，合计 `Tests run: 7, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。拒绝审计写入 `workspace_id=null`、`scope_type=PLATFORM`、`action_code=ADEN_WORKSPACE_FORBIDDEN`、`resource_type=WORKSPACE`、`outcome=REJECTED` 和空 JSON details；没有保存目标资源正文。
- 退出码：0。
- 结果：Pass。
- 过程偏差：第一次从模块目录直接调用脚本时，脚本内 Maven 未显式指定 Reactor POM，导致 `Could not find the selected project in the reactor`；MySQL 仍按 finally 清理。脚本随后改为固定 `-f platform-backend/pom.xml`，同一调用方式重跑通过。
- 限制：验证的是隔离测试库，不是现有业务库或生产迁移；审计写失败时对外仍保持 404 并记录服务日志，后续运维告警留到 IMP-10。

### EV-IMP03-ADMIN / CLEANUP — 聚合编译与环境恢复

- 时间：2026-09-13 02:05～02:06（Asia/Shanghai）。
- 工作目录：`platform-backend/` 和仓库根。
- 命令：`mvn -pl ruoyi-admin -am -DskipTests compile`；随后检查模块 `target/aden-mysql-local-*`、`mysqld.exe` 进程与 `MySQL80` 服务。
- 实际：15 个 Reactor 模块全部 SUCCESS，BUILD SUCCESS；临时实例目录 0、`mysqld.exe` 进程 0，`MySQL80` 保持 Stopped / Automatic。
- 退出码：0。
- 结果：Pass。
- 限制：未启动 `ruoyi-admin`，不证明真实 Redis / JWT 会话或 HTTP 网络联调。

## 6. IMP-04 完成证据

### EV-IMP04-DOMAIN-01 — Task 聚合与 actor-command 矩阵

- 时间：2026-09-13 02:15～02:18（Asia/Shanghai）。
- 工作目录：`platform-backend/`。
- 命令：先定向运行 `AdenTaskTransitionsTest,AdenTaskAggregateTest,AdenTaskTransitionMatrixTest`，再运行 `mvn -pl ruoyi-aden -am test`。
- 实际：新增 TaskId、TaskVersion、TaskType、CapabilityCode、CorrelationId、TaskStep、Task 聚合、版本化领域事件和公开 Operator 命令 adapter；唯一 `state × actor × command` 表枚举 OPERATOR / VALIDATOR / COORDINATOR / RUNNER 的全部组合。定向测试 451/451 通过；最终全量 `Tests run: 499, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。
- 退出码：0。
- 结果：Pass（阶段内增量）。
- 限制：只完成 ADEN-TASK-019～020 的领域主体；CAS 与幂等指纹证据见下一项，Event / Outbox / Inbox / Audit 原子持久化及并发故障验证仍属于 IMP-04 未完成范围，因此 IMP-04 保持 InProgress。

### EV-IMP04-CAS-IDEMPOTENCY-01 — 四条件 CAS 与规范化请求指纹

- 时间：2026-09-13 02:22～02:27（Asia/Shanghai）。
- 工作目录：`platform-backend/` 与 `platform-backend/ruoyi-aden/`。
- 命令：定向运行 `AdenRequestFingerprintTest,MyBatisAdenTaskCasRepositoryTest,AdenTaskMapperContractTest`；运行 `./scripts/run-local-mysql-tests.ps1`；最终运行 `mvn -pl ruoyi-aden -am test`。
- 预期：Task 更新必须同时比较 `workspace_id + task_id + version + expected_state`，成功精确增加一个版本，旧 writer 返回稳定领域冲突；JSON 对象字段及嵌套对象字段顺序不影响创建/命令请求 hash，数组顺序和实际值仍参与 hash。
- 实际：MyBatis CAS XML 包含全部四个谓词且禁止 `${}`；单元测试证明零更新行映射为 `ADEN_VERSION_CONFLICT`、跨 Task 标识或 version 跳变不会进入 SQL。真实 MySQL 中两个 Workspace 可存在相同 taskId，只更新指定 Workspace 的 `QUEUED/v6 → RUNNING/v7`，另一个保持 `QUEUED/v6`，使用旧事实重试稳定冲突。规范化指纹递归排序对象键并输出 64 位小写十六进制 SHA-256，数组顺序不同产生不同 hash。Spring 运行时已装配 CAS repository 和 fingerprint，异常映射补齐 409/412 等 current 契约状态。
- 实际计数：定向单测 8/8；本机 MySQL `AdenWorkspaceMySqlTest` 4/4 + migration 4/4，合计 8/8；最终无数据库环境全量 `Tests run: 507, Failures: 0, Errors: 0, Skipped: 0`，均 BUILD SUCCESS。
- 退出码：全部最终命令为 0。
- 结果：Pass（阶段内增量）。
- 清理：一次性实例监听 `127.0.0.1:9611`；结束后临时目录 0、`mysqld.exe` 进程 0，现有 `MySQL80` 保持 Stopped。
- 限制：当前证明 ADEN-TASK-021 的 CAS 与 ADEN-TASK-022 的 canonical hash 基础；Inbox 中“同 key 同 hash 回放、同 key 异 hash 冲突”的事务持久化仍与 ADEN-TASK-023 一起实现，IMP-04 尚未完成。

### EV-IMP04-LEDGER-OUTBOX-01 — 原子账本、取消安全点与 Outbox 恢复

- 时间：2026-09-13 02:40～02:50（Asia/Shanghai）。
- 工作目录：`platform-backend/` 与 `platform-backend/ruoyi-aden/`。
- 命令：`mvn -pl ruoyi-aden -am test -DskipTests=false`；`./scripts/run-local-mysql-tests.ps1`。
- 预期：创建、提交校验和取消命令必须在同一事务写 Task / Step / Event / Outbox / Inbox / Audit；同 key / hash 回放、异 hash 冲突；任一账本写失败整体回滚；Outbox 支持 claim token、过期恢复、指数退避和 DEAD，且事务内不执行网络发送。
- 实际：真实 MySQL 证明 `DRAFT/v1 → VALIDATING/v2 → QUEUED|FAILED/v3` 连续版本和事件；标题先规范化再参与 hash；重复创建、提交和取消回放原结果，异请求复用 key 返回冲突；取消从 `QUEUED/v3` 只进入 `CANCEL_REQUESTED/v4`。注入 Audit 写失败后 Task / Step / Event / Outbox / Inbox / Audit 与 Workspace 水位全部为 0。Outbox 领取后可确认发布，失败按 100ms 起始延迟重试，claim 过期可由新 token 重新领取，旧 token 写入被拒绝，达到第 2 次尝试进入 DEAD。
- 实际计数：阶段中间全量 511/511 通过；真实 MySQL `AdenWorkspaceMySqlTest` 7/7 + migration 4/4，合计 11/11，BUILD SUCCESS。
- 退出码：0。
- 结果：Pass。
- 清理：一次性 MySQL 随机端口与随机库由脚本在 `finally` 中关闭、删除；现有 `MySQL80` 保持 Stopped。
- 限制：本阶段只证明账本和恢复端口，实际 SSE fan-out 仍在 IMP-06。

### EV-IMP04-CONCURRENCY-01 — 连续序号与整事务有界重试

- 时间：2026-09-13 02:50～02:59（Asia/Shanghai）。
- 工作目录：`platform-backend/` 与 `platform-backend/ruoyi-aden/`。
- 命令：`mvn -pl ruoyi-aden -am test -DskipTests=false`；`./scripts/run-local-mysql-tests.ps1`。
- 预期：同 Workspace 并发 writer 由 Workspace 水位锁串行化，已提交事件序号连续；死锁 / 锁超时只能对携带幂等键且未产生事务外副作用的完整事务做最多 3 次重试，退避时间可注入测试。
- 实际：4 个线程同时创建不同任务，真实 MySQL 最终得到 4 个不同 taskId、`event_seq=[1,2,3,4]`、水位 4、4 条 Outbox；整事务执行器对模拟锁异常完整调用 3 次并按 10ms / 20ms 退避，普通异常只调用 1 次，达到上限保留原异常。最终无数据库全量 `Tests run: 513, Failures: 0, Errors: 0, Skipped: 0`；本机 MySQL `AdenWorkspaceMySqlTest` 8/8 + migration 4/4，合计 12/12，均 BUILD SUCCESS。
- 失败与修正：第一次并发 MySQL 运行因合成 fixture 使用了不符合正则的第二个冒号而在进入数据库事务前失败（12 个用例中 1 Error）；将 `fixture:concurrent:N` 修正为 `fixture:concurrent-N` 后原命令重跑通过。该失败不作为通过证据。
- 退出码：最终命令均为 0。
- 结果：Pass。
- 环境 / 清理：JDK 17、Maven、MySQL Community Server 8.0.46；最终实例端口 `127.0.0.1:14437`。结束后 `aden-mysql-local-*` 目录 0、临时 `mysqld.exe` 进程 0，现有 `MySQL80` 为 Stopped。
- 限制：未连接真实账号、Provider、外部网络或生产数据库；真实 Runner claim / lease / receipt 属于 IMP-05。

## 7. IMP-05 完成证据

### EV-IMP05-DOMAIN-AUTH-01 — Runner 领域与独立认证边界

- 时间：2026-09-13 03:03～03:09（Asia/Shanghai）。
- 工作目录：`platform-backend/`。
- 命令：`mvn -pl ruoyi-aden -am '-Dtest=AdenRunnerDomainTest,AdenRunnerCredentialCodecTest,AdenRunnerAuthenticationFilterTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`。
- 预期：Runner / Credential / Session / Delivery / Lease / FencingToken / Receipt / CapabilitySnapshot 形成显式领域事实；设备 token 使用 `credentialId.secret`，CSPRNG 生成 Secret、HMAC-SHA-256 keyed digest、pepper key id 与常量时间 digest 比较；Runner path 只接受端点规定的 `AdenCredential` 或 `AdenRunner` scheme，不能降级尝试 RuoYi JWT 或另一 Runner scheme。
- 实际：完成 18 个 Runner 领域 / 值对象与 Delivery receipt 状态约束；领取单调增加 fence，旧 fence、失租、乱序回执均拒绝，取消终态必须先有 cancel 请求并收到安全点回执。凭据 codec 生成 32 字节随机 Secret，只在组合 token 返回，持久化材料为 64 位 keyed digest；认证 filter 对 Session exchange 仅调用 credential port，对 claim/heartbeat/receipt 仅调用 session port，`Bearer` 或错误 scheme 在调用 provider 前即返回 401 `ADEN_RUNNER_AUTH_INVALID`。新增 `@Order(1)` Runner chain，匹配 `/api/v1/aden/runner/**` 且不装配 RuoYi JWT filter。
- 实际计数：定向 7/7；随后完整回归 `Tests run: 520, Failures: 0, Errors: 0, Skipped: 0`，均 BUILD SUCCESS。首次定向命令因 PowerShell 将未加引号的点号属性拆成 Maven 生命周期参数而退出 1，修正参数引用后原范围重跑通过，失败命令未作为通过证据。
- 退出码：最终命令 0。
- 结果：Pass（阶段内增量）。
- 限制：当前完成 ADEN-TASK-026 和 ADEN-TASK-027/028 的领域、安全编码及 filter-chain 基础；数据库 enroll/revoke、Session 换代、claim/heartbeat/receipt 与 simulator 尚未完成，因此 IMP-05 保持 InProgress。

### EV-IMP05-IDENTITY-MYSQL-02 — enroll、Session 换代与 revoke 持久化

- 时间：2026-09-13 03:10～03:20（Asia/Shanghai）。
- 工作目录：`platform-backend/ruoyi-aden/`。
- 命令：`scripts/run-local-mysql-tests.ps1`；脚本在模块 `target/` 初始化 MySQL 8.0.46、绑定随机 loopback 端口、创建随机测试库并在结束后清理。
- 预期：enroll 只返回一次 `credentialId.secret`，数据库无 Secret；credential 可换 Session，第二次换代使旧 Session `SUPERSEDED` 且 `current_session_epoch` 单调递增；revoke 同事务使 credential / Session 失效并将 Runner 隔离。
- 实际：设备与 Session keyed digest 均可验证且无明文 Secret；首个 Session 可认证，第二个 Session epoch=2 且首个失效；revoke 后 credential / 当前 Session 均不可认证，Runner 为 `QUARANTINED`；Workspace 连续 4 条 Event / Outbox / Audit。首轮测试发现新建 Runner Event 的 aggregateVersion=0 违反数据库检查约束，修正 Runner 初始版本为 1 后重跑通过。
- 实际计数：`AdenWorkspaceMySqlTest` 9/9、`AdenMySqlMigrationTest` 4/4，总计 13/13。
- 结果：Pass（阶段内增量）。

### EV-IMP05-PROTOCOL-MYSQL-03 — TaskPackage、claim、heartbeat 与原子 receipt

- 时间：2026-09-13 03:20～04:11（Asia/Shanghai）。
- 工作目录：`platform-backend/` 与 `platform-backend/ruoyi-aden/`。
- 命令：`mvn -pl ruoyi-aden -am test`；`scripts/run-local-mysql-tests.ps1`。
- 预期：Validator 通过的同一事务冻结 TaskPackage 并创建唯一 attempt 1 READY Delivery；claim 按 Workspace→Inbox→Session→Delivery 锁序、数据库 UTC、capacity 与 `SKIP LOCKED` 领取并增加 fence；同 key/hash 只回放仍存活的原 batch，异 hash 冲突；heartbeat 严格递增并返回取消指令；STARTED / PROGRESS / COMPLETED receipt 原子更新 Inbox、Delivery、Step、Task、Event、Outbox、Audit，重复最终 receipt 只回放，Audit 失败整体回滚。
- 实际：READY Delivery 与 package hash 同 SUBMIT 提交；真实 MySQL 完成 fence=1 的 claim、原批次回放、capacity=1 和异 hash 拒绝；heartbeat 同时续 Session / Delivery；三类 receipt 将 `QUEUED/v3 → RUNNING/v4 → RUNNING/v5 → SUCCEEDED/v6`，receipt sequence 为 1/2/3，Session in-flight 归零。注入 Audit 失败后 Delivery 保持 LEASED、receipt sequence=0、Inbox / Workspace 水位均未前进。活动 Delivery 的取消请求经 heartbeat 返回 `CANCEL_AT_SAFE_POINT`，仅安全点 receipt 后 Task / Step / Delivery 才进入 CANCELED。
- 故障记录：第一次加入 claim 单测时，测试 ObjectMapper 未提供 Instant module；回放摘要改为显式 ISO-8601 字符串后通过。第一次真实 receipt 测试中 PROGRESS 复用 aggregateVersion，触发 `uq_aden_event_aggregate_version`；修正为状态不变的内部进度同样推进 Task version 后通过。两次失败均未计作通过。
- 实际计数：无数据库 Java 回归 526/526；一次性 MySQL 集成与迁移 14/14；最终退出码均为 0。
- 结果：Pass（阶段内增量）。

### EV-IMP05-PYTHON-SIM-04 — 六包无权限 Python Runner simulator

- 时间：2026-09-13 04:11（Asia/Shanghai）。
- 工作目录：`aden-runner/`。
- 命令：`uv lock`；`uv sync --extra test --frozen`；`uv run --extra test --frozen pytest`；`uv run --extra test --frozen ruff check .`；`uv run --extra test --frozen pyright`。
- 预期：`simulator/protocol/transport/lease/receipts/fixtures` 六包完成 Session→claim→heartbeat→receipt 合成闭环；transport 只允许 loopback Origin、禁环境代理和 redirect；TaskPackage hash / `externalActionsEnabled=false` 必须成立；fixture、fence、单调序号与崩溃点可确定性验证，Secret 不进入配置 repr 或日志。
- 实际：新增 httpx 0.28.1 锁依赖和六包实现；非 loopback、带路径或非法 scheme 配置全部 fail closed；固定 fixture 不调用 UIA / Shell / 浏览器 / 文件 / 外部写入；`AFTER_CLAIM / AFTER_STARTED / BEFORE_FINAL` 可注入崩溃。初次组合检查发现 protocol strict datetime 不接受 HTTP ISO 字符串、URL netloc bytes 拼接错误及 3 个 lint / 8 个 pyright 问题，逐项修正后完整重跑通过。
- 实际计数：pytest 18/18；Ruff 0 error；Pyright 0 error / 0 warning。
- 结果：Pass（阶段内增量）。
- 限制：尚未启动真实 RuoYi HTTP 服务做 Java↔Python 进程级联调；后续恢复 / 并发证据见 EV-IMP05-RECOVERY-CONCURRENCY-05。

### EV-IMP05-RECOVERY-CONCURRENCY-05 — 租约恢复与双 Runner 竞争

- 时间：2026-09-13 04:11～04:25（Asia/Shanghai）。
- 工作目录：`platform-backend/ruoyi-aden/`。
- 命令：`scripts/run-local-mysql-tests.ps1`；直接使用本机 MySQL Server 8.0.46 二进制初始化一次性实例，不使用 Docker Desktop。
- 预期：未 STARTED 的过期租约只重置同一 Delivery；已 STARTED 且无取消请求时终结旧 Delivery 并创建 attempt 2；取消中的过期 Delivery 进入 `OUTCOME_UNKNOWN` 且不重派。两个 Session 同时 claim 时必须各自获得不同 Delivery，不得重复占有，Session `in_flight` 与 Delivery owner 一致。
- 实际：三条租约分别得到 requeued=1、retried=1、outcomeUnknown=1；未开始项保留 attempt 1 / fence 1 并清 owner，已开始项生成 attempt 2 / fence 0，取消项保持 Task `CANCEL_REQUESTED` 且只有一条 `OUTCOME_UNKNOWN`。双线程由同一闩锁同时发起 claim，两个结果各含 1 条且 delivery id 不同；数据库为 2 条 LEASED、2 个不同 owner Session，Session `in_flight` 合计 2。
- 实际计数：先完成 `AdenWorkspaceMySqlTest` 12/12 + migration 4/4；补齐并发 Session、同 key claim 和失败回执后，最终 `AdenWorkspaceMySqlTest` 15/15、`AdenMySqlMigrationTest` 4/4，总计 `Tests run: 19, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。
- 退出码：0。
- 清理：脚本关闭一次性 MySQL，并删除模块 `target` 下本轮数据目录；现有 `MySQL80` 服务未启动、未写入任何业务库。
- 补充实际：两个同 credential 的 Session exchange 并发提交后 epoch 精确为 1 / 2 且仅 epoch 2 ACTIVE；模拟第三次响应丢失后再次 exchange 得到 epoch 4，epoch 3 token 失效。相同 claim key 并发请求只形成一个 Inbox / 一个 LEASED Delivery，结果一份 ACCEPTED、一份 replay。`FAILED_RETRYABLE` 首轮曾暴露“attempt 2 已创建但 claim SQL 只接受 QUEUED Task”的缺陷，修正为 READY Delivery 可在 QUEUED / RUNNING Task 下领取后，attempt 2 能 STARTED 并由 `FAILED_FINAL` 原子关闭 Task。
- 过程偏差：新增失败回执测试第一次因测试枚举名写错在 testCompile 失败；第二次实跑暴露上述 RUNNING 重试领取缺陷并失败。两次均由脚本关闭 / 清理临时 MySQL，且均未计作通过证据。
- 结果：Pass（阶段内增量）。
- 限制：Session exchange × revoke / receipt 的更多交叉竞态与 Java↔Python 进程级 loopback 联调仍待收口。

### EV-IMP05-PYTHON-RETRY-06 — receipt 响应丢失的稳定回放

- 时间：2026-09-13 04:37～04:39（Asia/Shanghai）。
- 工作目录：`aden-runner/`。
- 命令：`uv run --extra test --frozen pytest`；`uv run --extra test --frozen ruff check .`；`uv run --extra test --frozen pyright`。
- 预期：receipt 请求已发出但响应丢失时，本地不得推进 sequence 或生成新 idempotency key；同进程重连 / 重跑应发送完全相同的待确认 receipt。
- 实际：`ReceiptFactory` 在服务端成功响应前保留 pending receipt；第一次合成 `httpx.ReadError` 后，第二次运行使用相同 claim id，STARTED 的 key、body、fence 与 sequence 完全一致，确认后才允许生成后续 PROGRESS / COMPLETED。
- 实际计数：pytest 19/19；Ruff 全通过；Pyright 0 errors / 0 warnings。
- 退出码：0。
- 结果：Pass（阶段内增量）。
- 限制：pending receipt 为当前 simulator 的进程内事实；进程彻底退出后仍由服务端 lease / fence / Inbox 恢复路径裁决，不宣称具备生产级本地持久队列。

### EV-IMP05-REGRESSION-07 — 阶段增量回归与契约审查

- 时间：2026-09-13 04:37～04:40（Asia/Shanghai）。
- 命令：`mvn -f platform-backend/pom.xml -pl ruoyi-aden -am test`；`scripts/check-docs.ps1`；人工比对 `contracts/aden/openapi/runner-v1.openapi.json`、Runner Schema、Java Controller 与 Python transport。
- 实际：无数据库 Java `Tests run: 526, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS；文档检查覆盖 239 个 Markdown、19 个 HTML，结构 / 中文命名 / 链接全部 PASS。契约比对发现 Java / Python 暂用的 claim / heartbeat / receipt 路径、DTO 结构及数字型 int64 与已批准机器契约不一致。
- 结果：测试 Pass；契约对齐未完成，因此 IMP-05 继续 InProgress，不能据单端测试提前判定阶段完成。
- 下一步：以 `contracts/aden` current v1 为事实源统一 Java Controller 与 Python transport / models，再做进程级 loopback 验证；不反向静默修改已批准契约。

### EV-IMP05-CONTRACT-CLOSE-08 — Runner v1 三端契约收口

- 时间：2026-09-13 04:40～05:08（Asia/Shanghai）。
- 工作目录：仓库根、`platform-backend/`、`aden-runner/`。
- 命令：`mvn -f platform-backend/pom.xml -pl ruoyi-aden -am test`；`platform-backend/ruoyi-aden/scripts/run-local-mysql-tests.ps1`；`.venv/Scripts/python.exe -m pytest`；`.venv/Scripts/ruff.exe check .`；`.venv/Scripts/pyright.exe`；`contracts/aden/scripts/validate_contracts.py`；`contracts/aden/scripts/probe_consumers.py`。
- 预期：Java 与 Python 精确采用批准的五条 Runner v1 路由和 DTO；所有 int64 以 canonical 十进制字符串传输；Session 与 Delivery heartbeat 分离；TaskPackage 嵌套且 hash 对齐；receipt 判别联合、未知字段和错误类型 fail closed；既有 MySQL 并发 / 租约 / receipt 行为不回归。
- 实际：Java Controller 改为 `/sessions`、`/sessions/{sessionId}/heartbeats`、`/deliveries:claim`、`/deliveries/{deliveryId}/heartbeats`、`/deliveries/{deliveryId}/receipts`，无旧 alias；`sessionEpoch / fencingToken / taskVersion / lastReceiptSequence` 即使为 `Long.MAX_VALUE` 仍输出字符串。claim header 幂等键与 body `claimRequestId` 分离；receipt `data` 按七种 kind 校验 required / allowed keys、时间、hash、reason、percent 和长度。Python httpx transport、Pydantic 模型与 simulator 同步 canonical 路径 / body / response，响应丢失仍冻结原回执。新增 Java Controller 契约测试 8/8；Java 全量 534/534；本机 MySQL 15 个持久化 + 4 个 migration，共 19/19；Python 25/25，Ruff 全通过，Pyright 0 error / 0 warning；契约门通过 4 Schema、2 OpenAPI、18 operation、26 正例、9 反例和 17 个配置键，三语言边界探针通过。
- 过程偏差：首次 Maven 定向命令的 `-Dsurefire.failIfNoSpecifiedTests=false` 未整体引用，被 PowerShell 解析为生命周期参数；修正引用后 8/8 通过。Python 首次使用系统解释器运行 lint 时发现 Ruff / Pyright 未安装于全局环境，随后使用工程 `.venv` 的 frozen 工具链重跑；一次 lint import 顺序和一次 Pyright 容器类型提示经机械格式化 / 显式类型修正后全绿。以上失败未计入通过结果。
- MySQL / 清理：MySQL Community Server 8.0.46 一次性实例仅监听 `127.0.0.1:4406`，随机数据库与模块 `target` 数据目录在脚本 `finally` 中清理；Docker Desktop 未安装、未调用，现有 `MySQL80` 未启动、未连接、未写入。
- 退出码：所有最终命令 0。
- 结果：Pass；IMP-05 Completed，进入 IMP-06。
- 限制：当前证明契约组件、真实 MySQL 账本与 Python MockTransport simulator；未冒充 Java↔Python 独立进程 HTTP E2E，该证据按批准任务保留到 IMP-09 的完整本地闭环。

## 8. IMP-06 完成证据

### EV-IMP06-OPERATOR-SSE-01 — Operator REST、投影、Outbox 与可恢复事件流

- 时间：2026-09-13 05:08～05:54（Asia/Shanghai）。
- 工作目录：仓库根、`platform-backend/ruoyi-aden/`、`contracts/aden/`。
- 命令：`mvn -f platform-backend/pom.xml -pl ruoyi-aden -am test`；`platform-backend/ruoyi-aden/scripts/run-local-mysql-tests.ps1`；`python contracts/aden/scripts/validate_contracts.py`；`python contracts/aden/scripts/probe_consumers.py`；`scripts/check-docs.ps1`；清理后核查临时目录、`mysqld.exe` 与 `MySQL80`。
- 实际实现：补齐 Workspace bootstrap 一致性快照、Task 创建 / 游标列表 / 详情 / 公开命令、强 ETag、Runner 摘要、四项 CapabilityProjection、最小审计查询及 SSE。游标使用 HMAC-SHA-256 并绑定 Workspace、用途、查询 / filter hash 和签发时间；`aden_event` 是唯一发送与 replay 来源，按 Workspace 提交序号连续性 fail closed，Outbox 只唤醒 drain。Aden 自有 drain / send 有界执行器不覆盖共享 MVC executor；连接采用用户 / Workspace / 全局上限、每连接有界唤醒队列、发送超时、硬 TTL、心跳权限复核和失败清理。429 / 503 返回 `Retry-After: 1` 与 `retryable=true`。
- 自动化覆盖：Operator canonical 路由、201 / Location / `Long.MAX_VALUE` ETag、命令联合类型、428；四种 command / cancel 权限组合与 VIEWER；HMAC tamper / foreign cursor、410 TTL、账本 gap；Outbox wake / ack / fail；SSE 顺序 replay、慢消费者溢出、send timeout、每用户连接上限；真实 MySQL 的任务分页、bootstrap 水位 / cursor、能力、Runner、Audit 和公开事件 allowlist。
- 最终结果：无数据库 Java `Tests run: 552, Failures: 0, Errors: 0, Skipped: 0`；本机 MySQL `AdenWorkspaceMySqlTest` 16/16 + migration 4/4，共 20/20；契约门为 4 Schema、2 OpenAPI、18 operation、26 正例、9 反例、17 个配置键，三语言边界探针通过；文档检查覆盖 239 个 Markdown 与 19 个 HTML，结构、中文命名和链接均通过；最终命令退出码均为 0。
- MySQL / 清理：MySQL Community Server 8.0.46 一次性实例仅监听随机 loopback 端口 `127.0.0.1:1811`；测试后临时实例目录 0、`mysqld.exe` 进程 0，现有 `MySQL80` 保持 Stopped / Automatic。Docker Desktop 未安装、未调用，也不是项目依赖。
- 跨项目限制：额外执行 `mvn -f platform-backend/pom.xml -pl ruoyi-admin -am -DskipTests compile` 时，在 Reactor 到达 Aden / Admin 前被 `ruoyi-fashion/FashionRequirementRunService.java` 两处缺少 `java.util.List` 的当前工作树编译错误截断；该文件不属于本 Feature，未越界修改。Aden 自身 151 个主源文件已独立编译并通过上述全量回归，此项不伪装为 Admin 全 Reactor 通过，留到 IMP-09 本地联调前复核。
- 结果：Pass；IMP-06 Completed，进入 IMP-07。
- 限制：当前证明单 JVM / 单实例进程级 broadcaster 与真实 MySQL 账本恢复，不宣称多实例 fan-out、生产 HA、MQ、真实账号或桌面 E2E；这些均不在本阶段目标内。

## 9. IMP-07 完成证据

### EV-IMP07-AGENT-FAKE-01 — provider-disabled 离线候选链

- 时间：2026-09-13 05:55～06:12（Asia/Shanghai）。
- 工作目录：`aden-agent-runtime/`。
- 命令：`uv lock`；`uv sync --extra test --frozen`；`uv run --extra test --frozen pytest`；`uv run --extra test --frozen ruff check .`；`uv run --extra test --frozen pyright`；`uv run --frozen aden-agent-runtime`；同一 fixture 两次执行 `uv run --frozen aden-agent-runtime tests/fixtures/agent-job.json` 并比较输出；本地 Schema / instance CLI；`uv build`。
- 实际实现：Python 3.12 / Hatchling / uv 工程包含 `bootstrap/contracts/client/runtime/agents/model_ports/tools/policy/observability` 九个有职责的逻辑包。Experimental AgentJob / RunSpec / ContextView / ToolManifest / Candidate 使用 `extra=forbid`、canonical UUID / int64 string、UTC 时间与判别联合；业务自有 `ModelAgentPort` 隔离 SDK，`DisabledModelPort` fail closed，`FakeModelPort` 产生确定性 proposal / no-action。三个产品 Agent Family 只注册且保持禁用，当前唯一可执行项为 `SYNTHETIC_CORE` 测试 harness。
- 安全与故障：runtime 记录 `CLAIMED → VALIDATED → MODEL_COMPLETED → CANDIDATE_VALIDATED → SUBMITTED`，模型调用前后和提交前分别复核取消、deadline、lease 与 fencing。工具有效范围为服务端 manifest 与本地 allowlist 交集，只接受 `read/proposal`；未知来源、重复引用、敏感 context / summary、超 candidate byte budget、失租、旧 fence、取消和迟到结果均拒绝。Fake transport 对同 hash 重放返回 `REPLAYED`，异 hash 冲突；源码静态扫描与运行时 socket 拦截证明正常路径无网络连接，也无 HTTP listener、数据库或 Provider Secret。
- 最终结果：pytest 31/31；Ruff 全通过；Pyright `0 errors, 0 warnings, 0 informations`；同一 fixture 两次输出相同 `candidateId=2b64fb06-c5c6-50d9-a191-0b6ac575dd12`、`resultHash=36135e79dac79fbf87bc405153adf516487d7cc812d1c3a429d0fcdd285bda2a`；构建 `aden_agent_runtime-0.2.0.tar.gz` 与 `aden_agent_runtime-0.2.0-py3-none-any.whl` 成功；最终命令退出码均为 0。
- 可选项：未安装 PydanticAI extra。该项在批准任务中明确为可选；当前自有端口与 Fake 已满足 AC-16，避免为了未启用的 Provider 扩大依赖与网络面。未来若验证，只允许 `TestModel / FunctionModel` 位于 `ModelAgentPort` 后。
- 结果：Pass；IMP-07 Completed，进入 IMP-08。
- 限制：没有 Agent Worker credential / session / job 服务端表和 API round-trip，没有真实模型、Provider、费用或业务质量承诺；Candidate 只提交给纯内存 Fake transport，不能据此推断 Java Task 已被 Agent 推进。

## 10. IMP-08 完成证据

### EV-IMP08-DESKTOP-SECURE-01 — Electron main / preload 安全传输基座

- 时间：2026-09-13 06:13～06:36（Asia/Shanghai）。
- 工作目录：`aden-desktop/`、`contracts/aden/`。
- 命令：`npm run contracts:generate`；`npm run typecheck`；`npm test`；`npm run build`；`npm run smoke:electron`。
- 实际实现：main composition root 拆为 `bootstrap/config/security/auth/transport/ipc`；窗口保持单实例、sandbox、context isolation、Node isolation，并默认拒绝 permission、webview、新窗口和非信任导航。打包态 API 只允许显式 HTTPS，未打包开发 / 测试只允许 loopback HTTP，credentials/query/fragment fail closed。REST client 冻结 Origin、使用 `redirect: manual`、限制超时与响应大小、拒绝非 JSON，并同时处理 HTTP 与 RuoYi `AjaxResult.code`；401 清 Token 与 epoch，403 保留登录态，Token 只存在 main 内存。
- SSE / IPC：main SSE 只接受 2xx `text/event-stream`，携带 Authorization 与最后 ack 的 `Last-Event-ID`；UTF-8 parser 只在完整空行产出单 data 事件，限制行、事件、data 和累计 buffer，半帧断线丢弃。事件经有界 batch + ack 队列送 renderer，只有匹配 workspace/session/workspace epoch 的 ack 才推进 cursor；超时、速率、队列、payload、gap、400/410、未知 Schema 或错误 content-type 进入 degraded/bootstrap。preload 只暴露 captcha/login/logout、Workspace bootstrap 和事件 ack / 订阅，不暴露任意 channel、URL、header、Token 或 Node 对象。
- 契约类型：`contracts/aden` current Schema 通过确定性脚本生成 immutable TypeScript 类型，build 前检查漂移；外部 Bootstrap/Event 仍经过严格 runtime boundary，`9007199254740992` 与 `9223372036854775807` 全程保留 canonical string，不经 JavaScript number。
- 自动化结果：Vitest `9 files / 46 tests` 全部通过，覆盖 301/302/307/308 同源/异源零重放、401/403/429/503、超时、非 JSON、超大正文、RuoYi 双层错误、真实 JPEG magic、Token 清理、SSE 超大/永不换行/半帧/content-type/gap、ack 阻塞、IPC sender/payload 和 packaged file URL。TypeScript strict 检查退出 0；`electron-vite` 生产构建生成 `out/main/index.js`、`out/preload/index.mjs`、`out/renderer/index.html`，入口与 renderer 受限标识扫描通过；隐藏 Electron 实窗验证 `require/process` 不可见且 `window.open` 被拒绝，退出 0。
- 过程偏差：第一次类型检查发现 web tsconfig 经 preload 类型导入拉入 main 实现，改为共享纯类型契约后通过；第一次 API 限额用例因测试上限同时截断预期的 RuoYi 错误响应而失败，调整为互不遮蔽的边界后完整重跑通过。两次失败均未计为通过证据。
- 结果：Pass；IMP-08 Completed，进入 IMP-09。
- 限制：本阶段没有交付业务页面、安装器、签名、Fuses、自动更新或持久登录；实窗 smoke 不访问后端。真实 RuoYi / Redis / MySQL / Runner / Electron 纵向闭环保留到 IMP-09，不能从本阶段推断 E2E 已通过。

## 11. IMP-09 完成证据

### EV-IMP09-RENDERER-E2E-01 — Task Center 与真实本地合成纵向闭环

- 时间：2026-09-13 06:36～09:36（Asia/Shanghai）。
- 关联：ADEN-TASK-056～064；AC-02/03/04/06/07/08/09/10/11/12/14/15/17。
- 实际实现：renderer 增加 Vue Router memory history、Pinia session/workspace/tasks/runners/connection/capabilities stores，以及登录、共享 Task Center、任务详情、Runner 状态、能力总览和 About。所有业务数据来自服务端 snapshot / allowedCommands；客户端通过 session/workspace epoch 丢弃迟到响应，以 ETag + Idempotency-Key 提交命令，通过 main→renderer batch ack 控制 SSE 背压，未知事件、gap 与 degraded 均回到 bootstrap。
- E2E 环境：Windows 11、Java 21.0.10（编译 release 17）、Spring Boot 4.0.6、MySQL Community Server 8.0.46 本机二进制、Redis 5.0.14.1、Electron 44.3.0、Node 24.15.0、Python 3.12.4 / uv 0.11.18。MySQL、Redis、RuoYi 均为本轮随机 loopback 端口和临时目录；不启动或连接 `MySQL80`，不使用 Docker Desktop。
- 命令：`npm test`、`npm run build`、`npm run smoke:electron`；`aden-desktop/scripts/run-synthetic-e2e.ps1`。E2E 默认先执行 `mvn -pl ruoyi-admin -am clean install -Dmaven.test.skip=true`，导入只读 RuoYi baseline，执行 Aden baseline/V1～V4/migrate/validate 和权限 SQL，再启动隔离 Redis、RuoYi、Electron 与独立 Python Runner。
- 实际场景：合成用户经真实 `/captchaImage → /login → /getInfo` 登录；真实 API 创建 Workspace、enroll Runner；页面创建并提交成功任务，Runner 完成 Session/claim/heartbeat/STARTED/PROGRESS/COMPLETED，SSE 页面进入 `SUCCEEDED`；第二任务提交后请求取消，Runner heartbeat 获得安全点指令并回执，页面进入 `CANCELED`；最后真实 `/logout` 回登录页。运行结束未捕获 renderer console error 或 pageerror。
- 过程偏差：首次整包启动依次暴露并修复 Boot 4 Jackson 3、多个 Clock / transaction manager 注入歧义、`final` 事务服务 CGLIB、非 clean admin 旧包、Electron sandbox 不接受 ESM preload、TaskPackage Java 纳秒与 Python 微秒 hash、SSE 事件名/非 TASK batch 未 ack、懒初始化未启动 Aden scheduler，以及退出登录时未处理异步拒绝。每次失败均未计为 Pass，修复后才完整重跑。
- 阶段结果：Pass；IMP-09 Completed。阶段收口时 Desktop 为 12 个文件 / 52 项测试、生产三入口 bundle 与实窗 smoke 通过，完整 E2E 输出 `ADEN_SYNTHETIC_E2E_OK`。
- 限制：无人值守 E2E 使用隔离 test profile 的 captcha-disabled 分支；captcha-enabled challenge 图片/格式和错误处理已有组件证据，但真实验证码的人工作答保留给 Stage 9 UAT。没有真实账号、Provider、业务数据、外部动作、UIA、安装包或发布。

## 12. IMP-10 审查与封板证据

### EV-IMP10-CODE-REVIEW-01 — 当前 Aden 差异审查与阻断项关闭

- 时间：2026-09-13 09:36～10:10（Asia/Shanghai）。
- 范围：按 `code-review-expert` 清单审查 `platform-backend/ruoyi-aden`、`aden-desktop`、`aden-runner`、`aden-agent-runtime`、`contracts/aden` 共 380 个非生成文件、约 27,628 行，并复核 RuoYi 窄集成配置。检查 SRP / DIP、鉴权与 Workspace 隔离、Secret、SQL/SSRF/路径、并发锁序、事务、幂等、资源上限、错误恢复、64-bit 边界与跨语言契约。
- P0：0。
- P1（已关闭）：3。其一，receipt retry 与 lease recovery 新建 TaskPackage 时未限制 `deadlineAt` 到微秒，Python 重建 canonical JSON 后可能 hash 不一致；两处统一 `ChronoUnit.MICROS` 并在真实 MySQL attempt 2 上断言嵌入 hash。其二，renderer 普通网络失败后再次点击会生成新 Idempotency-Key；现在相同 payload 复用原 key，命令同时冻结原 ETag，不同 payload 才生成新 key。其三，SSE 401 只清 renderer session，未立即清 Workspace 投影与路由；现在原子清投影、清会话并返回登录页。
- P2（已关闭）：2。E2E 仅关闭 Fashion image worker，干净聚合包新增的 delivery worker 仍可能实例化旧 Jackson 2 依赖；编排器显式关闭两项无关 worker。Windows `mysqld` 会派生实际监听子进程，原脚本持有的父 PID 不足以观测和兜底清理；编排器改为按随机端口解析实际 listener PID、核对命令行必须属于本轮目录，并按该 PID 关停。
- 开放问题：P0/P1/P2 均为 0。P3 残余为 Playwright Electron 启动时 Node 输出 `DEP0190`，当前参数全部由测试代码和随机本地路径生成，无用户输入，也不进入生产应用；后续升级 Playwright/Electron 时复核。
- 删除候选：没有生产源码可安全删除。清理了监听断言失败留下的一个精确 `test-results/aden-e2e-local-*` 临时目录和一张过期失败截图；二者均为可再生成测试产物，不影响源码与证据事实。
- 结果：Pass；审查结论 `APPROVE`（仅指当前 L2 合成 CORE 技术切片，不是用户验收或发布批准）。

### EV-IMP10-REGRESSION-02 — 全量自动化、契约与真实 MySQL

- 时间：2026-09-13 09:42～09:54（Asia/Shanghai）。
- Java：`mvn -f platform-backend/pom.xml -pl ruoyi-aden -am test`，`Tests run: 554, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS；数据库条件类在该命令中为 0，由下一条独立执行。
- MySQL：`platform-backend/ruoyi-aden/scripts/run-local-mysql-tests.ps1`，`AdenWorkspaceMySqlTest` 16/16、`AdenMySqlMigrationTest` 4/4，总计 20/20，BUILD SUCCESS。第一次新增纳秒测试用 `+789ns` 被 MySQL 四舍五入后触发领域更新时间门，未计为通过；改用仍能证明截断且不会越过数据库舍入边界的 `+100ns` 后完整重跑通过。
- Desktop：`npm test` 为 12 files / 55 tests；`npm run build` 完成契约生成漂移检查、TypeScript strict、`out/main/index.js`、sandbox 兼容 `out/preload/index.cjs` 和 renderer bundle；`npm run smoke:electron` 通过。第一次定向 npm 命令误在仓库根执行并因不存在 `package.json` 退出，未进入测试、未计 Pass；修正工作目录后重跑。
- Runner：`uv run --extra test --frozen pytest` 为 28/28；Ruff all checks passed；Pyright `0 errors, 0 warnings, 0 informations`。
- Agent Runtime：`uv run --extra test --frozen pytest` 为 31/31；Ruff all checks passed；Pyright `0 errors, 0 warnings, 0 informations`。
- 契约：`python contracts/aden/scripts/validate_contracts.py` 与 `probe_consumers.py` 通过 4 JSON Schema、2 OpenAPI、18 operations、717 refs、6 local bundles、26 正例、9 反例、17 个配置键和 Python/Node/TypeScript/Java 消费者探针。
- 文档：`scripts/check-docs.ps1` 检查 243 个 Markdown 与 19 个 HTML，五类目录、中文命名和相对链接全部 PASS。
- 结果：Pass；所有最终命令退出码 0。

### EV-IMP10-LOOPBACK-E2E-03 — 干净聚合包、真实 listener 与最终纵向回归

- 时间：2026-09-13 09:48～10:10（Asia/Shanghai）。
- 构建：`aden-desktop/scripts/run-synthetic-e2e.ps1` 的默认非跳过构建完成 RuoYi 18 模块 `clean install`，确保 `ruoyi-admin.jar` 包含当前 Aden；随后完成 Desktop production bundle 和 Runner frozen sync。
- 过程失败与修复：干净包首次启动被当前工作树 Fashion delivery worker 的旧 Jackson 2 依赖截断；E2E 只在启动参数关闭无关 `fashion.image.worker-enabled` 与 `fashion.delivery.worker-enabled`，未修改 Fashion 业务代码。第一次增加 listener 门时按 `Start-Process` 父 PID 查询 MySQL，但 Windows `mysqld` 实际派生子 PID，断言正确失败；用该轮 `mysqladmin` 关停并删除精确临时目录后，脚本改为按端口解析真实 PID 并复跑。
- 最终命令：`aden-desktop/scripts/run-synthetic-e2e.ps1 -SkipBuild`；仅复用同一轮刚完成的干净 Java/Desktop 产物，脚本和运行环境修复后重新创建全部临时数据与进程。
- 运行观测：MySQL `127.0.0.1:4448`、Redis `127.0.0.1:4449`、RuoYi `127.0.0.1:4450` 均由 `Get-NetTCPConnection` 按真实 listener 回读，并逐一核对 OwningProcess 命令行属于本轮目录或 `ruoyi-admin.jar`；任何非 `127.0.0.1/::1` 监听均会使 E2E 失败。源码目标扫描只命中 MyBatis DTD、精确 loopback 默认值和 `example.invalid` 安全拒绝 smoke，不存在真实微信、电商、ERP、Provider、UIA、Shell 或外发目标。
- 最终实际：`ADEN_SYNTHETIC_E2E_OK`；成功链、取消链、SSE 页面状态和 logout 均通过；无 renderer console/page error。`finally` 后无 Aden E2E 所有进程或 `aden-e2e-local-*` 目录，`MySQL80` 为 Stopped / Automatic。
- 结果：Pass。

### 12.4 AC 技术结果与 UAT 输入

| AC | 技术结果 | 主要证据 | Stage 9 用户动作 |
| --- | --- | --- | --- |
| AC-01～03 | Pass | 18 模块 clean build、554 Java、20 MySQL、真实 REST E2E | 浏览任务创建/提交状态 |
| AC-04 | Pass | 取消领域/数据库测试与真实 `CANCEL_REQUESTED→CANCELED` E2E | 确认取消文案与操作可理解 |
| AC-05～08 | Pass | MySQL 原子回滚、Inbox/Outbox、双 Runner、epoch/fence/lease/重复回执与 Runner 28 项 | 无额外动作 |
| AC-09～10 | Pass | Desktop 55 项、strict typecheck、CJS preload bundle、Electron smoke | 检查页面布局与导航 |
| AC-11～12 | Pass | SSE replay/gap/slow/ack 测试、bootstrap 恢复、Runner crash/retry/fence 测试及 E2E | 可选断网/刷新体验观察 |
| AC-13 | Pass | 静态目标扫描、三进程 PID+listener loopback 观测、provider-disabled Agent | 无额外动作 |
| AC-14～16 | Pass | 四能力投影/共享筛选、审计投影、Agent Runtime 31 项零外联 Fake | 查看四能力状态与下一门禁 |
| AC-17 | Pass（技术） | captcha enabled 图片/格式/错误组件测试；captcha disabled 真实登录/退出 E2E；401/403/Token/epoch 清理测试 | 在隔离 captcha-enabled 环境手工输入一次验证码；用户决定待确认 |

建议的最小 UAT 顺序：启动隔离环境 → captcha-enabled 登录 → 选择唯一合成 Workspace → 创建并提交成功任务 → 观察状态与详情 → 创建并请求取消 → 查看能力总览 → 退出登录。用户决定保持“待确认”；未发生验收时不创建或伪造 `验收记录.md`。

## 13. 剩余限制与非发布结论

- Docker Desktop / Docker CLI 不属于项目依赖；MySQL 验证直接使用已安装的 MySQL 8.0.46 可执行文件创建一次性随机端口实例，现有 `MySQL80` 未启动、未连接、未写入。
- 人工 captcha-enabled UAT、实机安装、签名、Fuses、自动更新、部署、生产 migration 和发布均未执行；Stage 9 用户决定为“待确认”，上线就绪因无部署目标为 `Skipped`，发布为 `NotReleased`。
- Python Agent 仍为 `provider_disabled` 的离线 Fake；Runner 仍为低权限合成 simulator。真实模型、个人微信、电商、ERP、桌面控制、Shell、任意文件、外部发送、下单与付款全部未实现、未授权、未调用。
- 当前只证明单个 `ruoyi-admin`、进程内 broadcaster/scheduler 和本机 loopback 拓扑；不宣称多实例 fan-out、生产 HA、MQ、真实账号或业务质量。
- 无 Git 暂存、提交、分支、推送、PR、标签或发布动作；工作区中原有的其他项目未提交修改继续由用户所有。
