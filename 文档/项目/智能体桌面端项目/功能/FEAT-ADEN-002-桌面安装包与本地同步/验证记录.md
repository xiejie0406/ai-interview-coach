# Aden 桌面安装包验证记录

> 文档类型：Feature 验证事实源；Feature：FEAT-ADEN-002；owner：Codex；创建与更新：2026-09-26  
> 当前结果：本地安装、配置、同步与安装版合成 UI 逐项复验 Pass；云端构建 NotRun；用户接受决定：待确认。

## 对象与环境

- Aden Desktop 0.2.0；Windows 11 10.0.26200 x64；Electron 44.3.0；electron-builder 26.16.1；PowerShell 7.6.5；Node 24.15.0。
- 本地安装路径：`C:\Users\admin\AppData\Local\Programs\Aden Local Test`；合成服务只使用随机 loopback 端口、临时 MySQL/Redis/RuoYi 和 Runner simulator，不使用真实账号或业务数据。
- 本地版 NSIS：`frontend/desktop/aden-desktop/dist/local-test/Aden-Local-Test-0.2.0-Setup.exe`；最终 SHA-256 `CA87EEB704A2D3D01D809928622FF87DB8C3B5770FF7296941788BDA35EBB742`。构建目录已被 `.gitignore` 排除。
- [逐项清单](证据/2026-09-26-安装版验收/验收清单.md)于 09:12 建立；早于清单的技术执行在下方如实保留，UI 截图只按清单建立后的复验记录。报告入口：[HTML 验收报告](证据/2026-09-26-安装版验收/verification-report.html)。

## 已发生的技术执行

| 时间（2026-09-26，本机） | 对象 / 命令与步骤 | 预期与实际 | 结果和限制 |
| --- | --- | --- | --- |
| 08:37 | `npm.cmd test -- tests/runtime-config.test.ts` | 原配置矩阵 6/6；新增 local-test 参数的回环 HTTP 规则通过 | Pass；后续进一步收紧为本地包只允许回环主机，见 09:14 复验 |
| 08:38–08:44 | `npm.cmd run pack:local` | 构建 `out` 和 NSIS；`win-unpacked/resources/app/out` 与 `local-test.json` 可读，安装器约 124 MB | Pass；未签名，默认 Electron 图标；构建器对 `asar:false` 提示风险，本渠道只用于本机合成测试 |
| 08:45 | NSIS `/S` 安装、安装版 `ADEN_DESKTOP_SMOKE=1` | 安装到当前用户路径；安全烟测退出 0 | Pass；当时为第一版安装器，最终安装器另于 09:19 复验 |
| 08:47 | `powershell -NoProfile -File ...` | Windows PowerShell 5.1 对现有 UTF-8 无 BOM 中文脚本解析报错，未启动服务 | Fail（调用环境）；改用已安装的 `pwsh`，没有修改业务代码 |
| 08:47–08:55 | `pwsh ... run-synthetic-e2e.ps1 -SkipBuild -InstalledDesktop` | 安装版实际从 `file:///C:/Users/admin/AppData/Local/Programs/Aden%20Local%20Test/resources/app/out/renderer/index.html` 加载，合成用户登录，Workspace 为 LIVE；Runner 首次在新工程路径构建超过 20 秒超时 | Fail（Runner 测试依赖未预热）；没有把整条流程计为通过。脚本恢复安装配置并关闭本轮 loopback 服务 |
| 08:55 | `uv sync --extra test --frozen`（`python/aden-runner`） | Runner 在新工程路径构建/同步成功 | Pass；只处理隔离合成测试依赖 |
| 08:55–08:59 | 同一已安装版 E2E 复验 | 输出 `ADEN_SYNTHETIC_E2E_OK`；合成登录、成功任务、取消任务和退出均执行 | Pass；这轮执行没有按后来发布的 r3 清单截图，后续逐项截图复验 |
| 09:00–09:04 | `npm.cmd run pack:release-candidate` | 生成未签名候选 NSIS；`resources/app.asar` 存在、`local-test.json` 不在发布包 | Pass（结构）；尝试启动 `dist/release/win-unpacked/Aden.exe` 被自动执行策略拒绝，发布形态 GUI 运行未验证；该包早于 09:10 的回环收紧改动，需最终重建 |
| 09:05–09:06 | `pwsh ... sync-local-install.ps1`、`npm.cmd test` | main/preload/renderer 哈希与新构建一致，旧 `out` 被保留为备份；完整桌面测试 56/56 | Pass；随后发现本地包还允许公网 HTTPS，已收紧并重建 |
| 09:10–09:19 | 终版 `npm.cmd run pack:local`、同步脚本、`npm.cmd test -- tests/runtime-config.test.ts`、NSIS `/S` 覆盖安装、smoke | 本地包只允许精确回环主机；6/6 配置测试，最终安装器安装退出 0，HKCU=1/HKLM=0，安装主进程与包内哈希一致，smoke 退出 0 | Pass；最终安装器哈希见上 |
| 09:17–09:21 | 清单建立后的首次 `-InstalledDesktop` 截图复验 | MySQL 迁移、Redis 启动成功；RuoYi 启动失败。默认 `ruoyi-admin.jar` 在本轮被并行构建写成 89,853 字节薄 JAR，缺少完整 Logback 依赖；尚未进入桌面 UI | Fail（共享构建产物冲突），UAT-03～05 保持 NotRun/待截图；临时服务和数据清理。修复为显式选择 08:19 的完整 JAR 并复制到本轮隔离目录后启动，未改动共享 JAR |
| 09:22–09:25 | `-BackendJarPath ruoyi-admin-interview.jar` 的隔离副本复验 | RuoYi、安装版登录和 Workspace LIVE 成功；合成任务达到 SUCCEEDED。登录页和 Workspace 两张实际截图已保存并回读；保存成功状态全页截图时 Playwright 30 秒超时，流程停止，取消与退出尚未执行 | Fail（截图采集）；UAT-03 保留局部证据、UAT-04/05 不计通过。改为可见区域截图、关闭动画并延长至 60 秒，待复验 |
| 09:26–09:29 | 调整为可见区域截图后重跑隔离 E2E | 新随机 MySQL 实例启动，但共享 `ruoyi-aden` 的 Maven 迁移 CLI 在此期间被其他构建改变，拒绝既有 `--password-env` 参数；没有进入桌面 UI | Blocked（共享构建环境持续变化）。脚本回收本轮临时实例；不再无依据重试。UAT-03/04 保留原失败与部分截图，UAT-05 未执行 |
| 09:30–09:33 | `npm.cmd run pack:release-candidate`、`pwsh ... sync-local-install.ps1`、安装目录回读与 smoke | 最终发布形态候选包构建成功，`app.asar` 存在、无 `local-test.json`、签名 NotSigned；安装目录三个入口与最终本地构建哈希一致，保留 `out-backup-*`，同步后 smoke 退出 0 | Pass（AC-03 和 AC-05 的结构部分）；发行形态 GUI 启动未验证，不能上线 |
| 09:41–09:44 | 共享迁移 CLI 重建后，按清单再次运行 `pwsh ... run-synthetic-e2e.ps1 -SkipBuild -InstalledDesktop -BackendJarPath ...ruoyi-admin-interview.jar` | 基线迁移与验证通过，RuoYi/Redis/MySQL 均只监听回环；安装版从固定 file URL 加载，合成登录、Workspace LIVE、任务 SUCCEEDED、任务 CANCELED、退出回登录页全部发生；输出 `ADEN_SYNTHETIC_E2E_OK`，五张逐项截图和 `e2e-result.json` 已保存。脚本结束后配置与临时服务已恢复/清理 | Pass（UAT-03/04/05 最终复验）；此前 Runner 超时、薄 JAR、截图超时和 CLI 参数冲突的失败记录仍保留，不覆盖 |

## 未完成及发布边界

- 最终安装版的五个关键 UI 状态已逐项截图并回读，UAT-03/04/05 最终复验 Pass；成功任务截图时连接徽标短暂处于 CONNECTING，不影响任务卡 SUCCEEDED 的判定，取消任务截图连接状态为 LIVE。用户对界面与限制的接受决定仍待确认。
- release candidate 是未签名候选，不是线上发布。签名、应用图标、HTTPS 服务部署/配置、自动更新、回滚、最终用户接受和对外发布仍缺；ASAR 本身不代表防篡改。
- `-SkipBuild` E2E 复用了 08:19 的 `ruoyi-admin.jar`，不作为本轮后端干净构建证据；本 Feature 没改 Java 代码。
- HTML 报告已生成；尝试用 Codex 内置浏览器打开本地 `file://` 文件时被 Browser use URL policy 自动拒绝，策略同时禁止以其他浏览器或本地 HTTP 服务间接绕过。报告浏览器视觉检查未执行，仅继续做静态结构和截图文件核对。
- 09:36 静态报告核对：清单与 HTML 都覆盖 UAT-01～06，结果一致（Pass 3、Fail 2、Blocked 1、NotRun 0）；HTML 的 9 个相对链接均存在，2 张实际截图文件均存在并已用本地图片查看工具逐张回读。此结果不提升被策略拒绝的浏览器视觉检查。
- 09:42 增加手动 GitHub Actions Windows 候选包流程；`.github/workflows/aden-desktop-windows-package.yml` 使用 `workflow_dispatch`、只读仓库权限、`npm ci` 锁定依赖、桌面测试、候选包结构与 SHA-256 检查并上传短期 artifact。首次用本地 Node `yaml` 模块解析失败（模块未安装）；改用已安装的 PyYAML BaseLoader，YAML、手动触发、只读权限和 7 个步骤静态解析通过。没有 Git 暂存/提交/推送或 GitHub Actions 实际运行，UAT-07 保持 NotRun。
- 09:45 最终报告静态核对：清单与 HTML 均有 UAT-01～07，结果一致（Pass 6、Fail 0、Blocked 0、NotRun 1）；HTML 的 19 个相对链接全部存在、5 张实际截图文件全部存在并逐张回读。Codex 内置浏览器对本地文件的 URL 安全策略拒绝仍有效，报告浏览器视觉检查未执行。
- 09:52 已安装版人工体验窗口启动：脚本先完成 `ADEN_SYNTHETIC_E2E_OK` 自检，再在本机随机回环 API `127.0.0.1:1348` 开启图片验证码并打开 `Aden Local Test.exe`；安装版主进程 PID 35908 可回读。此人工窗口供用户操作，不属于 09:44 已封存的逐项截图证据。用户关闭窗口后脚本会恢复安装配置并清理本轮临时服务；窗口运行期间本轮临时实例仍在运行。
- 09:58–10:04 按并行环境隔离要求开展新一轮归属复核和定向验收，见[本轮清单](证据/2026-09-26-并行环境复验/验收清单.md)与[HTML 报告](证据/2026-09-26-并行环境复验/verification-report.html)。本轮 5 Pass、1 Blocked（当前安装版被人工使用、源码和安装路径共享，未重跑同步）、1 NotRun（云端作业）。旧安装版截图经安装包、exe、三个入口文件和后端 JAR 版本核对后逐张复用；没有冒充新截图。配置矩阵 6/6 Pass，25 个报告相对链接均存在，5 图回读。内置浏览器的本地报告 URL 策略阻止视觉检查；没有切换通道绕过。未修改共享服务或数据、未执行 Git 写操作。
