# Aden 安装版并行环境复验清单

> 对象：FEAT-ADEN-002，Aden Desktop 0.2.0；执行日期：2026-09-26；执行者：Codex；用户接受决定：待确认。  
> 本轮为并行环境归属复核和定向验收。上一轮证据见[安装版验收清单](../2026-09-26-安装版验收/验收清单.md)及[原始验证记录](../../验证记录.md)。上一轮结果不自动计作本轮结果。

## 执行前环境与边界

- 代码目录：`D:/2025Ai/26-05-23/java-workspace`；Git HEAD `fe35a459ff92d913fe6bde293b2340add392e14f`、`main`，`git worktree list` 只发现这一处工作树。Aden 桌面目录、`ruoyi-aden` 和本 Feature 目录均为未跟踪目录；同一工程有其他任务的修改，不能用 HEAD 代表本次完整代码版本。
- 安装版：`C:/Users/admin/AppData/Local/Programs/Aden Local Test/Aden Local Test.exe`；页面入口为 `file:///C:/Users/admin/AppData/Local/Programs/Aden%20Local%20Test/resources/app/out/renderer/index.html`。本 Feature 无独立网页前端 URL；其他任务的 5173/5174 页面及 8081 后端不纳入本轮操作。
- 当前交互测试实例：临时目录 `frontend/desktop/aden-desktop/test-results/aden-e2e-local-4d29bd8af29d44b7b13861ba633c86c8`；安装版主进程 PID 35908，隔离用户资料在该目录的 `electron-profile`；RuoYi PID 28280 监听 `127.0.0.1:1348`，运行同目录的 `ruoyi-admin-e2e.jar` 副本；MySQL PID 35112 监听 `127.0.0.1:1346`，数据库名 `aden_e2e`；Redis PID 35252 监听 `127.0.0.1:1347`、数据库号 15。进程归属以命令行路径、PID 和端口共同核对。此实例仍供用户手动体验，不执行停止、重启或写入。
- 合成数据：上一轮脚本在隔离实例创建一名 `aden_e2e` 操作员、一个 Workspace 与两项任务；上一轮完成后清理。当前交互实例由同一脚本新建，已进行合成自检，但用户可能继续操作，故本轮不假定现有行数，也不查询或更改其业务数据。
- 共享风险：安装目录是当前用户的单份安装，源码目录和 `ruoyi-admin/target` 是共享路径；即使本轮服务端口独立，也不构成完整任务隔离。不得借本轮覆盖安装目录、共享 JAR、共享前端代理或其他任务进程。
- 网页浏览器通道：默认 Codex 内置浏览器；本 Feature 的业务 UI 是 Electron 安装版。前轮尝试在内置浏览器打开本地 HTML 报告，受到 URL 安全策略拒绝；不得换浏览器或本地 HTTP 间接绕过。报告视觉检查另记真实结果。

## 执行前逐项清单

每项初始证据结果均为 `NotRun`；适用 UI 的截图状态均为“待截图”。非 UI 项的截图状态为“不适用”，使用文件、注册表、输出或日志作为证据。旧证据先核对文件版本和对应步骤，再决定能否在本轮复用；无法核对则保留历史，不计本轮通过。

| ID | 目标 / AC | 前置、角色、输入与步骤 | 可判定预期 | 所需证据 | 初始结果 | 初始截图状态 |
| --- | --- | --- | --- | --- | --- | --- |
| ADEN-PKG-UAT-01 | 当前用户安装与可读代码目录 / AC-01 | 只读核对本机安装器、安装目录、当前用户卸载项、`resources/app/out` | 安装版位于当前用户目录，编译文件存在且可读，与上一轮安装器版本一致 | 安装器和 exe 哈希、注册表及路径回读 | NotRun | 不适用：文件和注册表回读 |
| ADEN-PKG-UAT-02 | 本地 API 配置安全 / AC-02 | 只读核对已安装配置、运行版主程序哈希与既有配置测试证据 | 当前交互实例指向精确回环地址；配置规则的代码版本与前轮一致 | 配置摘要、文件哈希、既有配置矩阵结果 | NotRun | 不适用：配置和测试输出 |
| ADEN-PKG-UAT-03 | 安装版登录、工作区与退出 / AC-04 | 合成操作员；比对安装版三个入口文件与上一轮 E2E 版本，回读前轮登录、LIVE、退出结果及三张截图；不抢占当前人工窗口 | 同一版本证据显示安装目录加载页面、登录后 LIVE、退出回登录页 | E2E JSON、三张可辨截图、版本哈希 | NotRun | 待截图；旧图核对后可复用 |
| ADEN-PKG-UAT-04 | 合成任务成功 / AC-04 | 同一隔离合成 E2E；回读 `SUCCEEDED` 终态及截图，核对安装版版本 | 任务终态 `SUCCEEDED` 可见，结果记录一致 | E2E JSON、成功状态截图、版本哈希 | NotRun | 待截图；旧图核对后可复用 |
| ADEN-PKG-UAT-05 | 合成任务取消 / AC-04 | 同一隔离合成 E2E；回读 `CANCELED` 终态及截图，核对安装版版本 | 任务终态 `CANCELED` 可见，结果记录一致 | E2E JSON、取消状态截图、版本哈希 | NotRun | 待截图；旧图核对后可复用 |
| ADEN-PKG-UAT-06 | 源码同步和发布候选结构 / AC-03、AC-05 | 不执行共享路径写入；比对源码构建与安装目录哈希，回读此前同步日志与发布候选包结构、签名 | 已有同步证据匹配当前文件，候选包含 ASAR 且无本地配置；候选 GUI 运行单独说明 | 三入口哈希、历史同步结果、候选包结构 | NotRun | 不适用：文件与日志回读 |
| ADEN-PKG-UAT-07 | 云端手动打包 / AC-06 | 查验手动 workflow 与实际 GitHub Actions run、artifact、哈希 | 作业实际通过、下载产物可核对 | workflow、run URL、日志、artifact | NotRun | 不适用：云端日志和产物 |

## 执行记录

按编号逐项追加实际输入、操作时间、预期、实际、结果、截图状态、证据路径和限制；共享环境冲突必须保留 `Blocked`，不得用旧结果覆盖。

### 2026-09-26 09:58–10:04 归属和版本核对

- `git worktree list --porcelain`：仅上述 `main` 工作树；`git status --short` 共 4014 条状态记录（随其他任务变化），不能据此拆分归属。桌面源码、`ruoyi-aden`、本 Feature 和云端 workflow 均未跟踪，未执行 Git 写操作。
- 交互测试的 `pwsh` 父进程 PID 25056 仍在；安装版主进程 PID 35908，RuoYi 1348/PID 28280，隔离 MySQL 1346/PID 35112，Redis 1347/PID 35252 均在。共享 8081/PID 30752 与前端 5173/PID 4364、5174/PID 18268 未操作。这里仅证明进程与监听归属；没有读取当前合成库行数或探测跨任务 Cookie，不能据此宣布完整隔离。
- 本地安装器 SHA-256 `CA87EEB704A2D3D01D809928622FF87DB8C3B5770FF7296941788BDA35EBB742`；安装版 exe 与本地包解包 exe SHA-256 均为 `D7DC24D551F669203CBCD72F605DF537EC6E8887C2C0171E036F6C94BF585221`。`out` 源目录与当前安装目录均为 5 个文件，逐文件哈希差异 0；与本地包 `win-unpacked` 三入口相同。主进程入口哈希 `90B37870D5A74E3C36974621C472E0F2917A9710752F9B7515039D6E919DC6F3`，preload 为 `706D217305FCEB8A3E9277741F218FD43999A36F2217977F0721EC43AA722012`，renderer HTML 为 `2EC43F699C0C270A4AAFD44F02ABAFBA7346113EC3B3BDCAE261629F96639C45`。
- 当前 1348 后端使用临时目录内的 JAR 副本，SHA-256 `5977D5B7279BF80D1206AEF3934723DD1914190DDAE70525B0936FE3E81E135D`，与脚本指定的 08:19 完整 JAR 相同。安装配置 `channel=local-test`、API 主机 `127.0.0.1`、端口 1348；未记录凭据。
- [前轮 E2E JSON](../2026-09-26-安装版验收/screenshots/e2e-result.json)完成于 09:44，记录安装目录 file URL、两项合成任务终态 `SUCCEEDED`/`CANCELED`。本轮逐张打开并核对前轮五张截图：登录表单、Workspace LIVE、退出后空白表单、任务 SUCCEEDED、任务 CANCELED 均可辨认。因安装 exe、三个入口文件和临时后端 JAR 与前轮记录所绑定的本地包/运行产物一致，按规范复用这五张同版本截图；它们仍是 09:44 的历史实际操作，并非 10:04 新拍摄。

### 本轮逐项结果

| ID | 本轮实际输入与操作、预期和实际 | 证据结果 | 截图状态与证据路径 | 限制 |
| --- | --- | --- | --- | --- |
| ADEN-PKG-UAT-01 | 10:00 只读回查安装器、exe、安装路径、HKCU/HKLM 卸载项和 5 个 `out` 文件；预期当前用户安装且与包一致；实际 HKCU 有 `Aden Local Test 0.2.0` 1 项，HKLM 两处 0 项，安装 exe 与解包 exe 同哈希，`out` 差异 0。 | Pass | 不适用；本节哈希、注册表及路径回读 | 没有在本轮重装，安装动作证据沿用[前轮记录](../2026-09-26-安装版验收/验收清单.md)。 |
| ADEN-PKG-UAT-02 | 10:03 读取安装配置并执行 `npm.cmd test -- tests/runtime-config.test.ts`；预期本地版只允许精确回环主机，release 仅 HTTPS，拒绝凭据/query/fragment；实际 6/6 测试通过，当前安装配置指向 `127.0.0.1:1348`。 | Pass | 不适用；测试输出及上方配置摘要 | 只验证配置规则和本机当前设置，不代表公网/正式服务可用。 |
| ADEN-PKG-UAT-03 | 回读 09:44 同版本安装版 E2E：预期登录后 Workspace LIVE、退出回登录；实际 JSON 确认安装目录 file URL，三张图依次显示登录、LIVE 和退出后空表单。 | Pass（复用同版本证据） | 已截图并逐张回读：[登录](../2026-09-26-安装版验收/screenshots/ADEN-PKG-UAT-03-login.png)、[LIVE](../2026-09-26-安装版验收/screenshots/ADEN-PKG-UAT-03-workspace.png)、[退出](../2026-09-26-安装版验收/screenshots/ADEN-PKG-UAT-03-logout.png)、[结果 JSON](../2026-09-26-安装版验收/screenshots/e2e-result.json) | 本轮不抢占正在供用户体验的窗口；没有新登录操作或新截图。 |
| ADEN-PKG-UAT-04 | 回读 09:44 同版本安装版 E2E：预期合成任务到 `SUCCEEDED`；实际 JSON 与截图均显示终态。 | Pass（复用同版本证据） | 已截图并回读：[成功任务](../2026-09-26-安装版验收/screenshots/ADEN-PKG-UAT-04-succeeded.png)、[结果 JSON](../2026-09-26-安装版验收/screenshots/e2e-result.json) | 截图时连接徽标短暂为 CONNECTING，不影响任务终态判断；本轮未重复写入。 |
| ADEN-PKG-UAT-05 | 回读 09:44 同版本安装版 E2E：预期另一合成任务到 `CANCELED`；实际 JSON 与截图均显示终态。 | Pass（复用同版本证据） | 已截图并回读：[取消任务](../2026-09-26-安装版验收/screenshots/ADEN-PKG-UAT-05-canceled.png)、[结果 JSON](../2026-09-26-安装版验收/screenshots/e2e-result.json) | 本轮未重复写入。 |
| ADEN-PKG-UAT-06 | 只读比对当前 `out` 与安装目录 5/5 文件哈希一致，回读前轮同步退出码 0 和备份；发布候选 `app.asar` 存在、无本地配置、签名为 NotSigned。预期本轮重新改源码、同步和重启时仍保持一致；当前安装版窗口正运行，唯一安装目录会被同步覆盖，源码/构建路径也共享，故未执行本轮同步。 | Blocked | 不适用；本节哈希、[前轮同步记录](../../验证记录.md)及候选包结构回读 | 阻塞仅针对本轮重新同步与重启；此前同步成功不改写。发布候选 GUI 运行仍未验证。需在用户结束体验并获得独立安装/构建资源或无冲突窗口后定向复验。 |
| ADEN-PKG-UAT-07 | 只读核对 `.github/workflows/aden-desktop-windows-package.yml` 仍为手动 workflow；预期实际云端作业、artifact 与哈希；本地文件未跟踪，暂无运行 URL 或云端产物。 | NotRun | 不适用；workflow 配置、无 run/artifact 的事实记录 | 未获 Git 写授权，不推送或触发云端作业；静态配置不计云端通过。 |

本轮统计：Pass 5、Fail 0、Blocked 1、NotRun 1、NotApplicable 0。前轮 6 Pass/1 NotRun 的历史结论保持原样；两轮的执行条件和结果分开。用户接受决定仍为“待确认”。

### 报告交付核对

- 已生成[本轮 HTML 报告](verification-report.html)。静态核对 7 个 UAT ID 均在清单和报告中、结果一致；报告 25 个相对证据链接无缺失、5 张图片文件均存在且已逐张回读。
- 已调用 Codex 文件面板打开报告源码，结果为 queued；此前 Codex 内置浏览器对本地 `file://` 报告的 URL 安全策略明确拒绝并禁止借其他浏览器或本地 HTTP 间接绕过。**报告浏览器视觉检查未执行**；不把静态检查记为视觉通过。
- 执行后再次核对 1346/1347/1348 监听 PID 分别仍为 35112/35252/28280；未停止、重启、覆盖或写入这些服务。Git 未暂存、提交、推送、创建分支或工作树。
