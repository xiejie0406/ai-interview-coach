# Aden Local Test 安装版逐项验收清单

> 对象：Aden Desktop 0.2.0，本地测试安装版与未签名发布形态候选包；环境：Windows 11 当前用户、合成隔离 MySQL/Redis/RuoYi/Runner；执行者：Codex；清单建立：2026-09-26 09:12（此前技术验证另记）  
> 数据：隔离库中的合成操作员、Workspace 和 2 项合成任务；测试后临时服务与数据库清理。用户接受决定：待确认。

本清单按 2026.09-r3 验收规范建立。每项初始结果为 NotRun；适用 UI 截图初始为“待截图”。执行中保留失败尝试、修复与复验，不把先前未截图的运行追记为已截图。实际步骤和结果更新在[验证记录](../../验证记录.md)，HTML 展示见[验收报告](verification-report.html)。

| ID | 目标 / AC | 前置、角色与计划步骤 | 可判定预期 | 所需证据 | 初始结果 | 初始截图状态 |
| --- | --- | --- | --- | --- | --- | --- |
| ADEN-PKG-UAT-01 | 本机安装与代码目录 / AC-01 | 当前用户运行 Local Test NSIS；回读 HKCU 卸载项与 `resources/app/out` | 安装成功、无 HKLM 覆盖，编译文件可读 | 安装器哈希、路径/注册表/文件回读 | Pass | 不适用：目录和注册表以回读记录 |
| ADEN-PKG-UAT-02 | 本地配置安全 / AC-02 | 读取安装配置；用精确回环及非回环地址运行配置矩阵 | 本地版仅回环主机，发布版仅 HTTPS | [配置矩阵结果](../../验证记录.md)、安装配置回读 | Pass | 不适用：规则以测试输出和配置回读记录 |
| ADEN-PKG-UAT-03 | 安装版登录和退出 / AC-04 | 合成操作员在已安装 exe 登录、进入 Workspace、退出 | 安装目录加载页面，登录后工作区 LIVE，退出回登录页 | [登录页](screenshots/ADEN-PKG-UAT-03-login.png)、[工作区](screenshots/ADEN-PKG-UAT-03-workspace.png)、[退出页](screenshots/ADEN-PKG-UAT-03-logout.png)、[E2E 输出](screenshots/e2e-result.json) | Pass | 已截图：三张逐张回读 |
| ADEN-PKG-UAT-04 | 合成任务成功 / AC-04 | 已登录操作员创建、提交任务，Runner 模拟器执行 | 任务显示 SUCCEEDED | [成功状态截图](screenshots/ADEN-PKG-UAT-04-succeeded.png)、[E2E 输出](screenshots/e2e-result.json) | Pass | 已截图：SUCCEEDED 可辨 |
| ADEN-PKG-UAT-05 | 合成任务取消 / AC-04 | 已登录操作员创建、提交另一任务并请求取消 | 任务显示 CANCELED | [取消状态截图](screenshots/ADEN-PKG-UAT-05-canceled.png)、[E2E 输出](screenshots/e2e-result.json) | Pass | 已截图：CANCELED 可辨 |
| ADEN-PKG-UAT-06 | 代码同步和发布形态 / AC-03、AC-05 | 退出安装版后同步 `out`；回读哈希并重新启动；检查 ASAR 候选包 | 同步内容一致、旧版可恢复；候选包含 ASAR 且无本地配置 | 同步输出/哈希、启动退出码、候选包结构与签名状态 | Pass（结构与同步） | 不适用：以文件哈希、启动码和包结构记录 |
| ADEN-PKG-UAT-07 | 云端手动打包 / AC-06 | GitHub Actions 手动运行 Windows 构建，回读 artifact 与哈希 | 云端作业通过，下载的候选安装器及哈希可核对；不发布 | workflow 配置、实际 run URL、日志和 artifact 哈希 | NotRun | 不适用：云端运行日志和 artifact 回读 |

验收范围不含真实账号、真实业务数据、签名发布或用户接受决定。发行包 GUI 启动若被执行策略阻止，按实际标 Blocked，不以文件存在替代。

## 已执行更新

- 2026-09-26 09:14，UAT-02：`npm.cmd test -- tests/runtime-config.test.ts`，6/6 Pass。最终本地版配置保留 `http://127.0.0.1:8081`；测试包含本地包拒绝非回环 HTTP/HTTPS、发布包拒绝 HTTP、凭据/query/fragment 拒绝。截图不适用。
- 2026-09-26 09:19，UAT-01：最终安装器 SHA-256 `CA87EEB704A2D3D01D809928622FF87DB8C3B5770FF7296941788BDA35EBB742`；`/S` 当前用户安装退出码 0，HKCU 卸载项 1、HKLM 0；安装版 `resources/app/out/main/index.js` 与最终包内同文件哈希一致，`resources/app/local-test.json` 可读；安装版 smoke 退出码 0。截图不适用。
- 2026-09-26 09:25，UAT-03/04：安装版从固定安装目录加载，登录和 Workspace LIVE 成功，任务到达 SUCCEEDED；[登录页](screenshots/ADEN-PKG-UAT-03-login.png)与[工作区](screenshots/ADEN-PKG-UAT-03-workspace.png)已逐张回读。成功状态的全页截图 30 秒超时，E2E 因此退出；UAT-03 缺退出步骤，UAT-04 缺要求截图，均保留 Fail 并复验。UAT-05 尚未运行。
- 2026-09-26 09:29，UAT-03～05 复验：改为可见区域截图后再次启动隔离环境，但共享 Maven 迁移 CLI 拒绝原脚本参数，未进入桌面 UI。UAT-03/04 的原失败和局部截图保留；UAT-05 记 Blocked，截图待采。临时数据库与进程由脚本清理。
- 2026-09-26 09:33，UAT-06：最终 `npm.cmd run pack:release-candidate` 退出码 0，`pwsh -NoProfile -File scripts/sync-local-install.ps1` 退出码 0；main/preload/renderer 三入口同步后哈希全部一致，安装目录保留 1 个 `out-backup-*`，同步后 smoke 退出码 0，配置恢复为默认回环地址。发布候选 `app.asar` 存在、本地配置不存在，安装器 122,221,872 字节，签名状态 NotSigned。发布候选 GUI 启动曾被自动执行策略拒绝，故本项 Pass 只覆盖清单中的结构与同步断言，不代表发布运行或上线就绪。
- 2026-09-26 09:41，新增 UAT-07：用户要求区分本地与线上打包；此前仅有本机构建，追加云端手动流程验收。无 Git 写授权且未触发 GitHub Actions，结果保持 NotRun。
- 2026-09-26 09:42，UAT-07 配置静态核对：手动 workflow 的 YAML 可解析，包含只读权限与 7 个步骤；无云端 run URL、日志或 artifact，本项仍为 NotRun。
- 2026-09-26 09:44，UAT-03/04/05 复验：已安装 exe 从固定安装目录加载；隔离合成 E2E 输出 `ADEN_SYNTHETIC_E2E_OK`，[结果 JSON](screenshots/e2e-result.json)记录两任务终态 SUCCEEDED/CANCELED；登录、Workspace LIVE、成功、取消、退出五张截图均保存并逐张回读。结果从此前 Fail/Blocked 更新为 Pass，截图状态“已截图”；前两轮失败事实保留在上文和[验证记录](../../验证记录.md)。测试后配置回到 `http://127.0.0.1:8081`，本轮临时后端监听已关闭。截图时成功任务页连接徽标短暂为 CONNECTING，但任务终态 SUCCEEDED 明确可见；取消页连接状态为 LIVE。
