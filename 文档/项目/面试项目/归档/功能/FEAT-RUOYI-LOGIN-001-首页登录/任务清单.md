# 主页 RuoYi 登录迁移任务与执行包

> 文档类型：任务
> 文档状态：Draft
> owner / 责任边界：platform / identity / frontend
> 创建时间：2026-08-23
> 更新时间：2026-08-23
> Feature ID：FEAT-RUOYI-LOGIN-001
> 风险等级：L3
> 产出/适用阶段：6 任务拆分
> 阶段状态：InProgress
> 关联：[`功能规格.md`](功能规格.md)、[`技术设计.md`](技术设计.md)

## 1. TASK 索引

| TASK | 目标 | 文件范围 | 验证 | 状态 |
|---|---|---|---|---|
| TASK-01 | RuoYi API client、Token、错误与代理 | `frontend/src/shared/api/**`、`frontend/src/shared/session/**`、`frontend/vite.config.*` | typecheck/build、静态扫描 | Completed（实现） |
| TASK-02 | 登录页验证码、四字段、登录后 getInfo/getRouters | `frontend/src/features/identity/**`、router/layout | 浏览器/Network | Completed（实现；运行验证 Blocked） |
| TASK-03 | 停用注册和旧 profile/me 调用 | identity pages/api、session | 静态扫描 | Completed（实现） |
| TASK-04 | 跨端旧入口和代理冲突清单 | `apps/portal-web`、`apps/mobile`、`apps/admin-web` | 只读扫描/文档 | Completed（实现/扫描） |
| TASK-05 | 后端主体边界复核 | `apps/platform-backend/ruoyi-interview` | 代码审查/403 | InProgress（运行验证待授权凭据） |

## 2. 执行包

- 允许修改：本 Feature 目录、`frontend/src`、`frontend/vite.config.ts/js`；必要的跨端代理/兼容包装仅限不创建旧认证状态。
- 允许验证：`npm run build`（frontend）、RuoYi `mvn -DskipTests package` 与启动/HTTP 冒烟、Playwright 流程和 Network 检查。
- 禁止：删除/移动历史文件、数据库写入或迁移、发布、Git 操作、记录密码/Token/Cookie/Secret。
- 停止条件：出现第二套 Token/Session、旧 auth 写入、前端信任 user_id/role/tenant、秘密泄露或后端主体无法从 SecurityContext 获取。
