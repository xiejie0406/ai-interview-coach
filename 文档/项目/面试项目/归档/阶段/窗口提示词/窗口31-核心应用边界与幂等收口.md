# 窗口 31 提示词：Core Application 边界与幂等收口

```text
你负责审查并修正窗口 23 产出的 Core Application 候选，使逻辑域边界、幂等回放、身份会话、owner 隐藏和平台端口语义可被后续 Evaluation/Voice/Adapter 安全复用。不得把静态修正描述为已运行。

项目目录：
D:\2025Ai\26-05-23\ai-interview-coach

启动条件：
- 窗口 23 已停止写入并完成交接。
- 开始前读取窗口 23 实际新增文件；不要按建议类名假定实现存在。
- 不得与窗口 23 或 28 并发编辑 application/interview。

只允许编辑：
- backend/interview-application/src/main/java/com/aiinterviewcoach/application/identity/**
- backend/interview-application/src/main/java/com/aiinterviewcoach/application/catalog/**
- backend/interview-application/src/main/java/com/aiinterviewcoach/application/practice/**
- backend/interview-application/src/main/java/com/aiinterviewcoach/application/interview/**
- backend/interview-application/src/main/java/com/aiinterviewcoach/application/billing/**
- backend/interview-application/src/main/java/com/aiinterviewcoach/application/platform/**
- backend/interview-application/src/main/java/com/aiinterviewcoach/application/governance/**
- backend/interview-application/src/main/java/com/aiinterviewcoach/application/shared/**（仅真正跨域且无业务 owner 的最小类型）

禁止修改：
- domain、adapters、boot、contracts、frontend、POM、migration、测试代码、docs/product、docs/architecture/技术架构.md、docs/phases/phase-*.md。

必须修正 A：逻辑域边界
1. 禁止一个逻辑域 import 另一个逻辑域的 `internal` 包；特别检查 practice/interview/billing/governance 对 `identity.internal.ActivePrincipalGuard` 的依赖。
2. 身份状态校验通过 identity 公共 application port/use case 暴露最小 Principal 决策；不要把 IdentityRepository 或内部 service 暴露给其他域。
3. `ServerSideDigest`、OperationAccepted 重放等若确实跨域，放入职责明确的 public platform/shared 边界；不要让业务包依赖 `platform.internal`。
4. application 继续 framework-free，不依赖 adapters/boot/Spring/JPA/HTTP。

必须修正 B：幂等四态
1. 每个使用 IdempotencyGuard 的命令都必须显式处理 NEW、IN_PROGRESS、REPLAY_SUCCESS、REPLAY_FAILURE；禁止 REPLAY_FAILURE fall-through 后再次修改聚合、扣额度、创建 Job/Outbox 或发外部请求。
2. REPLAY_SUCCESS 必须返回第一次保存的精确结果，包括 resource IDs、HTTP 语义所需字段、statusPath/streamPath 和原 acceptedAt；禁止用当前请求时间重建异步回执。
3. 同 key 不同 request hash 稳定冲突；同 key 同请求只返回原结果。幂等记录、业务聚合、Job/Outbox 必须由同一本地事务 owner 提交。
4. 对异常路径明确何时调用 failReplayable；未记录的系统异常不能伪装成功，也不能静默允许同 key 重做不可逆 effect。

必须修正 C：注册与 Session
1. 当前 `TenantId.of("public")` 与 migration 的 tenant FK 不一致。禁止插入虚构默认 tenant 或删除 FK 来掩盖问题。
2. 为匿名注册幂等建立显式 global/pre-tenant scope port/模型，或在该决策未关闭时让注册候选 fail closed 并登记 migration/port 缺口；不得声称当前链路可持久化。
3. 注册的 proof 验证、identifier 占用、User/PersonalTenant/Membership/Consent、幂等结果必须有清晰事务与一次性 proof 重放语义。
4. 对齐当前 identity.yaml：register、login、logout、getCurrentAccount、updateProfile；不得凭旧提示词添加不存在的 refresh endpoint。若账号找回渠道/策略仍 Pending，保持 unavailable，不猜写。
5. Login 的 Idempotency-Key 必须进入边界；Session issue/rotate/revoke 不得因重试产生多个未受控 Session。

必须修正 D：owner/version/失败语义
1. tenant 内其他用户资源不得返回能泄漏存在性的 POLICY_DENIED/403；按公共契约统一 owner-scoped 404。
2. 所有 mutation 重验 expected aggregate/item version；server-owned Worker 也必须重验 lease/attempt/version。
3. Interview/Practice/Billing 的异步受理必须确有 durable Job/Outbox；没有 Agent route/ActionGuard 的 RunInterviewStep 继续明确 unavailable，不能 mock 问题或成功。
4. Governance 至少列明 Consent、Audit append、Privacy deletion orchestration、Admin permission 的公共 port/use-case 缺口；有现成领域模型才实现，没有则 fail closed，不跨范围修改 domain。
5. answer、transcript、prompt、registration proof、identifier、session handle、idempotency key 不进入 toString、异常 metadata、事件或日志正文。

只读静态检查：
- 跨逻辑域 `application.<domain>.internal` import 为 0。
- 所有 IdempotencyGuard 使用点都有四态处理；列出逐文件结果。
- 所有 OperationAccepted replay 使用原始 acceptedAt/paths/IDs。
- 匿名注册不再隐式依赖不存在的 public tenant。
- identity OpenAPI operation 到 use case 的映射与缺口清晰。
- package/path/import 可定位；无反向依赖、无 PaiCLI 引用、无敏感 toString。

不得运行构建、测试、服务、migration、外部系统或 Git。

交接必须列出：
- 修改文件。
- 跨域 internal import 修正表。
- IdempotencyGuard 四态覆盖表。
- identity register/login/logout/getCurrentAccount/updateProfile 映射与仍 Pending 的账号找回决策。
- 需要窗口 30/24/29 承接的 persistence/REST/security/boot 缺口。
- 未执行项与证据状态 NotRun。
```
