# 窗口 50 提示词：RuoYi 语音运行链路剩余收口

> 文档类型：Phase / 执行提示模板
> 文档状态：Draft
> owner / 责任边界：platform / interview / frontend / quality；只处理窗口 49 实施后的剩余缺口
> 创建时间：2026-08-23
> 更新时间：2026-08-23
> Feature / 任务 ID：FEAT-INTERVIEW-001 / TASK-RUOYI-VOICE-03
> 风险等级：L3
> 产出阶段：6 任务拆分
> 阶段状态：WaitingForApproval
> 证据结果：NotRun
> 前置：[`窗口49-若依语音运行链路收口.md`](窗口49-若依语音运行链路收口.md)、[`../../architecture/若依平台收敛设计.md`](../../../架构/若依集成/若依身份与后端单体收敛设计.md)
> 关联记录：[`../../development-records/2026-08-23-若依语音面试迁移.md`](../../../开发记录/若依集成/2026-08-23-若依语音面试迁移.md)、[`../../development-records/2026-08-23-若依收敛-批次02.md`](../../../开发记录/若依集成/2026-08-23-若依收敛-批次02.md)

```text
你负责完成 TASK-RUOYI-VOICE-02 实施后的剩余收口，任务 ID 为 TASK-RUOYI-VOICE-03。不要从旧 backend 重新迁移一遍，也不要把已存在的候选实现平行重写。必须先以当前工作区源码为准复核，因为窗口 49 的开发记录已经落后于实际差异。

项目目录：D:\2025Ai\26-05-23\ai-interview-coach
Feature / TASK：FEAT-INTERVIEW-001 / TASK-RUOYI-VOICE-03
风险：L3
起始局部阶段：6 任务拆分，执行包批准后进入 7 开发实现，再进入 8 审查验证

一、强制首读与状态声明

1. 完整读取当前会话注入的用户级 AGENTS 指令、`C:\Users\admin\.codex\AGENTS.md`、用户级规范索引及：
   - `agent-governance.md`
   - `architecture-spec.md`
   - `development-spec.md`
   - `quality-delivery-spec.md`
   - `documentation-spec.md`
2. 读取项目根 `AGENTS.md`；当前已知该文件不存在，必须重新确认并记录治理缺口，不得伪称已读取。
3. 完整读取：
   - `docs/specs/README.md`
   - `docs/phases/window-prompts/窗口49-若依语音运行链路收口.md`
   - `docs/architecture/若依平台收敛设计.md`
   - `docs/phases/第19期-若依身份与后端单体收敛.md`
   - 两份关联开发记录
   - `contracts/openapi/voice.yaml`
   - `contracts/asyncapi/voice-events.yaml`
4. 读取当前源码、配置、测试报告和工作区实际差异。项目根当前已知不是 Git worktree，只能重新只读确认；不得执行 Git。
5. 第一次更新用中文声明：已加载规范、L3、局部阶段、当前代码基线、A/B/C/UAT/发布/Git 授权状态、预计产物和停止条件。

二、必须重新确认的当前工作区基线

以下是 2026-08-23 窗口 49 中已经发生的本地差异，不是运行验收结论：

1. AsyncAPI 的旧 Cookie Session 认证描述已改为 RuoYi Bearer principal + 一次性主体绑定 socket ticket。
2. `ruoyi-interview` 已新增 V3/V4/V5 候选 Flyway：业务同意与最小 Interview 前置、Voice Artifact/Transcript/Execution、durable stream；操作者字段改为 `BIGINT ruoyi_user_id`，没有 `identity.membership` 外键。
3. `JdbcInterviewRepository`、`JdbcVoiceRepository`、`JdbcConsentRepository` 已按新字段名局部对齐；恢复投影仍需做全链 SQL/schema 覆盖审计。
4. AES-256-GCM sensitive envelope key 和本地音频根目录改为外部配置；缺失时 fail-closed。`LocalFileObjectStorageAdapter` 已增加不覆盖、tenant/artifact 引用校验和失败清理。
5. Voice capability 已按对象存储、ASR、cipher 可用性 fail-closed；权限意图已改为 `interview:voice:upload`、`interview:voice:confirm`、`interview:session:recover`，MySQL 权限脚本未执行。
6. WebSocket 已增加 RuoYi upload 权限、envelope/generation 校验、重复 ACK、gap NACK、大小/时长限制、`client.tts.cancel` 与 TTS 候选桥。
7. SSE Controller 已接入 `DurableStreamPort` 的 replay/cursor/410/snapshot 逻辑；主 React SSE hook 已改用同一 RuoYi JWT 的 fetch stream，不再使用无法带 Authorization header 的原生 EventSource。
8. Portal Web 与 Mobile H5 已写入 TTS chunk 内存播放/取消候选代码，但中断前尚未重新构建；非 H5 仍明确降级。
9. 主 React 的 `VoicePreparation` 仍只本地录音并显示“当前不会建连或上传”，尚未把 `useVoiceSocket`、音频分片、ASR final、Transcript 修正确认、TTS 播放串起来。
10. 最近一次已记录的 Maven 测试是 `9` 项全部通过，最近一次 RuoYi 打包也曾通过；但它们发生在最后的 `VoiceTurnExecution.emitServerSequence` 修正和 Portal/Mobile TTS 改动之前，不能作为当前最终差异 Pass。
11. PostgreSQL migration 实际执行、唯一 RuoYi 服务启动、REST/WS/SSE 联机、401/403、真实 ASR/TTS、真实云对象存储、浏览器 UAT 均为 NotRun。

开始修改前必须验证上述每条是否仍成立。发现用户后续修改时与其共存，不覆盖、不恢复、不纳入无关范围。

三、目标与非目标

目标：关闭当前工作区中仍阻碍“范围内实现 + A 包证据”的缺口，并形成一个可以进入 B 包实际数据库/联机验证的稳定基线。

目标链路：

`RuoYi principal/RBAC -> consent/preflight -> voice session/ticket -> WS start/chunk/stop/cancel -> controlled storage -> PostgreSQL Artifact/Transcript/version/execution -> user correction/confirm -> stable answer/next turn -> TTS state/chunk/cancel -> durable stream -> authenticated SSE replay/410/snapshot -> three clients recover`

非目标：

- 不新增第二个 Boot main、SecurityFilterChain、账号、Cookie Session、query token 或独立认证入口。
- 不删除、移动、重命名或归档旧 `backend/`、`frontend/` 和历史迁移。
- 不新增未经 OpenAPI/AsyncAPI/Approved 设计支持的 endpoint、event、状态、字段或权限意图。
- 不实现 Realtime speech-to-speech，不扩展报告、学习、支付或无关后台。
- 不猜云供应商、bucket/region、Secret、模型、voice、价格、保留期或数据出境条件。
- 不把静态 SQL、fake、Testcontainers、构建或截图描述为真实 PostgreSQL、真实 Provider、真机或 UAT 通过。

四、剩余任务顺序

### TASK-VOICE-REMAIN-01 当前差异与契约矩阵

- 形成 `operation/event -> controller/handler -> use case/port -> adapter -> schema -> client -> test/EV` 矩阵。
- 逐项核对 OpenAPI、AsyncAPI、REST、WS、SSE、主 React、Portal、Mobile H5/非 H5。
- 删除或修正只描述旧状态的活跃代码注释/UI 文案；历史记录不静默重写。
- 若必须改变公共 API/event/schema/权限意图，停止并退回阶段 5/6。

### TASK-VOICE-REMAIN-02 PostgreSQL 可达 SQL/schema 完整性

- 不只检查 `JdbcVoiceRepository`；列出从 voice preflight/session/confirm/snapshot/durable stream 可达的全部 JDBC SQL 和表。
- 核对 V1-V5 是否足以支撑真实 Voice 垂直链。重点检查 `catalog`、`billing`、`platform.job/idempotency/outbox`、`evaluation` 等被 Interview 创建、确认、恢复投影直接访问但新 Flyway 尚未拥有的最小事实。
- 在 `ruoyi-interview/src/main/resources/db/migration/` 仅新增后续版本；不得修改 V1-V5，因为其执行可能性未知。
- 保持 `BIGINT ruoyi_user_id`、tenant 隔离、不可变版本、乐观锁、幂等、sequence/cursor/retention 和删除约束；不得恢复 `identity.*`，不得建 PostgreSQL 到 MySQL 的物理外键。
- 增加 Testcontainers PostgreSQL migration/repository 测试；Docker 不可用时记录 Blocked，不以字符串断言冒充 migration Pass。

### TASK-VOICE-REMAIN-03 WebSocket 与 TTS 状态机收口

- 审查当前 `VoiceCaptureCoordinator`/`VoiceWebSocketHandler` 的最后差异，确保 client/server sequence、重复 ACK、gap NACK/resync、generation、codec、chunk/总字节/时长、背压、取消、断线清理、旧连接写入均符合 AsyncAPI。
- 证明 `client.tts.cancel` 可在 SPEAKING 期间被接收，停止后只改变 Voice execution/音频 Artifact，不改变 Interview Session。
- 证明 TTS `STARTED -> chunk* -> COMPLETED`、`CANCELLED`、`FAILED/degraded` 均有终态；无下一题、socket 已断和 Provider 未配置时不能停在 THINKING/SPEAKING。
- Provider 调用不得占用无界线程、无限重试或把 Secret/文本/音频写入日志。
- test-only deterministic ASR/TTS fake 必须只在 `src/test`，不得进入生产 profile。

### TASK-VOICE-REMAIN-04 长连接 SSE

- 当前 SSE 候选是有限 replay 后完成并由客户端重连；审查并改为真正有界资源的长连接：先 replay，再等待新 durable 事件，定时 heartbeat，断开清理，最大连接时长/空闲策略明确。
- 支持空 cursor、有效 `Last-Event-ID`、重复去重、顺序、过期 410/snapshot、无效/外来 cursor 不泄漏存在性、最大 replay 数。
- SSE 只发送 allowlist 中的 ID、版本、状态、计数和 reasonCode，不发送 Token、音频、完整回答/转写、Prompt、Provider/对象存储 Secret。
- 主 React 继续使用同一 RuoYi JWT 的 fetch stream；不创建第二套 session/ticket，除非已批准设计明确要求且重新获批。

### TASK-VOICE-REMAIN-05 主 React 完整接入

- 将 `VoicePreparation` 从“仅本地录音”改为真实使用既有 `useVoiceSocket`；不要平行实现另一套 socket state machine。
- Recorder 需要在用户手势后按服务端 chunk/window/duration 限制发送，不把完整音频写入 localStorage/sessionStorage/日志。
- 覆盖 hello、flow-control、ACK/NACK、ASR final、低置信编辑、ConfirmTranscript、TTS chunk/play/cancel、断线 snapshot 恢复、失败转文字和资源释放。
- 复用 `AudioPlayer` 或最小扩展，不在组件卸载后保留 Blob URL、MediaStream、timer、socket 或 pending chunk。
- 删除“当前不会建连或上传”等与实现冲突的活跃 UI 文案，但不得伪称已取得运行证据。

### TASK-VOICE-REMAIN-06 Portal / Mobile 对齐

- 复核刚写入但未构建的 Portal/Mobile H5 TTS 内存播放与取消；补齐 `COMPLETED/FAILED/CANCELLED`、播放拒绝、资源释放和 socket 关闭行为。
- Portal/Mobile 必须落实 server flow-control、ACK window、重复/乱序、总字节/时长/codec 限制，不能只发送 chunk 而忽略背压。
- 三端共享同一 reasonCode/状态语义；不复制互相冲突的协议常量。
- 非 H5 仅在 `uni.connectSocket`、录音文件分片和子协议认证可满足已批准契约时接入；否则保持 Blocked/文字降级，不把 H5 构建描述为小程序/原生通过。

### TASK-VOICE-REMAIN-07 最小测试、文档与交接

- 增加直接覆盖下列风险的测试：
  - PostgreSQL V1-latest migration 与 Repository round-trip、tenant/owner 隔离、乐观锁、不可变 version、stream cursor/410。
  - socket ticket 主体绑定/过期/单次；WS duplicate/gap/generation/backpressure/limit/cancel/disconnect。
  - deterministic ASR/TTS fake 的成功、失败、取消；生产 disabled adapter fail-closed。
  - SSE replay/live heartbeat/disconnect cleanup/401/403/owner denial。
  - Secret、Token、Cookie、socket ticket、原始音频、完整转写/回答日志扫描。
- 新建或更新 `docs/development-records/2026-08-23-若依语音运行链路收口.md`，记录当前实际差异、命令、退出码、EV、失败修复、未执行项和下一门；旧记录只增加纠错/后续链接，不静默改写历史结论。
- 更新任务矩阵和配置/reasonCode/默认关闭清单，不把计划写成结果。

五、允许修改范围

- `apps/platform-backend/ruoyi-interview/src/main/**`
- `apps/platform-backend/ruoyi-interview/src/test/**`
- 必要时 `apps/platform-backend/ruoyi-interview/pom.xml` 的 test-scope 或最小实现依赖
- `apps/platform-backend/ruoyi-admin/src/main/resources/application*.yml` 的 Voice 配置键
- `apps/platform-backend/sql/ruoyi-interview-permissions.sql`，仅维护未执行的权限增量
- `contracts/openapi/voice.yaml`、`contracts/asyncapi/voice-events.yaml`，仅修复已批准语义下的内部不一致
- `frontend/src/features/voice/**`、`frontend/src/features/interview/**` 及最小共享 token/API 类型
- `apps/portal-web/src/features/interview/**` 及最小 API/client
- `apps/mobile/pages/interview/**`、`apps/mobile/api/interview/**` 及最小 socket/client
- 本 TASK 的任务、开发记录和验证记录

不得修改：旧 `backend/` 生产源码/迁移、RuoYi 身份/RBAC 事实表、无关业务模块、部署文件。不得删除、移动、重命名、归档或压缩历史内容。

六、授权包

A 包：本地实现与非外部验证，当前为 Pending，必须由用户在执行会话明确批准。

批准后允许：

1. 上述范围的源码、配置、测试和文档修改。
2. `mvn -pl ruoyi-interview -am test`，工作目录 `apps/platform-backend`；可能下载 test-scope 依赖并使用本机 Docker/Testcontainers。
3. `mvn -pl ruoyi-admin -am -DskipTests package`，同目录。
4. `npm run build`，工作目录 `frontend`。
5. `npm run build`，工作目录 `apps/portal-web`。
6. `npm run build:h5`，工作目录 `apps/mobile`。
7. 只在 Admin 代码/共享契约受影响时运行 `npm run build:prod`，工作目录 `apps/admin-web`；否则记录 NotApplicable 和理由。
8. 对首次失败做一次与当前差异直接相关的修复后重跑；第二次仍失败、需要换命令、安装额外系统软件或扩大模块时停止。

B 包：PostgreSQL migration、MySQL 权限脚本、唯一 RuoYi 服务启动、REST/WS/SSE 联机、浏览器/HTTP 验证，当前 NotRequested。执行前必须列出精确命令、数据库类型/端口、写入、账号角色、测试数据、清理和恢复方式并单独获批。

C 包：真实 ASR/TTS/云对象存储，当前 NotRequested。供应商、区域、数据条款、模型/voice、费用上限、凭据注入、最小音频、留存/删除和停止条件逐项批准后才能调用。

UAT、上线准备、发布、回滚和 `git add/commit/push/PR/tag` 均 NotRequested。不得执行。

七、停止条件

立即停止并报告：

- 需要第二套账号/session/filter chain，或接受前端 userId/tenant/role 为权威值。
- 需要改变公共 API/event/schema/权限意图或产品 AC，而不是修复内部不一致。
- V1-V5 与实际 PostgreSQL history 无法安全前向兼容，或需要删除、重命名、破坏性回填。
- 发现 Secret、Token、Cookie、socket ticket、原始音频、完整转写/回答进入日志、源码、截图或仓库。
- 本地路径越出配置根、跨 tenant/owner 访问、对象覆盖、重复 chunk 重复持久化或已确认事实在断线后丢失。
- 真实外部调用、数据库/权限写入、服务启动、浏览器 UAT、费用或数据出境未获相应包授权。
- 需要触碰禁止目录、运行未列命令、扩大测试、安装系统软件或执行 Git/部署。

八、完成定义

A 包完成仅允许声明“范围内实现 + 自动化/构建证据”：

- 主 React、Portal、Mobile H5 的同一协议路径均已编码并通过各自构建。
- PostgreSQL migration 在 Testcontainers 中从空库执行，Voice/stream Repository 关键约束与 round-trip 通过；没有 Docker 时对应结果为 Blocked。
- WS、TTS、SSE 的确定性测试覆盖成功、拒绝、失败、取消和恢复，不依赖真实 Provider。
- RuoYi 打包通过，Secret/敏感日志扫描通过，所有未验证真实条件明确列为 NotRun。

只有 A + 获批 B 且 migration/唯一进程/REST/WS/SSE/RBAC/恢复取得实际证据，才能声明完整本地技术验证。只有获批 C 并完成真实最小调用、回读和删除，才能声明对应 Provider/云能力通过。UAT 仍需用户独立验收。

九、最终交接

最终汇报必须包含：TASK/阶段/状态/风险、输入基线、实际修改文件、契约与代码矩阵、迁移与配置、REST/WS/SSE/TTS/三端状态、每条实际命令/工作目录/退出码/EV、失败与有限重试、A/B/C/UAT/发布/Git 状态、NotRun/Blocked、真实 Provider/云边界、残余风险、恢复方式和下一阶段门。
```

## 使用说明

在新会话粘贴上面的完整提示词。若只希望继续本地实现与列明的自动化/构建验证，在同一条消息末尾明确补充：

```text
批准 A 包，按 TASK-RUOYI-VOICE-03 执行；B/C 包、UAT、发布和 Git 不授权。
```
