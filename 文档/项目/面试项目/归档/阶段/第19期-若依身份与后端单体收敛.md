# 第 19 期：RuoYi 身份与后端单体收敛

> 文档类型：Phase / 跨功能路线审查
> 文档状态：Approved
> Phase ID：PHASE-19
> 风险等级：L3
> 产出/适用阶段：5–7 前瞻规划
> 阶段状态：InProgress
> owner：platform / identity / backend
> 证据结果：NotRun
> 前置：`docs/architecture/若依平台收敛设计.md` 已获用户实施授权；当前进入阶段 7 的首个实现切片
> 关联：[`../architecture/若依平台收敛设计.md`](../../架构/若依集成/若依身份与后端单体收敛设计.md)、[`../specs/README.md`](../../../../规范/README.md)、[`../architecture/若依迁移计划.md`](../架构/若依迁移计划.md)

## 1. 本期为什么存在

当前仓库里同时存在两套后端事实源：

1. `backend/` 的独立 AI 身份与独立 Boot 进程；
2. `apps/platform-backend/` 的 RuoYi 账号、登录、验证码、Token、菜单权限与 RBAC。

本期的唯一目的，是把这两套事实源收敛为一套：RuoYi 负责身份与安全，`ruoyi-interview` 负责 AI 业务。它不是产品 feature，不讨论新业务，只讨论平台收敛。

## 2. 本期目标

- 系统只保留一套登录态和权限判断。
- 只保留一个 Spring Boot 后端进程。
- AI 业务 Controller 全部切到 RuoYi principal。
- PostgreSQL 业务表只保存 `ruoyi_user_id` 引用。
- 旧 `identity.*` 不再创建新账号或新 session。

## 3. 非目标

- 不新增业务功能。
- 不做真实发布。
- 不做数据库破坏性删除。
- 不允许把旧 AI 身份重新升级成第二套权威源。

## 4. 依赖输入

| 输入 | 状态 | 说明 |
|---|---|---|
| `docs/architecture/若依平台收敛设计.md` | Approved | 本期设计输入；已进入实现 |
| RuoYi 安全实现 | 现有事实 | `RuoYiApplication`、`TokenService`、`JwtAuthenticationTokenFilter`、`SysLoginController`、`CaptchaController` |
| 旧 AI identity 实现 | 现有事实 | `IdentityController`、`SecurityConfiguration`、`AIC_SESSION`、`identity.*` migrations |
| 三端前端现状 | 现有事实 | portal-web 仍用旧 AI session；admin-web / mobile 已走 RuoYi 风格 |

## 5. TASK 索引

| TASK | 垂直切片 | 上游 ID | 模块 / 文件范围 | 依赖 | 验证 / EV | 状态 |
|---|---|---|---|---|---|---|
| TASK-RUOYI-01 | 身份源切换 | DES-RUOYI-CONVERGENCE-001 | `ruoyi-framework`、`ruoyi-system`、`ruoyi-admin`、`ruoyi-interview/controller`、`apps/portal-web`、`apps/admin-web`、`apps/ruoyi-app` | 先有 RuoYi principal 适配 | 登录、当前用户、退出、权限拒绝 | NotStarted |
| TASK-RUOYI-02 | backend 单体迁入 | DES-RUOYI-CONVERGENCE-001 | `backend/*` -> `apps/platform-backend/ruoyi-interview` | 先有模块映射 | 只有一个 Boot main、一个安全入口 | InProgress |
| TASK-RUOYI-03 | 双数据源与用户引用 | DES-RUOYI-CONVERGENCE-001 | `ruoyi-interview/infrastructure/persistence`、Flyway、业务表 | 先有 `ruoyi_user_id` 约定 | 抽样核对业务表 owner 引用 | NotStarted |
| TASK-RUOYI-04 | REST / SSE / WebSocket 切换 | DES-RUOYI-CONVERGENCE-001 | Controller、stream 适配、socket ticket、前端 API | 先有 principal / RBAC | 恢复、续传、权限校验 | NotStarted |
| TASK-RUOYI-05 | 旧 identity 退役 | DES-RUOYI-CONVERGENCE-001 | 旧 schema、旧控制器、旧 session/cookie 代码 | 先完成前四项 | 旧接口不再创建第二套账号 / 会话 | NotStarted |

### 5.1 本轮子任务状态

| 子任务 | 目标 | 范围 | 状态 | 证据 |
|---|---|---|---|---|
| TASK-RUOYI-02A | 新增 `ruoyi-interview` 并接入 `ruoyi-admin` | `apps/platform-backend/pom.xml`、`ruoyi-admin/pom.xml`、`ruoyi-interview/pom.xml` | Completed（实现） | 静态聚合与文件检查；构建 NotRun |
| TASK-RUOYI-02B | 迁移框架无关 domain/application | 旧 `interview-domain`、`interview-application` 中排除 `identity` 的源码 | Completed（实现） | 包名/旧 identity 残留检查；构建 NotRun |
| TASK-RUOYI-02C | 接入 RuoYi principal 只读桥 | `ruoyi-interview/application/security`、`configuration` | InProgress | 源码存在性与 SecurityUtils 引用检查；运行验证 NotRun |

## 6. 执行包

### 6.1 允许修改

- `apps/platform-backend/ruoyi-admin`
- `apps/platform-backend/ruoyi-framework`
- `apps/platform-backend/ruoyi-system`
- `apps/platform-backend/ruoyi-common`
- 新增的 `apps/platform-backend/ruoyi-interview`
- `backend/` 仅在迁移映射和归档阶段作为兼容参考
- `docs/architecture/若依平台收敛设计.md`
- `docs/specs/README.md`
- `docs/phases/README.md`

### 6.2 当前不授权

- 不写数据库。
- 不改线上配置。
- 不做 `git add` / `commit` / `push`。
- 不删除 `backend/`。

本轮已授权的本地实现范围：

- 新增 `apps/platform-backend/ruoyi-interview` Maven 子模块并由 `ruoyi-admin` 聚合。
- 新增 RuoYi principal 只读桥接和模块边界文档。
- 后续代码迁移仍按 TASK 分片执行；每个涉及构建、测试、启动或外部连接的动作需在执行前单独记录授权和范围。

### 6.3 计划验证入口

| 验证项 | 入口 | 通过信号 |
|---|---|---|
| 登录与验证码 | `/captchaImage`、`/login`、`/logout`、`/getInfo` | 只读到 RuoYi principal |
| AI 业务权限 | `@PreAuthorize("@ss.hasPermi('interview:*')")` | 缺权限返回 403 |
| `ruoyi_user_id` 引用 | PostgreSQL 业务表抽样 | 不出现旧 AI user 作为权威字段 |
| 单后端进程 | `apps/platform-backend` | 只有一个 Boot main |
| 长连接主体 | SSE / WebSocket | 连接主体与 RuoYi principal 一致 |

## 7. 停止条件

出现以下任一情况立即停止：

- 任何旧身份接口继续创建第二套账号或 session。
- AI Controller 继续信任前端传入 `user_id`、`role`、`tenant`。
- PostgreSQL 业务表开始复制密码或权限。
- MySQL / PostgreSQL 跨库写入出现未定义不一致。
- WebSocket / SSE 需要引入第二套登录事实源才能跑通。
- 发现密码、Cookie、Token、API Key、Secret 泄漏。

## 8. 回滚条件

- 仅在 authority switch 之前允许回滚到旧前端 / 旧 controller 适配层。
- 一旦 RuoYi 成为唯一权威源，不允许回滚成双身份事实源。
- 回滚只能恢复代码与路由，不允许恢复旧 password/session 作为正式身份。

## 9. 下一阶段及进入条件

进入阶段 7 的条件：

1. `ruoyi-platform-convergence` 设计获批。
2. TASK-RUOYI-01~05 的文件范围与验证入口确认。
3. 未出现需要重新决策的权限、数据或外部副作用变化。

## 10. 交接结论

- 当前结论：InProgress；已完成模块骨架切片，后续迁移未完成。
- 已发生的本地修改、未执行验证和下一步入口见 `docs/development-records/2026-08-16-若依收敛-批次01.md`。
