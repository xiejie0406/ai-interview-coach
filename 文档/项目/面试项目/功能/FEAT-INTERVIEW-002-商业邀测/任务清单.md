# S1 开发任务与候选执行包

> 文档类型：唯一实施任务 / 执行包；版本：0.1.0；文档状态：Draft；规范基线：2026.08-r1  
> Feature：FEAT-INTERVIEW-002；风险：L3；创建 / 更新：2026-09-09  
> 产出阶段：6 任务拆分（提前准备）；产物阶段状态：InProgress  
> owner：Codex 编写；独立复核见[控制页](README.md)；批准记录：本轮仅资料准备获授权，以下实现与运行包尚未批准  
> 输入：[规格 0.1.0](功能规格.md)、[设计 0.1.0](技术设计.md)、[原型 0.1.0](交互原型.md)、[契约 0.2.0](契约/openapi.json)；结果：[验证记录.md](验证记录.md)

## 1. 第一阶段的可见结果

内部固定中文中级 Java 技术场景，完成“明确开始 → 看题 / 听题 → 录音 → 停止 → 确认 final → 保存答案 → 有依据的追问或结束 → 回读内部摘要”。没有报告分数、HR 配置、购买或对外邀测入口。

S1 分两道证据门：**S1-A 是本地实现及 fake 验证，S1-B 是另行授权的真实语音验证。** S1-A 通过只能说明本地逻辑，S1-B 通过才可说明选定环境、设备和供应商的一题真实链路。S2 扩展双轮整场，S3 完成报告和数据治理后才讨论真实用户邀测。当前 PRD 的文本训练仍由 FEAT-INTERVIEW-001 维护，本包不把语音变为其 P0。

## 2. 入场条件与范围

本包 ID：EXEC-PILOT-S1-A，版本 0.1.0。当前已接受的是 Spec 的六项 DEC；REQ/BR/AC 细则与本包均待审，**已批准可实施 TASK 集合为空**。准备资料不受此限制。

入场前关闭：S1 适用规格 Approved、交互关键分支评审、公共契约 / 隐私独立复核、下述 TASK-01 的物理 DDL 与 DTO 映射、UI tokens 基线定位。责任人见控制页，不能由同一作者自审记录替代独立复核。具体业务取舍回到 Spec 更新，不能在实施中自行换时钟、扣次或答案规则。

允许范围候选：ruoyi-interview 的 interview / voice / billing 及直接相关 Adapter、Portal 面试页和 API 调用、新 v2 契约文件、本 Feature 文档。只新增/修改 Java、SQL、TS、Vue、CSS、JSON、YAML、Markdown；SQL 只编写，不执行迁移。新增候选类名可在所属包内按现有命名细化，跨模块、依赖、API 语义或数据范围变化需更新本包。

本包排除：改写既有 V1–V12 迁移、全站 UI 改版、身份体系迁移、改变现有 v1 响应、真实资料/凭据/付费调用、数据库/Redis/Docker/浏览器或应用启动、套餐售卖、支付、Git 和发布。当前工作区已有 PRD / 题库 / 视觉收敛资料变更，必须保留。

## 3. 文件与层级边界

以下路径均相对仓库根；标“候选新增”的文件尚不存在，不是实现证明。

| 层 | 现有落点 | 本包职责 / 新增候选 |
| --- | --- | --- |
| HTTP / DTO | `ruoyi-backend/ruoyi-interview/src/main/java/com/ruoyi/interview/controller/rest/interview/InterviewController.java`、`controller/rest/voice/VoiceController.java` | 同包新增 `PilotInterviewController.java` 与 v2 DTO；仅解码、鉴权入口、ETag / 错误映射，禁止编排第二套答案事务 |
| 用例 | 上述 Java 根下 `application/interview/internal/`、`application/voice/internal/` | 复用 Start / Committer / Recover；候选 `DefaultPresentInterviewQuestion.java`、`DefaultRenewInteractionLease.java`、`DefaultCompleteInterview.java`，用例对接既有事务 / Job / Outbox |
| 领域 | Java 根下 `domain/interview/InterviewSession.java`、`SessionPolicy.java`、`domain/voice/`、`domain/billing/UsageReservation.java` | 维护状态、版本、预算与去重；候选 `TrainingClock.java` / `InterviewCompletion.java`，不调用 HTTP 或模型 |
| 持久化 | Java 根下 `infrastructure/persistence/interview/JdbcInterviewRepository.java`、`voice/JdbcVoiceRepository.java`、`billing/JdbcBillingRepository.java` | 新字段及唯一约束、CAS、时钟边界 / 租约持久化；新增迁移放 `src/main/resources/db/migration/`，实施时重新分配序号 |
| 语音传输 | Java 根下 `controller/websocket/VoiceWebSocketHandler.java`、`VoiceCaptureCoordinator.java`、`infrastructure/provider/VolcengineSpeechToTextAdapter.java` / `VolcengineTextToSpeechAdapter.java` | 流式连接、codec、ACK、背压、停止与取消；不得以整段上传伪装流式首包 |
| Agent / 组装 | Java 根下 `application/interview/internal/DefaultInterviewAgentCandidate.java`、`DefaultProgressInterview.java`、`infrastructure/agent/`、`configuration/InterviewUseCaseConfiguration.java` / `ProviderConfiguration.java` | 构造受限上下文，校验动作、来源、工具与预算；候选 `infrastructure/agent/PilotInterviewerPrompt.java` / `PilotInterviewerActionValidator.java`；配置默认关闭 |
| 页面 / 状态 | `frontend/portal-web/src/features/interview/InterviewSetupPage.vue`、`InterviewRoomPage.vue` | 候选 `api/pilot.ts`、`api/pilot-types.ts`、`composables/usePilotInterview.ts`、`media/pilot-audio.ts`；组件展示、组合函数管理交互、媒体模块处理采集播放 |
| 共用前端边界 | `frontend/portal-web/src/shared/api/client.ts`、`src/router/index.ts`、`src/styles.css` | 仅增加受控 v2 调用与内部入口，沿用主题；默认 v1 不变，不暴露任意 API origin |
| 契约 | `文档/项目/面试项目/功能/FEAT-INTERVIEW-002-商业邀测/契约/` | 批准后按 S1 子集新增 `contracts/openapi/interview-pilot-v2.yaml`、`contracts/asyncapi/interview-pilot-v2.yaml`、`contracts/schemas/interview-pilot-v2.schema.json`；三者是候选新增，不覆盖旧 v1 文件 |

## 4. 顺序任务

所有 TASK 阶段状态为 NotStarted。表中 REQ/AC/DES 数字均展开为本 Feature 的 `REQ-PILOT-*`、`AC-PILOT-*`、`DES-PILOT-*`。

| TASK-PILOT | 上游 / 依赖 | 工作及完成条件 | 文件范围 / 验证 |
| --- | --- | --- | --- |
| 01 契约与入场映射 | REQ 01/02/04/06/07；AC 01/03/04/07/09/12/14；DES 01/02/04/06；无依赖 | 逐端点映射 DTO → 用例 → 聚合 / 表 / 索引；补 activation、clock、epoch、completion、plan/session 唯一约束的物理 DDL 表格；核对既有行如何兼容及实际消费者；完成独立复核后才放行后续代码 | `技术设计.md`、本包、三份候选 JSON；输出字段名/类型/null/default/唯一键/backfill/回退说明；静态审查 EV-DOC-01–04，不执行 DDL |
| 02 开场、计时与恢复 | REQ 01/04/07；AC 01/02/07/08/14；DES 01/02/04；依赖 01 | 固定技术计划；预留但未呈现不启用；题就绪后明确开始，单次原子启用；持久时钟 / heartbeat / epoch；暂停与失联不可被旧心跳恢复；系统等待可归因 | Start/Plan/Recover、Session、Billing、Snapshot / DTO、JDBC、新迁移及组装；EV-RUN-01 的时钟 / 启用场景，DB 真实性留 S1-B |
| 03 答案与完成事务 | REQ 02/04/06；AC 03/04/08/09/12；DES 02/04 | 依赖 02；文本与 final 显式确认复用 InterviewAnswerCommitter；同题最多一个正式版本 / 下一步 Job；结束 CAS 冻结 inputHash；NONE 有 pending 则拒绝，DISCARD 可解释；完成恢复不回答题；S1 不排评分 Job | Committer、DefaultConfirmTranscript、ApplySessionCommand、Complete 候选、Snapshot、持久化；EV-RUN-01 竞争/重试/越权，SQL 约束另验 |
| 04 语音会话与媒体实现 | REQ 02/04/06/07；AC 02/03/07/08/12/15；DES 01/02/06；依赖 02/03 | 保持 Port 边界，补实流所需帧编解码、partial/final、180s上限、输出首包与取消；无外呼时 fake 只提供标识清楚的事件；权限拒绝、采集停止、断线清队列、文字降级均可进入合法状态 | voice 用例 / WS / Provider Adapter、前端 media、voice 配置类；EV-RUN-01 的封包 / 取消场景；连接真实服务属于 S1-B |
| 05 技术面试官决策 | REQ 03/06/07；AC 05/08/12/15；DES 03/07；依赖 03 | 五段 prompt：角色、当前轮/蓝图、引用回答、剩余预算/允许工具、结构化输出；固定角色 TECHNICAL；只读题库/锚点/确认答案工具，服务端注入主体；回答充分 NEXT、证据缺口 FOLLOW_UP、歧义 CLARIFY；主问题最多2次追问、全场2次澄清；不评分、不输出标准答案；一次主调用与最多一次修复共用8s期限 | Candidate/Progress、Agent prompt/validator、Provider 路由；记录 prompt/模型/蓝图版本和脱敏 decision code；EV-RUN-01 的动作/引用/注入样例，只能证明防线，不证明模型质量 |
| 06 前端串联 | REQ 01/02/04/06；AC 01/02/03/04/07/08/09/12；DES 06；依赖 02–05 | 接入 v2、首题确认前无正文、final确认、未知提交回读、412保留草稿、暂停和完成状态；刷新读服务器，不假装草稿已保存；提供键盘 / 读屏反馈；无报告分数和 HR 入口 | 表内 Portal 文件；EV-BUILD-02/03，浏览器实际交互与视觉另包 EV-LIVE-02；构建通过不等于可用性通过 |
| 07 定向验证与交接 | REQ 01–04/06/07 的 S1 部分；AC 01–05/07–09/12/14/15 的局部；DES 07；依赖 02–06 | 仅新增下节4个测试类及其必要同包 fixture；执行批准的命令；记录实际发现测试数、断言/失败/跳过；更新 AC—EV 与开发记录；待独立代码复核后整理 S1-B 真链路包 | `src/test/java` 的明确名单；本 Feature `验证记录.md`，候选 `文档/项目/面试项目/开发记录/2026-09-09-商业邀测第一切片.md`；未实际开发前不建完成记录 |

02–06 的所有公共数据/接口变化必须先回到 01 的审阅版本，不把“在同目录”作为无边界扩展依据。只读独立复核不等于用户业务验收。

## 5. 最小测试代码候选

使用已有 JUnit Jupiter / 测试依赖，禁止为本包新增前端测试框架。下列4个候选类均位于 `ruoyi-backend/ruoyi-interview/src/test/java/com/ruoyi/interview/`；可合并同类场景，不堆实现镜像。

| 候选测试类 | 能发现的业务破坏 | 不能证明 |
| --- | --- | --- |
| `application/interview/PilotActivationClockTest.java` | 重复 presentation 启用两次；读题前扣次；系统等待被计入；旧 epoch 恢复暂停；到期继续采集；用可控 Clock 比较预期段总和 | 真实数据库并发和生产时钟恢复 |
| `application/interview/PilotAnswerCompletionTest.java` | final 自动提交；确认/结束竞争丢最后答案；相同 key 不同 body 被接受；完成重试改输入；跨主体引用；晚到结果落地 | JDBC 原子性 / 进程重启后的恢复 |
| `infrastructure/agent/PilotInterviewerDecisionTest.java` | 不支持的角色/动作、伪造引用、问答注入、超长问题、过预算/过量工具调用被接受；NEXT 不能使用未审核问题 | 真实模型提问是否自然、有价值 |
| `infrastructure/provider/PilotVoiceStreamProtocolTest.java` | 分片粘连/拆分误解码、partial/final混淆、重复/乱序、取消后晚到输出仍播放、背压忽略 | 供应商兼容、真实麦克风或网络质量 |

仅复用直接相关既有 `domain/voice/VoiceTurnExecutionTest.java` 作为回归。fake / 内存实现只放测试作用域，必须显式注入；测试不得加载真实 Spring 运行配置、联网、连接数据库或启用调度器。数据库锁/唯一键、Testcontainers 和迁移测试暂不包括在本包，不能用内存并发替代。

## 6. 拟执行命令与证据（本轮全部 NotRun）

环境候选：当前 Windows 工作区；离线、无新依赖、无服务启动。已静态定位 Maven/Node/Python，但没有运行版本探测。父 POM 未显式固定 Surefire；本机缓存有 3.5.2 目录，不代表插件/传递依赖完整。为避免 Maven 默认版本漏发现 JUnit 5，C02 显式指定该版本，首次实施前核对缓存与用例是否发现；缺失立即停止，不能在线补齐。

| ID / 证据 | cwd | 精确候选命令 | 影响、预期和限制 |
| --- | --- | --- | --- |
| C01 / EV-BUILD-01 | `D:\2025Ai\26-05-23\java-workspace\platform-backend` | `mvn -o -pl ruoyi-interview -am -Dmaven.test.skip=true compile` | 编译依赖模块到各自 target；不打包/启动/运行测试；预期退出0且无 v1 源码兼容错误；离线缺依赖则 Blocked |
| C02 / EV-RUN-01 | 同 C01 | `mvn -o -pl ruoyi-interview -am test-compile org.apache.maven.plugins:maven-surefire-plugin:3.5.2:test "-Dtest=PilotActivationClockTest,PilotAnswerCompletionTest,PilotInterviewerDecisionTest,PilotVoiceStreamProtocolTest,VoiceTurnExecutionTest" "-Dsurefire.failIfNoSpecifiedTests=false" "-DfailIfNoTests=false"` | 仅上述类；依赖模块允许无匹配测试；目标模块5个类必须各实际运行≥1个、0失败/错误/跳过。读取本轮时间戳的 `ruoyi-interview/target/surefire-reports/TEST-*.xml`，不得把退出0或旧报告当成功 |
| C03 / EV-BUILD-02 | `D:\2025Ai\26-05-23\java-workspace\portal-web` | `.\node_modules\.bin\vue-tsc.cmd --noEmit` | 仅类型检查；依赖不存在停止，不安装；预期退出0 |
| C04 / EV-BUILD-03 | 同 C03 | `npm --offline run build` | 已有脚本为 `vue-tsc --noEmit && vite build`；写入/更新 dist，执行前确认无用户手工产物；预期退出0；不打开浏览器、不部署；C03已执行的类型检查会由既有脚本重复一次 |

每项命令各一次；因本切片缺陷且修复仍在批准范围内，可修复后将受影响命令额外重跑一次。允许诊断为读取本轮编译/测试日志、相关源码/配置、差异和报告；不换模型/端口、装依赖或启用服务。第二次仍失败、发现无关既有失败或缓存缺失时保留证据并停止相关验证，不循环修到全绿。

将命令、cwd、开始/结束时间、退出码、工具版本、差异基线、具体测试数、失败栈摘要和日志路径写 verification。日志先检查秘密/原文后归档；没有运行就标 NotRun。C04 会覆盖构建产物，若 dist 中有用户文件，暂停该项而不是清理。

## 7. 专业门禁与后续运行包

| 专业门 | S1-A 要交付 | 仍须单独完成的证据 |
| --- | --- | --- |
| 系统架构 | 停止/恢复/独立成本上限、默认关闭、错误可归因；NFR-PILOT-01/02/03/05/07 的实现入口 | S1-B 真链路、PostgreSQL 并发与重启、受控容量/成本；完整 NFR 不以 fake 达标 |
| 软件架构 | DTO/领域隔离、CAS/幂等、输入冻结、工具白名单、版本固定；独立代码审查记录 | SQL 迁移与回退兼容、实际消费者回归；旧 v1 无回归不能仅靠编译证明 |
| 前端工程 | 类型/构建、服务器快照主导、媒体释放、明确失败与可恢复；DES-06 | 实机权限/录音/播放/刷新/键盘/读屏；NFR-PILOT-06 性能实测 |
| UI 设计 | 复用 styles.css 与 Element Plus，补录实际 tokens、文字状态线框和组件分工 | 视觉截图、缩放/触屏/读屏实测与独立复核；目标矩阵见 技术设计.md，不冒充全矩阵已测 |

S1-B 只在环境、账号、具体模型/地区、数据、场景、精确命令、费用上限和失败停止条件明确后另建运行包：使用虚构回答和受控音频；验证流式 ASR→手动确认→技术追问→TTS，注入断线/超时/取消；测“停止→final”“确认→首音频”两个时段。真实语音不能用文本 fake 截图代替。需至少200轮才能评价设计中 P95 目标；少量冒烟只报单次观察。PostgreSQL / Docker / 浏览器 / 本地服务也在该包明确授权，当前不填虚构端口或账号。

## 8. 后续切片追溯（不是本包执行范围）

| 后续 TASK | 目标 / 上游 | 进入条件 |
| --- | --- | --- |
| TASK-PILOT-08 / S2 | 用户配置、手填资料、HR→技术交接、全场蓝图/时间；REQ-01/03/04，AC-01/05/06/07/08；DES-01/02/03/04 | S1证据与独立审查、HR/技术蓝图/话术评审、资料用途与边界确认 |
| TASK-PILOT-09 / S3 | 证据流水线、分项报告、历史复练和反馈查询/复核；REQ-05，AC-10/11；DES-05 | 专家样本、评分锚点、报告完整契约和独立质量验证 |
| TASK-PILOT-10 / S3 | 数据授权/撤回/删除/备份不复活、测试权益补偿、容量成本与邀测；REQ-06/07，AC-12/13/14/15；DES-05/07 | 治理接口闭合、独立隐私审查、实账/运维与用户验收；准备上线另包 |

AC-06/10/11/13 在 S1 不执行；AC-12/14/15 仅覆盖入口/状态与假依赖局部，其余留 S3。S4 商业支付尚无本版 REQ/TASK，不用旧候选价格直接上架。

## 9. 授权与停止条件

| 动作 | 当前授权 | 决定范围 / 有效期 |
| --- | --- | --- |
| 资料 / 契约草案编写及只读核查 | Approved | 2026-09-09 本轮指令；本轮资料准备完成为止 |
| S1-A 新生产代码及迁移文件编写 | NotRequested | 本包 TASK-02–06、表列文件；需基于评审后版本决定 |
| 测试代码编写 | NotRequested | 仅本包4个新增类与必要 fixture；与运行授权分开 |
| C01–C04 构建 / 定向测试 | NotRequested | 当前路径、离线依赖、单次及上述一次有限重跑；不可扩展为全量验证 |
| 独立审查 / 用户验收 / S1-B | NotRequested | 人员/环境/场景具体化后分别决定 |
| Git / 发布 / 部署 / 回滚 | NotRequested | 本包不申请，也不包含 |

任一授权在目标分支/工作区、规范/契约版本、范围/风险或依赖策略实质变化后失效，需要更新记录。运行前只读复查工作区；如果用户正在改同一业务文件，先合并事实与差异范围，不覆盖对方内容。

停止条件：发现 v1 真实消费者需要不兼容修改；时钟/扣次/最后答案无法原子化；迁移可能丢现有数据；需要秘密、真实用户或外部费用；假依赖通过但无法证实真实流式；独立复核发现阻断项；出现未批准文件/依赖/命令。停止受影响工作，记录错误原文与影响，回到对应上游，不删数据或自动回滚。

本包准备完成后的下一步是 S1 范围与设计复核；包内执行授权不能替代评审门，也不代表业务已交付。
## 本会话追加执行包：现有 v1 语音链路修复

2026-09-09 用户明确要求落实语音存储、真实 ASR/TTS、WS、前端媒体、评分与端到端验收，并同意使用现有供应商额度做验证。本包覆盖现有 v1 本地链路，不把上文商业 v2/S2–S4 全部判为批准或实现。

- 需求/验收：配置可加载；能力状态动态返回且区分配置与真实外部验证；音频能保存/读回/删除；WS 鉴权、取消、超时和文字降级可验证；评分依据服务端题目与 Rubric 正文及已确认回答，禁止虚构总分。
- 设计：复用已有 JdbcVoiceRepository、Flyway、LocalFileObjectStorageAdapter、Redis ticket、VoiceCaptureCoordinator、供应商适配器和 Portal。修复配置/生命周期缺口，后台静态提示改为受权限保护的只读能力 API。
- 任务：1 核对已有迁移/配置；2 修复装配和动态状态；3 媒体/持久化故障修复；4 评分内容源和报告；5 定向测试及真实浏览器/供应商冒烟；6 更正既有报告，保留更正原因。
- 范围：ruoyi-backend/ruoyi-interview、ruoyi-admin 配置与本地启动脚本、portal-web、admin-web 相关页面、本 Feature 记录。
- 环境：现有本地 8081/5173/5174、PostgreSQL/Redis；仅虚构测试回答和本次创建的测试音频。不删除用户原有数据，不重建数据库，不执行 Git 或发布。供应商仅做少量有界调用，不运行负载测试。
- 恢复：修改前保留工作区差异；服务替换前备份本地 jar；迁移先核验历史，不改已执行 SQL；失败保留证据，恢复旧 jar/关闭新增开关。关键隐私/权限独立复核尚未完成，不宣称高风险交付门通过。
