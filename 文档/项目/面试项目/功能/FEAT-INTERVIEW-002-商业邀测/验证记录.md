# 首版审查与验证记录

> 文档类型：审查 / 验证记录；版本：0.1.0；文档状态：Draft；规范基线：2026.08-r1  
> Feature：FEAT-INTERVIEW-002；风险：L3；创建：2026-09-09；更新：2026-09-26  
> 当前阶段：8 审查验证；阶段状态：InProgress  
> owner：Codex 工程验证；独立复核、业务验收未完成；批准记录：2026-09-11 用户要求先完善代码再运行测试，并授权本地数据库、浏览器测试设备和有限真实供应商验证  
> 输入：[控制页](README.md)、[Spec](功能规格.md)、[设计](技术设计.md)、[原型](交互原型.md)、[执行包](任务清单.md)、contracts 0.2.0

## 2026-09-26 当前构建独立端口复验与残余修复

- 范围：按用户优先级修复文字面试和语音面试残余问题，复验当前代码，不覆盖共享 8081。用户已明确授权先备份本机 `ry-vue`、执行托管密钥迁移并用既有本地配置初始化面试启动密钥。备份为 `.runtime/backups/ry-vue-before-managed-secrets-20260926-114557.sql`，SHA-256 为 `3CD36F0B2A1E67C92CA6D7CEB3A3811B3BC5FDFB96C2A4939E4BFA82EA85D113`；恢复需停止依赖该共享库的服务后，从备份核对并回放，不能在共享服务运行中直接覆写。
- 根因与修复：本地同时有 MySQL 和 PostgreSQL 事务管理器，面试 JDBC 仓储原先未显式指定，计划请求触发 `NoUniqueBeanDefinitionException`；仓储事务注解改为 `interviewTransactionManager`。启动还缺少若依密钥表／面试 envelope 别名，按授权迁移与初始化后当前构建可运行。文字答案曾先提交成功再因空模型别名推进失败，形成 400 后重试 412；现对空别名使用确定性下一题回退，提交与推进置于同一面试事务。房间原“草稿已保存”没有持久化证据，改成当前页面草稿提示；房间计时从服务端开始时间恢复，GET 明确禁缓存。
- 运行与检查：独立打包 `mvn -q -pl ruoyi-admin -am -DskipTests package` 通过；`DefaultInterviewAgentCandidateTest` 定向测试通过；Portal `npm run build` 通过（保留大 chunk 告警）。8084 使用按 SHA-256 命名的不可变 JAR，当前哈希 `f25bff0e6ce93d99ad9072f029cc0e7bf10bd7b1b0913276123eeda4ed3a9507`、PID 32444，5176 的 `/api` 代理指向 8084。恢复 GET 的 `Cache-Control: no-store` 和 `serverNow` 已从内置浏览器同源请求回读。外部 Provider 调用、对象存储写入、Flyway 和种子关闭；共享 8081 未重启。逐项结果见本轮[验收清单](../../验收/2026-09-26-题库文字语音验收清单.md)和 HTML 报告。
- 当前构建文字复验：Java 后端／中级／30 分钟生成 6 题 DRAFT 计划，确认、开场与首题可见；会话 `7ef1c643-8c38-4c7f-94f4-c081b7cf9eb4` 提交两份文字答案后自动下一题，刷新可恢复；结束进入反馈页。报告需明确同意将确认答案送模型服务，本轮未同意也未调用外部服务，所以反馈内容未验。
- 当前构建语音复验：独立语音配置创建会话 `353e8adf-58ca-4f8d-8ad1-eea7c1554f60`；音频存储、ASR、TTS 未就绪时页面不显示可用录音入口，可文字回答并刷新恢复。未申请麦克风、未调用真实 ASR/TTS。2026-09-11 的 ASR 403 仍是历史供应商失败事实，不能以文字降级覆盖。
- 限制：PostgreSQL 与 Redis 未为本轮单独隔离；内置浏览器页面截图只现场回读，未导出归档；跳过、375px、模型追问、报告质量、语音转写确认、播报与计费／隐私专项未完成。正式 UAT 结论和用户接受仍以逐项清单为准。

## 2026-09-26 首题卡住修复记录

- 复现：会话 `e92abdad-368a-4118-88e7-7193f7d9b67c` 处于 `IN_PROGRESS`、版本 1、无题目，`INTERVIEW_STEP` Job 为 `PENDING`；旧 8081 进程读取该会话时还出现 `NoUniqueBeanDefinitionException`。用户截图中的 501 原始请求未被捕获，不能把它归因于该异常。
- 修复：开始会话与生成首题共用本地事务，首题失败时不保留空的已开始会话；对旧版本已卡住的无题会话，原开始接口接受当前版本并重试首题。Portal 显示空会话重试入口；本地语音能力检查未就绪时说明缺项并提供文字回答，不显示可用录音入口。
- 后端检查：`mvn -pl ruoyi-interview -am test -DskipITs` 编译通过，24 条中 23 条通过、1 条 `PostgresVoiceMigrationTest` 因该测试进程未配置测试库且本机 Docker 不可用而报错；保留此失败。独立 classifier 打包 `mvn -pl ruoyi-admin -am -DskipTests package` 通过。直接对修复版 8082 重试该会话，返回 HTTP 200、1 道题、版本 3；切换到 8081 后，原 Portal 页面刷新可显示首题。
- 前端检查：`npm run build` 类型检查与构建通过，保留已有大 chunk 警告；内置浏览器确认首题可见，语音配置未就绪时显示“音频存储、语音识别、语音播报未就绪”，并出现文字回答输入框。
- 限制：本地运行继续关闭外部 Provider 调用和音频存储写入；真实 ASR/TTS 未验证，2026-09-11 的供应商 403 结论未被本次运行推翻。未代替用户验收。

## 0. 2026-09-11 工程验证结论

当前可确认“题库可浏览、文字面试可完成回答—追问—结束—反馈闭环”。真实语音已走到浏览器测试麦克风、WebSocket、音频持久化和 ASR 供应商握手，但 ASR 返回 HTTP 403；TTS 因前置 ASR 失败未调用，真实语音反馈闭环未完成。

为避免把凭据类型猜错，火山语音已改为显式选择 `api-key`（新版控制台）或 `access-token`（旧版控制台），两套凭据混用、缺项或未选模式时能力接口明确返回未就绪。当前 `.env` 只有一个非空 `INTERVIEW_VOLCENGINE_SPEECH_API_KEY`，但未能从本地事实确认其控制台类型和账号已开通的 Resource ID，因此停止重复外呼。

| 证据 | 命令 / 场景 | 实际结果 | 限制 |
| --- | --- | --- | --- |
| EV-RUN-02 | 新旧控制台鉴权头构造单测 | Pass；4 条，0 失败 / 错误 / 跳过 | 不联网，不证明凭据有效或服务已开通 |
| EV-RUN-03 | Resource ID 映射与显式覆盖单测 | Pass；3 条，0 失败 / 错误 / 跳过；非法显式值不会静默回退 | 不证明账号实际开通了该 Resource ID |
| EV-DB-01 | `mvn -pl ruoyi-interview -am test`，同进程加载本地测试数据库配置 | Pass；23 条，0 失败 / 错误 / 跳过；PostgreSQL 16.13 随机空库实际应用 V1–V14，durable stream JDBC 往返成功，临时库已清理 | 首次未加载 `.env` 的运行因 Docker 和数据库连接均不可用而失败，随后按原因修正环境并重跑；不能隐去首次失败 |
| EV-BUILD-02 | `portal-web`：`npm run build` | Pass；类型检查及 Vite 生产构建成功 | 存在既有约 1.03 MB 主 chunk 警告 |
| EV-BUILD-03 | `admin-web`：`npm run build:prod` | Pass；Vite 生产构建成功 | 存在既有约 1.74 MB、1.04 MB 主 chunk |
| EV-E2E-01 | `portal-web`：`npm run test:e2e` | Pass；4 条 Chromium 流程通过：题库、文字闭环、语音失败降级、375px 窄屏结束入口；2 条真实供应商场景按开关跳过 | 默认用例通过路由 Mock，不替代真实服务 |
| EV-LIVE-TEXT-01 | 本地服务创建文字计划、开始、提交回答、真实模型追问、完成、生成反馈 | Pass；真实 DeepSeek 链路完成，单次约 26.5 秒 | 单次冒烟，不代表质量、容量或 P95 达标 |
| EV-LIVE-VOICE-01 | Chromium 测试麦克风录音、WAV 分片、WS、存储、ASR | Blocked；麦克风 API、WS、存储和 ASR 外呼已发生；供应商握手返回 `ASR_HANDSHAKE_HTTP_403` | 物理麦克风未验证；ASR 未成功；TTS 和语音反馈未执行；供应商明确失败后未重复调用 |

当前结论：工程验证部分通过，语音供应商接通阻断；正式用户验收仍待用户或业务负责人确认；上线就绪 `NotAssessed`，发布事实 `NotReleased`。

## 1. 2026-09-09 文档检查结论（历史）

本轮交付的是功能规格、候选契约和 S1 开发执行包。三份 JSON 能解析，JSON Schema 通过 2020-12 元模式检查，407 个本地引用可解析；24 个 REST 路径 / 26 个操作的 operationId、路径参数和鉴权继承通过定向静态检查。20 种语音帧及 SSE 共 21 个 AsyncAPI 消息映射/方向检查通过；23 个代表性 Schema 正反例符合预期。

这些结果在 2026-09-09 当时不证明服务端已经实现状态机、鉴权、流式语音或评分；当时没有执行 Java/前端业务代码、Maven、npm、数据库、浏览器、供应商或 UAT。后续实际工程验证见第 0 节。OpenAPI / AsyncAPI 专用标准 lint 仍未运行，局部脚本检查不替代完整标准验证。

| EV | 范围 / 方法 | 实际结果 / 证据 | 限制 |
| --- | --- | --- | --- |
| EV-DOC-01 | Python 3.12.4 标准库读三份 UTF-8 JSON；递归检查本地 $ref / required；REST operationId / path 参数 / security | Pass；[最终记录](证据/contract-checks-final.json)，包含输入 SHA-256 与数量 | 未验证端点部署、HTTP响应和跨字段业务不变量 |
| EV-DOC-02 | 已安装 jsonschema 4.26.0；2020-12 check_schema；23个正反例 | Pass；同一最终记录的 examples；schema hash 与初查一致，修正 REST 后复用了这部分结果 | 检查有限代表例，不是完整请求/响应样本库；UUID/格式不证明资源存在或有权限 |
| EV-DOC-03 | AsyncAPI message → channel/component → Schema 帧 type 对应，服务端 receive/send 方向 | Pass；21项，包括 client.playback.started；输入 hash 未变 | 未使用 AsyncAPI 官方 lint；没有 WS 真实封包/背压执行 |
| EV-DOC-04 | 人工比对7条REQ、10条BR、15条AC与设计/任务/线框 | Pass（作者自查）；追溯见下表；S1/S2/S3范围已分开，待审与已接受分开 | 非独立评审；DDL、UI tokens、真实模型与费用尚未冻结 |
| EV-DOC-05 | 新 Markdown 链接、代码围栏、候选契约证据 hash、入口插入与工作区差异 | 结果以[文档检查记录](证据/document-checks.json)为准 | 只检查本轮文件与新增入口，不宣称历史目录所有链接已修复 |

### 1.1 发现与修正

| 发现 | 处置 / 证据 | 当前状态 |
| --- | --- | --- |
| 首次扫描定价草案仍有 security=[]，会覆盖 Bearer 继承 | [初查 Fail 原记录](证据/contract-checks.json)保留；移除覆盖，禁售标记和403保留；重新检查所有 REST 鉴权/引用/参数，最终 Pass | 文档缺陷已修正；服务端禁售门未实现/验证 |
| 旧候选仍有12次角色追问、教练答案和宽岗位工具参数 | 新0.2.0收窄当前主问题追问≤2、全场澄清≤2、assistance=NONE、检索难度MID；计划总追问预算与单题预算分别说明 | Schema已更新；调度器约束待实现 |
| 仅隐藏首题会留下预读与扣次歧义 | Spec / design / prototype约定确认前用户快照正文null且TTS不可领取，确认后启用/展示；客户端不能自己报到期原因 | 设计候选已统一，非运行证明 |
| 当前共享API客户端自动使用/api/v1 | 执行包限定新增显式v2选项、保持默认v1与登录路径，不把候选URL直接接进现有页面 | 待实现及回归 |

初查中 JSON 结构 / 23例 / AsyncAPI 部分均通过，仅 REST 公共定价覆盖导致总结果 Fail。最终记录保留 previousEvidence 和更正原因，未把初次失败改写为成功。

## 2. AC—TASK—运行证据矩阵

本表数字为本 Feature 的 AC-PILOT / TASK-PILOT，是 2026-09-09 形成的原始计划快照；2026-09-11 已执行范围与结果以第 0 节为准。不能从 EV-DOC 推定 AC 通过。

| AC | TASK / 切片 | 计划证据 | 当前结果 / 未覆盖 |
| --- | --- | --- | --- |
| 01 | 01/02/06；08 S2配置 | EV-RUN-01、EV-BUILD-02、EV-LIVE-02 | NotRun；Schema拒绝例仅局部静态证据 |
| 02 | 02/04/06 | EV-RUN-01、EV-LIVE-01/02 | NotRun；权限拒绝/播放与降级需实机 |
| 03 | 03/04/06 | EV-RUN-01、EV-LIVE-01 | NotRun；final与正式答案隔离需服务端验证 |
| 04 | 03/06 | EV-RUN-01、EV-DB-01 | NotRun；数据库唯一约束与竞争不由fake证明 |
| 05 | 05；08 S2质量 | EV-RUN-01、EV-LIVE-01、EV-QUALITY-01 | NotRun；格式合法不代表有价值追问 |
| 06 | 08 S2 | EV-QUALITY-01 | NotRun；S1只含技术角色 |
| 07 | 02/04/06；08全场 | EV-RUN-01、EV-DB-01、EV-LIVE-01 | NotRun；有效时间与截止窗口未运行 |
| 08 | 02/03/04/05/06 | EV-RUN-01、EV-DB-01、EV-LIVE-01 | NotRun；进程重启/晚到事件需真实持久化验证 |
| 09 | 03/06 | EV-RUN-01、EV-DB-01 | NotRun；输入冻结/恢复意图/最后答案不丢 |
| 10 | 09 S3 | EV-QUALITY-01、EV-LIVE-02 | NotRun；无综合分反例已做文档验证，尚无报告实现证据 |
| 11 | 09 S3 | EV-LIVE-02、EV-DB-01 | NotRun；反馈查询/复练完整契约待S3 |
| 12 | 01/03/04/05/06；10 S3 | EV-RUN-01、EV-SEC-01 | NotRun；主体、ticket、Origin和工具越权需安全专项 |
| 13 | 10 S3 | EV-SEC-01、EV-DB-01 | NotRun；撤回/删除/备份不复活尚未实现闭环 |
| 14 | 02；10 S3 | EV-RUN-01、EV-DB-01 | NotRun；先验启用幂等，补偿/权益台账后验 |
| 15 | 04/05；10 S3 | EV-LIVE-01、EV-OPS-01 | NotRun；真实Provider、容量和成本均未测 |

EV-BUILD-01/02/03 与 EV-RUN-01 的具体命令在执行包 C01–C04。EV-DB-01（PostgreSQL一致性/恢复）、EV-LIVE-01（真实语音）、EV-LIVE-02（界面/设备/视觉）、EV-QUALITY-01（专家质量）、EV-SEC-01（安全/隐私）、EV-OPS-01（运行/成本）仅为后续证据预留 ID，未生成占位通过记录，也尚无执行授权。

## 3. 差异边界与未完成门

本轮新增 FEAT-INTERVIEW-002 资料包及附属证据，只在六份既有文档增加入口；未改业务代码、旧0.1.0契约、生产contracts、锁文件或运行配置。恢复上下文后发现工作区已存在 PRD v0.3 与题库/流程/视觉收敛资料，保留其正文；git diff中的既有PRD大范围变化不归因于本轮入口插入。以文档检查的正文保留校验为依据，不用整个工作区diff统计冒充本轮改动。

仍待完成：规格/交互细则批准、独立公共契约和隐私复核、物理字段/约束映射、实际UI tokens、完整专用契约lint及消费者兼容检查；后续还有模型/地区/配额/成本、岗位专家样本、隐私政策/删除/反馈运营。这些限制已进入 tasks，不据此阻止已授权的资料准备，也不据资料存在进入开发/上线完成。

当前没有用户验收结论。上线就绪 NotAssessed；发布事实 NotReleased。下一步：评审 EXEC-PILOT-S1-A 0.1.0 及其上游细则，关闭适用入场门，再决定包内代码和运行动作。
