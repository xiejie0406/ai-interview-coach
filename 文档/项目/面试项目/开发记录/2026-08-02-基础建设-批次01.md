# Foundation Wave 01 开发记录

> 文档类型：开发记录  
> 文档状态：Draft  
> owner / 责任边界：主协调 Agent 记录实际差异；产品/架构/质量决定仍由各自 owner 负责  
> 创建时间：2026-08-02  
> 更新时间：2026-08-02  
> Feature ID：FEAT-INTERVIEW-001  
> 风险等级：L3  
> 产出/适用阶段：7 开发实现的前置/局部工作流  
> 阶段状态：InProgress  
> 证据结果：NotRun  
> 关联：[`功能/FEAT-INTERVIEW-001-AI面试教练/README.md`](../功能/FEAT-INTERVIEW-001-AI面试教练/README.md)、[`归档/功能/FEAT-INTERVIEW-001-AI面试教练/旧版任务清单.md`](../归档/功能/FEAT-INTERVIEW-001-AI面试教练/旧版任务清单.md)、[`归档/架构/实施技术契约候选包.md`](../归档/架构/实施技术契约候选包.md)

## 1. 本波目标与边界

- 目标：在与 PaiCLI 完全独立的新目录中，建立五个后端 Maven module、稳定公共契约、最小 Boot/健康边界和 React 生产壳，并为后续逻辑域并行编码提供单向依赖基线。
- 范围：`pom.xml`、`backend/**` 的模块与第一波生产源码、`frontend/**`、`contracts/**`、无秘密配置模板和本开发记录。
- 非目标：不修复或覆盖两份截断的 Canonical 文档；不把候选产品行为标为 Approved；不接真实 Provider、音频、支付、对象存储、Redis、云或生产数据。
- 假设：工作名和 Java package 暂用 `AI Interview Coach` / `com.aiinterviewcoach`；在没有公开发布、持久化生产数据或外部 consumer 时可受控重命名。

## 2. 授权快照

| 动作 | 状态 | 依据与范围 | 未包含 |
|---|---|---|---|
| 本地生产源码/配置修改 | Approved | 用户明确要求启用多个子 Agent 并以“编码全部完成”为目标；限本项目和现有任务候选范围 | 不改变 PaiCLI，不调用外部系统 |
| 文档联动 | Approved | 用户要求先完善文档并持续推进 | 不伪造 Gate、Approved、EV 或 UAT |
| 测试代码 | NotRequested | 本波不创建 | 单元/集成/UI/Golden/e2e 均不新增 |
| 构建、测试、启动 | NotRequested | 无精确命令授权 | 不执行 Maven、npm、服务或容器命令 |
| 真实 Provider/音频/支付/云 | NotRequested | 无账号、数据、费用和环境授权 | 不调用、不上传、不写入 |
| Git | NotRequested | 无 add/commit/push/PR/tag 授权 | 不执行任何 Git 动作 |

## 3. 版本来源与锁定范围

2026-08-02 只读查询 Maven Central metadata 与 npm registry `latest`，未安装依赖：

| 组件 | 本波声明 | 查询到的 release/latest | 适用边界 |
|---|---:|---:|---|
| Java | 21 | 项目既有推荐 | LTS 编译目标；未运行本机兼容检查 |
| Spring Boot | 4.1.0 | Maven Central release 4.1.0 | 根 parent；实际依赖解析 NotRun |
| Spring Modulith BOM | 2.1.0 | Maven Central release 2.1.0 | 仅 BOM 候选；尚未添加运行依赖 |
| React / React DOM | 19.2.8 | npm latest 19.2.8 | 前端声明；未安装 |
| Vite | 8.2.0 | npm latest 8.2.0 | 要求 Node `>=22.12.0`；未运行 |
| React Router DOM | 7.18.2 | npm latest 7.18.2 | 路由声明；未运行 |
| TanStack Query | 5.101.4 | npm latest 5.101.4 | 服务端状态；未运行 |
| Zustand | 5.0.14 | npm latest 5.0.14 | 仅短期 UI 状态；未运行 |

版本被记录为本波可复现输入，不代表兼容性已经验证；若构建证据失败，回到依赖决策而不是静默换版本。

## 4. 已发生动作

- 创建根聚合 POM、`backend/pom.xml` 和 `domain/application/adapters/boot/test-support` 五 module POM。
- 创建 `.editorconfig`、`.gitignore`、不含真实 Secret 的 `.env.example`。
- 创建 `ErrorEnvelope v1`、`EventEnvelope v1`、公共 OpenAPI/AsyncAPI 草案。
- 创建 Boot main、健康端点、correlation ID、统一错误和默认拒绝安全基线；建立只创建 schema namespace 的 `V001` migration。
- 创建 React/Vite 路由、API client、公共异步/错误状态和 public/auth/workspace/admin/status 页面第一波源码。
- 启动互斥子 Agent：`domain/application` 与 `adapters/boot`，只允许在其声明范围内增量写入。

## 5. 当前事实与限制

- 代码正在形成，只能称“局部实现中”。
- `application.yaml`、POM、TypeScript 与 Java 尚未经过编译或运行验证。
- 前端页面会调用尚未全部存在的 API；错误/空态用于真实失败反馈，不等于后端能力已实现。
- 两处 Canonical 截断、Gate A/C、原型评审和用户批准状态没有被本波改变。
- 真实 Provider、语音质量、数据地域、删除 SLA、支付渠道和生产配置仍必须在各自实现包前关闭。

## 6. 停止与恢复

- 发现 domain 依赖 Spring/JPA/HTTP、跨域 Repository、前端保存长期 token、日志写入敏感正文、migration owner 冲突或 PaiCLI 依赖时立即停止对应写入线。
- 需要改变公共 API、schema、产品状态或外部数据政策时退回阶段 3/5/6，由单一 owner 修订候选。
- 子 Agent 完成后由主协调 Agent执行只读集成审查；未获运行授权时结果继续为 `NotRun`。
