# 2026-08-03 产品文档合并与 Wave 3 控制记录

> 记录类型：文档治理与并行实施控制记录  
> 文档状态：Draft  
> 风险等级：L3  
> 当前主阶段：阶段 3 功能规格 / WaitingForApproval  
> 运行证据：NotRun  

## 1. 本次完成

- 将 `docs/review/product-decision-pack-v2.md` 的独有调研来源、用户画像、完整旅程、信息架构、全量候选功能审计、PRD 完整性审计、范围包和候选决策点，合并到 `docs/review/产品全量审计与权威规格恢复候选.md` 的附录 A。
- 保留 `产品全量审计与权威规格恢复候选.md` 作为 `docs/review` 唯一产品审查入口；旧文件已按用户“最终只保留一份”的明确要求删除。
- 更新 Prototype、Implementation Readiness 和窗口 01/02 的引用，未发现残余旧文件名引用。
- 修正 Feature 控制页：主阶段保持阶段 3 `WaitingForApproval`；阶段 4–6 产物是前瞻 Draft/`NotStarted`，不是已批准或正式进入下游阶段。
- 新增窗口 28、29、30，分别负责 Interview/Voice 原子收口、Boot/Session Security 装配、Persistence 契约回填。
- 更新 Wave 3 索引和最终静态审查项，明确重叠目录必须串行。

## 2. 关键依赖判断

窗口 22 可以与 Core Application 并行，但 Evaluation/Learning 旧模型已进入其 migration 设计输入；窗口 27 会改变 Evaluation state/stage、Evidence、Rubric、ReportVersion、LearningPlan/LearningItem 权威字段。因此必须增加窗口 30，在窗口 22、27、28 都交接后回填 Entity/SQL 映射，再启动 REST/SSE/WebSocket Adapter。

推荐顺序（窗口 32 与交叉审查补入后）：

1. 窗口 21、22、23 首批并行并完成交接。
2. 窗口 23 → 31，随后窗口 21/31 → 27 契约收口。
3. 窗口 23/27/31 → 28 InterviewSnapshot/Voice Answer 原子收口。
4. 窗口 22/27/28/31 → 30 Persistence 契约回填。
5. 窗口 30 → 32 Durable Stream/Replay。
6. 窗口 24、25 以冻结契约并行；窗口 29 等待 24。
7. 所有写窗口释放目录后，窗口 26 做最终只读静态审查。

## 3. 只读检查

- `docs/review` 当前只有 `产品全量审计与权威规格恢复候选.md` 一个文件。
- 合并文件可定位原旧稿中的 `SRC-01`、`CAND-FUNC-PUB-001`、三种范围包和 `CAND-DEC-PROD-015`。
- 全项目 Markdown 搜索未发现 `product-decision-pack-v2` 残余引用。
- Wave 3 索引当前包含 1 个只读协调窗口和 12 个执行/审查窗口；新增窗口 32，显式记录 23→31→27、23/27/31→28、22/27/28/31→30、30→32→24、24→29 的串行关系。

以上是文本级静态检查，不是构建、测试、UAT 或运行证据。

## 4. 未执行

- 未修改被截断的 `docs/product/产品需求文档.md` 或 `docs/architecture/技术架构.md`。
- 未运行 Maven/npm 构建、测试、服务、数据库或 migration。
- 未调用真实 Provider、ASR/TTS、支付、对象存储或部署系统。
- 未执行 Git。

## 5. 剩余阻断

- Gate A/Gate C、Canonical PRD 截断处理和技术架构截断处理仍待用户决定。
- 现有源码和契约均是候选；即使静态可定位，也不能证明可编译、可运行或满足 AC。
- 测试代码、构建/测试执行、浏览器 UAT、外部能力验证、部署与 Git 仍需各自独立授权。

## 6. 后续提示词拆分

静态审查确认旧窗口 24 同时覆盖 64 个 OpenAPI operation、SSE、WebSocket、Security，旧窗口 25
覆盖全部 frontend feature，不适合作为直接并行执行包。已新增 Wave 4 窗口 33–48：

- 33 协调；34/41 两条基础边界。
- 35–38 四组 REST owner，39 SSE，40 WebSocket。
- 42–44 三组前端 feature owner。
- 45/46 两次串行 reconciliation，47 Boot wiring，48 最终只读审查。

Wave 4 索引链接完整，窗口 33–48 编号连续；64 个 operation 分配为 health 现有 owner + 35–38
合计 63 个 owner。选择该方案后不得再并行运行旧 24/25/29/26。
