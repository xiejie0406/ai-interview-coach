# FEAT-FASHION-001 验证记录

> 文档类型：技术验证记录；版本：0.17.16；文档状态：Draft
> Feature：FEAT-FASHION-001；风险：L2  
> 当前阶段：Stage 8 审查验证；阶段状态：InProgress  
> owner：Codex 汇总实际证据；用户验收另见[验收记录](验收记录.md)  
> 创建日期：2026-09-12；更新日期：2026-09-25
> 输入：[任务清单](任务清单.md)、[数据库设计 v2.3](../../架构/数据库设计.md)、[`fashion-ai-runtime`](../../../../../python/fashion-ai-runtime/README.md)、[`ai-runtime.openapi.yaml`](../../../../../contracts/fashion/ai-runtime.openapi.yaml)

## 1. 当前结论

`IMP-01`～`IMP-09` 已完成实现、自审和最低自动验证，九个阶段的证据结果均为 **Pass**。IMP-10 可在本地执行的容量、并发、完整 Java 进程与 Python 进程重启、Java↔Python loopback 真网络、逻辑备份恢复、安全、契约、文件兼容和共享后台 18 模块聚合打包检查已通过。IMP-10 阶段退出门仍为 **Blocked**：尚缺生产候选环境、真实 Provider 账号与费用授权、获批真实样本、候选环境目标 Office/WPS 以及正式 UAT。

上述 Pass 关闭 IMP-01～IMP-09 退出门；REQ 01～17 及 AC 01～31 已取得生产实现和自动化/桌面场景证据，正式 UAT 仍为 NotStarted。AC 32 和上线就绪结论没有关闭；当前 `ReleaseReady=No`、`Release=NotReleased`，不得把本地候选验证写成生产候选环境或用户验收通过。

原型已经完成，但原型验证不替代本生产 Feature 的 Java、Vue、MySQL、真实服务、文件兼容或 UAT 证据。

## 2. IMP-01 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 定向测试 | `mvn -f platform-backend/pom.xml -pl ruoyi-fashion -am test` | 25 tests，0 failure/error；覆盖配置 fail-closed、Base64 key、active/previous、401/403/429、双向向量、契约和 telemetry | Pass |
| Java 总后台装配 | `mvn -f platform-backend/pom.xml -pl ruoyi-admin -am -DskipTests package` | 15 个 reactor module 全部 SUCCESS，`ruoyi-admin.jar` 生成 | Pass |
| Admin 类型 | `npm --prefix admin-web run typecheck` | `vue-tsc` 退出码 0 | Pass |
| Admin 单测 | `npm --prefix admin-web run test:unit` | 2 files / 20 tests | Pass |
| Admin 生产构建 | `npm --prefix admin-web run build:prod` | Vite 6.4.3，2556 modules，构建成功 | Pass |
| Admin 浏览器 | `npm --prefix admin-web run test:e2e:fashion` | Chrome 1/1；验证同源边界和实际 SVG symbol。Java 未启动导致 `/captchaImage` 代理 `ECONNREFUSED`，测试场景不依赖该响应 | Pass（有限场景） |
| Admin 产物扫描 | 对 `admin-web/dist` 搜索 Python 地址、`X-Fashion-*`、服务身份和 Provider Secret 模式 | 0 matches | Pass |
| Python 测试 | `uv run --project fashion-ai-runtime --all-extras --frozen pytest -p no:cacheprovider fashion-ai-runtime/tests` | 113 passed，1 个 Starlette TestClient 弃用 warning | Pass |
| Python 静态门 | Ruff＋Pyright strict | Ruff 全过；0 errors / 0 warnings / 0 informations | Pass |
| Runtime 生成契约 | 执行 `scripts/export_openapi.py` 并比较前后 SHA-256 | 前后均为 `4415D4683481DADE5237DCF80D9797115561EABFAC15ED3E012E75DB741B9E3F` | Pass |
| Python 依赖审计 | `uvx pip-audit --path fashion-ai-runtime/.venv/Lib/site-packages` | No known vulnerabilities；本地包自身不可从 PyPI 审计而跳过 | Pass |
| Admin 依赖审计 | 官方 registry，High 门和 production-only High 门 | 退出码 0；0 High/Critical，仍报告既有 ECharts 1 Moderate | Pass（有记录风险） |

安全场景实际覆盖：缺失/伪造/分块/实际超限正文在解析前返回 413；无效认证和跨 key 重放为 401；签名有效但 service/audience 无权限为 403；nonce store 容量耗尽为 429；共享契约定义 408/409/410/422/503/504；request/correlation ID 与 W3C Trace Context 传播、低基数路由标签、白名单日志和无 exporter 边界均有测试。

依赖版本、许可证、扫描输出、失败修正和剩余限制详见[IMP-01 依赖与安全复核](证据/阶段-01/依赖与安全复核.md)。

## 3. IMP-02 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| 隔离 MySQL | 向 `FashionProvidedMySqlMigrationTest` 提供临时 MySQL 8.0.46 URL | 3 tests，0 failure/error；RuoYi baseline、Flyway V1、重复迁移、约束、命名锁、回滚、平台脚本均实际执行 | Pass |
| Schema 精确性 | `information_schema` 查询＋DDL/设计对照 | `fq_*` 恰好 T01～T16；独立 history 不计业务表；禁建表为 0；JSON/DECIMAL/DATETIME(3)/row_version/FK/唯一/检查均有证据 | Pass |
| Java 回归 | `mvn -f platform-backend/pom.xml -pl ruoyi-fashion -am test` | 51 tests，0 failure/error，6 条条件测试 skipped；隔离 MySQL 3 条已由上一行单独实际执行 | Pass |
| 后台装配 | `mvn -f platform-backend/pom.xml -pl ruoyi-admin -am -DskipTests package` | 15 个 reactor module 全部 SUCCESS，`ruoyi-admin.jar` 生成 | Pass |
| 权限/资源/配置 | Spring Security MockMvc、客户 Guard、字典 Guard、严格 JSON/配置注册表 | 未登录 401、已登录缺权限 403、越权拒绝、停用/未知字典拒绝、未知配置/字段/范围拒绝 | Pass |
| Admin | typecheck、Vitest、生产构建 | `vue-tsc` 0；3 files / 22 tests；Vite 2561 modules 构建成功 | Pass |
| 依赖复核 | Maven dependency tree＋本地 POM 许可元数据＋GitHub Advisory 定向核对 | Flyway 11.14.1、Connector/J 9.7.0、Testcontainers 2.0.5、Spring Security Test 7.0.5；许可与已知受影响版本已留痕 | Pass（定向复核） |

详细环境、SQL 统计、命令失败与修正、临时实例回收和残余边界见[IMP-02 MySQL 与权限底座验证](证据/阶段-02/MySQL与权限底座验证.md)。该记录没有把隔离数据库结果冒充非隔离数据库迁移，也没有把设置页底座冒充完整业务页面。

## 4. IMP-03 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 回归 | `mvn -f platform-backend/pom.xml -pl ruoyi-fashion -am test` | 61 tests，0 failure/error，7 条条件测试 skipped | Pass |
| 隔离 MySQL 商品/图片业务 | 向 `FashionProvidedMySqlMigrationTest` 提供临时 MySQL 8.0.46 URL | 4 tests，0 failure/error/skipped；商品导入、图片确认、冲突和不污染价格库存均实际执行 | Pass |
| 后台装配 | `mvn -f platform-backend/pom.xml -pl ruoyi-admin -am -DskipTests package` | 15 个 reactor module 全部 SUCCESS，生成 `ruoyi-admin.jar` | Pass |
| 安全和文件控制 | Spring Security MockMvc、流式 XLSX/CSV、图片 magic/大小/像素/对象键测试 | 商品 API 401/403、独立权限、公式/多 Sheet/数值编码、WebP 与损坏图边界均通过 | Pass |
| Admin 类型与单测 | `npm --prefix admin-web run typecheck`、`npm --prefix admin-web run test:unit` | `vue-tsc` 退出码 0；5 files / 26 tests | Pass |
| Admin 生产构建 | `npm --prefix admin-web run build:prod` | Vite 2590 modules，构建成功 | Pass |
| Admin 浏览器 | `npm --prefix admin-web run test:e2e:fashion` | Chrome 2/2；商品编辑/上下架、CSV 预览发布、素材预览确认通过 | Pass |

隔离 MySQL 最终统计为 `products=3`、`stock_rows=0`、有效商品批次 1、有效图片批次 2、无效图片 2、并发冲突 1、错色关联 0、库存污染 0；款号 `000123` 保留前导零。浏览器场景模拟 Java API，只证明前端流程和同源边界，不证明 Java 已通过浏览器联机或京东真实下载链路。完整命令、失败修正、临时实例回收和限制见[IMP-03 商品与图片素材中心验证](证据/阶段-03/商品与图片素材中心验证.md)。

## 5. IMP-04 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 回归 | `mvn -f platform-backend/pom.xml -pl ruoyi-fashion -am test` | 65 tests，0 failure/error，8 条条件测试 skipped | Pass |
| 隔离 MySQL 价格/库存 | 向 `FashionProvidedMySqlMigrationTest` 提供临时 MySQL 8.0.46 URL | 5 tests，0 failure/error/skipped；零值、缺行、旧时间、幂等、冲突、回滚与恢复通过 | Pass |
| 后台装配 | `mvn -f platform-backend/pom.xml -pl ruoyi-admin -am -DskipTests package` | 17 个 reactor module 全部 SUCCESS，生成 `ruoyi-admin.jar` | Pass |
| Admin 类型与单测 | `npm --prefix admin-web run typecheck`、`npm --prefix admin-web run test:unit` | `vue-tsc` 退出码 0；6 files / 28 tests | Pass |
| Admin 生产构建 | `npm --prefix admin-web run build:prod` | Vite 2598 modules，构建成功 | Pass |
| Admin 浏览器 | `npm --prefix admin-web run test:e2e:fashion` | Chrome 3/3；价格缺行阻断、零值发布/恢复、库存零值发布及既有场景通过 | Pass |
| Admin 产物扫描 | 对 `admin-web/dist` 搜索 Python 地址、服务身份和 Provider Secret 模式 | 0 matches | Pass |

隔离 MySQL 最终统计为 `imp04_products=2`、`imp04_stock_rows=2`、`price_zero=1`、`restore_success=2`、`invalid_batches=2`、`conflict_batches=1`、`rollback_batch_validated=1`、`role_grants=0`。临时实例已停止并回收精确测试目录，既有 MySQL 未受影响。完整命令、失败修正和限制见[IMP-04 价格库存严格全量验证](证据/阶段-04/价格库存严格全量验证.md)。

## 6. IMP-05 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 回归 | `mvn -f platform-backend/pom.xml -pl ruoyi-fashion -am test` | 69 tests，0 failure/error，9 条条件测试 skipped | Pass |
| 隔离 MySQL 客户/方案/Agent/Run | 向 `FashionProvidedMySqlMigrationTest` 提供全新临时 MySQL 8.0.46 URL | 6 tests，0 failure/error/skipped；密文、权限、发布、接管、fencing、取消、重试、商品采用通过 | Pass |
| Python typed output 与契约 | pytest、Ruff、Pyright、确定性 OpenAPI 导出 | 117 passed / 1 warning；Ruff 全过；Pyright 0/0/0；提交契约与运行时模型一致 | Pass |
| Admin 类型与单测 | `npm --prefix admin-web run typecheck`、`npm --prefix admin-web run test:unit` | `vue-tsc` 退出码 0；7 files / 31 tests | Pass |
| Admin 构建与浏览器 | `npm --prefix admin-web run build:prod`、`npm --prefix admin-web run test:e2e:fashion` | Vite 2535 modules；Chrome 4/4 | Pass |
| Provider 禁用与脱敏 | capability/503、密文列、日志/指标白名单、产物与源码定向扫描 | Provider 未配置时 fail closed；客户原文、手机号和 Secret 不进入指标标签；无仓库生产密钥 | Pass |

需求 typed analyzer 场景把采购量 100、四个递进品类档位、每档三个候选及每套 300 元预算分别写入独立字段；它证明契约和应用链路，不代表真实模型质量。商品建议只允许品类、颜色、季节及风格/场景/人群/外观标签，品牌、面料、尺码、价格、库存和客户字段被排除。完整命令、真实 MySQL 结果、执行中失败与修正见[IMP-05 客户方案与需求确认验证](证据/阶段-05/客户方案与需求确认验证.md)。

## 7. IMP-06 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 回归 | `mvn -f platform-backend/pom.xml -pl ruoyi-fashion -am test` | 76 tests，0 failure/error，10 条条件测试 skipped | Pass |
| 隔离 MySQL 选品持久化 | 向 `FashionProvidedMySqlMigrationTest` 提供全新临时 MySQL 8.0.46 URL | 7 tests，0 failure/error/skipped；selection Run、候选尺码 SKU、组合和数量分配均实际落在已有 16 表 | Pass |
| Python typed 选品与契约 | pytest、Ruff、Pyright、确定性 OpenAPI 导出 | 124 passed / 1 warning；Ruff 全过；Pyright 0/0/0；契约 SHA-256 为 `93AF7CB4225E4EDE20F30AA94A90DAB1B94BF672A57BAFE25390A27E2E4FD187` | Pass |
| Admin 类型、单测与构建 | `vue-tsc`、Vitest、Vite | 类型检查退出码 0；7 files / 32 tests；2536 modules 构建成功 | Pass |
| Admin 浏览器 | Playwright Chrome | 4/4；方案工作台 disabled/无解恢复及既有回归通过 | Pass（Mock Java API） |

硬过滤、候选冻结、伪造 SKU 拒绝、独立/递进、锁定、去重、无解、商品变化 409、采用事务、尺码分配与刷新恢复的完整命令、发现、修正及限制见[IMP-06 一至四品类选品与人工搭配验证](证据/阶段-06/一至四品类选品与人工搭配验证.md)。隔离 MySQL 已停止并回收，Provider 仍 disabled，未执行 UAT。

## 8. IMP-07 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 全量回归 | `mvn -pl ruoyi-fashion -am test` | 89 tests，0 failure/error，11 条数据库条件测试 skipped | Pass |
| Java 图片/观测定向 | `FashionQuoteImageServiceTest,FashionTelemetryTest` | 最终代码 15 tests，0 failure/error/skipped | Pass |
| 隔离 MySQL 图片持久化 | `run-isolated-mysql-tests.ps1` 启动临时 MySQL 8.0.46 | 8 tests，0 failure/error/skipped；图片任务、领取、部分结果、复核、采用和过期历史均实际落在既有 16 表 | Pass |
| Python 图片契约与静态门 | pytest、Ruff、Pyright、确定性 OpenAPI 导出 | 127 passed / 1 warning；Ruff 全过；Pyright 0/0/0；契约 SHA-256 `913BF2E1EC59B03D5A6A19B7AB48ACCDB4EA3BFC6331BD15BAFC45E1ED4A9FC9` | Pass |
| Admin 类型、单测与构建 | `vue-tsc`、Vitest、Vite | 类型检查退出码 0；8 files / 34 tests；2560 modules 构建成功 | Pass |
| Admin 浏览器 | Playwright Chrome | 5/5；图片样例、上传、逐项复核、采用和既有回归通过 | Pass（Mock Java API） |
| 权威边界 | 16 表、Admin 直连 Runtime、Python 数据库访问扫描 | 16 张 `fq_*`；Admin 无 Runtime 内部地址；Python 无 MySQL/JDBC/业务 SQL 写入 | Pass |

外部上传、样例与拼版来源分离；部分成功保留结果；同请求键不同意图返回 409；人工查看不等于复核，完整复核后才能采用；组合/原图/权限变化使旧图过期；UNKNOWN 只按原键回查且不自动退款；取消晚到受 rowVersion fencing 阻断。完整命令、失败修正、临时实例回收及限制见[IMP-07 图片生成异步状态与人工复核验证](证据/阶段-07/图片生成异步状态与人工复核验证.md)。真实 Provider 仍 disabled，未执行视觉质量 UAT。

## 9. IMP-08 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 全量回归 | `mvn -pl ruoyi-fashion -am test` | 110 tests，0 failure/error，12 条条件数据库测试 skipped | Pass |
| Java 报价定向 | `QuotePricingCalculatorTest,FashionQuotePricingServiceTest,FashionQuotePricingControllerSecurityTest` | 20 tests，0 failure/error/skipped | Pass |
| 隔离 MySQL 报价持久化 | `pwsh -NoProfile -File ruoyi-fashion/scripts/run-isolated-mysql-tests.ps1` | MySQL 8.0.46，9 tests，0 failure/error/skipped；最终实例只绑定 `127.0.0.1:11123` 并在完成后回收 | Pass |
| 真实并发与冻结 | `FashionProvidedMySqlMigrationTest` 第九场景 | 精确总额 `24050.00`；价格变化使旧 hash 失效；并发确认只有一次版本推进；当前价格/库存/图片变化不改写历史冻结值；复制修订成功 | Pass |
| 后台装配 | `mvn -pl ruoyi-admin -am package -DskipTests` | 18 个 reactor module 全部 SUCCESS，生成 `ruoyi-admin.jar` | Pass |
| Admin 类型、单测与构建 | `vue-tsc`、Vitest、Vite | 类型检查退出码 0；9 files / 35 tests；2561 modules 构建成功 | Pass |
| Admin 浏览器 | Playwright Desktop Chrome | 6/6；精确金额、库存 90 阻断、修正、确认冻结、复制修订及既有回归通过 | Pass（Mock Java API） |
| 权威边界 | 16 表、Admin Runtime 引用、Python 数据库访问扫描 | 16 张唯一 `fq_*`；禁建表 0；Admin Runtime 内部引用 0；Python MySQL/JDBC/业务 SQL 引用 0 | Pass |
| Python | 本阶段未修改 Python 或 Runtime 实现 | 未重复运行；IMP-07 的 Python 结果仅保留为既有基线 | NotApplicable |

金额只在 Java 使用 `BigDecimal` 计算；方案头与组合数量、尺码行数量、同 SKU 跨组合库存、库存业务时间、商品/价格/税率/币种、主图权限及采用图人工复核均在正式确认前重检。超 10% 折扣或任一单价调整需要他人对精确内容 hash 审批；内容变化、过期和自审均拒绝。零单价、100% 优惠和零应付正式报价始终阻断。完整命令、失败修正、临时实例回收和限制见[IMP-08 确定性报价库存复核与版本冻结验证](证据/阶段-08/确定性报价库存复核与版本冻结验证.md)。

## 10. IMP-09 退出门证据

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 全量回归 | `mvn -pl ruoyi-fashion -am test` | 123 tests，0 failure/error，13 条条件数据库测试 skipped | Pass |
| 隔离 MySQL 交付持久化 | `pwsh -NoProfile -File ruoyi-fashion/scripts/run-isolated-mysql-tests.ps1` | MySQL 8.0.46，10 tests，0 failure/error/skipped；实例完成后自动停止和清理 | Pass |
| 后台装配 | `mvn -pl ruoyi-admin -am package -DskipTests` | 18 个 reactor module 全部 SUCCESS，生成 `ruoyi-admin.jar` | Pass |
| Admin 类型、单测与构建 | `vue-tsc`、Vitest、Vite | 类型检查退出码 0；11 files / 37 tests；2569 modules 构建成功 | Pass |
| Admin 浏览器 | Playwright Chrome | 7/7；交付定向复跑 1/1；覆盖任务轮询、下载、窄屏/焦点、异常查询和保留 dry-run | Pass（Mock Java API） |
| 真实桌面文件 | WPS 演示 12.1.0.28043 打开 Renderer 输出 | 12 页可见；封面标题可编辑；以 PPTX 另存成功；另存文件 XML 包含新增文字，SHA-256 为 `6BD77C87FB8CAF1A1F45F95E54D63369A10CC6EF09685F92B950565D18AFAFD3` | Pass |
| 权威与安全边界 | 16 表、Admin Runtime 引用、Python数据库引用、敏感模式扫描 | `fq_*` 精确 16 张，禁建表 0；浏览器不直连 Python；Python 无 MySQL/JDBC/业务 SQL；交付文件不含内部字段 | Pass |

PPTX/CSV/ZIP/JPG 均从单一已确认报价快照生成；渲染过程不持有数据库事务，任务完成受 lease/fencing 和摘要校验保护。下载重新鉴权并在成功刷出后计数；真实删除保持关闭，保留 dry-run 能区分候选、引用保护和已延长。完整命令、修正过程、文件摘要、桌面软件证据与限制见[IMP-09 文件交付与运维保留验证](证据/阶段-09/文件交付与运维保留验证.md)。

## 11. IMP-10 本地验证与阻断结论

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 全量回归 | `mvn -pl ruoyi-fashion -am test` | 129 tests，0 failure/error，18 条条件测试 skipped；其中 10 条普通 MySQL、容量/恢复各 1 条及 3 条真网络条件测试均由独立脚本实际运行 | Pass |
| 完整 Java 进程重启 | `run-local-java-restart-tests.ps1` | 随机 loopback 端口和独立 MySQL 8.0.46；导入若依基线并执行 Fashion migration；AI Run/图片/交付三类 Worker 开启；`ruoyi-admin.jar` 首次及不同 Java PID 再启动均 HTTP 就绪，前后 `fq_*` 均为 16，持久化标记为 1；AI Worker 在首次启动及重启后各自动把 1 条已过 deadline 的 Run 推进为 `failed/DEADLINE_EXCEEDED`，两条均保持 `run_attempt=0`、`current_step_no=0` 且无 Run Step；临时目录和相关 Java/MySQL 进程残留为 0 | Pass（本机完整进程与真实 Worker 调度；未领取任务、未调用 Provider） |
| Java↔Python 真网络与重启 | `run-live-runtime-integration-tests.ps1` | 随机 loopback 端口启动 Uvicorn；真实 HMAC capabilities、非空 POST、correlation/trace 和 503 错误映射 3/3 通过；停止并启动不同 Python 服务进程后再次 3/3 通过；端口、进程和临时日志均回收 | Pass（本机 HTTP socket） |
| 观测、通知与保留 | Java operations/telemetry 测试、Admin 运维场景、真网络 trace、retention dry-run | trace 跨进程保持同一 trace ID；低基数指标、异常列表和阈值候选可读；未配置通知渠道时显式返回/展示 `not_configured`，不声称已送达；保留只列候选与引用保护，不执行删除 | Pass（本地机制；采集端/通知渠道 NotRun） |
| 隔离 MySQL 业务回归 | `pwsh -NoProfile -File run-isolated-mysql-tests.ps1` | 当前工作树使用 PowerShell 7.6.5 复跑 MySQL 8.0.46，10/10 tests、0 skipped；Flyway 首次迁移和再次幂等检查成功；实例进程与临时目录均回收 | Pass |
| 十万级容量与恢复 | `run-isolated-capacity-recovery-tests.ps1` | 当前工作树权威复跑使用 Druid 5/10/20 连接池并预热 20 个连接：100000 商品、100000 导入明细；100 次“分页检索＋精确总数”P95 688 ms；20 并发总 P95 2801 ms，其中检索 P95 495 ms、计数 P95 2630 ms；MySQL 重启 2880 ms；逻辑恢复到新库 35278 ms，抽样 RPO 0，原库/恢复库各 1/1 恢复测试通过；进程与临时目录残留为 0 | Pass（本地隔离环境） |
| 大文件和 PPTX | 100000 行价格 CSV、22 页/44 图片 PPTX | CSV 6000069 bytes，解析 765 ms；PPTX 68111 bytes，生成 5641 ms；可编辑商品原图清单已进入包结构 | Pass（本地） |
| Python 回归与安全 | pytest、Ruff、Pyright、确定性 OpenAPI 导出、恶意提示/URL 文本 | 151 passed / 1 个既有弃用 warning；Ruff 全过；Pyright 0/0/0；契约 SHA-256 前后均为 `913BF2E1EC59B03D5A6A19B7AB48ACCDB4EA3BFC6331BD15BAFC45E1ED4A9FC9`；disabled Provider 仍 fail closed | Pass |
| Admin 与后台装配 | 类型、单测、构建、Chrome 以及 Maven package | 当前工作树复跑类型检查成功、11 files / 37 tests、2574 modules 生产构建成功；Chrome 7/7 沿用本阶段既有证据；`ruoyi-fashion` 5 模块定向 package SUCCESS；共享 `ruoyi-admin` 18/18 模块 SUCCESS 并生成 `ruoyi-admin.jar`。期间一次 APS 编译瞬时失败未修改 APS 源码，最终复跑通过 | Pass（浏览器使用 Mock Java API） |
| 权威与泄露边界 | 物理表、禁建表、源码/产物定向扫描 | `fq_*` 恰好 16 张、禁建表 0；Python 无业务数据库引用；Admin 无 Runtime 内部引用；交付包不含内部成本、备注或追踪字段 | Pass |
| 候选环境输入门 | `test-candidate-validation-preflight.ps1`、`run-candidate-validation-preflight-tests.ps1` | 9/9；未填写模板离线输出 `ready=false`、33 个精确差距；完整合成输入输出 `ready=true`、0 个差距并由 Python 严格模型实读；无时区授权时间、结束不晚于开始的 UAT 窗口、低于一分/超过两位小数的费用、偏离 PRD 8/10 的门槛、明文密码字段、输入输出重叠分别拒绝；获批费用/门槛写入带清单摘要的报告并原子替换；保持无外部动作且临时残留 0 | Pass（仅输入门机制；真实输入仍 NotProvided） |
| 真实视觉试点判定器 | `test_visual_trial_evaluator.py`、`fashion-evaluate-visual-trial` | 8 tests；固定 40 个预登记任务、每组 10 个和一致 Provider/配置/评审口径；验证 8/10 通过、7/10 失败、失败样本仍在 40 分母、带时区时间及复核后采用顺序、未结账单/零采用证据 Blocked、关键款色错误/四品类缺件 Fail、费用/耗时聚合、任务替换拒绝、实际输入字节 SHA-256 原子报告及未填写模板退出码 2/不产报告；安装后 CLI `--help` 成功 | Pass（离线判定机制；真实 40 任务 NotRun） |
| 候选就绪最终门 | `test_candidate_readiness.py`、`fashion-evaluate-candidate-readiness` | 15 tests；实际读取原始授权清单并核对预检/视觉/技术 UAT 三方摘要，预检后清单变化即阻断；精确核对 7 个技术 gate、UAT-01～06、业务接受决定、获批费用、视觉 40/10/8 及双方安全声明；16 表、封闭分母、失败保留、无外部动作与不含发布决定均不可漂移；执行身份/时区和证据文件实读 SHA-256 受严格校验；`accepted` 不得夹带条件；输出重叠在判定前退出且授权清单摘要不变，授权清单含 `clientSecret` 时不产报告；最终报告固定 `ReleaseAuthorized=false`、`NotReleased` | Pass（离线判定机制；真实候选证据/UAT NotRun） |
| 生产候选环境与 UAT | 真实 Provider、获批样本、目标 Office/WPS、候选环境端到端、业务验收 | 未提供相应环境、账号、费用上限、样本授权与验收人，未执行外部调用或发布 | Blocked |

完成性审计再次扫描当前工作区：非模板候选清单、视觉试点结果和就绪证据均为 0，`authorizationStatus=approved` 清单为 0。随后对原始模板执行离线 `-AllowIncomplete` 预检，当前结果仍为 `ready=false`、33 个缺口；机器报告位于 `platform-backend/ruoyi-fashion/target/completion-audit-candidate-preflight.json`。该报告证明下一动作必须由实际授权/环境/数据/UAT 输入触发，不是对这些外部项的通过证据。

本轮同时修正容量、协议和完整容器装配漂移：商品和字典值导入上限由 5000 行恢复为 PRD 约定的 100000 行；上传策略固定为导入 30 MiB、单图 20 MiB、ZIP 压缩 200 MiB/解压 500 MiB/1000 项，并阻断超过 100 倍的解压膨胀；Java Runtime adapter 显式固定 HTTP/1.1，避免 JDK h2c 使 Uvicorn 丢失 POST 正文；Fashion JSON databind/core 统一迁移到平台 Jackson 3；受 Repository 异常翻译或方法安全切面代理的实现类移除 `final`，完整共享后台已能启动和重启。完整环境、指标、失败修正、恢复动作和候选环境清单见[IMP-10 生产候选本地验证与上线就绪评估](证据/阶段-10/生产候选本地验证与上线就绪评估.md)。

因此 `IMP-10` 当前是 `WaitingForApproval / Blocked`，不是 `Completed / Pass`。本地可执行检查已经完成，不代表真实 Provider 质量、生产容量、目标客户端兼容、UAT、上线就绪或发布。

## 12. Python 开工前骨架验证（历史）

| 字段 | 实际记录 |
| --- | --- |
| 时间 | 2026-09-12 |
| 环境 | 仓库隔离环境 `.runtime/fashion-ai-runtime-venv`；未修改全局 Python |
| 命令 | `uv run --project fashion-ai-runtime --extra test --extra agent --frozen pytest -p no:cacheprovider` |
| 当前结果 | `41 passed, 1 warning` |
| 结果来源 | 主任务现有运行结果；本 Feature 资料包创建过程未重新执行。主任务最终复核若发现差异，应保留原记录并追加更正，不覆盖历史事实 |
| warning | 测试客户端相关弃用警告；不影响本次退出码，但应在依赖升级任务中处理 |
| 覆盖 | health、capabilities、disabled Provider 503、稳定 422、严格 ID/期限/版本、客户引用边界、需求草案、品类档位、预算、未知客户/商品/SKU/价格/库存字段拒绝、提交契约对照 |
| 未覆盖 | 服务启动、端口、网络、真实 Provider、真实客户/商品数据、Java 消费者、数据库、并发 worker、性能和生产认证 |

`41 passed` 只证明测试实际覆盖的当前 Python 骨架，不能换算为 REQ 01–17 或 AC 01–32 通过。

## 13. 开工前独立审查结论（历史）

| 审查项 | 结论 | 影响 |
| --- | --- | --- |
| fail-closed disabled 骨架 | 可保留 | 可以继续作为安全开发起点；不得称真实 AI |
| Java 集成契约冻结 | 当时未批准；2026-09-13 已完成 IMP-01 复核 | 当前以双 OpenAPI、共享 Schema、服务认证规范和双向向量为准 |
| Provider-ready | 当时未批准；当前仍未成立 | 服务认证、解析前 body limit、持久化租约/fencing/idempotency 和费用状态已完成；真实 Provider、质量与生产运行证据仍未完成 |
| 业务事实归属 | 方向成立 | 客户、商品、价格、库存、预算、报价和采用必须由 Java/MySQL 权威实现并验证 |
| 数据库设计收敛 | v2.3 设计边界已确认，IMP-02 已在隔离 MySQL 验证 | P0 仅允许 6 模块 16 张 `fq_*` 业务表；Java/MySQL 持有六张 AI 生命周期表，Python 不得建立第二状态源；非隔离数据库仍未迁移 |

上述内容保留 2026-09-12 开工前事实。`IMP-01` 项已由后续证据更新；真实 Provider 与业务质量仍不能由基础测试数量消除。

## 14. 尚未执行的生产验证

| 范围 | 状态 | 当前事实 |
| --- | --- | --- |
| `ruoyi-backend/ruoyi-fashion` 数据库、权限及商品/数据/客户/方案/Run/图片/报价/交付 | Pass（IMP-02～09） | 16 表 migration、限定持久化、商品/素材/价格/库存、客户/方案、Run、选品搭配、图片、确定性报价/冻结、文件交付及运维保留 API 已通过；候选环境仍未部署 |
| MySQL v2.3 T01–T16 migration、精确表名、约束、事务、并发与恢复 | Pass（本地隔离环境） | 10 万商品、10 万明细、20 并发、重启及逻辑恢复已通过；候选环境数据库仍未迁移或演练 |
| `frontend/admin-web/src/views/fashion` 设置、商品、数据、客户、方案、Agent、选品、图片、报价、交付和运维页面 | Pass（IMP-02～09） | 类型、单测、构建及 Chrome 场景已通过；浏览器证据使用 Java API Mock，候选环境联调未执行 |
| Java 与 Java ↔ Python 真实进程 | Pass（本地进程） | 完整 `ruoyi-admin.jar` 在隔离 MySQL 上以不同 Java PID 重启并复核 HTTP/16 表/持久化标记，AI Worker 在两次进程中均实际推进已超时 Run 且未领取/调用 Provider；本机真实 loopback socket 已验证 HMAC、非空 POST、correlation/trace、503 映射及 Python 不同 PID 重启前后调用；候选环境私网 TLS、共享 nonce 和多实例仍为 NotRun |
| trace、告警与保留 | Pass（本地机制） | Java/Python 真网络 trace、低基数指标、异常列表、阈值候选、`not_configured` 通知状态和保留 dry-run 已验证；候选环境 collector、实际通知渠道和生产删除均未执行 |
| 解析前请求体限制 | Pass（IMP-01） | ASGI 直接用例已覆盖缺失/伪造/分块超限；真实反向代理链仍待候选环境复核 |
| 通用 Agent Run lease/fencing/idempotency 持久化 | Pass（本地） | 隔离 MySQL 已验证领取、续租、过期接管、旧 fencing 拒绝、取消、重试、deadline 及 MySQL 重启/恢复；候选环境多实例演练未执行 |
| 真实文本 Provider | NotRun | Provider 保持 disabled；没有外部调用和费用 |
| 真实视觉 AI | NotRun | 机制、样例、上传和拼版已验证，但没有真实模型调用、四品类质量样本或真实采用率证据 |
| PPTX/图片/ZIP 生产导出 | Pass（本地） | 生成、结构、摘要和 WPS 真实打开/编辑/另存已通过；生产候选环境 PowerPoint/WPS 兼容回归仍为 NotRun |
| 上线就绪评估 | Blocked | 本地工程门已通过；候选环境、真实 Provider/样本/费用、目标客户端和 UAT 未完成，`ReleaseReady=No` |
| UAT | NotRun | 产品与架构基线、编码授权和自动化 Pass 均不等于业务接受实现 |

## 15. 开工前资料包动作边界（历史）

- 没有启动 FastAPI、Java、Vue、浏览器、MySQL、Redis 或其他服务。
- 没有调用 Provider、外部 API、真实账号或产生费用。
- 没有执行 DB、migration、Java 或 Vue 实现与验证。
- 没有写入真实客户、商品、价格、库存或报价数据。
- 没有执行 Git 暂存、提交、分支、推送、标签或发布。

后续每项验证必须记录环境、命令/场景、开始结束时间、实际输出、数量、失败、证据位置和限制；未运行保持 NotRun。

## 16. 数据库文档静态收敛检查

本节只记录 2026-09-12 对数据库设计 v2.3 的文字收敛检查；其历史结论不替代后续 IMP-02 实际数据库证据。

| 检查项 | 状态 | 结果边界 |
| --- | --- | --- |
| 当前 Feature 资料包统一引用数据库设计和 6 模块 16 表；技术设计与任务清单列出 T01–T16 精确表名 | Pass（仅文档） | 只读解析数据库设计得到 `DB_TABLE_COUNT=16`，与任务清单比对得到 `TASK_MISSING_COUNT=0` |
| Feature 文档不把 `fq_import_template`、`fq_ai_feedback`、独立快照/价格版本/库存版本/审计表写成 P0 建表目标 | Pass（仅文档） | 旧名只出现在“禁止创建/不另建”的负向约束中，不构成 migration 输入 |
| 当时的任务与数据库验证状态 | Pass（仅文档、历史） | 当时 `IMP-02` 为 `NotStarted / NotRun`；2026-09-13 已由第 3 节实际证据更新为 `Completed / Pass` |

检查方式：使用 `rg` 搜索 v2.3、旧表名和旧版术语，并用 PowerShell 只读提取当前数据库设计 T01–T16 表名后与任务清单比较。首轮表名比较命令因 PowerShell 引号解析错误未执行检查；修正只读表达式后重新运行，得到上述 16/0 结果。没有因此执行或验证任何数据库动作。

## 17. 十阶段执行包静态核对

本节记录 2026-09-12 对待审执行包的实际文档检查，只证明计划完整、自洽和链接可解析，不证明任何实施阶段完成。

| 检查 | 实际结果 | 结论 |
| --- | --- | --- |
| 阶段编号与状态行 | 顶层阶段标题 10 个、状态表 10 行，均按 `IMP-01`～`IMP-10` 连续且唯一 | Pass（仅文档） |
| REQ 主关闭归属 | 共 17 个、去重后 17 个，精确覆盖 REQ 01～17 | Pass（仅文档） |
| AC 主关闭归属 | 共 32 个、去重后 32 个，精确覆盖 AC 01～32 | Pass（仅文档） |
| 每阶段最低结构 | 10 个阶段均包含涉及模块、关联需求、数据表、技术栈、主要文件范围和退出门 | Pass（仅文档） |
| 数据库表清单 | 从数据库设计解析出 T01～T16 共 16 个唯一 `fq_*` 表名，与任务包一致 | Pass（仅文档） |
| 关键跨阶段约束 | Fashion MySQL 限定 Bean、统一 catalog-write 锁、三个 Java Worker、六类平台字典、配置/观测/保留任务和对应文件范围均已进入任务包 | Pass（仅文档）；只证明计划有明确落点，不证明实现存在 |
| 原型派生文档 | `node prototype/fashion-workbench-v2/build-docs.cjs` 成功；随后 `verify-delivery.cjs` 检查 16 个文件、196 个本地链接、4 份派生副本，断链 0，REQ 17，AC 32 | Pass |
| 全仓文档规则 | 编码前检查 232 个 Markdown、19 个 HTML；2026-09-13 最终复核检查 245 个 Markdown、19 个 HTML | Pass |

上述静态核对发生在编码授权之前：当时没有创建 `ruoyi-fashion`，没有新增 Admin Fashion 页面，没有生成或执行 migration，没有连接 MySQL、启动服务、调用 Provider 或执行 Git 写操作。2026-09-12 用户随后批准十阶段执行包并授权编码；2026-09-13 `IMP-01`～`IMP-09` 已 Completed / Pass，`IMP-10` 为 WaitingForApproval / Blocked。IMP-02～IMP-10 仅连接临时隔离 MySQL；未连接非隔离数据库、未调用 Provider、未执行 Git 写操作或发布。

## 18. 共享若依模块总开关增量验证（2026-09-25）

本次仅验证 `ruoyi-fashion` 的 Spring 装配条件，未启动若依进程、连接数据库或调用 Python/Provider。`fashion.enabled` 缺省开启；显式 `false` 时，Spring 组件扫描不再发现 Fashion 的生产 Controller、Service、Repository、配置和 Worker。细分的迁移、AI Runtime Worker、图片 Provider 及图片/交付 Worker 开关保持原有语义。

| 检查 | 实际结果 | 结论 |
| --- | --- | --- |
| 组件覆盖静态检查 | 69 个 `@Component`、`@Service`、`@Repository`、`@RestController` 或 `@Configuration` 入口，均有 `@FashionModuleEnabled` | Pass（静态） |
| Maven 定向测试 | 在 `platform-backend/` 执行 `mvn -pl ruoyi-fashion -am "-Dtest=FashionModuleEnabledTest" "-Dsurefire.failIfNoSpecifiedTests=false" test`；2026-09-25 11:43:18 +08:00 结束，reactor `BUILD SUCCESS`，2 tests、0 failure、0 error | Pass（组件扫描条件） |
| 差异格式 | `git diff --check -- platform-backend/ruoyi-fashion` 退出码 0 | Pass |
| 完整 `ruoyi-admin` 启动及数据库就绪 | 未执行 | NotRun；不能据此认定四模块联机或 Fashion 业务可用 |

首次 Maven 命令的 `-D` 参数未在 PowerShell 中加引号，Maven 因参数拆分返回未知生命周期错误；修正后的一次编译因其他子任务同时使用 reactor 的 `target` 目录而主动中止。上表只记录最终串行执行的通过结果。
