# FEAT-FASHION-001 生产任务清单

> 文档类型：唯一生产任务事实源；版本：0.4.32；文档状态：Draft  
> Feature：FEAT-FASHION-001；风险：L2  
> 当前治理阶段：Stage 8 审查验证；阶段状态：InProgress  
> 产品与架构基线：2026-09-12 用户已确认产品定位、九个功能模块、六个数据库模块、T01～T16、三个代码项目及 Java/MySQL 权威边界  
> 批准记录：2026-09-12 用户批准本文件定义的 `IMP-01`～`IMP-10` 十个实施阶段并授权进入编码；2026-09-13 `IMP-01`～`IMP-09` 退出门通过，`IMP-10` 本地验证项及共享后台 18 模块聚合打包已通过，并等待候选环境、真实 Provider/费用/数据和 UAT 授权  
> owner：Codex 按实际工程维护；用户维护产品方向、执行包批准与业务验收决定  
> 创建日期：2026-09-12；更新日期：2026-09-13  
> 输入：[功能规格](功能规格.md)、[技术设计](技术设计.md)、[产品正文](../../产品/产品需求文档.md)、[数据库设计 v2.3](../../架构/数据库设计.md)；证据：[验证记录](验证记录.md)

## 1. 使用规则与当前门

本文件是 FEAT-FASHION-001 唯一生产任务事实源，不再建立第二份阶段计划。`Stage 1～10` 是用户级治理流程；本文件的 `IMP-01`～`IMP-10` 是本 Feature 的十个生产实施阶段，两套编号不得混用。

十个实施阶段按退出门依次推进：前一阶段的实现、自审和最低验证完成后，才启动下一阶段；阶段内部可按 Java、Admin、Python 和验证工作线并行。实施阶段与细分任务状态使用 `NotStarted / InProgress / WaitingForApproval / Blocked / Completed / Skipped / Cancelled`，证据结果使用 `NotRun / Pass / Fail / Blocked / NotApplicable`。实现完成不自动等于 AC 通过，自动验证不自动等于用户验收。

十阶段执行包已经用户批准。`IMP-01`～`IMP-09` 已于 2026-09-13 完成实现、自审和最低验证，退出门结果均为 Pass。`IMP-10` 的本地全量、共享后台聚合打包、容量、安全、重启、逻辑备份恢复和上线清单已执行。因 AC 32 还要求真实候选环境、真实 Provider 质量试点和用户/业务 UAT，当前状态为 `WaitingForApproval / Blocked`。IMP-02～IMP-10 的现有数据库证据只使用临时隔离 MySQL，没有连接非隔离数据库或启用真实 Provider。真实账号、真实客户或商品数据、付费调用、非隔离数据库、部署、发布及 Git 写操作仍需相应明确授权。

### 1.1 IMP-10 本地验证后的现状

| 基线 | 当前事实 | 结果边界 |
| --- | --- | --- |
| 产品与数据设计 | PRD 已定义 REQ 01～17、AC 01～32；数据库 v2.3 已收敛为 6 模块、16 张 `fq_*` 业务表 | DDL 已在隔离 MySQL 验证；非隔离数据库未迁移 |
| Java | `ruoyi-fashion` 已完成底座、商品/素材、价格/库存严格全量、客户/方案、Agent Run、1～4 品类选品、图片任务/复核、确定性报价/冻结、PPTX/CSV/ZIP/JPG 交付及运维保留 API | 129 项回归、本地容量与恢复、完整 `ruoyi-admin` Java 进程重启及 Java↔Python loopback 真网络联调通过；生产候选环境、真实 Provider 和多实例仍待验证 |
| Admin | 设置、商品/素材/数据更新、客户、方案、选品搭配、图片、报价/冻结、交付、运维和 Agent 版本页面及测试已建立 | 浏览器仍只允许调用 Java；候选环境兼容和业务 UAT 未执行 |
| Python | 已实现需求分析、商品属性建议、冻结候选搭配和图片输入的严格 typed contract、安全中间件、观测护栏、真实视觉试点 evaluator 及候选就绪最终离线门 | 151 passed / 1 warning；Ruff、Pyright 通过；Provider 固定 disabled，不代表真实模型质量、候选环境或 Provider 可用 |
| 契约 | Runtime 生成契约、Java Gateway 契约、共享 Schema、认证规范和双向向量已落盘 | 冻结项、Java 持久化/消费及本机真实 socket/HMAC/trace/错误映射已通过；候选环境私网 TLS 链路未验证 |
| 验收/发布 | UAT NotStarted；上线就绪 Blocked；NotReleased | 原型完成、文件存在、用户批准计划或工程 Pass 都不能冒充业务接受或发布 |

### 1.2 十阶段状态总表

| 阶段 | 名称 | 主实现 REQ | 主验收 AC | 实现状态 | 证据结果 |
| --- | --- | --- | --- | --- | --- |
| IMP-01 | 工程基线与双向安全契约 | 支撑 REQ 01～17，不单独关闭业务 REQ | 为 AC 16、24、27、32 建立安全前提 | Completed | Pass |
| IMP-02 | MySQL、持久化与 RuoYi 权限底座 | 支撑 REQ 01～17，不单独关闭业务 REQ | 为 AC 16、24、32 建立数据与权限前提 | Completed | Pass |
| IMP-03 | 商品资料与图片素材中心 | REQ 01、04 | AC 01、05 | Completed | Pass |
| IMP-04 | 当前价格、库存与严格全量发布 | REQ 02、03 | AC 02、03、04、21 | Completed | Pass |
| IMP-05 | 客户方案、商品理解与需求确认 | REQ 05、06 | AC 06、22 | Completed | Pass |
| IMP-06 | 一至四品类选品与人工搭配 | REQ 07、08 | AC 07、08 | Completed | Pass |
| IMP-07 | 图片工作台与视觉任务 | REQ 09、10、11 | AC 09、10、18、27、28 | Completed | Pass |
| IMP-08 | 确定性报价、库存复核与版本冻结 | REQ 12、13、14、15 | AC 11、12、13、19、20、23、25 | Completed | Pass |
| IMP-09 | PPTX 与文件交付 | REQ 16、17 | AC 14、15、16、17、24、26、29、30、31 | Completed | Pass |
| IMP-10 | 生产候选验证、UAT 与上线就绪评估 | 回归 REQ 01～17，不新增需求 | AC 32；回归 AC 01～31 | WaitingForApproval | Blocked |

REQ 主归属核算：`2 + 2 + 2 + 2 + 3 + 4 + 2 = 17`。AC 主归属核算：`2 + 4 + 2 + 2 + 5 + 7 + 9 + 1 = 32`。一个编号只有一个主关闭阶段；其他阶段可以提供前置或回归证据，但不能重复声明完成。

## 2. 全局实施边界

### 2.1 产品定位与首期完成口径

本系统是企业内部销售使用的“服装选品、搭配、报价与文件交付工作台”，服务团购/批发客户。它从企业维护的 SKU 商品、当前销售价、当前库存和已授权图片出发，经过客户需求确认、一至四品类候选、图片复核和确定性报价，最终交付可编辑 PPTX 及配套文件。AI 只生成结构化草案、候选、解释和图片，不拥有客户、商品、价格、库存、正式报价或写入权限；Java/MySQL 是唯一业务控制面和事实源。

P0 完成口径是内部人员能在生产候选环境走通“商品/数据 → 客户需求 → 搭配 → 图片 → 报价确认 → 文件交付”，并取得权限、事务、恢复、容量、真实 Provider 试点及 UAT 证据。客户自助 Portal、移动端、订单/支付、ERP 实时同步、自研京东扩展、多客户/渠道价、超过四品类和自动发布均不在首期。

### 2.2 产品模块、数据模块与代码项目

九个产品功能模块为：平台基础、商品中心、数据更新、客户需求、选品搭配、图片工作台、报价中心、交付中心、AI Agent 控制。数据库仍只按六个物理模块建设：商品资料、库存、客户、报价方案、导入管理、AI Agent。

| 产品功能模块 | 主要能力 | 主实施阶段 |
| --- | --- | --- |
| 平台基础 | Maven/Admin 装配、认证、RBAC、业务配置、错误、文件安全、对象存储、观测和进程内 Worker | IMP-01、IMP-02、IMP-09 |
| 商品中心 | SKU 商品、来源/品类/颜色/尺码字段、图片素材和人工修正 | IMP-03、IMP-05 |
| 数据更新 | 商品、价格、库存、图片批次的校验、全量发布和恢复 | IMP-03、IMP-04 |
| 客户需求 | 客户归属、方案草稿、自然语言需求和人工确认 | IMP-05 |
| 选品搭配 | 一至四品类、独立/递进、候选、锁定替换和人工采用 | IMP-06 |
| 图片工作台 | 原图拼版、视觉/生图任务、复核、采用、取消和降级 | IMP-07 |
| 报价中心 | 尺码数量、库存时效、税费折扣、商务例外和版本冻结 | IMP-08 |
| 交付中心 | PPTX、CSV、ZIP/JPG、预览、鉴权下载和失败恢复 | IMP-09 |
| AI Agent 控制 | Agent/版本、对话/消息、Run/Step、租约、预算、幂等和观测 | IMP-01、IMP-05～IMP-07、IMP-10 |

P0 只有三个可部署代码面：

1. `platform-backend/ruoyi-fashion`：Java 业务控制面、RuoYi 权限、MySQL 事实、事务、Run 控制、确定性报价和文件编排。
2. `admin-web/src/api/fashion` 与 `admin-web/src/views/fashion`：内部工作台，只调用 Java API。
3. `fashion-ai-runtime`：唯一 Python AI 执行面，不持有业务数据库。

允许的共享装配与事实源范围还包括 `platform-backend/pom.xml`、`platform-backend/ruoyi-admin/pom.xml`、`platform-backend/ruoyi-admin/src/main/resources/application*.yml`、`platform-backend/sql/{ruoyi-fashion-permissions.sql,ruoyi-fashion-dictionaries.sql,ruoyi-fashion-config.sql}`、`admin-web` 的包与测试配置、`contracts/fashion` 及本 Feature 文档/证据。它们不构成新的部署项目。P0 不修改 `portal-web`、`mobile`、上游若依基线或浏览器扩展，不新增第四个部署项目。

| 数据库模块 | 编号与精确表名 | 主使用阶段 |
| --- | --- | --- |
| 商品资料 | T01 `fq_product` | IMP-03～IMP-08 |
| 库存 | T02 `fq_stock` | IMP-04、IMP-06、IMP-08 |
| 客户 | T03 `fq_customer` | IMP-05、IMP-08 |
| 报价方案 | T04 `fq_quote`、T05 `fq_quote_combo`、T06 `fq_quote_detail`、T07 `fq_quote_image`、T08 `fq_quote_file` | IMP-05～IMP-09 |
| 导入管理 | T09 `fq_import_batch`、T10 `fq_import_detail` | IMP-03、IMP-04 |
| AI Agent | T11 `fq_ai_agent`、T12 `fq_ai_agent_version`、T13 `fq_ai_conversation`、T14 `fq_ai_message`、T15 `fq_ai_run`、T16 `fq_ai_run_step` | IMP-05～IMP-07 |

T01～T16 只在 IMP-02 创建一次，后续阶段不得边做功能边加表；字段和关系的唯一正文仍是[数据库设计 v2.3](../../架构/数据库设计.md)。

### 2.3 固定技术栈

| 层 | 技术栈 | 本 Feature 的使用边界 |
| --- | --- | --- |
| Java | Java 17 编译目标、Spring Boot 4.0.6、RuoYi 3.9.2、平台 Jackson 3、Spring MVC、Spring Transaction、Spring JDBC `NamedParameterJdbcTemplate` | 沿用 `ruoyi-interview` 的模块化单体、Application/Domain/Infrastructure/Controller 结构；JSON databind/core 统一使用 `tools.jackson`；Fashion 不再额外生成 16 套 MyBatis Mapper/XML |
| 数据库 | MySQL 8、InnoDB、utf8mb4、`DECIMAL`、乐观锁、唯一键/FK、Testcontainers MySQL | 新业务表恰好为 T01～T16；Flyway 如启用只允许独立 location 和独立技术 history 表，不能增加第 17 张 `fq_*` 业务表 |
| Admin | Vue 3.5、Vite 6.4、Element Plus 2.13、Axios、Pinia、增量 TypeScript、Vue Test Utils、Vitest、Playwright | 新 Fashion 文件使用 TypeScript，`allowJs` 保留现有若依 JavaScript；不整体迁移 Admin，不把业务真相放 Pinia/localStorage |
| Python | Python 3.12、FastAPI、Pydantic v2、PydanticAI Core、HTTPX、Uvicorn、OpenTelemetry、pytest、Ruff、Pyright | 显式 Workflow 管流程；Provider、Tool、Guardrail 通过 Port 隔离；不引入第二套 durable workflow 或业务数据库 |
| 契约 | OpenAPI 3.1、JSON Schema v1、稳定错误信封、W3C Trace Context | Python Runtime API 与 Java AI Control Gateway 分开定义，共用 ID、deadline、幂等、租约和错误 Schema |
| 文件 | 受控对象存储 Port、SHA-256、Apache POI XSLF、JDK ZIP、CSV 安全转义 | 二进制不进 MySQL；POI 和图片处理依赖须在 `IMP-01` 完成版本、漏洞、许可和兼容复核后锁定 |

P0 单主 MySQL 使用一个规范化应用命名锁 `fashion:{database}:catalog-write` 保护所有商品资料/主图、当前价格、当前库存的发布与人工修改，以及报价确认时的最终重读。公共 `FashionCatalogWriteLock` 在 `IMP-02` 建立；解析文件、AI 推理和外部调用必须在锁外完成。锁定连接按“获取锁 → 完成 commit/rollback → `afterCompletion`/显式连接封装释放”执行，禁止提前释放；后续各阶段不得自创不兼容的锁名或顺序。

### 2.4 统一目标目录

目录只在对应阶段需要时创建，不预建空包。

```text
platform-backend/ruoyi-fashion/
├─ pom.xml
├─ src/main/java/com/ruoyi/fashion/
│  ├─ application/{product,importing,material,customer,selection,image,quote,delivery,agent,settings,operations,shared}/
│  │  ├─ agent/worker/
│  │  ├─ image/worker/
│  │  └─ delivery/worker/
│  ├─ domain/{product,stock,importing,customer,quote,delivery,agent,shared}/
│  ├─ infrastructure/{persistence,storage,airuntime,files}/
│  ├─ controller/rest/{product,importing,material,customer,quote,image,delivery,agent,settings,operations,common}/
│  └─ configuration/{persistence,worker,telemetry}/
├─ src/main/resources/
│  ├─ db/fashion-migration/
│  ├─ fashion/import-templates/
│  └─ fashion/presentation-templates/
└─ src/test/

admin-web/src/
├─ api/fashion/{client,product,stock,importBatch,material,customer,quote,selection,quoteImage,quoteFile,aiRun,settings,operations,types}.ts
└─ views/fashion/{product,import,material,quote,workbench,image,delivery,settings,operations,components,composables,constants}/

fashion-ai-runtime/
├─ src/fashion_ai/{main.py,settings.py,api,application/workflows,agents,domain,ports,providers,tools/java_gateway,guardrails,telemetry,prompts}/
├─ evals/
└─ tests/{unit,contract,integration,fault_injection,security,eval}/

contracts/fashion/
├─ ai-runtime.openapi.yaml
├─ ai-control-gateway.openapi.yaml
├─ schemas/v1/
└─ examples/v1/
```

## 3. IMP-01 工程基线与双向安全契约

> 状态：Completed；证据：Pass；完成日期：2026-09-13；本阶段不关闭业务 REQ。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 平台基础、AI Agent 控制、共享契约 |
| 关联需求 | 支撑 REQ 01～17；重点建立 AC 16、24、27、32 的身份、资源、失败与证据前提 |
| 技术栈 | Java 17/Spring Boot/RuoYi；Vue 3/Vite/增量 TypeScript；Python 3.12/FastAPI/Pydantic；OpenAPI 3.1/JSON Schema |
| 数据表 | 不建表；只冻结 T01～T16 是唯一 `fq_*` 清单 |

### 3.1 实现任务

1. `IMP-01.1` 创建可编译但不含业务实现的 `ruoyi-fashion` Maven 模块，接入父 POM 和 `ruoyi-admin`，不新增启动类。
2. `IMP-01.2` 为 Admin 增加 `allowJs` 的增量 TypeScript、`vue-tsc`、Vitest、Vue Test Utils、Playwright 和 Fashion Java API 薄客户端；不改造既有业务页面。
3. `IMP-01.3` 将 Runtime 现有扁平骨架逐步迁入 `api/application/domain/ports`，保留现有三个接口兼容；在 ASGI JSON 解析前实施实际字节上限。
4. `IMP-01.4` 建立双向契约：Python 提供 `ai-runtime.openapi.yaml`，Java 提供 `ai-control-gateway.openapi.yaml`；冻结字符串 ID、版本、deadline、错误、取消、幂等、Run attempt、lease 和 fencing 语义。
5. `IMP-01.5` 实现 Java ↔ Python 服务身份、重放防护和密钥轮换边界。具体机制在实现前用最小 Spike 复核，不把临时共享字符串当生产认证。
6. `IMP-01.6` 复核并锁定新增依赖的版本、许可证、安全风险和锁文件，重点覆盖 POI、Flyway MySQL、前端测试工具、PydanticAI、HTTPX、图片处理库和 OpenTelemetry。
7. `IMP-01.7` 建立 Java/Python 的 W3C Trace Context 传播、脱敏结构化日志、统一业务关联 ID 和 OpenTelemetry trace/metrics 基座；只记录允许字段，不把未配置采集端写成已上线监控。

### 3.2 主要文件范围

```text
platform-backend/{pom.xml,ruoyi-admin/pom.xml,ruoyi-fashion/**}
platform-backend/ruoyi-fashion/src/main/java/com/ruoyi/fashion/configuration/telemetry/**
admin-web/{package.json,package-lock.json,tsconfig.json,vitest.config.ts,playwright.config.ts}
admin-web/src/api/fashion/client.ts
fashion-ai-runtime/{pyproject.toml,uv.lock,src/fashion_ai/**,tests/**}
contracts/fashion/**
```

### 3.3 实现结果与退出门

- 三个工程可定向构建/测试，Runtime Provider 仍为 disabled。
- 401/403/408/409/410/413/422/429/503/504 的适用语义、重试性和错误信封可做契约测试。
- 伪造或缺失 `Content-Length`、分块超限正文仍在解析前返回安全 413。
- Java 和 Python 的契约生成/消费测试成立，浏览器产物中没有 Python URL、服务凭据或 Provider Secret。
- 依赖复核、实际命令和结果写入验证记录；任何失败保持 Fail，阻断 `IMP-02`。

实际结果：上述退出门已通过。Java 25 个测试、Python 113 个测试、Admin 20 个单测和 Chrome 1 个边界用例通过；Java/Admin 构建、Ruff、Pyright、契约确定性导出与敏感产物扫描通过。Provider 仍为 disabled，未创建数据库或业务 API。详细命令、失败修正和残余风险见[验证记录](验证记录.md)与[IMP-01 依赖与安全复核](证据/阶段-01/依赖与安全复核.md)。

## 4. IMP-02 MySQL、持久化与 RuoYi 权限底座

> 状态：Completed；证据：Pass；完成日期：2026-09-13；本阶段不关闭业务 REQ。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 平台基础、六个数据库模块、Java 公共持久化 |
| 关联需求 | 支撑 REQ 01～17；重点建立 AC 16、24、32 的真实服务端权限与数据基础 |
| 技术栈 | MySQL 8、Spring JDBC、Spring Transaction、Testcontainers MySQL、RuoYi `@PreAuthorize` |
| 数据表 | 一次性创建 T01～T16；复用既有 `sys_menu`、`sys_dict_type/sys_dict_data`、`sys_config`、`sys_oper_log`，不新增平台表；后续阶段不得随功能继续加表 |

### 4.1 实现任务

1. `IMP-02.1` 只从数据库设计 v2.3 生成向前 migration，精确创建 16 张 `fq_*` 表；循环外键按安全顺序后加。
2. `IMP-02.2` 建立独立 `db/fashion-migration` 和显式绑定 RuoYi MySQL `dynamicDataSource` 的 migration 配置，默认关闭自动执行、禁止 clean；既有非空库首次启用前先做 baseline 决定。
3. `IMP-02.3` 显式定义 `fashionJdbcTemplate` 与 `fashionTransactionManager`/`fashionTransactionTemplate`，全部绑定 `dynamicDataSource`；所有 Fashion Repository 和写事务必须用限定名注入，禁止因 `@Primary` 误用 `interviewJdbcTemplate` 或 PostgreSQL 事务管理器。
4. `IMP-02.4` 建立公共 ID、时间、状态、JSON、`row_version`、事务和 JDBC 映射支持，以及按业务域组织的 Repository Port/Adapter；提供绑定 `fashionTransactionManager` 的 `FashionCatalogWriteLock`，统一 `fashion:{database}:catalog-write` 获取、事务完成后释放和超时语义。
5. `IMP-02.5` 新增 Fashion 菜单和权限点脚本；只写 `sys_menu`，不创建账号/角色、不默认写 `sys_role_menu`。
6. `IMP-02.6` 复用 RuoYi `sys_dict_type/sys_dict_data`，以幂等脚本建立 `fashion_product_source`、`fashion_product_category`、`fashion_product_color`、`fashion_product_unit`、`fashion_product_season`、`fashion_warehouse` 六类平台字典及最小初始值；`size_system`、币种、税口径和状态机使用版本化代码枚举，不新建字典表。
7. `IMP-02.7` 复用 RuoYi 登录主体和操作日志，另实现客户归属、平台字典有效值和字段白名单 Guard；Admin 通过 Java/RuoYi 字典接口取标签，不硬编码另一套值。不新建身份、角色、字典或审计表。
8. `IMP-02.8` 建立类型化 Fashion 配置注册表和读写 API：业务默认值、保留期、告警阈值和 AI CNY 额度复用 `sys_config` 并记录 before/after；人员范围走 RuoYi RBAC/客户归属，Agent 发布配置走 T11/T12，Provider Secret 只进环境 Secret，模板实体仍是版本化资源。Admin 建立设置页骨架与权限，不允许任意配置键读写。

### 4.2 主要文件范围

```text
platform-backend/ruoyi-fashion/src/main/resources/db/fashion-migration/**
platform-backend/ruoyi-fashion/src/main/java/com/ruoyi/fashion/{configuration/persistence,domain/shared,application/**/port,infrastructure/persistence}/**
platform-backend/ruoyi-fashion/src/test/**/{migration,configuration,persistence,security}/**
platform-backend/ruoyi-admin/src/main/resources/{application.yml,application-local.yml}
platform-backend/sql/{ruoyi-fashion-permissions.sql,ruoyi-fashion-dictionaries.sql,ruoyi-fashion-config.sql}
platform-backend/ruoyi-fashion/src/main/java/com/ruoyi/fashion/{application/settings,controller/rest/settings}/**
admin-web/src/api/fashion/settings.ts
admin-web/src/views/fashion/settings/**
admin-web/tests/{unit,e2e}/fashion/settings/**
```

### 4.3 实现结果与退出门

- 隔离 MySQL 空库和已有 RuoYi 库升级均通过，失败演练能证明业务数据未被部分修改。
- `information_schema` 中 `fq_%` 精确等于 T01～T16，`fq_import_template`、`fq_ai_feedback`、价格/库存版本、报价快照、审计、预算账本、回调、outbox 等禁建表为 0。
- FK、唯一键、索引、JSON、金额、时间和乐观锁约束取得实际测试证据。
- Spring Context 证明 migration、Repository、事务和命名锁都使用 RuoYi MySQL；Fashion Bean 不会注入 `interviewJdbcTemplate`，回滚证明写入与锁使用同一数据源和物理连接。
- 公共命名锁组件用并发时序测试证明获取、commit/rollback、释放的顺序，不允许业务阶段绕过该组件直接修改受保护当前事实。
- 未登录、缺权限和越权资源访问分别得到真实拒绝；菜单不可见不能替代 API 拒绝。
- 六类 Fashion 平台字典脚本可重复执行且无重复项；服务端拒绝不存在或已停用的来源、品类、颜色、单位、季节和仓库编码，Admin 显示值来自同一字典接口。
- 设置 API 只暴露白名单键、类型和范围，更新留下 before/after 与操作者；配置 Secret、扩大人员范围或绕过角色授权均被拒绝。
- Flyway 技术 history 如启用，须在证据中明确它不是 `fq_*` 业务表且没有被当成第 17 张业务表。

实际结果：上述退出门已通过。迁移在临时隔离 MySQL 8.0.46 上执行并验证恰好 16 张 `fq_*` 表，独立 Flyway history 不计入业务表；约束、平台脚本幂等、RuoYi 数据保留、命名锁并发/回滚、MySQL Bean 绑定、401/403、客户越权 Guard、类型化设置和 Admin 类型/单测/构建均取得实际证据。没有连接非隔离数据库，没有创建角色授权，也没有关闭业务 REQ 或 AC。详细命令、失败修正和边界见[IMP-02 MySQL 与权限底座验证](证据/阶段-02/MySQL与权限底座验证.md)。

## 5. IMP-03 商品资料与图片素材中心

> 状态：Completed；证据：Pass；完成日期：2026-09-13；前置：IMP-02 退出门通过；主关闭 REQ 01、04；主验收 AC 01、05。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 商品中心、数据更新中的商品/图片导入 |
| 关联需求 | 主关闭 REQ 01、04；主验收 AC 01、05 |
| 数据表 | `fq_product`、`fq_import_batch`、`fq_import_detail` |
| 技术栈 | Spring MVC/JDBC/Transaction；`FashionCatalogWriteLock`/`GET_LOCK`；流式 XLSX/CSV/ZIP；对象存储 Port；SHA-256；Vue/Element Plus |

### 5.1 实现任务

1. `IMP-03.1` 实现 SKU 粒度商品查询、详情、批量编辑、上下架和缺资料原因；来源、品类、颜色、单位、季节和仓库编码由服务端按六类 RuoYi 平台字典校验，按来源＋款号＋颜色在页面分组，不拆 SPU/颜色/SKU 表。
2. `IMP-03.2` 实现商品固定模板的上传、字段映射、预览、行级错误、幂等创建和普通资料更新；普通商品导入不得静默覆盖当前价格或库存。
3. `IMP-03.3` 实现 Chrome 插件下载后的 ZIP/文件上传、映射表、重复图识别、待匹配、错色确认和主图选择；不开发插件或京东直连。
4. `IMP-03.4` 实现文件 magic、大小、像素、公式/宏、加密文件、CSV 公式注入、Zip Slip 和对象键白名单控制。
5. `IMP-03.5` 实现商品运营页面及本阶段 API 类型、加载/空态/行错/权限/重试状态。
6. `IMP-03.6` 商品新增、批量编辑、上下架、颜色/分类/主图修改只在公共 catalog-write 锁内完成；文件解析和图片处理在锁外完成，发布时重验范围摘要和 `row_version`。
7. `IMP-03.7` 执行 Java 文件解析/事务、商品修改与价格/库存发布并发、Admin 单测/构建和本阶段 Playwright，并把命令、结果和失败证据回写验证记录。

### 5.2 主要文件范围

```text
ruoyi-fashion/application/{product,importing,material}/**
ruoyi-fashion/domain/product/**
ruoyi-fashion/infrastructure/{persistence/product,persistence/importing,storage}/**
ruoyi-fashion/controller/rest/{product,importing,material}/**
ruoyi-fashion/src/main/resources/fashion/import-templates/**
ruoyi-fashion/src/test/**/{product,importing,material}/**
admin-web/src/api/fashion/{product,importBatch,material,types}.ts
admin-web/src/views/fashion/{product,import,material}/**
admin-web/tests/{unit,e2e}/fashion/{product,import,material}/**
```

### 5.3 实现结果与退出门

- 有效 XLSX/CSV 可建立预期 SKU，`000123` 等前导零保持为文本；重复 SKU、未知分类和重复提交可复查。
- 同款不同颜色不会自动错挂；缺映射、损坏图、重复图、超限和路径越界不会污染现有商品。
- 商品资料和图片导入的 before/after、原文件摘要和操作者可追溯。
- Maven、Admin 单测/构建和本阶段 Playwright 通过后，才关闭 REQ 01、04。

实际结果：上述退出门已通过。Java 回归 61 tests（0 failure/error，7 条条件测试 skipped），隔离 MySQL 8.0.46 商品/图片业务 4 tests 全部通过；Admin 类型检查、5 files / 26 tests、生产构建和 Chrome 2/2 场景通过。有效商品保留前导零，重复发布保持幂等；普通资料导入没有写价格或库存；错色、损坏图、缺映射和并发冲突均未污染现有商品。自动化证据关闭 IMP-03 退出门，但正式 UAT 仍为 NotStarted，真实京东授权页面下载试点仍留在生产候选阶段。详细命令、修正和边界见[IMP-03 商品与图片素材中心验证](证据/阶段-03/商品与图片素材中心验证.md)。

## 6. IMP-04 当前价格、库存与严格全量发布

> 状态：Completed；证据：Pass；完成日期：2026-09-13；主关闭 REQ 02、03；主验收 AC 02、03、04、21。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 数据更新、商品当前价、库存 |
| 关联需求 | 主关闭 REQ 02、03；主验收 AC 02、03、04、21 |
| 数据表 | `fq_product`、`fq_stock`、`fq_import_batch`、`fq_import_detail` |
| 技术栈 | Spring Transaction、MySQL 行锁/乐观锁、公共 `FashionCatalogWriteLock`/`GET_LOCK`、`BigDecimal`、`java.time` |

### 6.1 实现任务

1. `IMP-04.1` 实现价格、库存各自独立的固定模板、范围选择、校验、差异预览和严格全量发布。
2. `IMP-04.2` 校验 `scope_hash`、`base_data_hash`、目标 `row_version`、业务时间、币种、单位、重复键和缺失行。
3. `IMP-04.3` 通过公共 `FashionCatalogWriteLock` 固定同一 MySQL 物理连接，依次取得 `fashion:{database}:catalog-write`、执行并完成业务事务 commit/rollback，最后才释放；可采用经验证的事务同步 `afterCompletion` 或显式连接级事务封装，禁止在 `TransactionTemplate` 业务回调的 finally 中先释放锁再由 Spring 提交。锁失败、数据源错配或连接异常不继续写。
4. `IMP-04.4` 当前价格只更新 `fq_product`，当前库存只更新 `fq_stock`；逐行前后值和实际批次保存在导入明细。
5. `IMP-04.5` 恢复旧值时创建新的 restore 批次并重新校验，不设置价格/库存版本指针。
6. `IMP-04.6` 实现价格库存页面、批次详情、冲突重新校验、失败恢复和发布权限。
7. `IMP-04.7` 执行严格全量、并发锁、事务回滚、恢复、Admin 单测/构建和浏览器场景，并把实际证据回写验证记录。

### 6.2 主要文件范围

```text
ruoyi-fashion/application/importing/{price,stock,restore}/**
ruoyi-fashion/domain/{product,stock,importing}/**
ruoyi-fashion/infrastructure/persistence/{product,stock,importing}/**
ruoyi-fashion/controller/rest/importing/**
ruoyi-fashion/src/test/**/{price,stock,restore,concurrency}/**
admin-web/src/api/fashion/{importBatch,stock}.ts
admin-web/src/views/fashion/import/{price,stock,batch-detail,restore}/**
admin-web/tests/{unit,e2e}/fashion/import/**
```

### 6.3 实现结果与退出门

- 100 个目标 SKU 只有 99 行、重复键、空值、旧业务时间或口径冲突时整批不发布。
- 显式库存 0 可生效；事务中注入失败时所有当前行保持原值。
- 同范围并发批次不能静默覆盖，重复发布返回原结果或明确冲突。
- 并发时序测试证明第二个发布者只能在第一个发布者 commit/rollback 完成后取得同范围锁，提交失败也不会提前放行。
- XLSX、含 BOM/引号/换行 CSV、前导零、公式值和超限文件均有真实解析证据。

实际结果：上述退出门已通过。Java 回归 65 tests（0 failure/error，8 条条件测试 skipped），隔离 MySQL 8.0.46 价格/库存业务 5 tests 全部通过；Admin 类型检查、6 files / 28 tests、生产构建和 Chrome 3/3 场景通过。价格和库存显式零、缺行阻断、旧业务时间、冲突、事务中途失败回滚和 restore 新批次均已取得自动化证据。详细命令、修正和边界见[IMP-04 价格库存严格全量验证](证据/阶段-04/价格库存严格全量验证.md)。正式 UAT 仍为 NotStarted。

## 7. IMP-05 客户方案、商品理解与需求确认

> 状态：Completed；证据：Pass；完成日期：2026-09-13；前置：IMP-04 退出门通过；主关闭 REQ 05、06；主验收 AC 06、22。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 客户需求、商品理解、AI Agent 控制 |
| 关联需求 | 主关闭 REQ 05、06；主验收 AC 06、22 |
| 数据表 | `fq_customer`、`fq_product`、`fq_quote`、六张 AI Agent 表 |
| 技术栈 | Java JDBC 事务、公共 `FashionCatalogWriteLock` 与数据库租约；Java 内部 HTTP；FastAPI/PydanticAI typed output/HTTPX；Vue 工作台 |

### 7.1 实现任务

1. `IMP-05.1` 实现客户查询、归属、协作者、归档以及方案草稿的新建、复制、关闭和刷新恢复。
2. `IMP-05.2` 实现 Agent、不可变版本、Conversation、Message、Run、Run Step 的创建、领取、续租、接管、fencing、取消、超时、有限重试和幂等基础链路；P0 由 Java/RuoYi 进程内有界 Worker/Dispatcher 领取，不增加第四个部署项目。
3. `IMP-05.3` 实现商品属性建议和自然语言需求解析 Agent，只返回待人工确认的结构化草案和歧义项。
4. `IMP-05.4` 人工确认值与 AI 建议值分离；销售修正商品事实时通过公共 catalog-write 锁重验 `row_version` 后提交，后续流程只读取确认值。
5. `IMP-05.5` 客户切换后清除旧需求/价格确认，服务端重新检查客户访问权、当前价来源和 `row_version`。
6. `IMP-05.6` 实现客户方案列表、需求步骤和 Agent 不可变版本的受控查看/发布；Provider disabled 时明确显示能力未启用，不使用样例文本冒充 AI 成功，Provider Secret 不进入配置页面或数据库。
7. `IMP-05.7` 为 Conversation/Run/Step 建 trace/span、排队/运行/失败/接管/超时 metrics 和按关联 ID 查询的异常摘要；日志与指标禁止携带客户原文、联系方式或 Prompt Secret。
8. `IMP-05.8` 执行数据源归属、商品人工确认与价格/库存发布并发、Run 租约/接管/晚到、双向契约、需求解析、观测脱敏和 Admin 恢复场景，并把 Java/Python/Admin 证据回写验证记录。

### 7.2 主要文件范围

```text
ruoyi-fashion/application/{customer,product,quote,agent}/**
ruoyi-fashion/application/agent/worker/**
ruoyi-fashion/domain/{customer,product,quote,agent}/**
ruoyi-fashion/infrastructure/{persistence/customer,persistence/product,persistence/quote,persistence/agent,airuntime}/**
ruoyi-fashion/controller/rest/{customer,product,quote,agent}/**
ruoyi-fashion/src/test/**/{customer,product,quote,agent}/**
fashion-ai-runtime/src/fashion_ai/{api,application/workflows/requirement_workflow.py,agents/requirement_agent.py,domain,ports,providers,guardrails}/**
fashion-ai-runtime/tests/{unit,contract,integration,fault_injection}/**
admin-web/src/api/fashion/{customer,quote,aiRun}.ts
admin-web/src/views/fashion/{quote,workbench,settings}/**
admin-web/tests/{unit,e2e}/fashion/{quote,workbench,settings}/**
```

### 7.3 实现结果与退出门

- “采购 100 套、选择 4 个品类档位、每档 3 个候选”被解析为不同字段。
- AI 错认商品颜色或需求后，人工值优先；旧建议不能再次覆盖。
- 缺当前价格时明确阻断，不猜价、不沿用其他客户价格。
- 契约联测覆盖认证、413、deadline、503、相同键不同摘要、租约接管和旧 fencing token 拒绝。
- Worker 领取事务提交后才调用 Python；外部调用不占用数据库事务，结果提交必须带 lease/fencing 条件，Java/Python 重启后可安全重新领取。

实际结果：上述退出门已通过。Java 回归 69 tests（0 failure/error，9 条条件测试 skipped），隔离 MySQL 8.0.46 的客户/方案/Agent/Run/商品建议与采用 6 tests 全部通过；Python 117 passed，Ruff 和 Pyright 通过；Admin 类型检查、7 files / 31 tests、生产构建和 Chrome 4/4 场景通过。真实 Provider 保持 disabled，typed analyzer 只证明契约、字段分离和采用链路，不作为真实模型质量或 UAT 证据。详细命令、发现、修正与边界见[IMP-05 客户方案与需求确认验证](证据/阶段-05/客户方案与需求确认验证.md)。

## 8. IMP-06 一至四品类选品与人工搭配

> 状态：Completed；证据：Pass；完成日期：2026-09-13；主关闭 REQ 07、08；主验收 AC 07、08。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 选品搭配、AI Agent 控制 |
| 关联需求 | 主关闭 REQ 07、08；主验收 AC 07、08 |
| 数据表 | `fq_product`、`fq_stock`、`fq_quote`、`fq_quote_combo`、`fq_quote_detail`、六张 AI Agent 表 |
| 技术栈 | Java 确定性硬过滤；PydanticAI typed Agent/Tool；`quoteRowVersion`、`comboVisualHash`；Vue composables |

### 8.1 实现任务

1. `IMP-06.1` Java 按品类、上下架、可用图片、当前价格、库存、预算和排除项生成带版本的冻结候选集。
2. `IMP-06.2` Python 只对冻结候选排序和组成 1～4 品类 Look，输出候选、原因和排除说明，不创造 SKU。
3. `IMP-06.3` 实现独立/递进档位、每档候选数、组合去重、锁定、替换、保持锁定重搭和候选不足原因。
4. `IMP-06.4` 人工采用时在 Java 单事务重新校验 Run、候选、`quoteRowVersion`、`comboVisualHash`、商品事实和权限。
5. `IMP-06.5` 实现搭配工作台、Slot 编辑、候选抽屉、尺码初始分配和旧结果提示；页面刷新从 Java 恢复。
6. `IMP-06.6` 执行 1～4 槽位、独立/递进、硬过滤、无解、候选去重、采用冲突、Golden Set 和浏览器恢复验证，并回写证据。

### 8.2 主要文件范围

```text
ruoyi-fashion/application/{selection,quote}/**
ruoyi-fashion/domain/{product,stock,quote}/**
ruoyi-fashion/infrastructure/persistence/{product,stock,quote}/**
ruoyi-fashion/controller/rest/{quote,agent}/**
ruoyi-fashion/src/test/**/{selection,quote}/**
fashion-ai-runtime/src/fashion_ai/{application/workflows/selection_styling_workflow.py,agents/selection_agent.py,agents/styling_agent.py,tools/java_gateway,guardrails}/**
fashion-ai-runtime/evals/**
fashion-ai-runtime/tests/{unit,contract,integration,eval}/**
admin-web/src/api/fashion/selection.ts
admin-web/src/views/fashion/workbench/**
admin-web/tests/{unit,e2e}/fashion/workbench/**
```

### 8.3 实现结果与退出门

- 单品、二品、三品、四品类的槽位数量正确，尺码行不会增加品类数。
- 递进模式保持基础锁定项，独立模式不隐式继承。
- 无解、候选不足、锁定冲突和商品失效均显示真实原因。
- 商品替换后旧 AI/图片结果不能覆盖当前组合；晚到结果只保留审计和过期状态。

实际结果：上述退出门已通过。Java 回归 76 tests（0 failure/error，10 条条件测试 skipped），隔离 MySQL 8.0.46 的 7 tests 全部实际执行；Python 124 passed，Ruff 和 Pyright 通过；Admin 类型检查、7 files / 32 tests、2536 modules 生产构建和 Chrome 4/4 场景通过。冻结候选、显式零价、独立/递进、去重、无解、锁定/替换/重搭、尺码初始分配与采用冲突均有定向证据；数据库仍为 16 张 `fq_*` 业务表。真实 Provider 仍 disabled，UAT 未执行。详见[IMP-06 一至四品类选品与人工搭配验证](证据/阶段-06/一至四品类选品与人工搭配验证.md)。

## 9. IMP-07 图片工作台与视觉任务

> 状态：Completed；证据：Pass；完成日期：2026-09-13；前置：IMP-06 退出门通过；主关闭 REQ 09、10、11；主验收 AC 09、10、18、27、28。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 图片工作台、商品素材、AI Agent 控制 |
| 关联需求 | 主关闭 REQ 09、10、11；主验收 AC 09、10、18、27、28 |
| 数据表 | `fq_product`、`fq_quote`、`fq_quote_combo`、`fq_quote_detail`、`fq_quote_image`、`fq_ai_run`、`fq_ai_run_step` |
| 技术栈 | 对象存储 Port、SHA-256；HTTPX Provider Adapter；受审图片处理库；OpenTelemetry；Vue 异步任务 UI |

### 9.1 实现任务

1. `IMP-07.1` 实现外部图片上传、原图拼版和 1～4 张 Provider 结果的统一任务模型；有限输入、参数、结果和审核记录保存在一行有界 JSON。
2. `IMP-07.2` 由 Java 进程内 Image Worker 实现 queued/running/success/partial/failed/cancel_requested/cancelled/unknown 状态、请求幂等、lease、有限重试和结果回查；外部调用不占用数据库事务，提交结果时按 fencing 条件更新。
3. `IMP-07.3` 实现商品原图与候选图并排复核，逐件标记缺件、错款、错色、Logo/文字问题，查看与采用分离。
4. `IMP-07.4` 组合或主图变化后按 `visualHash` 将旧图标记待复核；历史确认版本不换图，取消后晚到结果不自动采用。
5. `IMP-07.5` 实现费用预留/未知/结算记录和预算拒绝，以及 Provider 请求量、失败/待查/取消、耗时、费用和采用率 metrics；UNKNOWN 不自动退款、不盲目重提。
6. `IMP-07.6` 实现图片工作台和任务总览，清楚区分 sample、upload、composition、provider、reuse 来源。
7. `IMP-07.7` 执行 fake Provider 故障注入、取消/晚到、部分成功、视觉复核、预算及 Admin 异步恢复验证，并回写 Java/Python/Admin 证据。

### 9.2 主要文件范围

```text
ruoyi-fashion/application/{image,material,agent}/**
ruoyi-fashion/application/image/worker/**
ruoyi-fashion/domain/quote/image/**
ruoyi-fashion/infrastructure/{storage,airuntime,persistence/quote}/**
ruoyi-fashion/controller/rest/image/**
ruoyi-fashion/src/test/**/{image,material,agent}/**
fashion-ai-runtime/src/fashion_ai/{application/workflows/image_workflow.py,agents/image_coordinator.py,providers/vision,providers/image,guardrails,telemetry}/**
fashion-ai-runtime/evals/datasets/images/**
fashion-ai-runtime/tests/{unit,contract,integration,fault_injection,security,eval}/**
admin-web/src/api/fashion/quoteImage.ts
admin-web/src/views/fashion/{image,workbench}/**
admin-web/tests/{unit,e2e}/fashion/image/**
```

### 9.3 实现结果与退出门

- 部分失败保留成功图片，重复点击不重复提交，取消和未知费用状态真实。
- 未复核图片不能进入最终组合；原图拼版不记成 AI 成功。
- fake Provider、外部上传和故障注入可以验证机制，但不能宣称真实模型质量通过。
- 真实 40 任务质量、数据条款和费用试点留到 IMP-10，并受单独授权约束。

实际结果：上述退出门已通过。Java 全量回归 89 tests（0 failure/error，11 条数据库条件测试 skipped），最终图片/观测定向 15 tests 通过；隔离 MySQL 8.0.46 的 8 tests 全部实际执行；Python 127 passed，Ruff 和 Pyright 通过；Admin 类型检查、8 files / 34 tests、2560 modules 生产构建和 Chrome 5/5 场景通过。外部上传、样例、原图拼版、部分成功、UNKNOWN 原键回查、取消晚到、逐项复核、视觉过期、预算和低基数观测均有证据；数据库仍为 16 张 `fq_*` 业务表。真实 Provider 仍 disabled，UAT 未执行。详见[IMP-07 图片生成异步状态与人工复核验证](证据/阶段-07/图片生成异步状态与人工复核验证.md)。

## 10. IMP-08 确定性报价、库存复核与版本冻结

> 状态：Completed；证据：Pass；完成日期：2026-09-13；前置：IMP-07 退出门通过；主关闭 REQ 12、13、14、15；主验收 AC 11、12、13、19、20、23、25。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 报价中心、选品搭配、平台商务例外 |
| 关联需求 | 主关闭 REQ 12、13、14、15；主验收 AC 11、12、13、19、20、23、25 |
| 数据表 | `fq_customer`、`fq_product`、`fq_stock`、`fq_quote`、`fq_quote_combo`、`fq_quote_detail`、`fq_quote_image` |
| 技术栈 | Java `BigDecimal`、显式 scale、`RoundingMode.HALF_UP`、数据库 `DECIMAL`、Spring Transaction、乐观锁、公共 `FashionCatalogWriteLock`/`GET_LOCK` |

### 10.1 实现任务

1. `IMP-08.1` 实现备选报价和合并采购两种金额归属，避免组合总额与主表总额形成重复事实源。
2. `IMP-08.2` 实现 SKU 级尺码数量守恒、同 SKU 跨组合汇总库存、库存业务时间和最大可供套数。
3. `IMP-08.3` 实现含税/未税、税率、百分比/固定折扣、运费/附加费用和分币舍入，Python 不参与金额计算。
4. `IMP-08.4` 实现销售折扣上限、负责人例外、输入 hash 和内容变化后例外失效；零单价、100% 优惠和零应付正式报价始终阻断。
5. `IMP-08.5` 确认时先取得公共 `fashion:{database}:catalog-write`，再在同一 MySQL 连接与事务中重新核对商品、当前价、价格批次、库存批次、业务时间、图片和权限并冻结 quote/combo/detail 三层；commit/rollback 完成后才释放锁。
6. `IMP-08.6` 已确认版本修改时复制三层为新 `version_no`，清空确认和例外，不建立快照表。
7. `IMP-08.7` 实现报价编辑、变化复核、确认和历史版本页面，正式金额只显示 Java 返回的十进制字符串。
8. `IMP-08.8` 执行金额向量、尺码守恒、共享 SKU 库存、过期/变化、例外失效、并发确认、确认与商品/价格/库存发布竞态、锁提交顺序和 Admin 编辑恢复验证，并回写证据。

### 10.2 主要文件范围

```text
ruoyi-fashion/application/quote/{calculate,validate,confirm,revise,expire,void}/**
ruoyi-fashion/domain/quote/**
ruoyi-fashion/infrastructure/persistence/{customer,product,stock,quote}/**
ruoyi-fashion/controller/rest/quote/**
ruoyi-fashion/src/test/**/{quote,pricing,stock,concurrency}/**
admin-web/src/api/fashion/quote.ts
admin-web/src/views/fashion/{quote,workbench}/**
admin-web/tests/{unit,e2e}/fashion/quote/**
```

### 10.3 实现结果与退出门

- PRD 示例精确得到 `24050.00`、`56.46`、未税 `118.65` 与含税 `105.00`。
- 同 SKU `60 + 50 = 110` 正确核库存；100 套对应 90 或 110 的尺码和均被阻断。
- 超 10% 优惠、过期库存、混合税口径和无效例外均返回可定位阻断原因。
- 确认版本不可修改，当前价格/库存/图片变化不能改写历史版本，并发确认只产生一个合法结果。
- Java 全量 110 tests 和报价定向 20 tests 均通过；临时隔离 MySQL 8.0.46 的 9 个场景全部通过，`fq_*` 仍精确为 16 张。
- Admin 类型检查、9 files / 35 tests、2561 modules 生产构建及 Chrome 6/6 通过；浏览器场景使用 Java API Mock，不冒充真实三端联调。
- 完整实现、执行命令、失败修正、实例回收和限制见[IMP-08 确定性报价库存复核与版本冻结验证](证据/阶段-08/确定性报价库存复核与版本冻结验证.md)。

## 11. IMP-09 PPTX 与文件交付

> 状态：Completed；证据结果：Pass；完成日期：2026-09-13；前置：IMP-08 退出门通过；主关闭 REQ 16、17；主验收 AC 14、15、16、17、24、26、29、30、31。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 交付中心、平台配置与运维、完整 Admin 主流程、下载权限 |
| 关联需求 | 主关闭 REQ 16、17；主验收 AC 14、15、16、17、24、26、29、30、31 |
| 数据表 | `fq_quote_file`、`fq_quote_image`、`fq_import_batch`、`fq_import_detail`；只读已确认 `fq_quote/fq_quote_combo/fq_quote_detail`、商品素材及既有 `sys_config/sys_oper_log`，不新增清理或指标表 |
| 技术栈 | Apache POI XSLF、JDK ZIP、CSV 安全转义、SHA-256、对象存储、RuoYi `sys_config/sys_oper_log`、OpenTelemetry、Vue/Element Plus、Playwright |

### 11.1 实现任务

1. `IMP-09.1` 从单一已确认报价版本生成 16:9 PPTX，文字和表格使用原生可编辑元素，图片嵌入且离线可见。
2. `IMP-09.2` 实现长商品名、多尺码、1～4 档位自动续页和固定摘要页；禁止整页截图、假后缀或空文件冒充 PPTX。
3. `IMP-09.3` 实现 CSV、原图/采用图、图片 ZIP/JPG 及 manifest，处理 CSV 公式注入和 ZIP 安全。
4. `IMP-09.4` 由 Java 进程内 Delivery Worker 实现 `fq_quote_file` 的请求幂等、queued/running/success/failed、同版本重试、摘要校验和失败恢复；生成过程不持有数据库事务，结果按 lease/fencing 条件提交。
5. `IMP-09.5` 下载时重新鉴权并使用白名单文件名/内容类型；客户文件不含成本、内部备注、Secret 或内部 trace。
6. `IMP-09.6` 完成商品、导入、客户、搭配、图片、报价、交付的 Admin 串联，覆盖刷新、409、会话过期、窄屏、键盘和焦点。
7. `IMP-09.7` 为导入、图片、库存过期和文件生成建立异常查询与 metrics，提供批次失败率、图片失败/待查数、库存过期数、PPT 耗时/失败率及关联 ID；接入可配置阈值，不把未接通知渠道写成告警已送达。
8. `IMP-09.8` 实现保留策略计算与 dry-run 待清理清单：方案/确认报价/必要审计默认 365 天、原始导入文件 90 天、失败未采用素材 30 天；被有效报价引用的素材/文件不得清理，管理员可延长。真实删除默认关闭，执行必须重新鉴权、复核引用、记录范围并取得对应环境的明确授权。
9. `IMP-09.9` 执行 POI/OOXML、金额与图片摘要、ZIP manifest、恶意 CSV、对象存储故障、下载越权、保留引用保护、观测脱敏和 Admin 全链路验证；并在本机可用的指定 PowerPoint/WPS 版本实际打开、编辑、另存 PPTX 后回写证据。桌面环境不可用时本阶段证据为 Blocked，不能关闭 AC 15 或进入 IMP-10。

### 11.2 主要文件范围

```text
ruoyi-fashion/application/delivery/**
ruoyi-fashion/application/delivery/worker/**
ruoyi-fashion/application/delivery/retention/**
ruoyi-fashion/application/operations/**
ruoyi-fashion/domain/delivery/**
ruoyi-fashion/infrastructure/files/{pptx,csv,zip}/**
ruoyi-fashion/infrastructure/storage/**
ruoyi-fashion/controller/rest/{delivery,operations}/**
ruoyi-fashion/src/main/resources/fashion/presentation-templates/**
ruoyi-fashion/src/test/**/{delivery,pptx,csv,zip,retention,operations,security}/**
admin-web/src/api/fashion/{quoteFile,settings,operations}.ts
admin-web/src/views/fashion/{delivery,operations,settings,workbench,components,composables}/**
admin-web/tests/{unit,e2e}/fashion/**
```

### 11.3 实现结果与退出门

- 导出中发生当前数据变化也不会产生半新半旧文件；生成内容与 `quote_hash` 一致。
- POI 可重新打开 PPTX，OOXML 结构、金额、商品、图序和摘要与确认版本一致；ZIP 可解压且 manifest 一致。
- 下载越权、恶意 CSV、渲染失败和对象存储失败均不记录成功或空文件。
- dry-run 能解释“可清理/被引用保护/已延长”，不会删除任何对象；隔离测试证明清理失败不改业务引用，生产真实删除不属于阶段计划自动授权。
- 异常查询和 metrics 可从批次/方案/Run/文件关联 ID 回查，日志、指标和告警内容不泄露客户正文、联系方式、Secret 或临时下载签名。
- AC 26 的浏览器本地备份/重置属于原型证据，生产侧实现数据库持久化、失败不覆盖和运维恢复，不复制一套浏览器业务账本。
- PowerPoint/WPS 的真实桌面打开、编辑和另存必须在本阶段取得，才能主关闭 AC 15；IMP-10 只在生产候选环境做兼容回归。
- Java 全量 123 tests、临时隔离 MySQL 8.0.46 的 10 个场景、Admin 11 files / 37 tests、2569 modules 构建和 Chrome 7/7 均通过；本机 WPS 12.1.0.28043 已真实打开、编辑并另存 12 页 PPTX。
- 完整实现、命令、失败修正、文件摘要、桌面可编辑证据、实例回收和限制见[IMP-09 文件交付与运维保留验证](证据/阶段-09/文件交付与运维保留验证.md)。

## 12. IMP-10 生产候选验证、UAT 与上线就绪评估

> 状态：WaitingForApproval；证据结果：Blocked（本地项 Pass，外部必需项 NotRun）；前置：IMP-01～IMP-09 全部退出门通过；主验收 AC 32；不自动发布。

| 项目 | 内容 |
| --- | --- |
| 涉及模块 | 三个代码项目、六个数据模块、九个功能模块及运行环境 |
| 关联需求 | 回归 REQ 01～17、AC 01～31；在生产候选环境主关闭 AC 32 |
| 数据表 | 不变更结构；只验证 T01～T16 的迁移、约束、数据一致性、容量和恢复 |
| 技术栈 | Maven/JUnit/Testcontainers、Vitest/Playwright、pytest/Eval/Ruff/Pyright、OpenTelemetry、MySQL、PowerPoint/WPS |
| 外部条件 | 真实账号、授权商品/图片、Provider 费用上限、候选环境和 UAT 人员须分别明确 |

### 12.1 实现与验证任务

1. `IMP-10.1` 执行 Java、Admin、Python、契约和 MySQL 全量定向回归，复核 `fq_*` 精确 16 表及全部禁建表为 0。
2. `IMP-10.2` 在隔离候选环境验证 10 万 SKU、10 万行导入、20 并发、商品查询 P95、批次校验时限、候选时限及 20 页/40 图 PPT 时限，并记录环境、窗口、样本数和实际指标。
3. `IMP-10.3` 执行未登录、无权限、跨客户、成本字段、下载地址、Prompt Injection、SSRF、恶意文件和秘密扫描专项。
4. `IMP-10.4` 演练 Java/Python/Worker 重启、租约接管、晚到结果、Provider 429/超时/unknown、对象存储失败，以及 RPO≤24 小时、RTO≤4 小时目标下的隔离数据库/对象引用恢复；未达到目标保持 Fail。
5. `IMP-10.5` 获得单独费用与数据授权后，执行 PRD 定义的 40 个真实视觉任务试点，保留全部失败样本、费用和耗时。
6. `IMP-10.6` 在生产候选环境对 IMP-09 已通过的 PowerPoint/WPS 打开、编辑、另存场景做兼容回归，并检查图片、字体、续页和离线可见。
7. `IMP-10.7` 由用户或业务负责人执行 UAT；Agent 只记录场景事实，不代签接受。
8. `IMP-10.8` 形成上线就绪评估、配置清单、停止条件和恢复步骤；ReleaseReady 不等于已发布。
9. `IMP-10.9` 验证 trace 跨 Java/Python 传播、指标口径、异常列表、阈值触发、通知失败可见性和保留 dry-run；生产清理只验证候选与引用保护，不自动执行真实删除。

### 12.2 主要文件与证据范围

```text
platform-backend/ruoyi-fashion/src/test/**
platform-backend/ruoyi-fashion/scripts/run-isolated-mysql-tests.ps1
platform-backend/ruoyi-fashion/scripts/run-isolated-capacity-recovery-tests.ps1
platform-backend/ruoyi-fashion/scripts/run-live-runtime-integration-tests.ps1
platform-backend/ruoyi-fashion/scripts/run-local-java-restart-tests.ps1
platform-backend/ruoyi-fashion/scripts/candidate-validation-manifest.example.json
platform-backend/ruoyi-fashion/scripts/test-candidate-validation-preflight.ps1
platform-backend/ruoyi-fashion/scripts/run-candidate-validation-preflight-tests.ps1
admin-web/tests/{unit,e2e}/fashion/**
fashion-ai-runtime/tests/**
fashion-ai-runtime/src/fashion_ai/evaluation/**
fashion-ai-runtime/tests/test_visual_trial_evaluator.py
fashion-ai-runtime/tests/test_candidate_readiness.py
fashion-ai-runtime/evals/**
contracts/fashion/**
文档/项目/智能选品项目/功能/FEAT-FASHION-001-服装智能选品生产首版/{证据/阶段-10,验证记录.md,验收记录.md}
```

### 12.3 证据位置和退出门

原始输出、截图和报告可按实际需要放入本 Feature 的 `证据/阶段-10/`，结论只回写[验证记录](验证记录.md)和[验收记录](验收记录.md)，任务状态只回写本文件，不再创建十份重复状态文档。

退出条件：AC 32 取得真实候选环境证据；真实四品类图片达到 PRD 门槛且失败样本未从分母删除；恢复后商品当前值、导入前后值、报价版本、素材引用和文件摘要一致；用户给出 UAT 决定。实际发布仍需明确发布对象、环境、版本、影响和回滚授权。

### 12.4 当前实现与阻断结果

- 已修正导入 5,000 行上限与 PRD 10 万行目标不一致的问题，并将数据/图片/ZIP 大小、条目数、解压总量和 100 倍解压比统一到 PRD。
- 当前工作树隔离 MySQL 权威复跑使用与共享后台一致的 Druid 连接池参数并在批量装载后刷新表统计：实际加载 100,000 SKU 与 100,000 导入明细；100 次“分页检索＋精确总数”P95 688 ms，20 并发总 P95 2,801 ms，其中检索 P95 495 ms、精确计数 P95 2,630 ms；100,000 行解析 765 ms；22 页/44 图 PPTX 5,641 ms。指标在断言前写入报告，失败运行也可审计，3 秒门槛、数据量和并发数均未放宽。
- MySQL 原地重启 2,880 ms 后数据一致；`mysqldump --single-transaction` 恢复到新库耗时 35,278 ms，16 表、商品、导入证据和 40 个素材引用回读一致；脚本完成后相关进程和临时目录残留为 0。
- 当前工作树复跑 Java 129 tests（0 failure/error，18 条条件测试 skipped）、Python 151 tests、Admin 类型检查、11 files / 37 tests 与 2574 modules 生产构建均通过；Chrome 7/7、OpenAPI 无漂移、Ruff/Pyright、权限/越权/恶意文件/秘密及权威边界扫描沿用同一阶段已取得的通过证据。
- Java 通过真实 loopback socket 调用 Uvicorn Runtime，HMAC、capabilities、非空 POST、correlation/trace 和 503 错误映射在 Python 进程重启前后各 3/3 通过；联调暴露并修复 JDK h2c 升级导致 Uvicorn 丢失 POST 正文、签名误报 401 的问题，Java Runtime hop 已固定为 HTTP/1.1。
- 新增共享后台完整进程重启脚本：隔离 MySQL、执行 Fashion migration，以 AI Run、图片、交付三类 Worker 开启状态启动 `ruoyi-admin.jar`，验证 HTTP、精确 16 表和持久化标记；停止后以不同 Java PID 再启动并全部复核通过，进程和临时目录残留为 0。演练使用一次性本地 HMAC 密钥并让 AI Worker 在首次启动及重启后各自动处理 1 条已过 deadline 的隔离 Run，两条均变为 `failed/DEADLINE_EXCEEDED`，且 `run_attempt=0`、`current_step_no=0`、Run Step 为 0，证明调度和数据库状态推进真实发生而任务没有被领取、没有调用指向不可达 loopback 的 Provider。该证据不冒充真实 Provider；演练同时发现并修复 Fashion 使用 Jackson 2 类型但平台提供 Jackson 3 Bean、以及受 Spring 切面代理的 `final` Repository/Controller 无法生成 CGLIB 代理两项生产装配缺陷。
- 新增候选环境离线输入门：模板只接受环境/配置/秘密管理引用，不接受 Secret 值；预检在任何网络、数据库写入或 Provider 调用前核对执行授权、HTTPS 地址、80 个获批商品及四品类覆盖、40 个真实视觉任务、首轮 3 图/最多重试 1 轮、固定每类 8/10 门槛、人民币费用上限、八类停止条件、目标 PowerPoint/WPS 和 UAT 决策人；授权时间必须是带时区 ISO 8601，UAT 窗口必须由两个带时区时间组成且结束晚于开始，费用上限必须不少于 0.01 元且最多两位小数，费用和质量门槛进入带清单 SHA-256 的预检报告。可重复自测 9/9：未填写模板稳定输出 `ready=false` 与 33 个差距，完整合成输入输出 `ready=true` 与 0 个差距且其报告由 Python 严格模型实读通过，无时区授权时间、反向 UAT 窗口、三位小数费用、偏离 8/10 的门槛、明文密码字段和输入输出重叠分别拒绝；报告采用同目录临时文件原子替换，临时文件及报告验证后清理且残留为 0。该门只证明执行包输入可机检，不代表外部授权或候选环境已经存在。
- 当前完成性审计再次扫描工作区，没有发现任何非 `.example.json` 的候选清单、视觉试点结果或就绪证据，也没有发现 `authorizationStatus=approved` 的候选清单；对当前模板实际执行 `-AllowIncomplete` 预检，得到 `ready=false` 和 33 个精确缺口，报告落在 `platform-backend/ruoyi-fashion/target/completion-audit-candidate-preflight.json`。这些缺口均要求外部环境、授权、真实数据或业务人员输入，不能由编码阶段代填。
- 新增真实视觉试点离线 evaluator：40 个预登记任务 ID 必须与结果全集完全一致，1/2/3/4 品类组各 10 个，并强制同一 Provider、模型/配置、输入输出规格和复核规则；自动计算首轮可用率、两轮内可交付率、各组 8/10、全部实际费用、每张采用图成本及采用耗时总计/中位数/P95。失败任务始终保留在分母；提交、复核和采用时间必须带时区且采用不得早于人工复核；未结账单或零采用证据为 `blocked`，质量、费用、未授权输入、关键款色标识错误或缺件为 `fail`。新增 8 个定向场景；未填写模板以退出码 2 拒绝且不生成报告。这只证明判定机制，不是 40 个真实任务结果。
- 新增候选就绪最终离线门：强制读取原始候选授权清单并重算 SHA-256，再与候选输入预检、真实视觉聚合、七个技术 gate、`UAT-01～UAT-06` 和业务决定中的摘要交叉核对；预检后清单发生任何字节变化即不就绪，并强制视觉报告费用上限等于获批预检上限。预检的 16 表/无外部动作/排除发布声明和视觉报告的封闭分母/失败保留/无 Provider/无发布决定声明均是精确 Literal 契约，不能漂移。已执行 gate/业务决定必须记录非空身份和带时区时间；只有 `accepted_with_conditions` 可以携带条件，`accepted` 携带条件会在模型层拒绝，最终门只接受真正无条件的 `accepted`。每个 gate 必须引用证据包内的相对文件，工具实际读取文件并重算 SHA-256，绝对路径、目录越界、不存在、超 50 MiB、疑似 Secret/私钥或摘要不一致均拒绝；原始授权清单含 `clientSecret` 等疑似凭据时不生成报告。输出路径在读取证据和执行判定前完成规范化并立即拒绝覆盖任一输入，回归用源文件 SHA-256 证明失败路径不改授权清单。模板显式列出全部字段但保持 `not_run/null`，经严格模型解析且不能冒充通过。15 个候选就绪场景、与视觉 evaluator 合计 23 个场景及 Python 全量 151 tests 通过，Ruff/Pyright 通过。直接调用判定函数但未完成原始授权清单或证据文件核验时固定不就绪；输出无论如何均为 `ReleaseAuthorized=false`、`NotReleased`。这只证明最终判定机制，不是候选环境、UAT 或发布事实。
- IMP-10.9 的本地可执行部分已验证：trace 跨 Java/Python 真实传播，低基数指标、异常列表、阈值候选和保留 dry-run 有自动化证据；当前未配置通知渠道时 Java/Admin 明确显示 `not_configured`，没有把“达到阈值”冒充“通知已送达”。候选环境采集端和实际通知渠道仍未部署。
- `ruoyi-fashion` 及其依赖 5 模块定向打包成功，共享 `ruoyi-admin` 当前工作树 18/18 模块聚合打包也已成功并生成 `ruoyi-admin.jar`。执行期间曾在正在变化的 APS 源码上出现一次非穷尽 `switch` 编译错误；重新读取时枚举与映射已经完整，未修改 APS 源码，随后权威复跑通过。
- 真实 Provider 40 个视觉任务、候选环境端到端指标/对象存储恢复、候选环境 PowerPoint/WPS 兼容和业务 UAT 尚无所需授权与环境，不能关闭 AC 32 或标记 ReleaseReady。
- 完整指标、失败修正、配置清单、停止/恢复条件和限制见[IMP-10 生产候选本地验证与上线就绪评估](证据/阶段-10/生产候选本地验证与上线就绪评估.md)。

## 13. 阶段验证命令基线

实际命令在每阶段执行前按项目脚本和环境复核，执行结果写验证记录。计划基线为：

```powershell
mvn -f platform-backend/pom.xml -pl ruoyi-fashion -am test
pwsh -NoProfile -File platform-backend/ruoyi-fashion/scripts/run-isolated-mysql-tests.ps1
pwsh -NoProfile -File platform-backend/ruoyi-fashion/scripts/run-isolated-capacity-recovery-tests.ps1
pwsh -NoProfile -File platform-backend/ruoyi-fashion/scripts/run-live-runtime-integration-tests.ps1
pwsh -NoProfile -File platform-backend/ruoyi-fashion/scripts/run-local-java-restart-tests.ps1
pwsh -NoProfile -File platform-backend/ruoyi-fashion/scripts/run-candidate-validation-preflight-tests.ps1
mvn -f platform-backend/pom.xml -pl ruoyi-admin -am package
npm --prefix admin-web run typecheck
npm --prefix admin-web run test:unit
npm --prefix admin-web run build:prod
npm --prefix admin-web run test:e2e:fashion
uv run --project fashion-ai-runtime --extra test --extra agent --frozen pytest -p no:cacheprovider fashion-ai-runtime/tests
uv run --project fashion-ai-runtime --extra test --extra agent --frozen ruff check --config fashion-ai-runtime/pyproject.toml fashion-ai-runtime/src fashion-ai-runtime/tests fashion-ai-runtime/evals
uv run --project fashion-ai-runtime --extra test --extra agent --frozen pyright --project fashion-ai-runtime/pyproject.toml fashion-ai-runtime/src fashion-ai-runtime/tests
```

其中 Admin 的类型/测试脚本和 Python 的 Ruff/Pyright 需要先在 IMP-01 建立，未建立前不得写成已运行。真实 Provider、真实数据库、浏览器和桌面应用验证只在相应授权和环境成立时执行。

## 14. 旧任务迁移与禁止项

旧 `P0-0～P0-4` 五组任务已按语义迁入 `IMP-01～IMP-10`，不再作为并行任务编号：旧安全/契约进入 IMP-01，Java/MySQL 进入 IMP-02～06/08/09，Python Runtime 进入 IMP-01/05～07，Admin 进入 IMP-01/03～09，联调验收进入 IMP-10。既有 Python disabled 骨架保留为开工前输入，不将任一新阶段提前标记为 Completed。

任何阶段都禁止：读取归档 44 表设计生成代码；新增 T01～T16 以外的 `fq_*` 表；让浏览器直连 Python；让 Python 直连业务 MySQL；让模型决定权限、最终金额或正式写入；用 Mock、样例或文件存在冒充真实接入；覆盖无关用户修改；执行未授权 Git、真实费用、部署、发布或不可逆迁移。

遇到以下情况停止受影响阶段并保留证据：需求或 AC 实质变化；需要第四个部署项目或第二业务数据库；migration 可能覆盖有效数据；跨客户或成本字段越权；确认报价无法复算；旧 fence 可产生副作用；外部任务费用或结果处于未知却无法回查；恢复演练不能恢复有效引用。只重开受影响的需求、设计或实施阶段，不机械推倒已成立证据。
