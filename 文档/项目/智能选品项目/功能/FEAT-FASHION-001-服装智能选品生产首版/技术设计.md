# FEAT-FASHION-001 技术设计

> 文档类型：Feature 技术设计；版本：0.4.13；文档状态：Draft  
> Feature：FEAT-FASHION-001；风险：L2  
> 产出阶段：Stage 5 技术设计；当前实施状态以[控制页](README.md)为准  
> owner：Codex 维护；IMP-01 Java 集成契约已完成复核，真实 Provider-ready 仍未成立  
> 创建日期：2026-09-12；更新日期：2026-09-13  
> 上游：[功能规格](功能规格.md)；跨项目设计：[技术架构设计](../../架构/技术架构设计.md)；数据库字段与关系唯一事实源：[数据库设计 v2.3](../../架构/数据库设计.md)

## 1. 设计边界

跨项目拓扑、部署、安全和运行决策以[技术架构设计](../../架构/技术架构设计.md)为上位入口。本文件只说明 FEAT-FASHION-001 的实施落点。产品与架构基线及 `IMP-01`～`IMP-10` 执行包已经用户确认；`IMP-01`～`IMP-09` 已于 2026-09-13 通过退出门，`IMP-10` 本地工程检查通过但受生产候选环境、真实 Provider 和 UAT 阻断。当前不能称 Provider-ready、ReleaseReady 或已发布。

P0 只有三个代码项目：`platform-backend`、`admin-web`、`fashion-ai-runtime`。`contracts/fashion` 是契约源，不部署；Portal、Mobile 和浏览器扩展不参与。

## 2. 实际技术栈与目标目录

| 层 | 当前事实 | P0 目标 |
| --- | --- | --- |
| Java | Java 17 编译目标、Spring Boot 4.0.6、RuoYi 3.9.2、平台 Jackson 3；相邻 `ruoyi-interview` 采用 Application/Domain/Infrastructure/Controller 与 `NamedParameterJdbcTemplate`。共享后台已在本机 JDK 21.0.10 以完整 Jar 进程验证启动和重启 | `ruoyi-fashion` 沿用模块化单体和 Repository Port/JDBC Adapter；JSON databind/core 统一使用 `tools.jackson`，不并存 Jackson 2 Mapper；不为 16 表机械生成 16 套 Mapper/XML |
| MySQL | RuoYi MySQL `dynamicDataSource` 和面试 PostgreSQL 共存；Fashion 已建立独立 migration、限定 JDBC/事务与 T01～T16，并在隔离 MySQL 8.0.46 通过 IMP-02～10 本地业务、容量与恢复验证 | 后续 Repository 继续限定 `fashionJdbcTemplate` 和 `fashionTransactionManager`；不新增第 17 张 `fq_*` 表 |
| Admin | Vue 3.5、Vite 6.4、Element Plus 2.13、Axios、Pinia；Fashion 已增量开启 TypeScript、`vue-tsc`、Vitest、Vue Test Utils 和 Playwright，已完成设置/商品/素材/价格/库存/客户/选品/图片/报价/交付/运维页面 | 后续 Fashion 文件继续使用 TypeScript，保留 `allowJs`，不整体迁移既有页面 |
| Python | Python 3.12、FastAPI、Pydantic v2、Uvicorn；需求分析、商品属性、冻结候选搭配严格契约及 40 任务离线 evaluator 已实现，当前 Provider disabled | PydanticAI Core＋显式 async Workflow＋HTTPX＋OpenTelemetry；Java 持久化 Run/Step，Python 不建第二状态源；真实评测只消费脱敏结果并保留封闭分母 |
| 契约/文件 | Runtime 需求、商品属性、选品与图片端点已进入 OpenAPI；对象存储、PPTX/CSV/ZIP/JPG 生产实现和本机 WPS 编辑证据已完成 | OpenAPI 3.1＋共享 JSON Schema；对象存储 Port＋SHA-256；Apache POI XSLF/JDK ZIP/CSV 安全输出，候选环境客户端兼容仍需验证 |

目标目录如下，按 `IMP-01`～`IMP-10` 的实际切片创建，不预建空包：

```text
ruoyi-backend/ruoyi-fashion/src/main/java/com/ruoyi/fashion/
├─ application/{product,importing,material,customer,selection,image,quote,delivery,agent,settings,operations,shared}
│  ├─ agent/worker
│  ├─ image/worker
│  └─ delivery/worker
├─ domain/{product,stock,importing,customer,quote,delivery,agent,shared}
├─ infrastructure/{persistence,storage,airuntime,files}
├─ controller/rest/{product,importing,material,customer,quote,image,delivery,agent,settings,operations,common}
└─ configuration/{persistence,worker,telemetry}

frontend/admin-web/src/
├─ api/fashion/{client,product,stock,importBatch,material,customer,quote,selection,quoteImage,quoteFile,aiRun,settings,operations,types}.ts
└─ views/fashion/{product,import,material,quote,workbench,image,delivery,settings,operations,components,composables,constants}

python/fashion-ai-runtime/
├─ src/fashion_ai/{main.py,settings.py,api,application/workflows,agents,domain,ports,providers,tools/java_gateway,guardrails,telemetry,prompts,evaluation}
├─ evals/{README.md,visual-trial-results.example.json,candidate-readiness-evidence.example.json}
└─ tests/{unit,contract,integration,fault_injection,security,eval}

contracts/fashion/
├─ ai-runtime.openapi.yaml
├─ ai-control-gateway.openapi.yaml
├─ schemas/v1
└─ examples/v1
```

Flyway 若启用会产生 migration history 技术表。数据库设计中的 16 张指全部 `fq_*` 业务表，不包括既有 `sys_*` 或经 `IMP-01` 明确批准的单一 migration 元数据表；不得借此增加其他业务、审计、预算、回调或 outbox 表。若实施审查决定所有新增物理表也必须绝对等于 16，则改用版本化 forward/verify/restore SQL 和发布清单，不虚称使用 Flyway。

商品来源、品类、颜色、单位、季节和仓库复用现有 RuoYi `sys_dict_type/sys_dict_data`，字典类型固定为 `fashion_product_source`、`fashion_product_category`、`fashion_product_color`、`fashion_product_unit`、`fashion_product_season`、`fashion_warehouse`。六类字典通过幂等 SQL 初始化，由 Java 服务端验证有效编码，Admin 只经 Java/RuoYi 字典接口读取；不建立 Fashion 字典表。`size_system`、币种、税口径、折扣类型和各状态机属于版本化代码枚举，不能混成可任意编辑的平台字典。

配置权威位置固定如下，不再为配置新增表：

| 配置类别 | 权威位置 | 写入与冻结规则 |
| --- | --- | --- |
| 人员与数据范围 | RuoYi 角色/权限＋`fq_customer`、`fq_quote` 归属字段 | 角色和客户归属分别校验，不能用 `sys_config` 保存人员 ID 列表 |
| 库存时效、报价默认税模式/税率 | 白名单 `sys_config` 键 | 管理员修改留 before/after；创建方案时复制到 `fq_quote`，历史版本不随默认值变化 |
| 保留期、告警阈值 | 白名单 `sys_config` 键 | 默认 365/90/30 天及业务阈值；只能影响新一轮计算，不绕过引用保护 |
| AI 每次/每日/月度 CNY 额度与 80% 阈值 | 白名单 `sys_config` 键 | 实际预留/结算仍写 T15/T16/T07；Secret 不进入配置表 |
| Agent prompt/model/tool/schema/provider 非秘密配置 | `fq_ai_agent_version` 不可变版本 | 发布新版本，不覆盖已运行版本；Provider Secret 仅来自环境 Secret |
| PPT 模板 | `resources/fashion/presentation-templates` 版本化资源；默认模板代码在 `sys_config` | 报价/导出冻结模板代码和版本，不能用同名文件覆盖历史含义 |

`sys_config` 只允许注册过的 Fashion 键和类型，更新通过 Fashion 设置 API、RuoYi 权限与操作日志；浏览器、Python 和模型都不能直接修改。

## 3. 调用与信任边界

```text
内部浏览器
    │ 仅调用 Java API
    ▼
frontend/admin-web/src/{views,api}/fashion ──► ruoyi-backend/ruoyi-fashion ──► fashion-ai-runtime
                              │                              │
                              ▼                              └─► 受控 Provider（当前 disabled）
                     MySQL 业务事实 / 对象存储引用
```

| 边界 | 规则 |
| --- | --- |
| 浏览器 → Java | 使用若依身份和 RBAC；服务端重新检查客户归属、字段权限、版本和状态 |
| Java → Python | 仅服务到服务调用；使用 HMAC-SHA256 v1 key ring、audience、实际正文摘要、时间窗与 nonce；JDK client 固定 HTTP/1.1，避免 Uvicorn cleartext h2c upgrade 丢失 POST 正文；生产部署仍必须使用私网 TLS |
| Python → Provider | adapter 默认关闭；密钥仅在运行环境注入，不能进入浏览器、请求正文、trace 或仓库 |
| Python → 数据 | 不直连服装业务 MySQL；只处理 Java 提供的最小、带版本上下文 |
| Python → Java 结果 | 结果是候选/草案；Java 按最新事实复核，通过人工采用事务后才写业务状态 |

浏览器不得直连 Python。CORS、隐藏 URL 或前端环境变量都不能替代这一约束。

## 4. Java 与 MySQL 权威模型

`platform-backend` 新增 `ruoyi-fashion` 模块，沿用现有若依构建、认证和模块装配。业务表和 migration 只能从[数据库设计 v2.3](../../架构/数据库设计.md)生成：首期恰好是商品资料、库存、客户、报价方案、导入管理、AI Agent 六个模块，共 16 张 `fq_*` 业务表，其中 10 张核心业务表、6 张 AI 生命周期表。T01～T16 migration 已在临时隔离 MySQL 8.0.46 反复执行，并完成约束、并发、10 万级容量、重启及逻辑备份恢复验证；非隔离数据库未迁移。

Spring Boot 4 的共享 `ObjectMapper` 使用 Jackson 3。Fashion 的 `JsonNode`、`ObjectMapper`、`TypeReference` 和异常类型统一采用 `tools.jackson`；兼容注解仍沿用平台提供的 annotation 包。需要被 `@Repository` 异常翻译或 `@PreAuthorize` 方法安全切面代理的实现类不得声明为 `final`。这两项约束已经由完整 `ruoyi-admin.jar` 启动/重启脚本实际验证，避免仅凭模块单测遗漏容器装配错误。

P0 migration 的精确表名为：`fq_product`、`fq_stock`、`fq_customer`、`fq_quote`、`fq_quote_combo`、`fq_quote_detail`、`fq_quote_image`、`fq_quote_file`、`fq_import_batch`、`fq_import_detail`、`fq_ai_agent`、`fq_ai_agent_version`、`fq_ai_conversation`、`fq_ai_message`、`fq_ai_run`、`fq_ai_run_step`。禁止读取归档第一版，禁止创建 `fq_import_template`、`fq_ai_feedback`，也不建设独立报价快照、价格版本、库存版本、审计、预算账本、回调收件箱或 outbox 表。

| 聚合/记录 | Java 权威规则 |
| --- | --- |
| 商品与 SKU | 商品、颜色、尺码、来源、图片引用和上下架状态由 Java 验证；SKU 稳定唯一 |
| 当前销售价 | P0 每个 SKU 恰好一套当前销售价，直接保存在 `fq_product`；多价表和价格版本表不预埋为已支持 |
| 库存与导入 | 当前库存保存在 `fq_stock`；价格/库存导入的批次、前后值、错误和恢复证据保存在 `fq_import_batch`、`fq_import_detail`，发布事务失败时当前行保持不变 |
| 客户方案 | 客户归属来自已认证主体和 Java 数据，不把名称、联系人、内部备注交给 Python |
| 搭配与采用 | 候选、锁定、替换、`quoteRowVersion/comboVisualHash`、来源 run 和人工采用动作分开记录 |
| 报价 | 金额使用 `DECIMAL`，由 Java 以十进制计算并按规则半入舍入到分；税费/折扣/费用、尺码数量、库存时效和授权例外均由 Java 确定性计算 |
| 确认版本与交付 | `fq_quote`、`fq_quote_combo`、`fq_quote_detail` 三层在确认时冻结并构成历史报价版本；导出只读该三层记录，不另建快照表，也不因当前价或库存变化而重算 |
| 审计与幂等 | 真实主体、动作、版本、请求键、采用/拒绝及失败原因由当前 16 表的业务字段与 RuoYi 平台能力追溯；前端提示不构成审计，不另建独立审计表 |

数据库采用 MySQL 作为本独立业务项目的唯一权威业务数据库。由于当前 Spring Context 还存在面试项目 PostgreSQL 及其 `@Primary interviewJdbcTemplate`，Fashion 的 migration、Repository、写事务和 MySQL 命名锁必须通过限定名绑定同一 `dynamicDataSource`，禁止依赖按类型自动注入。P0 由公共 `FashionCatalogWriteLock` 使用单一 `fashion:{database}:catalog-write` 串行保护商品资料/主图、当前价格、当前库存的发布与人工修改及报价确认；文件解析、AI 和外部调用均在锁外，命名锁只在事务 commit/rollback 完成后释放。`fq_ai_agent`、`fq_ai_agent_version`、`fq_ai_conversation`、`fq_ai_message`、`fq_ai_run`、`fq_ai_run_step` 六张表由 Java/MySQL 持有 Agent 生命周期权威状态；Python 只执行 Java 下发的有界任务，不建立第二套 Agent、对话、消息、Run、Step 或采用状态源。上述逻辑模型已由数据库设计 v2.3 固定为 P0 输入；IMP-02～IMP-09 已完成数据库、业务、Worker 和交付实现，IMP-10 已完成本地隔离容量、重启与逻辑恢复演练；候选环境迁移和多实例演练仍未执行。

## 5. Python Runtime 边界

当前 [`ai-runtime.openapi.yaml`](../../../../../contracts/fashion/ai-runtime.openapi.yaml)覆盖 health、capabilities、requirement-analysis、product-attribute-suggestion 和 selection-styling，并与 [`ai-control-gateway.openapi.yaml`](../../../../../contracts/fashion/ai-control-gateway.openapi.yaml)、共享 JSON Schema、认证规范和双向向量构成内部契约基线：

- 请求包含 UUID `request_id`、稳定 `run_id`、`idempotency_key`、`deadline_at` 和 `version`。
- 客户只以稳定 `customer_ref` 进入请求；响应草案禁止携带客户归属字段。
- 需求草案可表达方案名、受众、场景、季节、风格、颜色、排除项、人数/套数、交付日期、尺码、独立/递进模式、1–4 品类档位和多口径预算。
- 响应固定为待人工确认，且不能携带商品、SKU、价格或库存事实。
- Provider disabled 时返回 503；非法输入返回不回显正文的稳定 422；在 JSON 解析前按实际 ASGI 字节限制 413。
- 内部接口要求 HMAC 服务身份、request/correlation ID 和 W3C Trace Context；active/previous key 支持轮换，nonce 按服务全局防重放。

该契约与测试证明 fail-closed 输入输出、双向认证、解析前 body limit、候选和图片任务结构；Java 侧 Run 调度/持久化、费用状态和采用边界已有自动化证据。本机真实 socket 已验证 HMAC、非空 POST、correlation/trace、503 映射及 Python 进程重启前后调用，并据此修复 JDK h2c upgrade 与 Uvicorn 不兼容造成的空正文签名失败。它仍不证明真实视觉模型质量、生产 Provider 费用、私网 TLS、共享 nonce store、采集端或多实例部署已经完成。

真实视觉试点采用封闭分母：试点开始前登记 40 个唯一任务 ID，结果集必须与登记集合完全一致，且 1/2/3/4 品类组各 10 个；不得删除失败任务或用新任务替换。`fashion-evaluate-visual-trial` 只读取脱敏结果，强制 Provider/模型/配置、输入输出规格和人工复核规则一致，提交/复核/采用时间带时区且采用发生在复核后，按 PRD 计算首轮可用率、两轮内可交付率、每组 8/10、全部费用、每张采用图成本及采用耗时。账单未结或无采用证据保持 `blocked`；质量、费用、数据授权、关键商品身份或缺件触发 `fail`。聚合报告带源文件 SHA-256，但该工具不调用 Provider，也不替代真实样本、业务复核、UAT 或发布决定。

`fashion-evaluate-candidate-readiness` 是 IMP-10.7/10.8 的最终离线交叉门：先读取原始候选授权清单并重算 SHA-256，再核对候选输入预检、视觉聚合和技术/UAT 证据清单全部引用该摘要，视觉报告费用上限与预检获批上限完全一致，并精确覆盖七个技术 gate 与 `UAT-01～UAT-06`。质量门固定为 PRD 的每组 8/10；获批人民币费用上限不少于 0.01 元且最多两位小数，避免预检报告四舍五入改变授权语义。预检的 16 表、无网络/写库/Provider、Secret 拒绝和排除发布声明，以及视觉报告的封闭分母、失败保留、无外部动作和无发布决定声明均为 Literal 契约。已执行 gate 和业务决定必须记录非空身份及带时区时间；每个 gate 必须提供证据包内相对文件路径，工具实际读取不超过 50 MiB 的文件并重算 SHA-256；绝对路径、目录越界、缺失、疑似 Secret/私钥或摘要不一致即拒绝。只有原始授权清单、视觉、技术、六组 UAT 和业务决定全部通过才可写 `ReleaseReady=true`，而 `ReleaseAuthorized` 永远为 false；该本地判定不能代签 UAT，也不能触发部署或发布。

## 6. 通用 Agent Run 冻结语义

文本解析、候选排序、搭配和视觉任务复用 Java/MySQL 中 `fq_ai_run` 与 `fq_ai_run_step` 的同一运行语义，不为每个能力或 Python Runtime 复制一套任务状态。IMP-01 冻结的 ID、版本、deadline、错误、取消、幂等、attempt、lease 与 fencing 契约已经在后续阶段实现并测试：

1. `run_id`、`quoteRowVersion/comboVisualHash`、prompt/schema/provider 版本和截止时间固定，不从晚到结果读取“当前报价”。
2. 每次执行持有有期限 lease；续租和接管必须产生单调 fencing token，旧 worker 无权提交结果。
3. `idempotency_key` 的作用域、请求摘要、冲突返回和保留期明确；相同键不同输入必须拒绝。
4. 取消、超时、Provider 未知结果、部分成功和重试分别有状态，不能把“已受理”写成“已成功”。
5. trace 默认脱敏，不记录客户原文、联系人、密钥和未授权图片；eval 数据需单独批准用途。
6. Java 采用结果时校验 run、`quoteRowVersion/comboVisualHash`、商品/图片版本和人工身份；晚到或旧版本结果只留审计，不覆盖当前报价。

P0 不新增队列服务或第四个部署项目。Java/RuoYi 进程内的有界 Worker/Dispatcher 从 MySQL 领取 Agent、图片和交付任务：领取与租约落库后先提交事务，再调用 Python、Provider 或文件生成；结果使用 lease/fencing 条件在新事务提交。Python 不直连 MySQL，也不自行领取业务任务。重新领取、续租、取消、晚到结果拒绝以及本地 MySQL 重启/逻辑恢复均已有证据；候选环境 Java/Python 多实例恢复仍未演练。

共享若依启动时，`fashion.enabled` 为模块总开关，缺省 `true`；显式设为 `false` 并重启后，Fashion 的 HTTP、业务服务、持久化、迁移配置与进程内 Worker 均不装配。总开关不替代细分开关：`fashion.migration.enabled` 和 `fashion.ai-runtime.worker-enabled` 仍默认关闭，真实图片 Provider 仍须显式启用；图片与文件交付 Worker 沿用原有的独立默认值，启用模块时会轮询已有任务。

## 7. 安全、大小和失败处理

| 风险 | 必须实现的控制 | 当前状态 |
| --- | --- | --- |
| 未认证内部调用 | Java ↔ Python 服务身份、密钥轮换或证书、授权范围、重放防护 | IMP-01 单实例 HMAC 基线 Pass；多实例共享 nonce store 和私网 TLS 留给部署阶段 |
| 大正文抢占内存 | 在 FastAPI/Pydantic JSON 解析前限制 Content-Length 和实际流量字节，处理缺失/伪造长度 | IMP-01 Pass；默认 64 KiB，最大配置 10 MiB |
| 越权客户/字段 | Java 从登录主体解析客户范围；Python 不接收客户名称、联系人或内部备注 | 客户范围、字段白名单和 Python schema 已通过自动化检查 |
| 假 AI/错误事实 | disabled 返回 503；候选标来源；Java 重验价格、库存、预算和采用 | 机制和完整 Java 采用链路已完成；真实 Provider 质量 NotRun |
| 重复/晚到任务 | 租约、fencing、幂等摘要、revision 和取消状态 | MySQL/Worker 竞争、取消、晚到拒绝和本地恢复已通过；候选环境多实例未验证 |
| Provider 费用失控 | 按数据库设计 v2.3 在受控配置中维护限额，并由 `fq_ai_run`、`fq_ai_run_step`、`fq_quote_image` 记录预留/实际费用与账单事件；超限拒绝、未知结果不擅自退款 | 预留/结算/超限/UNKNOWN 机制已通过；没有真实账单对账证据 |
| 敏感日志 | 白名单字段、正文脱敏、trace 分级、下载鉴权 | Java/Python 白名单日志、W3C/OTel 基座、鉴权下载和交付包扫描 Pass；采集端未部署 |

服务或 Provider 失败时，Java 保留可编辑方案，商品当前价、当前库存及已确认报价记录均保持原状；不得用静态 AI 文案填充成功。视觉能力失败可以按产品规则回到已确认原图拼版，但必须标明来源，不能记为真实生图成功。

## 8. 前端和交付

`frontend/admin-web/src/api/fashion` 与 `frontend/admin-web/src/views/fashion` 新增路由、API 类型、页面和状态组合逻辑。页面覆盖商品中心、导入批次、客户方案、需求、搭配、图片复核、报价和交付；服务器响应是状态真相，刷新后从 Java 恢复。

PPTX/ZIP 等交付物必须从 Java 已确认的 `fq_quote`、`fq_quote_combo`、`fq_quote_detail` 三层记录生成，`fq_quote_file` 保存文件摘要、大小、模板版本和生成状态；下载时重新鉴权。生成进程、库和模板在 `IMP-09` 内实现和验证，但不得新增第四个部署项目，也不得让浏览器用整页截图或假扩展名冒充可编辑文件。

## 9. 兼容与恢复

- `ruoyi-fashion` 以新增模块和向前兼容 migration 落地，不修改已执行迁移；顺序采用新增 → 回填 → 验证 → 切换。
- 首期价格模型只承诺一 SKU 一套当前销售价；P1 多价表需单独 migration 和消费者兼容计划。
- Provider、视觉生成和导出均以开关默认关闭；关闭开关不删除历史任务、采用记录或已确认报价三层记录。
- 数据迁移和发布前须记录备份、校验、回退程序及负责人；本 Feature 当前未进入发布执行。
