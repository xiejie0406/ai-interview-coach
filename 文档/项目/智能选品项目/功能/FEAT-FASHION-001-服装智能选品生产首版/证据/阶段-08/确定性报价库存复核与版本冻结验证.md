# IMP-08 确定性报价库存复核与版本冻结验证

> 文档类型：实施阶段证据；版本：1.0.0；文档状态：Draft  
> Feature：FEAT-FASHION-001；实施阶段：IMP-08；风险：L2  
> 执行日期：2026-09-13；结果：Pass  
> 输入：[任务清单](../../任务清单.md)、[功能规格](../../功能规格.md)、[数据库设计 v2.3](../../../../架构/数据库设计.md)

## 1. 结论

IMP-08 的实现、自审和最低自动验证已完成，退出门结果为 **Pass**。REQ 12、REQ 13、REQ 14、REQ 15 及 AC 11、AC 12、AC 13、AC 19、AC 20、AC 23、AC 25 已取得生产代码、隔离 MySQL 和自动化场景证据，但不等于用户 UAT、非隔离数据库迁移或生产发布。

报价计算只在 Java 使用 `BigDecimal`、显式 scale 与 `RoundingMode.HALF_UP`；Python 未参与金额、库存或确认。报价确认在公共目录写锁和同一 MySQL 事务中重新读取当前商品、价格、库存、主图、采用图和权限事实，再冻结 `fq_quote`、`fq_quote_combo`、`fq_quote_detail` 三层。没有新增价格版本、库存版本、报价快照或审批表，数据库仍严格为 6 个模块、16 张 `fq_*` 业务表。

## 2. 实现与需求对照

| 任务 | 实际实现 | 关键护栏 |
| --- | --- | --- |
| IMP-08.1 | Java 实现备选报价与合并采购两种金额归属，组合小计和报价汇总由同一次确定性计算产生 | 备选组合分别计算；合并模式对共同采购行汇总一次，避免重复金额事实源 |
| IMP-08.2 | 实现 SKU 尺码数量守恒、同 SKU 跨组合汇总库存、库存业务时间和固定尺码配比下的最大可供套数 | 组合套数、行数量、总采购量不一致均阻断；共享 SKU 库存在合并模式统一校验 |
| IMP-08.3 | 实现含税/未税、税率、百分比/固定折扣、运费、附加费和分币舍入 | 金额字段使用 `BigDecimal`；写入前检查 `DECIMAL(16,2)` 范围；PRD 金额向量精确得到 `24050.00`、`56.46`、未税 `118.65`、含税 `105.00` |
| IMP-08.4 | 实现销售折扣上限、负责人审批、审批内容 hash、申请人/审批人隔离和有效期 | 超 10% 折扣或单价调整必须审批；自审拒绝；任何价格、库存、图片、商务条款或行内容变化使旧审批失效；零单价、100% 优惠和零应付始终阻断正式确认 |
| IMP-08.5 | 确认在短事务内取得 `fashion:{database}:catalog-write`，`FOR UPDATE` 重读并校验输入 hash，随后冻结三层并提交 | 先锁目录再锁报价；相同输入确认幂等；旧输入 hash、过期库存、图片无效、权限变化和并发旧版本均拒绝；锁在事务完成后释放 |
| IMP-08.6 | 已确认版本复制为新 `version_no`，复制 quote/combo/detail 结构并清空确认、审批、正式金额和采用图 | 历史版本不原地修改；新修订必须重新计算、重新审批、重新选图和重新确认 |
| IMP-08.7 | Admin 报价抽屉支持条款、尺码数量/单价、阻断问题、最大可供套数、当前事实变化、审批、确认、冻结历史和复制修订 | 浏览器只调用 Java API；金额字段保持 Java 返回的十进制字符串，不在前端重算正式金额 |
| IMP-08.8 | Java 单元/安全测试、真实隔离 MySQL 并发确认、Admin 单测/生产构建/Chrome 流程及边界扫描全部执行 | 测试覆盖库存 90 阻断后修正、同输入并发确认只有一次版本推进、当前事实变化不改写历史冻结值 |

## 3. 自动验证结果

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 全量回归 | `mvn -pl ruoyi-fashion -am test` | 110 tests，0 failure/error；12 条条件数据库测试因当前 Maven 进程未提供 Docker/外部 URL skipped | Pass |
| Java 报价定向 | `QuotePricingCalculatorTest,FashionQuotePricingServiceTest,FashionQuotePricingControllerSecurityTest` | 20 tests，0 failure/error/skipped；覆盖金额向量、数量守恒、审批范围与失效、自审拒绝、图片权限/采用有效性、并发输入和安全权限 | Pass |
| 隔离 MySQL 8.0.46 | `pwsh -NoProfile -File ruoyi-fashion/scripts/run-isolated-mysql-tests.ps1` | 9 tests，0 failure/error/skipped；最终临时实例仅绑定 `127.0.0.1:11123` | Pass |
| MySQL 报价场景 | `FashionProvidedMySqlMigrationTest` 第九场景 | 精确总额 `24050.00`；价格变化使旧 hash 失效；恢复后并发确认只有一个 `rowVersion` 推进；当前价格、库存和图片变化后历史冻结值不变；复制修订成功 | Pass |
| 后台装配 | `mvn -pl ruoyi-admin -am package -DskipTests` | 18 个 reactor module 全部 SUCCESS，重新打包 `ruoyi-admin.jar` | Pass |
| Admin 类型与单测 | `npm run typecheck`、`npm run test:unit` | 类型检查退出码 0；9 files / 35 tests | Pass |
| Admin 生产构建 | `npm run build:prod` | 2561 modules，构建成功 | Pass |
| Admin 浏览器 | `npx playwright test`，Desktop Chrome | 6/6；精确总额、库存不足、修正、确认冻结、复制修订及既有场景通过 | Pass（Mock Java API） |
| Schema 与边界扫描 | migration 表名、Admin Runtime 引用、Python 数据库引用 | `fq_*` 为 16/16 个唯一表名，禁建表 0；Admin Runtime 内部引用 0；Python MySQL/JDBC/业务 SQL 引用 0 | Pass |
| 双向契约静态一致性 | 计算两个已提交 OpenAPI 的 SHA-256 | Runtime 为 `913BF2E1EC59B03D5A6A19B7AB48ACCDB4EA3BFC6331BD15BAFC45E1ED4A9FC9`；Gateway 为 `F44D75C4393AB67E032F968CA12634FFCA9C3EC7CFF17CD4AF9BADC5F198A220` | Pass |
| Python 回归 | 本阶段未修改 Python 或 Runtime 契约实现 | 未重复运行；沿用 IMP-07 的 127 passed、Ruff/Pyright Pass 仅作既有基线，不计为本阶段新增证据 | NotApplicable |

浏览器中的 `/captchaImage` 因 Java 未启动出现 `ECONNREFUSED`，六条测试使用受控 Java API Mock，不依赖该响应。该证据证明前端交互与“浏览器只到 Java”边界，不代表浏览器、Java、Python 已完成真实网络联调。

## 4. 关键确认场景

1. 报价工作区读取当前事实后，以客户、方案版本、采购量、商务条款、备注、组合视觉、采用图片及各行商品/价格/库存/视觉版本计算精确输入 hash。
2. 正式确认前检查方案头采购量与组合套数、每个尺码行数量、1～4 品类、商品状态、当前价、税率/币种、库存时效、主图权限和采用图复核状态。
3. 采用图片必须匹配当前 `comboVisualHash` 和任务输入 hash，结果需成功、允许用于方案、对象 SHA 一致，且最新人工复核为通过；旧图或失败图不能冻结进正式报价。
4. 销售可直接确认不超过 10% 的普通折扣；超限折扣或任一手工单价需要负责人对精确内容 hash 审批。申请人不能审批自己的请求，过期或内容变化后的审批不能复用。
5. 正式确认使用目录写锁、事务和行锁做最终重读；两次相同输入并发确认只允许一次合法状态推进，相同已确认 hash 可安全幂等返回。
6. 已确认工作区显示冻结单价、库存、图片与金额，同时只把当前事实变化标记为变化，不回写历史值。后续修改通过复制修订完成。

## 5. 执行中发现与修正

1. 首次使用 Windows PowerShell 5 执行隔离脚本时，脚本的 UTF-8 无 BOM 内容被错误解析，失败发生在数据库创建前；改用仓库要求的 PowerShell 7 `pwsh` 后再运行，不将该次解析失败记为数据库验证。
2. 首轮真实 MySQL 场景发现 `JSON_TABLE` 返回列与表字段比较时存在 collation 混用，同时早期 IMP-08 测试商品污染候选数量；查询改为显式兼容 collation，报价专用测试商品改为未确认属性，随后连续完整重跑通过。
3. 自审发现初版未返回固定尺码配比下的最大可供套数；补充备选和合并两种算法，并在库存不足问题中返回可执行上限。
4. 自审发现方案头 `requested_qty` 与组合 `set_qty` 之间缺少显式一致性门；补充 `QUOTE_QTY_MISMATCH` 阻断和单元测试。
5. 自审发现只校验被采用图片的外层状态仍不足；补充结果对象 SHA、使用许可、当前视觉 hash、任务输入 hash 和最新人工复核通过校验。
6. Vitest 曾使用不支持的 `--runInBand` 参数，CLI 在测试运行前退出；去掉该参数后执行标准 `npm run test:unit`，最终 9 files / 35 tests 全过。
7. 数据层允许草稿保存零参考价，以便清晰显示问题；Java 正式确认仍无条件阻断零单价、100% 优惠和零应付。DDL 因此把明细价格检查调整为非负，而不是放宽正式报价规则。

## 6. 环境、回收与限制

- 最终隔离 MySQL 只绑定 `127.0.0.1:11123`，验证完成后正常 shutdown，并删除经过绝对路径校验的专用临时实例目录；未连接或改动非隔离数据库。
- 没有新增第 17 张 `fq_*` 表；审批、冻结和报价历史都复用 `fq_quote`、`fq_quote_combo`、`fq_quote_detail`、`fq_quote_image` 的既定字段和有界 JSON。
- 没有修改 Python 代码、启用 Provider、调用外部模型、产生费用或写入真实客户/商品数据。
- 没有部署、发布或执行 Git 暂存、提交、分支、推送、标签操作。
- UAT 仍为 `NotStarted`；真实浏览器到 Java 联调、候选环境竞态与恢复演练保留到 IMP-10。
