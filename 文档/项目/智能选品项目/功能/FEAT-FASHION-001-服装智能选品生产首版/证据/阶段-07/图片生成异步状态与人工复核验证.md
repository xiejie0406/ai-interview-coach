# IMP-07 图片生成异步状态与人工复核验证

> 文档类型：实施阶段证据；版本：1.0.0；文档状态：Draft  
> Feature：FEAT-FASHION-001；实施阶段：IMP-07；风险：L2  
> 执行日期：2026-09-13；结果：Pass  
> 输入：[任务清单](../../任务清单.md)、[功能规格](../../功能规格.md)、[数据库设计 v2.3](../../../../架构/数据库设计.md)

## 1. 结论

IMP-07 的实现、自审和最低自动验证已完成，退出门结果为 **Pass**。REQ 09、REQ 10、REQ 11 及 AC 09、AC 10、AC 18、AC 27、AC 28 已取得生产代码和自动化场景证据，但不等于用户 UAT、真实视觉模型质量或外部 Provider 数据条款验收。

本阶段没有新增业务表。外部上传、体验样例、商品原图拼版和 Provider 候选共用 `fq_quote_image`；图片输入、候选结果、有限复核历史和计费事件以有界 JSON 保存，组合采用关系继续写入 `fq_quote_combo`。数据库仍严格为 6 个模块、16 张 `fq_*` 业务表。

## 2. 实现与需求对照

| 任务 | 实际实现 | 关键护栏 |
| --- | --- | --- |
| IMP-07.1 | Java 建立统一图片任务服务、对象存储读写 Port、外部图片上传、确定性体验样例和商品原图拼版 | 上传校验格式/大小/SHA-256；图片来源明确标识，样例和拼版不冒充真实 AI |
| IMP-07.2 | 进程内 Worker 实现 queued/running/success/partial/failed/cancel_requested/cancelled/unknown、lease、有限重试、查询恢复和 rowVersion fencing | Provider 调用在数据库事务外；UNKNOWN 按原请求键查询，不盲目重提；取消后的晚到完成不能越过 fencing |
| IMP-07.3 | 原图与候选图并排复核，查看、复核、采用分离；通过必须确认数量、款式、颜色、标识、完整性、姿态和质量 | 查看不产生复核；失败候选不能通过；拒绝必须使用规范原因；未通过人工复核不得采用 |
| IMP-07.4 | 当前组合和商品图片按 `comboVisualHash`、槽位图片 hash 与权限快照重新计算输入 hash | 组合、商品主图或权限变化后旧图标记待复核并阻止采用；已确认方案不能替换历史图 |
| IMP-07.5 | Provider 费用支持 reserved/unknown/settled，月度预算在事务锁内检查；OpenTelemetry 记录低基数生命周期、终态耗时、CNY 费用和采用事件 | 预算不足不建任务；UNKNOWN 不自动退款；指标不接收请求键、方案号、客户内容或错误正文 |
| IMP-07.6 | Admin 新增图片工作台、任务状态恢复、上传、样例/拼版创建、逐项复核、拒绝和采用，并从方案工作台进入 | 浏览器只调用 Java API；真实 Provider 未配置时显示禁用；sample/upload/composition/provider/reuse 来源不混淆 |
| IMP-07.7 | Java fake Provider 覆盖部分成功、未知回查、取消晚到、预算、幂等、过期输入和版本冲突；Python 暴露严格图片输入契约但保持 Provider disabled；Admin 跑完整异步流程 | fake Provider 只证明机制，不作为真实模型效果证据 |

## 3. 自动验证结果

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 全量回归 | `mvn -pl ruoyi-fashion -am test` | 89 tests，0 failure/error；11 条数据库条件测试因无 Docker/外部 URL skipped | Pass |
| Java 图片与观测定向 | `FashionQuoteImageServiceTest,FashionTelemetryTest` | 最终代码 15 tests，0 failure/error/skipped；覆盖完整请求意图幂等、行版本、预算、部分成功、UNKNOWN 查询、取消晚到及低基数指标 | Pass |
| 隔离 MySQL 8.0.46 | `run-isolated-mysql-tests.ps1` 启动仅绑定回环地址的临时实例 | 8 tests，0 failure/error/skipped；图片插入、领取、部分结果、复核、采用、过期历史及既有 migration 场景均通过 | Pass |
| Schema 精确性 | 对 V1 migration 与隔离库执行检查 | `fq_*` 恰好 16 张；未增加图片任务、审核、预算或快照附表 | Pass |
| Python 回归与静态门 | Ruff、Pyright、pytest | Ruff 全过；Pyright 0/0/0；127 passed、1 个 Starlette TestClient 弃用 warning | Pass |
| Runtime 契约 | 确定性导出 `contracts/fashion/ai-runtime.openapi.yaml` | SHA-256 `913BF2E1EC59B03D5A6A19B7AB48ACCDB4EA3BFC6331BD15BAFC45E1ED4A9FC9`；图片请求隐私白名单和 disabled 503 已覆盖 | Pass |
| Admin 类型、单测与构建 | `npm run typecheck`、`npm run test:unit`、`npm run build:prod` | 类型检查退出码 0；8 files / 34 tests；2560 modules，生产构建成功 | Pass |
| Admin 浏览器 | `npx playwright test`，Desktop Chrome | 5/5；图片工作台创建样例、逐项复核、采用、上传及既有场景通过 | Pass（Mock Java API） |
| 边界扫描 | Admin 直连 Runtime、Python 数据库访问和 16 表静态扫描 | Admin 未发现 Python 地址/内部图片端点；Python 未发现 MySQL/JDBC/SQL 写入；表数为 16 | Pass |

浏览器启动时 `/captchaImage` 因 Java 未启动出现 `ECONNREFUSED`，五条测试使用受控 Java API Mock，不依赖该响应。该证据证明前端交互和“浏览器只到 Java”边界，不代表 Java、Python 和浏览器已完成真实网络联调。

## 4. 执行中发现与修正

1. 首轮图片浏览器用例直接对 Element Plus 隐藏的原生 checkbox 执行 `check()`，等待超时；改为点击可见标签后，图片定向用例和全量 5/5 均通过。业务代码未通过放宽复核规则来迁就测试。
2. 自审发现初版幂等只比较组合、类型、来源和输入 hash；已补为同时比较候选数量与净化参数。上传还比较内容 SHA-256，相同 `requestKey` 复用为不同意图时返回 409。
3. 自审发现人工复核请求携带的 `rowVersion` 未在服务入口比较；已在写复核历史前校验，并继续由仓储层条件更新形成第二道并发保护。
4. 自审发现月度预算若只读合计可能被并发任务超额预留；已在创建 Provider 任务的短事务内锁定预算配置行，再重新检查幂等和已承诺费用。
5. 自审发现 UNKNOWN 若没有 Provider task ID，可能再次提交；已改为仅初始 queued 可 submit，UNKNOWN 始终使用原 `requestKey` query。取消请求可抢占租约，旧 Worker 的晚到结果因 rowVersion fencing 被拒绝。
6. 观测初版只有任务状态计数；最终增加终态耗时和结算费用直方图，采用、复核和取消保持事件计数，所有标签使用固定白名单。

## 5. 环境、回收与限制

- 隔离 MySQL 仅绑定 `127.0.0.1:14164`，验证结束后脚本执行正常 shutdown，并删除经绝对路径校验的专用临时目录；未连接或改动非隔离数据库。
- 真实 Provider 配置仍为 disabled，Python 图片端点明确返回 503；没有外部图片模型调用、真实账号、费用或四品类质量样本。
- 本阶段没有使用真实客户/商品数据，没有部署、发布或执行 Git 写操作。
- UAT 仍为 `NotStarted`。40 个真实视觉任务、数据条款、费用和采用率试点保留到 IMP-10，并需要对应授权。
