# IMP-06 一至四品类选品与人工搭配验证

> 文档类型：实施阶段证据；版本：1.0.0；文档状态：Draft  
> Feature：FEAT-FASHION-001；实施阶段：IMP-06；风险：L2  
> 执行日期：2026-09-13；结果：Pass  
> 输入：[任务清单](../../任务清单.md)、[功能规格](../../功能规格.md)、[数据库设计 v2.3](../../../../架构/数据库设计.md)

## 1. 结论

IMP-06 实现、自审和最低自动验证已完成，退出门结果为 **Pass**。REQ 07、REQ 08 及 AC 07、AC 08 已取得生产代码和自动化场景证据，但不等于用户 UAT 或真实 Provider 质量验收。

本阶段没有增加数据库表：选品候选冻结在 `fq_ai_run.context_snapshot`，人工采用后的组合和 SKU/尺码数量分别写入 `fq_quote_combo`、`fq_quote_detail`，并持续使用已批准的 6 模块 16 张 `fq_*` 业务表。

## 2. 实现与需求对照

| 任务 | 实际实现 | 关键护栏 |
| --- | --- | --- |
| IMP-06.1 | Java 从已上架、已确认属性的 SKU 聚合来源＋品类＋款号＋颜色候选，绑定当前价、仓库库存、有效方案图、商品/价格/库存/图像版本和 SHA-256 摘要 | 排除项、预算、套数、可用库存和每品类最多 50 个候选由 Java 硬过滤 |
| IMP-06.2 | Python 新增 `/internal/v1/selection-styling` 严格 typed 请求/输出和选品 Workflow | 只允许引用 `frozen_candidates`；伪造 SKU、品类错位、摘要错误、预算越界直接拒绝 |
| IMP-06.3 | 支持 1～4 品类档位、独立/递进、每档 1～3 候选、组合去重、锁定/解锁、人工替换和保持锁定重搭 | 候选不足必须返回 `shortage_reasons`；递进档位必须继承前档，独立模式不强制继承 |
| IMP-06.4 | 采用由 Java 单事务完成，重验 Run 状态、业务权限、`quoteRowVersion`、冻结集摘要、候选事实和 `comboVisualHash` | 商品、价格、库存或图像变化时返回 409；锁定和替换也递增方案版本，旧 Run 不能覆盖人工结果 |
| IMP-06.5 | Admin 方案工作台增加冻结候选、档位、尺码库存、不足原因、当前组合、采用、锁定、替换和重搭交互 | 浏览器只访问 Java API；刷新从 Java 恢复；Provider disabled 和旧版本都显式提示 |

Java 实现沿用当前模块的 `application/selection`、`infrastructure/persistence/quote`、`controller/rest/{quote,agent}` 分层；Python 沿用现有分层命名实现为 `application/selection_styling.py` 与 `ports/selection_ranker.py`，没有为追求计划中的示例路径而建立第二套目录。

## 3. 自动验证结果

| 工作线 | 命令/场景 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| Java 回归 | `mvn -pl ruoyi-fashion -am test` | 76 tests，0 failure/error，10 条条件测试 skipped | Pass |
| Java 选品定向 | `FashionSelectionServiceTest` | 6 tests；覆盖 1～4 档位、尺码 SKU 聚合、显式零价、预算无解、采用落库、商品变化 409、锁定版本递增 | Pass |
| 隔离 MySQL 8.0.46 | 向 `FashionProvidedMySqlMigrationTest` 提供全新临时库 | 7 tests，0 failure/error/skipped；实际验证 2 个尺码 SKU 聚合为 1 个品类候选、100 件分配、selection Run/lease 和组合明细写入 | Pass |
| Schema 精确性 | 隔离库 `information_schema` 检查 | `fq_*` 仍恰好 16 张，未增加 SPU、颜色款、候选、锁定或快照表 | Pass |
| Python 回归 | `uv run --frozen pytest -p no:cacheprovider` | 124 passed，1 个 Starlette TestClient 弃用 warning | Pass |
| Python 选品/契约 | `pytest tests/test_contract.py tests/test_selection_styling.py` | 15 passed；覆盖 1～4 递进、锁定、独立模式、显式零价、无解、冻结集变化、伪造 SKU 和 disabled | Pass |
| Python 静态门 | `uv run --frozen ruff check .`＋`uv run --frozen pyright` | Ruff 全过；0 errors / 0 warnings / 0 informations | Pass |
| Runtime 契约 | 确定性导出 `ai-runtime.openapi.yaml` | SHA-256 `93AF7CB4225E4EDE20F30AA94A90DAB1B94BF672A57BAFE25390A27E2E4FD187` | Pass |
| Admin 类型/单测 | `vue-tsc --noEmit`、Vitest | 类型检查退出码 0；7 files / 32 tests | Pass |
| Admin 生产构建 | Vite 6.4.3 `build` | 2536 modules transformed，构建成功 | Pass |
| Admin 浏览器 | Playwright，Desktop Chrome | 4/4；方案工作台 disabled/无解恢复及既有商品、价格、库存流程通过 | Pass（Mock Java API 场景） |

Chrome 执行时 `/captchaImage` 因 Java 未启动出现一次 `ECONNREFUSED`，测试用例不依赖该响应，4 条用例全部通过。浏览器证据证明前端交互和同源边界，不代表 Java/Python 真实网络联调。

## 4. 执行中发现与修正

1. 首次隔离 MySQL 命令中 JDBC URL 的 `&` 被 shell 拆分，未进入测试；改用不含可选查询参数的本机 JDBC URL 后完整通过。
2. 隔离库第二次执行前未重建数据库，“业务 Schema 初始为空”前置断言按预期失败；精确重建临时 `fashion_test` 后重跑 7/7 通过。未连接或改动其他 MySQL 实例。
3. 前端类型和单测首次被并行 `pnpm` 依赖状态检查中断，并非用例失败；改为顺序调用已锁定的本地 `vue-tsc`/`vitest`/`vite`/`playwright` 二进制后全部通过，没有修改依赖版本或执行 `approve-builds`。
4. Pyright 首次发现 Selection Port 协议体、锁定默认列表和测试数据的 7 个严格类型问题；完成显式协议省略、类型化工厂和 `cast` 后为 0/0/0。
5. 自审发现锁定操作原本只递增组合版本；已改为同一事务同时比较并递增方案版本和组合版本，使锁定/解锁也能使旧 AI Run 过期。
6. 回归审查发现选品 SQL 和 Python 模型把 IMP-04 已批准的显式零价当成缺价；已改为“有当前价即有效，值可为零”并新增 Java/Python 测试。模型约束更改后第一次先跑回归、后导出契约，契约精确比对出现 1 个预期不一致；改为先导出契约再重跑后 124/124 通过。

## 5. 环境、回收与限制

- 隔离 MySQL 仅绑定 `127.0.0.1:19076`，最终通过 `mysqladmin shutdown` 停止，确认端口不可达后将精确临时目录移入回收站；其他已存在 MySQL 进程未处理。
- Runtime Provider 仍为 disabled，未调用外部服务、未产生费用；当前 Golden Set 只验证确定性护栏与 1～4 档位结构，不证明真实模型搭配质量。
- 未使用真实客户/商品数据，未连接非隔离数据库，未执行部署、发布或 Git 写操作。
- UAT 仍为 `NotStarted`；本记录只支持 IMP-06 实现退出门，不宣称完整产品上线就绪。
