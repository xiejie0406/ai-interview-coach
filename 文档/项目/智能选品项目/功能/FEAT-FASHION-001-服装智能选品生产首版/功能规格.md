# FEAT-FASHION-001 功能规格

> 文档类型：Feature 功能规格；版本：0.3.0；文档状态：Draft  
> Feature：FEAT-FASHION-001；风险：L2  
> 产出阶段：Stage 3 功能规格；当前实施状态以[控制页](README.md)为准  
> owner：用户维护产品范围；Codex 维护范围映射  
> 创建日期：2026-09-12；更新日期：2026-09-12  
> 唯一产品正文：[服装智能选品与报价系统产品需求文档](../../产品/产品需求文档.md)
> 数据库字段与关系唯一事实源：[数据库设计 v2.3](../../架构/数据库设计.md)

## 1. 规格职责

本文件只把产品正文的 REQ 01–17、AC 01–32 映射到生产 P0 和三个代码项目，不复制或改写完整需求、金额示例、异常规则和验收场景。发生语义差异时，以[产品需求文档](../../产品/产品需求文档.md)为准，并先更新其批准事实，再调整本映射。

## 2. 产品范围映射

| 产品正文 | P0 能力组 | 主责项目 | 协作项目 | P0 结果边界 |
| --- | --- | --- | --- | --- |
| REQ 01–05 | 商品资料、价格/库存严格全量更新、图片入库、商品理解与人工修正 | `platform-backend/ruoyi-fashion` | `admin-web/src/views/fashion`、`admin-web/src/api/fashion`；Python 只可提出属性草案 | 商品当前价读 `fq_product`，当前库存读 `fq_stock`；导入批次与明细保存前后值、错误和恢复证据，发布必须一次事务原子完成 |
| REQ 06–08 | 客户方案、1–4 品类档位、候选生成、锁定替换和约束复核 | Java 维护方案与采用事务 | Python 做需求解析、排序和搭配；Admin 做人工确认 | AI 候选不得直接成为已采用组合；旧运行不得覆盖新修订 |
| REQ 09–11 | 原图拼版、视觉 AI 任务、图片一致性复核、异步失败处理 | Java 管理任务、媒体引用、费用与采用 | Python 做 Provider 适配和视觉护栏；Admin 做复核 | 真实生成、费用、取消和采用必须有可追溯状态；disabled 不算 AI 成功 |
| REQ 12–15 | 确定性报价、备选/合并采购、尺码库存、已确认报价留档和变化处理 | `platform-backend/ruoyi-fashion` | Admin 展示和确认 | 金额以 `DECIMAL` 保存并按规则半入舍入到分；AI 不计算最终报价；`fq_quote`、`fq_quote_combo`、`fq_quote_detail` 的确认版本不重算 |
| REQ 16–17 | PPT 内容、预览、生成、下载和失败恢复 | Java 从同一份已确认报价三层记录生成/编排交付 | Admin 发起、预览和下载 | 文件必须来自一个确定的确认版本；实际可打开、编辑、下载才算成功 |

## 3. AC 覆盖路由

| AC 范围 | 验证主题 | 对应 P0 任务组 |
| --- | --- | --- |
| AC 01、05 | 商品和图片素材导入、映射、错误与幂等 | IMP-03 |
| AC 02–04、21 | 价格/库存严格全量、文件边界、并发和失败原子性 | IMP-04 |
| AC 06、22 | 商品/需求理解、人工修正、客户切换和缺价阻断 | IMP-05 |
| AC 07–08 | 一至四品类、递进/独立、锁定替换和旧结果隔离 | IMP-06 |
| AC 09–10、18、27–28 | 视觉一致性、异步任务、费用、取消、部分失败和图片失效 | IMP-07 |
| AC 11–13、19–20、23、25 | 报价、税费、折扣、库存、尺码、例外、备选与合并采购 | IMP-08 |
| AC 14–17、24、26、29–31 | 确认版本、PPTX/文件、下载权限、失败恢复和 Admin 全流程 | IMP-09 |
| AC 32 | 生产候选环境、真实 Provider、容量、安全、恢复与 UAT | IMP-10 |

完整输入、预期结果和精确金额只在产品正文第 13 节维护。这里的映射不代表相应 AC 已执行或通过。

## 4. P0 项目与角色范围

### 4.1 `platform-backend/ruoyi-fashion`

- 复用若依账号、认证、角色、菜单和权限，实施服务端客户归属与字段权限。
- 用 MySQL 保存数据库设计 v2.3 定义的 10 张核心业务表和 6 张 AI 生命周期表；字段、关系和表名不在本规格重复定义。
- 实施当前价格/库存表与导入批次明细的一致事务、乐观并发、业务幂等、预算和费用校验、确认报价三层冻结及下载授权。
- 只把最小必要、带版本的候选上下文交给 Python；收到结果后重新验证并由人工采用。

### 4.2 `admin-web/src/api/fashion` 与 `admin-web/src/views/fashion`

- 为内部商品运营、销售和负责人提供商品、导入、方案、搭配、图片复核、报价和交付页面。
- 前端状态不作为权限、价格、库存、预算、采用或报价真相。
- 所有请求只进入 Java API；不得通过配置或浏览器代码绕过 Java 直连 Python。

### 4.3 `fashion-ai-runtime`

- P0 目标能力是需求解析、候选排序、搭配、提示管理、文本/视觉 Provider adapter、输入输出护栏、trace 和 eval。
- 不直连服装业务 MySQL，不读取客户联系人/内部备注，不拥有商品、价格、库存、报价或采用状态；Agent、版本、对话、消息、Run、Run Step 六类生命周期事实也只由 Java/MySQL 持有。
- 当前只实现 disabled 契约骨架；返回 503 是正确 fail-closed 行为，不是需求解析已实现。

## 5. 明确排除与 P1

- `contracts/fashion/` 只保存共享契约，不单独部署。
- `portal-web/`、`mobile/`、Chrome/Edge 扩展及任何浏览器采集插件开发不进入本 Feature P0。
- 首期一个 SKU 一套当前销售价。多客户价、多渠道价、阶梯价、预约价、自动回退和复杂价格优先级统一进入 P1，必须另有需求、迁移和兼容方案。
- 全网爆款发现、销量预测、自动供应商选择、下单、支付、合同签署和客户自助门户不在 P0。

## 6. 完成定义

只有以下条件同时成立，才可将生产 P0 声明为已实现候选：

1. 三个代码项目的 P0 任务均有实际实现及定向验证，Java/MySQL 权威边界无绕行。
2. REQ 01–17 对应的 AC 01–32 形成可追溯证据；未执行项不得标 Pass。
3. 真实 Provider 和视觉 AI 在受控数据、费用和停止条件下完成质量验证，失败样本保留。
4. PPTX 和下载在指定桌面软件中实际打开、编辑、另存，并核对内容与已确认报价三层记录一致。
5. 用户或业务负责人在[验收记录](验收记录.md)给出明确结论；进入开发的批准不能替代验收。
