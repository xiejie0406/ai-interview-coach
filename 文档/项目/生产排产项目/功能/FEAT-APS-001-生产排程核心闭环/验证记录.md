# FEAT-APS-001 实现验证记录

> 文档类型：技术验证事实源；版本：0.9.24；文档状态：Draft  
> Feature：FEAT-APS-001；实施里程碑：IMP-01～IMP-09 Done / IMP-10 InProgress；治理阶段：Stage 7 / InProgress  
> 验证结论：IMP-10 报表/ETA、故障恢复、安全初筛、完整技术 AC 矩阵、固定容量形态有限 Go 和 Windows/JNI 目标运行时已有证据；仅业务 UAT 尚未收口  
> 执行者：Codex；首次执行日期：2026-09-12；最后更新：2026-09-13  
> 上游：[控制页](README.md)、[十阶段实施计划](任务清单.md)、[APS 引擎设计](../../架构/APS排程引擎需求与技术设计.md)、[甘特选型](../../决策/甘特图组件选型决策.md)

## 1. 验证范围和结论边界

本记录证明 IMP-01～IMP-09 的既有技术闭环，并继续记录 IMP-10 的增量事实。2026-09-13 已完成日报、人力产能、订单交期/ETA、CSV/打印、权限审计和机器契约纵切片；固定 1k/5k/20k 本机工程基准在确定性完整 hint 优化后，对独立单阶段合成形态形成有限 Go；单 Worker 遗留状态回收、优雅停机、发布切换故障回滚/重试和数据库逻辑恢复已有本地证据。PRD AC-01～24 与 21 项 P0 APS-AC 的技术回归矩阵均已通过。用户明确 Windows 为目标后，又在固定 JDK 17 下实际完成 OR-Tools JNI、前后端制品、Worker 和 API 启动复验。真实业务库迁移、真实账号/设备联调、复杂业务容量、UAT 或上线就绪仍没有完成证据，因此 IMP-10 保持 `InProgress`。

当前结论为：

- Java 17 后端骨架、依赖方向、API 双开关、Worker 三开关、独立非 Web 入口和 Windows OR-Tools JNI smoke 通过。
- OpenAPI 3.1、JSON Schema 2020-12、16 个正反例、JCS 基准与黄金摘要通过统一契约门禁；Schema 不能代替后续跨对象业务校验。
- 前端严格类型检查、10 个文件 27 项单测及系统 Chrome 17 项 APS 浏览器用例通过；最新 500 资源 / 5,000 分段首屏为 16.0850 秒，继续只判定适配路线“受限 Go”，不判定尚无批准阈值的性能通过。
- V001 与评审 DDL 正文一致且保持不可变；生产向前迁移 V002～V005 分别覆盖受控可中断、结构调整幂等、数量冲正与活动 run carry 身份，V006 为 M26 增加来源计划分配身份和复合外键。独立临时 MySQL 8.0.46 上 migrate/validate/重复 migrate、测试 V007 前进升级、当前 29 表/66 外键、约束反例、UTC 毫秒、事务回滚、乐观锁、权限 SQL 幂等及逻辑备份恢复均通过；V001 静态基线仍为 64 外键。
- APS 数据源、Flyway、API 和 Worker 均默认关闭；缺少独立 APS URL 时失败关闭，不回退到 RuoYi 主库；健康端点匿名可用，能力端点的真实 Spring Security HTTP 验证为无权限 403。
- M01～M05 的 CRUD/启停、单表多技能、绝对时间窗、事务导入、净日历、候选筛选、就绪问题、车间范围和人员导出权限已实现；隔离 MySQL、单测和 Chrome 页面夹具通过。
- M06～M18 的产品/工序/阶段资源要求、版本化路线 DAG、多产品订单、四类生产批、任务/依赖/物料展开、V1/V2 隔离和显式迁移确认已实现；`SAME_START` 保留且阻断发布/释放。
- M01～M18 可编译为稳定 hash 的 SolverInput；独立校验器不依赖 OR-Tools，能复验资源/日历/阶段/依赖/容量/批次/锁/实际占用/水位，并对非法输入失败关闭。
- 基础 CP-SAT 已实现有限资源、净日历、席位、人机阶段、完成依赖、可信开放占用、未来 carry 和交期目标；M19 请求/状态/取消、单 Worker CAS、M20～M23 原子候选、独立复验和 SSE 状态恢复已有实测证据。
- IMP-07 已实现比例/数量门槛、转移批对齐、连续释放尾批、有限产出/物料预约、待检不释放、受控拆批扣减、人工计划固定共享批、影响闭包、受控可中断 RUN 阶段、可信正式基线冻结、非冻结稳定性目标、O-100，以及作业/分段/资源分配三级时间锁、资源锁和全锁；M24 细粒度锁可由当前正式基线映射到重排输入，IMP-07 标记 `Done`。
- IMP-08 已实现 M19～M24 详情/稳定比较、三类计划视图、冲突解释、候选锁创建/删除、时间/资源调整、插单/拆批/固定合批和候选废弃。原候选不变，新 M19 `DRAFT` 携带稳定目标、调整锁及影响闭包；V003 的 `request_fingerprint` 保证生命周期更新 `change_note` 后仍可安全幂等重放。隔离 MySQL 已完成创建、领取、求解、M24 落库和差异复验。
- IMP-08 系统内发布已完成技术纵切片：串行化短事务复验事实水位、输入/候选摘要、当前 M24 与独立硬约束，原子废止旧正式版本并切换唯一 current；发布原因进入 M19，未配置外部接收方时只返回 `NOT_APPLICABLE`。用户已接受该发布语义，IMP-08 标记 `Done`。
- IMP-09 已实现 M25/M26 run 生命周期和实际占用、M27 部分/完工报工及更正、M28 产出批和质量处置、M29 不可变数量事件，以及返工/补产血缘、M13～M16 聚合、服务端执行水位、实际放行供应和现场工作台。同一 M26 按物理 `occupancyId` 聚合；开工只打开首阶段，暂停/恢复只处理当前阶段，换资源保留 `sourcePlanAllocationId`，阶段推进原子打开下一阶段且只有末阶段允许完工。活动 run 的当前阶段形成固定事实段，全部后续连续或受控可中断阶段进入同一 `CARRY` 作业，参加资源/日历/顺序/依赖约束并在最终阶段统一释放；未分配余量形成 `SPLIT`。部分共享 run 或历史占用缺少可唯一还原的成员/分配事实时仍失败关闭。
- IMP-10 已实现日冻结基线、跨日计划/实际计量、多技能人员去重、按 `valid_from/valid_to` 生效的技能潜力与峰值、多席位需求、`UNKNOWN` ETA、CSV/打印、权限审计和报表 OpenAPI；隔离 MySQL 已执行真实 Mapper SQL。Java 17 本机固定容量基准中，1k/5k/20k 独立单阶段形态均在 30 秒求解上限内得到经独立校验的可行候选，当前为有限 Go；复杂业务形态与未经验证的生产硬件未据此判定。Actuator、CycloneDX SBOM、依赖 high 门和敏感日志初筛已完成。Windows 目标运行时的 JNI、制品与进程启动已通过；生产级故障/安全演练与 UAT 仍保留真实缺口，IMP-10 为 `InProgress`。

## 2. 实际环境

| 项目 | 实际值 | 说明 |
| --- | --- | --- |
| 操作系统 | Windows 11 专业版 `10.0.26200`，x86_64 | 用户选定的当前目标运行系统；若正式主机改为 Windows Server 或其他版本须重跑 |
| CPU / 内存 | Intel Core i5-9300H，4 核/8 逻辑处理器，31.8 GiB | 当前目标验证和容量基线；不是尚未选定的其他生产主机 SLA |
| 目标构建 JDK | Microsoft OpenJDK `17.0.18+8-LTS` | Maven 验证命令显式设置 `JAVA_HOME` |
| 工作站默认 JDK | Eclipse Temurin `21.0.10` | 默认 Maven 环境；不改变 APS Java 17 字节码目标 |
| Maven | `3.9.15` | APS Enforcer 要求 `[3.9.0,4.0.0)` |
| Node.js / npm | Node `24.15.0`，npm `11.12.1` | 当前 Windows 目标的前端构建基线；不是 API/Worker 运行依赖 |
| 前端工具链 | Vite `6.4.3`、TypeScript `5.9.3`、vue-tsc `3.0.8`、Vitest `4.1.11`、Playwright `1.55.1` | 以本轮结束时的 `package.json` / lockfile 为准；同工作区并发更新后已重装复验 |
| 浏览器 | Google Chrome `152.0.7977.83` | Playwright 使用系统 Chrome；未下载捆绑 Chromium |
| Python | `3.12.4` | 契约工具链另锁四个精确依赖版本 |
| MySQL 工具 | MySQL Community Server `8.0.46` | 只启动临时、隐藏、随机本地端口的隔离实例；系统 `MySQL80` 服务始终停止，验证后进程和临时目录已清理 |
| Docker | 未安装，`docker` 命令不存在 | 当前 Windows 本地进程方案不需要 Docker，不是缺口 |
| WSL | `wsl.exe` 存在，但 Linux 子系统未安装 | 当前不安装、不使用；未来改变目标平台时另行验证 |

## 3. 技术验证证据

| EV | 关联目标 | 动作与工作目录 | 预期 | 实际结果 | 结果 | 限制 |
| --- | --- | --- | --- | --- | --- | --- |
| EV-IMP01-01 | Java 17、八模块、依赖守卫、JNI | 在 `platform-backend/` 显式使用 Microsoft JDK 17 运行 `mvn -f aps/pom.xml clean test --no-transfer-progress` | APS 聚合与八模块全部成功；安全门和最小原生模型通过 | 9/9 reactor 项目 `SUCCESS`；共 9 个测试通过：OR-Tools 1、API 4、Worker 4；OR-Tools `9.15.6755` 最小模型返回 `OPTIMAL` 且变量值为 1 | Pass | 当时只证明 Windows x86_64 JNI；完整目标运行时由 EV-IMP10-12 补齐 |
| EV-IMP01-02 | 共享平台装配兼容 | 在 `platform-backend/` 用 JDK 17 运行 `mvn -DskipTests compile --no-transfer-progress` | 既有模块与 APS 共同编译 | 当前共享后端 19/19 reactor 项目 `SUCCESS`，总耗时 34.332 秒 | Pass | 跳过全仓测试；不证明运行时业务联调 |
| EV-IMP01-03 | 独立、默认关闭 Worker | 运行 `mvn -f aps/pom.xml -pl aps-worker -am package -DskipTests`，随后执行 `java -jar aps/aps-worker/target/aps-worker-3.9.2.jar --spring.main.banner-mode=off` | 可打包；默认配置不启动 HTTP、轮询或求解，进程可正常退出 | 8/8 所需 reactor 项目成功；Worker 用 Java 17 启动，Spring 上下文约 8.432 秒完成，退出码 0；代码中尚未注册轮询器/求解 runner | Pass | 启动日志不是业务处理证据；未启用任何 APS 开关 |
| EV-IMP01-04 | 机器契约与失败关闭 | 在仓库根运行 `python contracts/aps/tools/verify_contracts.py` | 工具版本精确匹配；Schema、正反例、OpenAPI 相对引用和 JCS/hash 全部通过 | 4 个 JCS 向量、5 个黄金摘要、8 个合法实例、8 个非法实例、4 个 Schema、OpenAPI 相对 `$ref` 与跨契约不变量全部通过；退出码 0 | Pass | Java JCS 交叉实现已由 `aps-solver-contract` 覆盖；Java/TS 代码生成尚未执行，跨数组业务约束留给独立校验器 |
| EV-IMP01-05 | APS 增量 TypeScript 与组件单测 | 当前 lockfile 下先运行 `npm ci`，再在 `admin-web/` 运行 `npm run test:aps` | 锁文件可重装；APS TypeScript 无错误；映射、意图、回滚和生命周期通过 | `npm ci` 退出码 0，安装 621 个包；`vue-tsc` 通过；Vitest 4.1.11 的 4 个文件、10 个测试全部通过，总耗时 16.26 秒 | Pass | jsdom/假运行时不能证明真实浏览器渲染；安装时保留上游 deprecated 警告 |
| EV-IMP01-06 | DHTMLX/vis 真实组件兼容 | 在 `admin-web/` 运行 `npm run test:e2e:aps`，使用系统 Chrome | 同一 500/5,000 合成模型完成首次渲染、销毁清空、重建、再次渲染和二次销毁；无 console/page error | 当前 lockfile 重装后：DHTMLX 546.1 ms、vis 6,206.0 ms、双帧 ready 8,982.0 ms，用例约 1.5 分钟、整次约 2.7 分钟，1/1 通过；三次收口复核的 ready 范围为 4,906.3～11,575.0 ms，均无 console/page error | Pass（技术场景） | 没有固定性能门；同机波动使性能结论保持受限 Go；未覆盖正式业务交互、键盘、窄屏和弱网 |
| EV-IMP01-07 | 管理端生产构建 | 在当前 lockfile 下于 `admin-web/` 运行 `npm run build:prod -- --logLevel error` | 当前管理端与新增精确依赖可生产构建 | Vite 6.4.3 退出码 0 | Pass | 生产构建不证明页面已接入路由；`dist/` 是可重建输出 |
| EV-IMP01-08 | 依赖锁定与供应链初筛 | 运行 `npm ls` 核对六个直接工具/组件版本；分别运行生产依赖审计和包含 dev 的全量 `npm audit --registry=https://registry.npmjs.org` | 两个 APS 组件精确锁定；如实区分新增生产依赖与共享前端既有风险 | 当时六个版本与结构门基线一致；生产依赖仅报告 ECharts 一项 moderate；当时全量树报告 6 high、3 moderate | Pass（APS 生产依赖初筛）/ Fail（当时共享全量树） | 这是 IMP-01 历史快照；当前 lockfile 的复验已由 EV-IMP10-07 取代，目前无 high，不能继续用历史结果声称当前有 6 个 high |
| EV-IMP01-09 | IMP-01 结构与批准边界 | 在仓库根运行 `node scripts/aps/check-imp01-structure.cjs` | 模块、配置、契约工具、前端、Feature 状态和禁止边界全部存在且一致 | 29/29 检查通过，退出码 0 | Pass | 结构门不执行 Maven、Schema 或浏览器运行验证，必须与本表其他 EV 联读 |
| EV-IMP01-10 | 29 表设计基线未漂移 | 在仓库根运行 `node scripts/check-aps-database-design.cjs --no-write` | 29 表、无工厂表、单技能表和 `SAME_START` 阻断边界不变 | 17/17 检查通过；29 表、477 列、64 外键、268 个命名约束；退出码 0 | Pass | 静态 DDL 检查不表示 migration 或真实 MySQL 执行 |
| EV-IMP01-11 | 文档结构和本地链接 | 在仓库根运行全仓 `scripts/check-docs.ps1`，并对生产排产项目执行同口径定向 Markdown 相对链接检查 | 当前入口、中文命名和相对链接通过 | 定向检查通过；最终全仓复跑检查 235 个 Markdown、19 个 HTML，退出码 0 | Pass | 首次全仓运行曾因智能体桌面端 Feature 缺链接目标失败；该并发文档产物随后出现，最终复跑已通过。脚本不代替语义唯一性人工审查 |
| EV-IMP02-01 | V001、独立数据源、约束、事务与升级 | 在仓库根运行 `powershell -ExecutionPolicy Bypass -File scripts/aps/run-local-mysql-migration.ps1` | 不启动系统服务，在临时隔离 MySQL 上完成 migration 及正反例 | MySQL 8.0.46 上 V001 migrate/validate/重复 migrate、测试专用 V002 前进升级、29 表/64 外键、InnoDB、UTC 毫秒、唯一键/CHECK/外键拒绝、过期 `row_version` 写入失败、事务回滚全部通过 | Pass | 使用本机独立临时实例，不是 Testcontainers，也不是生产数据库 |
| EV-IMP02-02 | 权限 SQL 与无权限拒绝 | 同一隔离实例执行 `aps-permissions.sql` 两次；运行 APS Maven 测试 | 权限脚本幂等且不默认授予角色；未授权请求被拒绝 | 两次执行后恰为 29 个唯一 `aps:*` 权限码且无默认角色授权；MockMvc + Spring Security 验证健康端点匿名 200、已认证无权限能力端点 403 | Pass | 尚未实现车间范围及人员导出权限，它们属于 IMP-03 |
| EV-IMP02-03 | 备份与恢复演练 | 由本地 migration runner 使用 `mysqldump` 备份并恢复到第二个随机 schema | 结构与夹具可逻辑恢复 | 恢复库得到 29 张业务表和测试夹具；脚本退出后清理源库、恢复库、进程和临时目录 | Pass | 仅证明本机逻辑演练，生产恢复目标/RPO/RTO 尚未确定 |
| EV-IMP02-04 | 静态不可变与边界门 | 在仓库根运行 `node scripts/aps/verify-migration.cjs` | V001 正文、表/外键、权限、禁止边界和默认开关不漂移 | 10/10 检查通过；V001 可执行正文与评审 DDL 对应正文一致，29 表、64 外键、无工厂字段、仅一张技能表 | Pass | 静态门须与 EV-IMP02-01 的真实执行证据联读 |
| EV-IMP02-05 | APS 及共享后端回归 | 运行 `mvn -f platform-backend/aps/pom.xml test --no-transfer-progress`；运行 `mvn -f platform-backend/pom.xml -DskipTests compile --no-transfer-progress` | APS 单测和共享后端装配不回归 | APS 9/9 reactor 成功；共享后端 19/19 reactor 编译成功 | Pass | 常规 Maven 测试在未设置隔离库环境变量时跳过 DB IT；DB 实测由 EV-IMP02-01 单独提供 |
| EV-IMP03-01 | 资源/技能/日历领域规则 | 运行 APS domain 与 API 定向 Maven 测试 | 净窗口、候选筛选、UTC 输入和权限拒绝可重复验证 | Domain 5 个测试、API 11 个测试通过；三级技能淘汰二级资源，阻断请假覆盖，UTC 偏移输入被拒绝，资源端点无权限 403 且仓储零访问 | Pass | 这里只证明资源候选规则，不声称已经排程 |
| EV-IMP03-02 | M01～M05 真实持久化纵切片 | 运行 `scripts/aps/run-local-mysql-migration.ps1`，由 `ApsBaselineMigrationTest` 在临时 MySQL 调用真实 MyBatis/事务服务 | CRUD、范围、技能、日历、乐观锁和批量回滚通过 | 车间/中心/三名人员写入成功；非负责人车间访问拒绝；两技能人员资源只计一次；请假扣成两个净窗口；冲突批量导入整批回滚；成功更新返回递增 `rowVersion`，过期版本拒绝 | Pass | P0 普通用户范围由 `aps_workshop.manager_user_id` 决定；工作中心继承车间范围，无独立中心授权映射 |
| EV-IMP03-03 | 前端资源和就绪页面 | 在 `admin-web/` 运行 `npm run test:aps`、`npm run build:prod -- --logLevel error` | APS TS/单测和管理端生产构建通过 | 5 个 Vitest 文件、12 个测试通过；生产构建退出码 0 | Pass | 页面路由保持隐藏，权限 SQL不默认授予角色；未连接真实用户和业务库 |
| EV-IMP03-04 | Chrome 页面行为 | 运行 `playwright ... resource-page.spec.ts`，拦截本地 APS API 返回确定夹具 | 资源去重、多技能和日历可见，无 console/page error | 系统 Chrome 用例 1/1 通过，耗时 36.4 秒；首次因文本定位匹配两个可见节点失败，收紧定位后复验通过 | Pass | 使用浏览器 Mock API，只证明页面接线与渲染；后端真实数据证据见 EV-IMP03-02 |
| EV-IMP03-05 | IMP-03 OpenAPI | 运行 `python contracts/aps/tools/verify_contracts.py` | 资源路径加入统一机器契约且原 Solver 契约不回归 | OpenAPI 3.1 相对引用、资源路径、JCS/hash、四个 Schema 和正反例全部通过 | Pass | OpenAPI 不代表后续订单/求解路径已实现 |
| EV-IMP04-01 | 路线 DAG 与工时复算 | 在 `platform-backend/` 运行 `mvn -pl aps/aps-api -am test` | 分支/汇合、断联、循环、自依赖、门槛、资源就绪和终点规则可重复验证 | Domain 11 个测试、API 13 个测试通过；DAG 断联/循环和 `SAME_START` 发布阻断均返回对象级问题 | Pass | 不包含 IMP-05 的完整数据就绪与候选计划校验 |
| EV-IMP04-02 | M06～M18 MySQL 业务纵切片 | 在仓库根运行 `powershell -ExecutionPolicy Bypass -File scripts/aps/run-local-mysql-migration.ps1` | 产品路线、订单展开、幂等、版本冻结、派生批和回滚在真实 MyBatis/事务路径通过 | 临时 MySQL 8.0.46 上双产品订单展开为正常批、任务、显式跨产品依赖及物料需求；拆批/返工/补产均生成冻结任务；重复导入/展开不重复；门槛超量整批回滚；V2 不改变 V1 任务；29 表权限与备份恢复继续通过 | Pass | 是本机隔离库，不是生产 migration 或真实 ERP 联调 |
| EV-IMP04-03 | 路线与订单 REST/权限/契约 | 运行 API 测试和 `python contracts/aps/tools/verify_contracts.py` | IMP-04 路径、对象级错误和权限拒绝与机器契约一致 | 无权限路线/订单请求在仓储前返回 403；OpenAPI IMP-04 路径白名单、四个 Schema、当前 16 个正反例和 JCS/hash 全部通过；`ROUTING_ORDER=AVAILABLE` | Pass | Solver 相关能力仍为 `CONTRACT_ONLY` 或 `DISABLED` |
| EV-IMP04-04 | 管理端路线与订单页面 | 在 `admin-web/` 运行 `npm run test:aps` 和 `npm run build:prod -- --logLevel error` | TypeScript、模型单测和生产构建通过 | `vue-tsc` 通过；7 个 Vitest 文件/16 个用例通过；Vite 生产构建退出码 0 | Pass | 页面使用本地 API 接线；未做真实账号端到端联调 |
| EV-IMP04-05 | Chrome 页面行为 | 运行 `npm run test:e2e:aps -- tests/e2e/aps/manufacturing-pages.spec.ts` | 路线 DAG、显式依赖、多产品行和冻结任务网络可见且无页面错误 | 系统 Chrome 用例 2/2 通过，耗时 30.1 秒；console/page error 为空 | Pass | 浏览器 API 为确定夹具；后端真实持久化证据见 EV-IMP04-02 |
| EV-IMP05-01 | 求解契约与独立校验器 | 在 `platform-backend/` 运行 `mvn -pl aps/aps-api -am test`，并定向复跑 validator | Java DTO/JCS 对齐 Node 基准；校验器不依赖 OR-Tools并拒绝硬约束破坏 | Domain 11、Solver Contract 5、Constraint Validator 12、API 16 个测试通过；三份输入和候选摘要跨实现一致；跨午休、席位、互斥/累计容量、阶段/依赖、数量、共享批、锁、实际占用、水位和 `SAME_START` 均独立复验 | Pass | 本阶段只校验候选，不生成候选 |
| EV-IMP05-02 | M01～M18 到 SolverInput 编译 | 运行 `scripts/aps/run-local-mysql-migration.ps1` 的真实 MyBatis 纵切片 | 已释放多产品订单形成排序稳定、带 hash 且就绪 PASS 的输入 | MySQL 8.0.46 中编译 9 个冻结任务及资源净窗口，64 位 inputHash 可复算；migration、29 权限、备份恢复继续通过，退出码 0 | Pass | 本机隔离库，不是生产库；尚未写 M19～M24 候选 |
| EV-IMP05-03 | 校验 REST、黄金集与 O-100 就绪 | 调用 Controller/MockMvc 测试并运行 `python contracts/aps/tools/verify_contracts.py` | 无权限 403、结构错误失败关闭；固定黄金和 O-100 规则可重复 | `/validations` 的权限/非法 JSON/`SAME_START` 用例通过；固定 20 任务候选复验可发布；O-100 人机阶段、50 件门槛、20 件转移批和 A/B 同炉 200 容量输入就绪 PASS；统一契约门退出码 0 | Pass | O-100 的 CP-SAT 生成与最终时间结果属于 IMP-06/07 |
| EV-IMP06-01 | CP-SAT 基础有限产能与三层结果 | 在 `platform-backend/` 运行 `mvn -pl aps/aps-worker -am test` | 净日历、资源、席位、人机阶段、依赖、目标、取消和结果映射可重复 | Domain 11、Solver Contract 6、Validator 13、OR-Tools 9、Worker 5 个测试通过；覆盖设备/人员不重叠、自动运行释放人员、软交期最早可行、延期优先、未来 carry、可信占用、取消和证明无解 | Pass | 数量释放、共享批和可中断求解仍属于 IMP-07 |
| EV-IMP06-02 | M19～M23 真实 MySQL 候选链路 | 运行 `scripts/aps/run-local-mysql-migration.ps1` | 请求幂等、CAS 领取、取消、真实求解、原子保存与故障回滚通过 | 临时 MySQL 8.0.46 中同请求复用、异输入冲突、DRAFT→SOLVING→CANCELLED、真实 CP-SAT FEASIBLE 候选及 M20～M23 各一条链路通过；候选资源外键故障后 M20～M23 全部为 0 且 M19 保持 SOLVING；29 权限和备份恢复继续通过 | Pass | 本机隔离库；P0 明确不验证双 Worker fencing，也未连接正式计划指针 |
| EV-IMP06-03 | 计划 REST/SSE 与权限 | 运行 `mvn -pl aps/aps-api -am test` 及定向 SSE 测试 | 创建、状态、取消、SSE 权限隔离，轮询终态后停止 | API 19 个测试通过；无权限用户在服务访问前得到 403；SSE 从 MySQL 最终状态轮询并在 FEASIBLE 终态停止；OpenAPI 覆盖四个计划请求端点 | Pass | SSE 是进度提示，断线恢复仍以普通状态查询为准；未做真实账号浏览器联调 |
| EV-IMP06-04 | 主应用装配与统一契约 | 运行 `mvn -pl ruoyi-admin -am -DskipTests compile` 和 `python contracts/aps/tools/verify_contracts.py` | APS API/MySQL 适配器能随主应用编译，机器契约不漂移 | `ruoyi-admin -am` 18/18 reactor 模块 `SUCCESS`；JCS/hash、四个 Schema、当前 16 个正反例、OpenAPI 相对引用与路径白名单全部通过 | Pass | 编译不等于 API/Worker 已连接真实站点；`SOLVER` 能力因此仍报告 `DISABLED/SERVICE_UNAVAILABLE` |
| EV-IMP07-01 | 数量/物料释放与独立复验 | 运行 `mvn -pl aps/aps-solver-ortools -am test` | 比例/数量门槛、转移批、尾批、有限消耗、待检不释放和外部物料就绪由求解器与独立校验器一致执行 | Solver Contract 11、Validator 14、OR-Tools 21 个测试通过；覆盖 100 件按 20 件连续释放、50% 对齐到 60、双消费者等待 20/60、待检供给排除和累计合格供给就绪 | Pass | 数量/物料子项与 O-100 求解结果通过；执行数量账仍属于 IMP-09 |
| EV-IMP07-02 | 拆批数量归属与真实 MySQL 回归 | 运行 `scripts/aps/run-local-mysql-migration.ps1` | 拆批是父子间数量转移，父任务工时同步复算，原候选链路不回归 | MySQL 8.0.46 中父批 10 拆 2 后父批/父任务均为 8，父子合计仍为 10；真实求解、M19～M23 原子写入/回滚、29 权限及 29 表备份恢复继续通过 | Pass | 复杂跨批前置和目标物料拆分当前失败关闭，不宣称任意拆分 |
| EV-IMP07-03 | 固定共享批、作业锁与成员落库 | 运行 OR-Tools 定向测试及 `MybatisPlanRepositoryTest` | A60+B40 固定周期只占一次设备，成员量/容量/兼容键可追；时间/资源/全锁不被静默放宽 | A60+B40 形成 1 个 `SHARED_BATCH`、1 次设备分配和 100 总释放；M21 捕获值为 A60/B40；全锁固定 02:00～03:00 且选择指定设备；不支持的锁类型失败关闭 | Pass | 人工批配置的 API/校验/求解/落库已接入；可视化配置并入 IMP-08 工作台 |
| EV-IMP07-04 | 影响闭包与机器契约 | 运行 `ImpactClosureServiceTest` 和 `python contracts/aps/tools/verify_contracts.py` | 局部变化沿依赖/物料/批次/订单/资源完整扩散，计划请求共享批字段进入 OpenAPI 且旧契约不回归 | Application 2 个闭包测试通过；未知种子失败关闭；统一 JCS/hash、Schema 正反例、OpenAPI 引用和跨契约不变量全部通过 | Pass | 闭包尚未接入人工计划草稿与正式基线差异流程 |
| EV-IMP07-05 | 受控可中断、向前迁移与暂停资源保留 | 运行 `mvn -pl aps/aps-solver-ortools -am test`、`python contracts/aps/tools/verify_contracts.py` 及 `scripts/aps/run-local-mysql-migration.ps1` | RUN 阶段可跨窗口分段并计续作准备；资源连续/重选及独占资源暂停保留按配置执行；V002 可迁移且契约一致 | Solver Contract 11、Validator 14、OR-Tools 21 个测试通过；3 小时生产跨两个窗口形成 2 个物理段并累计 30 分钟续作准备，`holdOnPause` 阻止其他作业占用首末段之间空档；契约门退出码 0；MySQL 8.0.46 成功应用 V001+V002、重复迁移为零、测试 V003 前进升级、29 表权限与备份恢复通过 | Pass | 可中断+共享批、连续数量释放/内部物料流组合及非独占容量资源暂停保留当前显式拒绝 |
| EV-IMP07-06 | 可信基线冻结、稳定性目标与 O-100 | 运行 API/OR-Tools 全量测试、统一契约门及隔离 MySQL 纵切片 | 基线必须来自当前正式版本；冻结作业精确保持；非冻结作业优先减少扰动；O-100 时间与共享炉/人员约束可复算 | API 20、Solver Contract 11、Validator 14、OR-Tools 21 个测试通过；错误基线 hash 被拒绝，新 M19 保存 `base_version_id`，冻结作业起止与资源集合不变；非冻结作业在不增加延期时优先保持基线；O-100 的 B10 在第 144 分钟启动，A60+B40 共用一次 3 小时炉次，A30/B30 共用人员不重叠；隔离 MySQL 基线读取、重排和候选落库通过 | Pass | 客户端只提交版本/hash/冻结边界；人工计划草稿、差异展示和 UI 属于 IMP-08 |
| EV-IMP07-07 | 三级锁与 M24 基线继承 | 运行 `mvn -pl aps/aps-solver-ortools -am test` 及隔离 MySQL 纵切片 | JOB/SEGMENT/ALLOCATION 的 TIME/RESOURCE/FULL 均按目标粒度执行；当前正式基线锁由服务端可信映射 | Validator 14、OR-Tools 21 个测试通过；单独锁定第二个数量释放段 02:20～02:40 时源作业仍从 02:00 开始；隔离 MySQL 写入并读取 `SEGMENT + TIME` 与 `ALLOCATION + RESOURCE` 两条 M24 锁，重排后时间/资源保持，29 权限和恢复演练继续通过 | Pass | 动态可中断阶段之后无法稳定编号的分段锁失败关闭；人工锁写入 API/UI 在 IMP-08 实现 |
| EV-IMP08-01 | 计划版本详情、基线比较与三视图骨架 | 运行 API 全量测试、`npm run test:aps`、统一契约门及隔离 MySQL 纵切片 | M19～M24 详情可按权限读取；跨版本稳定匹配并明确变化维度；前端形成工序/设备/人员三套领域模型 | API 23 项通过；前端类型检查及当前 8 个文件/21 项单测通过；OpenAPI/JCS/Schema 统一门通过；MySQL 8.0.46 真实读取作业/成员/分段/分配/M24 锁和候选 hash，基线比较得到 1 个未变作业；29 权限与 29 表恢复继续通过 | Pass | 详情、比较和三视图投影由本项证明；后续读写、冲突与浏览器证据见 EV-IMP08-02～05 |
| EV-IMP08-02 | 候选锁与版本化调整 | 运行 API/Application 测试、`npm run test:aps`、统一契约门及 `scripts/aps/run-local-mysql-migration.ps1` | 锁值由服务端推导且受双版本保护；拖动生成新 DRAFT，原候选不变；幂等、求解、锁继承及差异可复验 | API 23 项、Application 8 项、前端 8 文件/21 项均通过；MySQL 8.0.46 中候选锁创建/删除成功，调整请求幂等复用，新 DRAFT 求解为 FEASIBLE，比较得到 1 个 TIME 变化；新候选 M24 使用版本级稳定主键并完整保留继承锁；29 权限与 29 表恢复继续通过 | Pass | 本项证明锁和时间/资源调整；结构调整、放弃候选与浏览器证据见 EV-IMP08-04/05，发布证据见 EV-IMP08-03 |
| EV-IMP08-03 | 系统内原子发布 | 运行 API 全量测试、统一契约门、主应用编译及 `scripts/aps/run-local-mysql-migration.ps1` | 只发布新鲜且独立复验通过的 FEASIBLE；唯一 current 原子切换；重复发布安全；旧基线不能覆盖新正式版本 | API 23 项通过；OpenAPI 机器契约和 18 模块主应用编译通过；MySQL 8.0.46 中首版发布、幂等重放、重排版替换均成功，旧版变为 SUPERSEDED，新版成为唯一 current，旧基线候选返回过期冲突；发布原因写入 M19，外发状态为 NOT_APPLICABLE；29 权限与 29 表恢复继续通过 | Pass（技术纵切片） | 事实新鲜度当前按单站点全局 M01～M18/M25～M29 更新时间保守阻断；系统 Chrome 验收见 EV-IMP08-05；用户发布语义复核、真实账号联调和生产并发压测仍未完成 |
| EV-IMP08-04 | 插单、拆批、固定合批与候选废弃 | 运行 API/Application/OR-Tools 全量测试、统一契约门和 `scripts/aps/run-local-mysql-migration.ps1` | 结构变化基于当前正式版本形成新 DRAFT；原正式版本不变；数量守恒、共享批、已开工拒绝和幂等重放可复验 | API 23、Application 8、OR-Tools 21 项通过；MySQL 8.0.46 中已释放订单插入后求解为 2 个任务，拆批 2→1.5+0.5 且重放不重复扣减，两个任务形成 1 个 `SHARED_BATCH` 且只产生 1 次共享资源分配；V003 指纹与 `change_note` 分离，候选放弃改写备注后同意图仍幂等复用，V004 前进探针通过；候选废弃保留历史 | Pass | 当前是固定人工合批，不做全局自动最优组批；跨批依赖/物料拆分继续失败关闭 |
| EV-IMP08-05 | 正式工作台浏览器与规模基线 | 运行 `npx playwright test --config playwright.aps.config.js` 和 `npm run test:aps` | 三视图、冲突、版本命令、空/加载/错误恢复、窄屏和键盘可用；记录 500/5,000 固定环境性能 | 系统 Chrome Playwright 9/9 通过；Vue 类型检查及 Vitest 8 文件/21 项通过；503 后第二次普通查询恢复；390×844 主控件不越界且键盘切到设备甘特；DHTMLX 769.8ms、vis 11983.1ms、首屏 15232.2ms，销毁/重建通过 | Pass（功能）/ 性能仅记录 | API 使用确定夹具；真实持久化见 EV-IMP08-04。没有批准的交互阈值，不能据该时长声称性能达标 |
| EV-IMP09-01 | M25～M29 执行、质量与数量纵切片 | 运行 APS API/OR-Tools Maven 测试与 `scripts/aps/run-local-mysql-migration.ps1` | run 状态、实际占用、报工、质量、数量移动和冲正均走权限、幂等、乐观锁与同事务边界 | JDK 17 下 API 25、Domain 19、Solver Contract 11、Validator 15、Application 11、OR-Tools 25 项测试通过；最终隔离 MySQL 脚本退出码 0，Infrastructure 8 项通过；部分报工、完成、预留、取消预留、投入、转序、预留后报废及更正均有真实 M25～M29 记录 | Pass（列明纵切片） | 是本机临时 MySQL，不是生产 migration 或真实 MES/QMS 联调 |
| EV-IMP09-02 | V004～V006、故障回滚与数量守恒 | 运行完整本地 MySQL migration/restore runner | V001 不改；V004 允许精确冲正 `PRODUCE`；V005 约束 carry 作业身份；V006 约束 M26 来源分配；链路中断不留半套 M27/M28/M29；权限与恢复不回归 | V001～V006 迁移和测试 V007 探针通过，当前 schema 为 29 表/66 外键；报工链故障注入后 M27/M28 均为空；预留量报废先追加 `UNRESERVE` 再记批次级 `SCRAP`；换资源后的活动 M26 保留非空来源计划分配；两次权限 SQL 后仍为 29 行；29 表夹具逻辑恢复成功 | Pass | 只证明当前固定测试数据和事务边界，不代替生产备份/回退演练 |
| EV-IMP09-03 | 现场执行页面与并发恢复 | 在 `admin-web/` 运行 `npm run test:aps`、`npm run test:e2e:aps` 和生产构建 | 页面覆盖 run 动作、阶段推进、占用换人换机、报工/更正、质量和数量移动；409 后保留输入并刷新事实 | `vue-tsc` 通过；Vitest 9 文件/24 项通过；系统 Chrome APS 全量 13/13 通过；Vite 生产构建沿用本里程碑已通过证据；空态、创建、状态动作、幂等阶段推进、质量/数量操作和 409 恢复均无 console/page error | Pass（浏览器夹具） | API 使用确定夹具；后端真实持久化证据见 EV-IMP09-01/02 |
| EV-IMP09-04 | 真实执行事实驱动重排 | 在隔离 MySQL 中把活动 M26、M28 放行量和服务端 `executionRevision` 编入 `SolverInput`，再走重排请求、Worker 领取、求解及候选保存；补充多阶段求解黄金用例 | 实际段、可信 carry 和未分配量不重不漏；未知释放阻塞到 horizon；当前事实固定、后续阶段求解且不回写 M26 假结束 | M26 开放占用、可信/未知释放口径、M28 已放行自由供应和服务端水位已进入输入；同一物理占用按 ID 聚合。可信 run 生成一个 `CARRY`，当前阶段固定，后续连续或受控可中断阶段进入同一作业；可中断用例跨两个窗口并计恢复准备，最终阶段释放全部在制剩余量，下游等待 carry 完成。余量生成 `SPLIT`；V006 后换资源仍保留可信 requirement/seat/capacity；真实 MySQL 部分报工场景可原子写入并读回 M20～M23 | Pass | 部分共享或历史 M26 缺少唯一成员/分配事实时按设计保持 `UNKNOWN`；真实站点联调属于 IMP-10 |

### 3.10 IMP-10 当前增量证据

| EV | 关联目标 | 动作 | 实际结果 | 结果 | 限制 |
| --- | --- | --- | --- | --- | --- |
| EV-IMP10-01 | AC-20～22、REQ-08/09 | Application/API 模块测试；报表服务固定时钟样例 | 日冻结分母与跨日裁剪、共享批物理人机时分摊、多技能人员总产能去重、技能资格有效期、多 PERSON 席位峰值、任一必需任务未知或权限范围只覆盖部分订单行时 ETA `UNKNOWN` 均通过；Application 17、API 27 项通过 | Pass | 这是确定夹具，不是业务 UAT |
| EV-IMP10-02 | 页面、CSV、错误与空态 | `npm run test:aps` 及 APS 全量 Playwright 复验 | TypeScript 与 10 文件/27 项单测通过；17/17 Chrome 用例通过，其中报表 4 项覆盖冻结/当前对比、人员去重/峰值、UNKNOWN 原因和 CSV 下载 | Pass | 页面接口使用路由夹具；真实 SQL 见下一项 |
| EV-IMP10-03 | MySQL 查询、冻结基线、权限和恢复 | `scripts/aps/run-local-mysql-migration.ps1` | 临时 MySQL 8.0.46 中真实执行三类报表 Mapper，人员技能按报表时间窗读取 `valid_from/valid_to`；首个当日发布版本保持冻结分母；V001～V006、测试 V007、权限 SQL 两次执行 29 行、29 表逻辑备份恢复全部通过 | Pass | 首轮暴露路线边草稿列名并失败，修正为 `predecessor_node_id` 后复验通过；不是生产库 |
| EV-IMP10-04 | REST 机器契约 | `python contracts/aps/tools/verify_contracts.py` | 6 个报表查询/CSV 路径及响应 Schema 纳入 OpenAPI；统一契约、JCS 与全部正反例通过 | Pass | 不等于代码生成或外部消费者联调 |
| EV-IMP10-05 | 1k/5k/20k 固定容量工程基准 | JDK 17 运行 `scripts/aps/run-capacity-benchmark.ps1 -Profile full`；100 个独占资源、30 天、固定种子、单线程、30 秒求解上限；测试断言每组必须有候选 | 初始模型 1k 首解 31.538 秒，5k/20k 无首解；加入只对无基线/锁/依赖/物料/实际占用/共享批/可中断的单阶段模型启用的完整确定性贪心 hint，并同步所有选择/时间/延期变量后，最终同 JVM 顺序运行：1k 总耗时 7.202 秒/首解 2.039 秒/73.8 MiB，5k 4.178 秒/2.901 秒/97.4 MiB，20k 13.922 秒/11.399 秒/578.1 MiB；三组均为 `FEASIBLE + COMPLETED`，目标/bound 分别为 11024/0、255054/0、4020066/0 | Pass / 有限 Go（固定形态） | hint 不改变硬约束，CP-SAT 仍求解且候选仍过独立校验；首解模式不声称最优。结果不外推为复杂业务形态或目标硬件 SLA；同 JVM 有原生库/JIT 预热，不能按三组总耗时的大小关系解释线性伸缩 |
| EV-IMP10-06 | Actuator、主工程装配与 SBOM | 为 `ruoyi-admin` 接入 Actuator；运行 18 模块主工程编译；启用 `aps-sbom` profile 生成 CycloneDX | 18/18 reactor 编译成功；CycloneDX 1.6 XML/JSON 均校验写出，聚合 144 个组件；APS 9/9 package 成功 | Pass | 当前只接入 Spring Boot 默认健康/度量基础，不代表已接生产 Prometheus/OpenTelemetry；SBOM 位于可重建 `target/` |
| EV-IMP10-07 | 依赖与敏感日志初筛 | 对生产依赖及全量 npm 树执行 `audit-level=high`；对 APS Java/前端源码执行敏感字样扫描 | 两个 npm high 门均退出码 0，仅余共享 ECharts 1 项 moderate；扫描命中测试环境变量、数据源 password setter 和 `PreAuthorize` 局部变量，未发现硬编码密钥/令牌 | Pass（high 门与静态初筛） | ECharts 修复要求破坏性升级至 6.1.0，需单独兼容评估；静态扫描不能代替 SAST/DAST 和人工安全审查 |
| EV-IMP10-08 | Java/前端/浏览器最终回归 | JDK 17 运行 APS 9 模块全量测试；运行 `npm run test:aps` 与 `npm run test:e2e:aps` | APS reactor 9/9 成功，131 项执行通过、2 项按设计跳过；前端 27/27、Chrome 17/17 通过；`ruoyi-admin` 含 Actuator 的 18 模块编译成功 | Pass | 常规 Maven 命令跳过容量基准和需隔离库变量的 MySQL IT；二者分别见 EV-IMP10-05/03；仍未执行真实角色 UAT |
| EV-IMP10-09 | Worker/发布故障恢复与开关回退 | JDK 17 运行 `aps-worker -am test`；隔离 MySQL 注入候选写入失败、遗留 `SOLVING` 和发布切换触发器故障 | Worker 在领取事务中按 5 分钟默认窗口回收遗留求解为 `FAILED/UNKNOWN` 并记录 `STALE_SOLVING_RECOVERED`；停机信号阻止新领取并通过取消探针结束在途求解；候选部分写入全回滚；发布切换失败后旧版仍是唯一 current、候选仍为 FEASIBLE，移除故障后同一候选发布成功；29 表恢复不回归 | Pass（本地技术故障注入） | Worker 7 项、Infrastructure 9 项及真实 MySQL runner 通过；不是生产进程杀停、网络分区或生产 RTO 演练 |
| EV-IMP10-10 | AC-03、AC-19、APS-AC-03/20 精确组合 | Domain、Application、OR-Tools 定向与九模块全量回归 | 并行支路 10:00/11:00 放行加 30 分钟搬运后严格在 11:30 汇合；人员班次仍存在时，物化的车间关闭窗口使资源不可排，批准的加班绝对窗口才恢复可排；O-100 在 9/14 08:00 开始、10:00 释放 B 门槛、13:00～16:00 合批炉次、A 17:00 完成、B 9/15 09:00 完成，F1 3 机时、王工 1 人时；A 完成时订单未完成，B 完成后生产完成，订单状态完成后才关闭 | Pass | Domain 20、Application 17、OR-Tools 25 项通过；是确定技术场景，不是用户 UAT |
| EV-IMP10-11 | UAT 执行包准备 | 审查 `验收记录.md`，运行文档/结构门 | UAT 包覆盖 AC-01～24 和 21 项 P0 APS-AC，记录环境、角色、数据、证据、缺陷和三选一签署；签署字段不能由 Agent 填写 | Prepared / NotExecuted | 技术执行包已准备，但真实 UAT 尚未执行 |
| EV-IMP10-12 | Windows 目标运行时、JNI、制品和进程启动 | 用 Microsoft JDK 17 执行 `scripts/aps/verify-windows-runtime.ps1`；脚本采集不可变基线，运行 JNI 测试、后端/前端打包、jar 清单、SHA-256、Worker/API smoke，并自建随机端口临时 MySQL | Windows 11 专业版 `10.0.26200` x64、i5-9300H 4 核/8 线程、31.8 GiB、JDK `17.0.18`、Maven `3.9.15`、Node `24.15.0`、npm `11.12.1`；OR-Tools JNI 1/1，后端与前端 package、两个 jar 清单、默认关闭 Worker `Started ApsWorkerApplication`、启用 APS API 壳的 `Started RuoYiApplication` 与 `/v3/api-docs` HTTP 200 全部 Pass。Worker jar SHA-256 为 `95E1C77DD1A920562D768C1496DAFFA0F1139D1E3C921590B6235465A7238633`，API jar 为 `FA752C13B9BCDBE5E49F0DD6F356282B01C1F74EBAE98723B0FCB3B1716F9EA3`；脚本退出码 0 | Pass（目标运行时技术验证） | API 使用 RuoYi 隔离基线、随机端口且 APS persistence 关闭；系统 `MySQL80` 全程保持停止，临时 MySQL 和 Java 进程已清理。这证明当前 Windows 目标技术启动，不等于正式服务安装、真实数据库迁移或 UAT |

### 3.11 PRD AC-01～24 技术回归矩阵

本表只判定自动化与本地隔离环境的技术覆盖，不代替第 9 项业务 UAT。当前汇总为 **24 Pass / 0 Partial / 0 NotRun**。

| AC | 技术结果 | 主要证据 | 尚缺或边界 |
| --- | --- | --- | --- |
| AC-01 | Pass | EV-IMP04-02/03 | 双产品独立展开、来源和单位可追；真实 ERP 联调留 UAT |
| AC-02 | Pass | EV-IMP04-01/02 | V1 冻结、V2 差异确认、缺项/循环失败关闭 |
| AC-03 | Pass | EV-IMP04-01、EV-IMP05-01、EV-IMP07-04、EV-IMP10-10 | 分支汇合严格复算 10:00/11:00 放行加 30 分钟搬运后的 11:30；非法提前由独立约束复验拒绝 |
| AC-04 | Pass | EV-IMP07-01、EV-IMP09-01 | 50% 向上对齐至 51/60 的契约数学、待检不释放和实际合格释放已覆盖 |
| AC-05 | Pass | EV-IMP07-01 | 20 件连续释放、双消费者等待和尾批守恒 |
| AC-06 | Pass | EV-IMP07-02/03、EV-IMP08-04 | A60+B40 共享炉、容量/兼容阻断、成员追溯和拆批守恒 |
| AC-07 | Pass | EV-IMP04-01、EV-IMP05-01/03 | `SAME_START` 保留来源、明确拒绝并阻断发布；移除后按普通规则排程 |
| AC-08 | Pass | EV-IMP03-01/02、EV-IMP06-01 | 技能等级、资格有效期、请假和合法窗口 |
| AC-09 | Pass | EV-IMP06-01、EV-IMP07-06 | 设备全阶段、人员到场阶段及自动运行释放人员 |
| AC-10 | Pass | EV-IMP06-01、EV-IMP10-01 | 同一人员不重叠、两席位不重复充数、累计产能按人员去重 |
| AC-11 | Pass | EV-IMP07-05 | 不可中断连续窗口与受控可中断分段、续作准备、暂停资源保留 |
| AC-12 | Pass | EV-IMP06-01、EV-IMP07-06 | 设备/人员有限容量、正排/倒排软交期、过去与不可用窗口拒绝、正式计划保护 |
| AC-13 | Pass | EV-IMP07-04、EV-IMP08-02/05 | 跨订单/物料/共享批/资源影响闭包、新候选与取消保持基线 |
| AC-14 | Pass | EV-IMP07-07、EV-IMP08-02/03 | TIME/RESOURCE/FULL 三级锁继承；锁与现实冲突由独立复验阻断发布 |
| AC-15 | Pass | EV-IMP08-03、EV-IMP10-09 | 旧基线拒绝、幂等发布、切换中数据库故障全回滚及移除故障后重试 |
| AC-16 | Pass | EV-IMP08-01/05 | 三类甘特稳定身份、设备全段、人员到场段和工序汇总 |
| AC-17 | Pass | EV-IMP09-01/02/03 | 报工幂等、80 合格/20 待检、换资源、乐观冲突与更正留痕 |
| AC-18 | Pass | EV-IMP09-01/02/04 | 隔离后的返工/报废/补产血缘、负荷回流及数量不重计 |
| AC-19 | Pass | EV-IMP09-01/04、EV-IMP10-01/10 | O-100 精确得到 2026-09-15 09:00；A 完成时订单未完成，B 完成后生产完成，订单状态完成后才关闭 |
| AC-20 | Pass | EV-IMP10-01/02/03 | 跨日裁剪、释放日归属、冻结原日分母、页面与真实 SQL |
| AC-21 | Pass | EV-IMP10-01/02/03 | 多技能人员去重、8 小时累计、下午双席位峰值缺口和资格有效期 |
| AC-22 | Pass | EV-IMP10-01/02 | 任一必需任务未排或范围不完整时 ETA 为 `UNKNOWN` 并保留原因/已知下界 |
| AC-23 | Pass | EV-IMP06-01/02/03、EV-IMP08-03、EV-IMP10-05/09 | 取消、超时有解/无解、输入过期、SSE 后备查询、在途停机和正式计划保护 |
| AC-24 | Pass（技术） | EV-IMP02-02、EV-IMP03-02、EV-IMP06-03、EV-IMP08-02/03、EV-IMP09-01/03、EV-IMP10-01/02/07 | 权限/范围、来源幂等、409 恢复、导出一致与审计已自动验证；真实角色与真实同步中断留 UAT |

### 3.12 APS-AC 技术回归矩阵

当前 23 项中 APS-AC-09、APS-AC-22 为已批准 P1，不计 P0 完成率；其余 21 项汇总为 **21 Pass / 0 Partial**。

| APS-AC | 范围/结果 | 主要证据 | 尚缺或边界 |
| --- | --- | --- | --- |
| APS-AC-01 | P0 / Pass | EV-IMP04-02、EV-IMP06-01 | 多产品只因真实依赖或资源冲突等待 |
| APS-AC-02 | P0 / Pass | EV-IMP03-01/02、EV-IMP06-01 | 技能等级与请假窗口失败关闭 |
| APS-AC-03 | P0 / Pass | EV-IMP03-01/02、EV-IMP10-10 | 人员原班次仍存在时，物化的车间关闭窗口覆盖可用性；批准加班窗口后才恢复可排 |
| APS-AC-04 | P0 / Pass | EV-IMP06-01、EV-IMP10-01 | 同一多技能人员不重叠且全站累计只计一次 |
| APS-AC-05 | P0 / Pass | EV-IMP06-01、EV-IMP07-06 | 设备 3 小时、人员 1 小时的人机阶段样例 |
| APS-AC-06 | P0 / Pass | EV-IMP07-05 | 连续窗口失败与可中断恢复准备 |
| APS-AC-07 | P0 / Pass | EV-IMP07-01 | 每 20 件释放、消费与等待守恒 |
| APS-AC-08 | P0 / Pass | EV-IMP07-03、EV-IMP08-04 | 共享炉只占一次，不兼容/超量阻断 |
| APS-AC-09 | P1 / ScopeOut | 引擎设计第 12 节 | 人员速率差异未进入 P0 数据模型，不计当前完成率 |
| APS-AC-10 | P0 / Pass | EV-IMP06-01、EV-IMP07-06 | 不可能交期保持资源可行并报告延期目标 |
| APS-AC-11 | P0 / Pass | EV-IMP07-07、EV-IMP08-03 | 三类锁与维修/事实冲突发布阻断 |
| APS-AC-12 | P0 / Pass | EV-IMP07-04、EV-IMP08-02/05 | 甘特意图产生完整影响闭包和新候选 |
| APS-AC-13 | P0 / Pass | EV-IMP05-01、EV-IMP08-03 | 事实水位变化返回 `STALE_INPUT`，不覆盖执行事实 |
| APS-AC-14 | P0 / Pass | EV-IMP06-01、EV-IMP10-05 | 时限有解映射 FEASIBLE / TIMEOUT_WITH_SOLUTION，保留目标与 bound |
| APS-AC-15 | P0 / Pass | EV-IMP06-01、EV-IMP10-05 | TIMEOUT_NO_SOLUTION 与 INFEASIBLE_PROVEN 区分 |
| APS-AC-16 | P0 / Pass | EV-IMP06-02、EV-IMP08-02/04 | 同幂等键同参复用、异参冲突 |
| APS-AC-17 | P0 / Pass | EV-IMP06-02、EV-IMP10-09 | 候选写入全回滚、遗留求解可审计失败、正式计划不变 |
| APS-AC-18 | P0 / Pass | EV-IMP06-01、EV-IMP07-06 | 30 日外任务结转，近端必需任务未排阻断 |
| APS-AC-19 | P0 / Pass | EV-IMP05-01、EV-IMP08-03 | 独立校验器可否决求解器可行结果并定位硬冲突 |
| APS-AC-20 | P0 / Pass | EV-IMP07-06、EV-IMP10-01/10 | O-100 精确复算 2026-09-15 09:00、F1 3 机时、王工 1 人时，成员量各自为 100 |
| APS-AC-21 | P0 / Pass | EV-IMP09-04 | 开放 M26 可信/不可信 carry 均不回写假结束 |
| APS-AC-22 | P1 / ScopeOut | 引擎设计第 12 节 | 多 Worker 租约与 fencing 已延期，P0 环境禁止第二 Worker |
| APS-AC-23 | P0 / Pass | EV-IMP06-01、EV-IMP07-06 | 可满足/不可满足倒排均受有限容量约束，不排入过去 |

## 4. 失败、修复与复验

| 现象 | 原因 | 处理 | 最终事实 |
| --- | --- | --- | --- |
| OpenAPI 以内存对象验证时相对 `$ref` 解析失败 | 内存对象丢失文件基址 | 统一入口改为从文件 URI 验证 | EV-IMP01-04 已通过相对引用；原失败不是 Schema 内容通过证据 |
| 本机缺 Playwright 捆绑 Chromium revision | 当前缓存没有对应浏览器二进制 | 配置明确使用已安装的系统 Chrome | EV-IMP01-06 只证明 Chrome 152，不外推其他浏览器 |
| PoC 初版出现外部字体网络错误 | DHTMLX 默认样式请求外部 Inter 字体 | 测试 Host/fixture 覆盖为本地系统字体 | 最终两轮 console/page error 均为空 |
| PoC 初版规模与生命周期覆盖不足 | 只用了小夹具且没有重建 | 扩到 500 资源/5,000 分段，并执行双组件销毁和重建 | EV-IMP01-06 覆盖当前技术场景；性能风险未删除 |
| DHTMLX 拖动后视觉位置可能先于服务端确认 | 初版只发意图，没有恢复确认模型 | 在 `finally` 中清空并重新解析当前领域模型；增加精确单测 | EV-IMP01-05/06 通过；正式服务端拒绝场景仍属 IMP-08 |
| 初版 ValidationResult、SolverResult 与 SolverInput 有发布和 P0 语义缺口 | 发布范围未区分，生命周期组合过宽，技能/carry/数量/物料/共享批字段不完整 | 独立复核后收紧状态矩阵，补齐字段与八个负例，增加统一契约门 | EV-IMP01-04 全部通过；业务确定性校验仍未实现 |
| 默认 npm 镜像的 audit endpoint 返回 404 | `npmmirror.com` 不实现 npm audit API | 不改全局配置，仅对复验命令指定官方 npm registry | high 门通过并保留一项既有 moderate 事实 |
| 包含开发依赖的 IMP-01 全量 npm audit 失败 | 当时共享管理端 `vite-plugin-svg-icons -> svg-baker` 链带入高风险公告 | 保留历史失败，不执行破坏性 `npm audit fix --force`；在 IMP-10 按当前 lockfile 重跑 | 当前生产依赖和全量树的 high 门均退出码 0，只剩 ECharts 一项 moderate；历史失败未被删除，也不再代表当前依赖树 |
| 人力报表首次把 ACTIVE 技能永久视为有效 | 初版查询聚合技能编码，但遗漏 M04 的 `valid_from/valid_to` 时间资格 | Mapper 按报表区间只读取相交资格，应用层在每个峰值时点复验有效期，并按资格区间和可用日历求交集小时 | 新增有效期单测通过；隔离 MySQL 三类报表 SQL、迁移、权限和恢复全链复验通过 |
| IMP-02 PowerShell runner 首次解析失败 | Windows PowerShell 5.1 对无 BOM UTF-8 脚本中的中文消息解析不一致 | 将脚本运行消息改为 ASCII，未改变 migration 或验证逻辑 | 修复后完整 migration、约束和恢复流程通过；首次失败不计为数据库执行证据 |
| CP-SAT 证明无解结果首次组装出现空值异常 | 无解状态没有 objective/bound，但相对 gap 计算仍隐式解引用 | 将 gap/relativeGap 显式按可空值处理，并增加证明无解回归 | OR-Tools 9 个测试和真实 MySQL 求解链路最终通过；原异常不计为成功结果 |
| 首次执行 V002 回归时 Flyway 版本断言仍期望 001 | 新增生产向前迁移后测试预期未同步 | 将当前版本断言更新为 002，并增加新增列断言 | 最终 V001+V002、重复迁移和测试 V003 升级均通过；首次失败不计为迁移成功证据 |
| 测试升级脚本从 V002 改名 V003 后仍报告重复版本 | Maven `target/test-classes` 残留旧 V002 测试产物 | 删除该精确可重建产物后重跑完整隔离库脚本 | 最终真实 MySQL 脚本退出码 0；未改动 V001 或业务数据 |
| 可中断求解首版在小样例上返回 UNKNOWN 超时 | 可选资源 interval 的 size 使用 `end-start` 表达式形成不必要的循环传播 | 复用物理段精确时长变量构造可选 interval，并加入跨窗/暂停保留回归 | OR-Tools 17 个测试最终通过，原 UNKNOWN 不计为可行候选 |
| 新增可中断字段后统一契约门报告黄金 hash 不一致 | JCS 摘要正确反映了输入结构变化 | 用同一 RFC 8785 实现复算并更新三份黄金输入及跨契约引用 | Java/Node JCS、Schema、OpenAPI 和跨契约不变量最终全部通过 |
| 基线稳定性目标首次编译缺少参数 | 新增扰动变量后，可行结果摘要调用未同步传递稳定性表达式 | 补齐方法签名与调用，再运行完整模块测试 | 最终 Solver Contract 11、Validator 13、OR-Tools 19 个测试通过；首次编译失败不计为测试证据 |
| 基线求解首次全量测试出现目标名称断言差异 | 无基线输入也输出了新增稳定性指标，破坏既有摘要兼容 | 只在存在基线扰动变量时输出 `BASELINE_DISTURBANCE_UNITS` | 旧样例摘要保持不变，基线样例显示新指标；全量测试通过 |
| O-100 定向 Maven 命令首次被 PowerShell 解析为生命周期阶段 | `-D` 参数未整体引用，命令行被 shell 错误拆分 | 引用完整 Maven 属性参数后重跑目标测试，并再跑全量模块测试 | 首次没有执行测试；最终 O-100 定向测试及 OR-Tools 19 项全量测试通过 |
| 新增基线黄金样例后 IMP-01 结构门仍期望 15 个实例/4 个 hash | 结构门的固定计数未随契约清单同步 | 更新为当前 16 个实例/5 个 hash，保持清单完整性检查 | 首次 28/29；修复后 29/29 通过，不把首次失败记为门禁通过 |
| 细粒度分配锁首轮编译类型不匹配 | 分配身份索引保存 `Choice`，布尔约束误把对象本身转换为 `Literal` | 显式提取 `Choice.selected()` 后再构造布尔或约束 | 首轮 Validator 14 项通过但求解器未执行；修复后按 EV-IMP07 后续证据复验 |
| M24 继承集成测试首次编译缺少类型导入 | 新增锁类型断言后测试文件未导入 `SolverInput` | 补充测试文件导入，不改生产行为 | 生产模块已编译，MySQL 测试首次未执行；补齐后重跑普通回归和隔离库纵切片 |
| M24 继承首次真实 MySQL 测试在锁顺序断言失败 | 查询按锁 UUID 排序，测试却错误依赖插入顺序；两条锁本身均已正确读取 | 改为无序精确集合断言，业务仍要求元素不缺不重 | 首次未进入重排求解断言，不计为继承通过；修复后重跑完整隔离库流程 |
| 静态门首次调用两个不存在的旧脚本名 | 对验证入口名记忆错误 | 用 `rg --files scripts/aps` 定位当前事实源，改跑 `verify-migration.cjs` 与 `check-imp01-structure.cjs` | 错误命令未执行任何检查；正确迁移门 10/10、结构门 29/29 通过 |
| IMP-08 首次 Maven 定向测试命令被 PowerShell 拆分 | 未对带点号的 `-Dsurefire.failIfNoSpecifiedTests` 参数做 PowerShell 边界处理 | 放弃该次命令，改跑 API 模块全量测试 | 首次在 Maven 生命周期解析阶段失败，未启动测试；随后 API 全量通过，加入 HTTP 越权回归后为 23 项 |
| IMP-08 文档门首次使用不存在的 `check-doc-links.cjs` | 沿用了旧入口记忆，当前文档唯一检查入口已改为 `scripts/check-docs.ps1` | 从 `scripts/` 清单重新定位并运行当前入口 | 错误命令未执行文档检查；正确入口随后检查 241 个 Markdown、19 个 HTML |
| 时间线身份拆分后一个旧单测文案失败 | 映射错误语义从“分段 ID”精确改为“时间线项 ID”，旧断言仍检查原文案 | 同步断言到新领域术语，不放宽重复身份校验 | 首次 17/18 通过；修复后重跑 APS 前端全量测试 |
| IMP-08 首轮结构门仍期望旧里程碑 | 结构脚本保留了 IMP-07 开工时的状态常量 | 将事实断言更新为 IMP-01～07 Done、IMP-08 InProgress | 首次 28/29；更新后必须重跑 29/29，不能用文档存在替代状态一致性 |
| IMP-08 锁接口 HTTP 越权测试首次返回 400 | 测试发送空 JSON，参数反序列化先于方法权限检查，未进入待验证的授权边界 | 改为发送结构合法但无权限的请求，并保持服务零交互断言 | 修复测试后无权限锁/调整请求均为 403，API 全量 23 项通过；原 400 不作为授权通过证据 |
| 前端调整请求首次类型检查报 `headers` 不存在 | 本地请求类型声明只覆盖 URL、method 和 data，未表达实际请求封装支持的 headers | 在请求配置声明补充 `Record<string,string>` headers，用于 `Idempotency-Key` | 修复时类型检查与 18 项测试通过，后续扩展为 8 文件/21 项继续通过；未修改全局请求运行语义 |
| 版本化调整首次真实 MySQL 输入校验失败 | 新基线声明了作业但 `freezeEndAt` 为空，违反基线契约 | 将调整版本冻结边界设为当前 `planningAnchorAt`，表示不额外冻结已完成候选中的未来作业 | 独立校验及完整 MySQL 调整—求解—比较链路通过；没有放宽校验器 |
| 继承锁首次在新候选落库时主键冲突 | 求解输入沿用约束来源 lockId，M24 却误把它直接当作跨版本全局主键 | M24 以 `planVersionId + inputLockId` 生成版本级稳定主键；后续调整只从当前 M24 继承权威锁集合 | 首次真实 MySQL 构建失败；修复后 7 个 APS 模块、迁移/升级、调整、锁继承、29 权限及 29 表恢复全部通过，脚本退出码 0 |
| 发布事实水位首次 SQL 报未知 `updated_at` | M29 数量事件按不可变事件设计只有 `created_at`，其余被复验事实表使用 `updated_at` | 查询按实际 DDL 对 M29 使用 `created_at AS updated_at`，未改表也未跳过数量事件 | 首次在发布事务执行前失败，无半套正式版本；修复后首次/替换发布及完整隔离库回归通过 |
| 结构调整最初把幂等指纹写入 `change_note` | 发布和废弃都会更新该用户备注，生命周期变化后重放将失去原意图身份 | 追加 V003 `request_fingerprint`，技术指纹创建后不修改，`change_note` 只保存用户原因 | V001 未改；V001～V003 和测试 V004 前进迁移、同意图重放与不同意图冲突均通过 |
| 多作业候选发布首次出现候选摘要不一致 | 求解结果数组和 M20～M23 查询使用不同顺序，且投影遗漏远期结转任务 | 求解候选和数据库投影统一稳定顺序，并由冻结输入恢复结转任务 | OR-Tools 21 项及真实 MySQL 多作业发布通过；没有放宽 hash 复验 |
| 固定合批首次求解报告基线作业无法映射 | 结构变化后仍继承被合并普通作业的基线与锁 | 编译基线时只排除完整受影响作业，未受影响基线与锁继续继承；若全部受影响则只保留 M19 版本血缘 | 合批和拆批真实 MySQL 求解通过；不允许只排除共享作业的部分成员 |
| 测试前进迁移改为 V004 后仍发现旧 V003 | Maven 可重建 `target/test-classes` 残留已删除的测试资源 | 隔离 MySQL runner 固定执行 `clean test` 后再迁移 | 最终 V001～V003 + 测试 V004 完整脚本退出码 0，不涉及业务数据删除 |
| 窄屏键盘用例首次用 Enter 激活标签失败 | Element Plus 标签遵循方向键切换而不是按钮式 Enter 激活 | 从当前工序标签按 ArrowRight，按 tablist 键盘语义验收 | 定向用例及 9 项全量 Playwright 最终通过 |
| 预留量直接报废首次违反 `ck_quantity_event_shape` | M29 的 `SCRAP` 是批次级事实，不能携带下游需求身份；直接从 `RESERVED` 报废还会遗留需求分配 | 同一事务先按需求追加 `UNRESERVE: RESERVED → AVAILABLE`，再追加不带需求身份的 `SCRAP: AVAILABLE → SCRAPPED`，并在适配器增加与数据库 CHECK 一致的形状校验 | 完整隔离 MySQL 回归通过；M18 分配同步减少，重复质量请求不重复搬量 |
| 执行事实回归曾错误期待“已全部预留或报废”的产出仍是自由供应 | 测试断言混淆 M28 总产出和可供求解器自由使用的 `AVAILABLE` 余额 | 将断言改为该来源任务不存在 `EXECUTION_RELEASED` 自由供应；不改业务查询 | 完整隔离 MySQL 回归通过；求解输入不重复使用已预留或报废量 |
| 执行页面浏览器用例首次找不到中文确认按钮 | `ElMessageBox.prompt` 未显式配置按钮文案，运行时显示英文 `OK` | 页面显式设置“确定/取消”，保持测试按用户可见中文语义操作 | 定向 3/3、APS 浏览器全量 12/12 通过 |
| 共享作业的开放 M26 最初按成员展开为多条求解占用 | 查询把一个 `aps_actual_occupancy` 与全部 M21 成员直接连接，导致同一物理资源区间重复 | 适配器按 M26 `occupancyId` 聚合，新增 `members` 成员剩余量投影；完整共享 run 只输出一个物理占用，部分共享派工因无法从 M25 还原成员分配而降为 `UNKNOWN` | 新增适配器 2 项回归通过；真实 MySQL、统一契约、Validator 和 OR-Tools 全量复验通过 |
| carry 契约增加字段后黄金 candidate hash 不一致 | JCS 摘要正确反映 `carryRunId` 进入候选结构，旧黄金摘要已失效 | 使用同一 RFC 8785 实现复算并同步两个引用候选摘要的黄金样例 | 统一契约门最终通过；没有跳过 hash 校验 |
| V005 首轮隔离库回归仍断言生产版本 004 和 64 个外键 | 新生产迁移已增加 carry 列、约束及一条 M20→M25 外键，测试期望未同步 | 将生产版本更新为 005、测试探针顺延 V006，并区分 V001 静态 64 外键与当前 schema 65 外键 | 最终 Flyway V001～V005、测试 V006、29 权限和 29 表恢复全部通过；前两次失败不计为迁移通过证据 |
| carry 首次只写入 M20、详情投影未读回 `carry_run_id` | 计划只读模型沿用 IMP-08 字段，发布前从 M20～M23 重建候选时会丢失新增身份并导致 hash 不一致 | 将 `carryRunId` 贯通 MyBatis 详情、应用候选投影、REST/OpenAPI 和前端类型，并增加 JCS 摘要等价测试 | Application 9 项、API 25 项、前端类型检查/9 文件 24 项、统一契约门及完整 MySQL 落库读回全部通过 |
| 阶段推进加入 OpenAPI 后统一契约门首次报告路径未实现 | 契约路径实现白名单未同步新增端点 | 将 `/phase-advances` 加入已实现路径清单，不放宽 Schema 或跨契约校验 | 统一契约门最终全部通过；首次失败不计为契约通过证据 |
| 阶段生命周期应用测试首次编译失败 | 测试用内存仓储遗漏既有 `hasUnsupportedSameStart` 端口实现 | 补齐测试替身的既有端口行为，不修改生产判断 | Application 最终 11 项通过；首次编译失败不计为运行证据 |
| API 与 MySQL runner 并行执行时 API 编译中途丢失 class 文件 | MySQL runner 执行 Maven `clean`，与另一 Maven reactor 共享 `target/` 并发冲突 | 等 MySQL runner 完成后串行重跑 API 全量测试 | 串行复验 API 25、Application 11 项全部通过；并行失败是验证进程互扰，不作为代码失败或通过证据 |
| 多阶段 carry 校验器首次编译失败 | carry 后续阶段列表变量重赋值后又被 lambda 捕获，不满足 Java effectively-final 规则 | 先提取不可变后续阶段 ID 集合，再执行候选段筛选 | 首次未运行求解器；修复后标准多阶段与可中断后续阶段均通过，OR-Tools 总计 25 项 |
| Worker 恢复首轮编译找不到 `Instant` | MySQL 仓储适配器新增方法后漏导入时间类型 | 补齐单一 import 后重跑 Worker 依赖链 | 修复后 8/8 reactor 成功，Worker 7 项通过；首次失败未执行 Worker |
| 遗留求解结果最初使用不存在的 `ResultKind.FAILED` | 把基础设施失败错误映射为求解结果类别 | 保留 `solverStatus=UNKNOWN`，将 `resultKind` 置空，并用 `STALE_SOLVING_RECOVERED` 原因码表达 | 编译与真实 MySQL 状态读取通过；没有把 Worker 故障伪装成超时或证明无解 |
| 发布故障回滚首轮复验在结果查询处报空 | 新断言把候选版本 ID 传给“按 ID 读取当前正式基线”的查询 | 改为查询原正式版本 ID，不修改发布事务 | 触发器故障后旧版仍 PUBLISHED、候选仍 FEASIBLE；移除故障后重试发布成功，完整 runner 退出码 0 |
| 完整 O-100 首次排成 4 小时炉次 | 手写测试把 `fixedCycleDurationSeconds` 误填为装卸加运行总时长 10,800 秒，而该字段按契约只表示 RUN 固定周期 | 改为 RUN 7,200 秒；装载/卸载各 1,800 秒，且 F1 在三个阶段均作为资源要求 | 精确场景最终得到 13:00～16:00、F1 3 机时、王工 1 人时及次日 09:00 完成；OR-Tools 25 项通过 |
| 本轮首条 Maven 命令未找到旧 JDK 17 路径 | 工作站 JDK 17 实际路径已从 `C:\Program Files\Microsoft\...` 变化为用户 `.jdks` 目录 | 用只读目录检查定位 `C:\Users\admin\.jdks\ms-17.0.18`，重新设置本次进程的 `JAVA_HOME` 后重跑 | 首次在 Maven 启动前失败，没有执行测试；随后 JDK 17 求解器依赖链与容量基准通过，旧路径不再作为环境事实 |
| 第一版容量 hint 仍使 5k/20k 超时无首解 | 选择、开始和结束已有提示，但目标层后建的 tardiness 变量未提示，不能形成 CP-SAT 的完整解提示 | 用贪心结束时间同步提示 tardiness；仅对 1,000 项以上且满足严格简单形态的模型关闭高成本 presolve并在首个可行解停止 | 最终完整 1k/5k/20k 基准三组断言均通过；结果为 `FEASIBLE` 而非 `OPTIMAL`，复杂模型不走此路径 |
| Windows 目标首轮 Worker smoke 在三个开关关闭时仍因缺少 JDBC URL 退出 | 独立 Worker classpath 中的 Druid 通用自动配置越过 APS 条件配置，错误创建默认 `dataSource` | 在 `ApsWorkerApplication` 明确排除 Spring 与 Druid 的通用数据源自动配置；真正启用 APS 持久化时仍由受三重开关保护的 `ApsDataSourceConfiguration` 装配 | 重建后 Worker 使用 JDK 17 出现 `Started ApsWorkerApplication` 并以 0 正常退出；首轮失败日志保留在目标验证输出中 |
| Windows verifier 第二轮把 Worker 的成功退出误判为失败，并且隔离 MySQL 首次启动未正确引用带空格的 `basedir` | 默认关闭 Worker 没有常驻轮询线程，启动完成后可正常退出；`Start-Process` 参数需要显式保留 `Program Files` 路径引号 | 就绪探针先读取启动标志再判断进程退出；为 MySQL `basedir/datadir/log/pid` 参数加显式引号 | 最终同一脚本从基线采集到 API HTTP smoke 全部通过并退出 0；系统 `MySQL80` 服务仍为 Stopped，失败未被记作通过 |

## 5. 未执行与阶段缺口

| 项目 | 结果 | 影响与最小下一步 |
| --- | --- | --- |
| Windows 目标运行时与 OR-Tools JNI | Done / Pass | 用户明确选择 Windows；`scripts/aps/verify-windows-runtime.ps1` 已采集 OS、CPU、内存、JDK、Maven、Node/npm，并实际验证 JNI、前后端制品、Worker 与 API 启动，见 EV-IMP10-12 |
| Linux/容器可移植性 | NotRun / 非当前范围 | `scripts/aps/verify-linux-runtime.sh` 仅保留为未来改变目标平台时的可移植性工具；Docker/WSL 未安装不阻断本 Feature，也不计入 IMP-10 完成率 |
| 1k/5k/20k 固定形态求解容量 | Done / 有限 Go（当前算法、开发机） | 完整确定性 hint 消除了原 5k/20k 无首解瓶颈，三组均在固定时限内返回经独立校验的可行候选；复杂依赖、共享批、可中断、锁和实际占用组合不走该快速路径，仍需在授权试点形态及目标硬件上复验目标质量和等待时间 |
| Java JCS 与 Node 基准交叉验证 | Pass | `aps-solver-contract` 已用 RFC 8785 实现复验数字、UTF-16 排序和 Node 黄金输入摘要；后续新增黄金样例仍必须同时进入两侧门禁 |
| Java/TypeScript OpenAPI/Schema 代码生成兼容 | NotRun | 生成方案和消费者尚未实现；不能用 Schema 通过冒充 DTO 已兼容 |
| 生产环境 migration 与生产恢复演练 | NotRun | IMP-02 只使用本机隔离临时 MySQL；生产执行必须在目标环境、备份和回退条件批准后实施 |
| 真实 HR 日历、循环班制与独立工作中心授权映射 | NotRun / P1 或后续设计 | P0 只维护来源可追的绝对窗口；普通用户按负责车间访问，工作中心继承所属车间范围，不暗增第 30 张范围表 |
| AC-03 精确汇合时间组合回归 | Done | 固定自动化场景得到 10:00/11:00 放行、30 分钟搬运和 11:30 汇合；非法提前继续由独立校验器拒绝 |
| AC-19 / APS-AC-20 完整 O-100 技术联测 | Done（技术场景） | 求解器精确复算 2026-09-15 09:00、F1 3 机时和王工 1 人时；报表投影复验 A/B 完成与订单关闭门。真实操作仍留 UAT |
| IMP-08 系统内发布语义复核 | Done | 用户于 2026-09-13 明确接受 IMP-08 发布语义；该接受不等于生产发布或 UAT |
| IMP09-D 活动 run carry 与多阶段续排 | Done | 当前阶段固定事实、后续连续/可中断阶段 CP-SAT 续排、最终释放、下游等待、阶段化执行和换资源来源身份均通过；缺事实场景继续 `UNKNOWN` 失败关闭 |
| 真实执行联调和 UAT | Prepared / NotExecuted | [UAT 验收记录](验收记录.md)已准备；真实角色、授权脱敏/试点数据、环境、责任人和验收结论尚未提供，不要求连接真实 MES/QMS/ERP 才能做受控试点 UAT |

## 6. 授权与数据事实

- 只修改了用户已批准的本地工程、契约、测试和生产排产文档；没有连接真实 ERP/MES/HR/WMS/QMS 账号，没有写入真实业务数据。
- 没有执行生产 migration、部署、发布、付费服务操作或 Git 暂存/提交/分支/推送。
- Maven/npm 只产生可重建的本地缓存、`target/`、`node_modules/`、`dist/` 和 Playwright 输出；当前浏览器报告位于仓库根 `output/playwright/aps-imp01/`，不作为源码事实源。
- 用户验收尚未开始，Agent 未代替用户给出接受决定。

## 7. 当前阶段判定

IMP-01～IMP-09 已满足既定技术阶段出口。IMP-10 的报表/ETA、固定容量形态有限 Go、Actuator/SBOM、安全初筛、故障恢复、完整技术 AC 矩阵和 Windows 目标运行时均已有列明证据；PRD AC-01～24 为 24/24 Pass，P0 APS-AC 为 21/21 Pass。复杂试点数据形态、真实角色/样本、UAT 责任人和业务接受结论尚未收口。真实业务库/账号、生产 migration、正式服务安装、部署和 UAT 保持 `NotRun`，当前不能形成 `ReleaseReady`。

## 8. 2026-09-26 若依排产菜单验证（EV-APS-MENU-01）

范围：对应[任务清单第 23 节](任务清单.md)的 APS-MENU-01～03。环境为本机 Windows、`frontend/admin-web` 正式 Vue 工程、若依 API `127.0.0.1:18081`、隔离 MySQL `33307/aps_preview_ruoyi` 与 `aps_preview_business`、浏览器 `127.0.0.1:4176`。隔离库没有业务计划；共享 `8081` 数据库未改动。

| 检查 | 实际执行与结果 |
| --- | --- |
| SQL 幂等与权限 | 执行 `ruoyi-backend/sql/aps-permissions.sql` 两次；查询得到 1 个目录、7 个页面、29 个已归属页面的按钮、0 个新增 `sys_role_menu` 授权、0 个重复页面路径。执行前用 `mysqldump --no-create-info` 备份隔离库 `sys_menu` 到忽略的 `target/aps-live-preview/sys-menu-before-aps-navigation.sql`。Pass。 |
| 前端静态检查 | 在 `frontend/admin-web` 执行 `node --check src/router/index.js` 与 `npm run typecheck:aps`，退出码均为 0。Pass；这不替代运行验证。 |
| 浏览器导航 | 新开正式前端标签页，左侧显示“生产排产系统”及 7 个入口；工艺、订单、资源、就绪、工作台、执行、报表路径逐项显示对应页面且无 404，资源页刷新后面包屑为“首页 / 生产排产系统 / 资源与日历”。Pass。 |
| 报表接口 | 首次检查时隔离 API 的报表开关关闭，页面显示读取失败；随后仅重启本地 `18081` API 并设置 `APS_REPORTING_ENABLED=true`、`APS_SITE_CODE=APS_PREVIEW`。复查报表页返回站点与时区信息，并正常显示“当前日期和权限范围内没有计划或实际事实”。Pass（空数据）。 |

排查中，新菜单初次出现时内容区空白，原因是旧隐藏 APS 路由与数据库菜单路由重复；移除 `frontend/admin-web/src/router/index.js` 中 7 条重复路由后，刷新与逐页导航均恢复。当前验证仅证明管理员会话和隔离数据环境中的菜单、路由及空状态；普通角色尚须显式授权，真实角色/代表性订单和计划的业务 UAT 未执行，IMP-10 阶段判定不变。

## 9. 2026-09-26 合成样本真实联机验收与 UTC 修复

本轮场景、逐项状态、截图及用户决定统一见[验收清单](evidence/20260926联机验收/验收清单.md)和由其证据生成的[HTML报告](evidence/20260926联机验收/verification-report.html)。用户要求创建代表性数据验证主链路；实际写入只在 `33307/aps_preview_business`，站点 `APS_PREVIEW`，浏览器/API 为 `4176/18081`。没有向外部 MES/ERP 发出生产指令。用户决定保持待确认。

L1 缺陷：APS 的 MySQL DATETIME 按 UTC 映射，而连接默认继承 Windows MySQL SYSTEM(+08)；CURRENT_TIMESTAMP 写入的审计时间被误读为未来，导致新计划无法发布。修复 `ApsDataSourceConfiguration` 每个新连接初始化 `SET time_zone = '+00:00'`。定向 `ApsDataSourceConfigurationTest` 5项通过，包含真实隔离MySQL临时表默认时间探针；首次测试遗漏数据源enable开关，修正测试配置后复验通过，原日志保留。

运行包隔离：当前共享平台源码存在新的 JWT 平台密钥依赖，不属于此次排产改动；首次整体构建后暴露隔离库未配置JWT，用户账号校验成功但签发令牌失败。随后采用现有稳定 `ruoyi-admin-interview.jar` 平台基线，仅替换已测试的 APS MySQL 依赖模块。逐类比较确认 APS API/应用/领域/契约/校验器类无差异，基础设施只差本次配置类；运行包条目差异见[证据](evidence/20260926联机验收/运行基线差异.json)。未改账号、密码、JWT密钥或共享平台源码。隔离若依库已补两个空的密钥基础表，无凭据写入。初次同名打包遇共享JAR占用；独立文件名打包成功，临时POM恢复且摘要一致，标准输出恢复为可运行JAR，共享后台未重启。

本次样本创建前业务库为空且有完整备份。UTC 修复前再次备份，仅修正本次样本中误写为未来的 created_at/updated_at，保留原始失败回执、脚本与恢复文件；不修改订单数量、业务时间、状态和计划计算结果。恢复文件不作为可直接覆盖后续用户数据的通用迁移。构建、测试及实际联机结果以报告逐项证据为准，不据此声明原完整 UAT 或上线就绪。
