# FEAT-APS-001 生产排程核心闭环：控制页

> 文档类型：Feature 控制页；版本：0.9.22；文档状态：Draft  
> Feature：FEAT-APS-001；风险：L2  
> 当前治理阶段：Stage 7 开发；阶段状态：InProgress；当前实施里程碑：IMP-10（技术实施中）  
> owner：用户维护产品方向与验收决定；Codex 按批准范围实施并维护工程事实  
> 授权记录：2026-09-12 用户批准按十阶段和推荐基线开工；2026-09-13 用户接受 IMP-08 发布语义与 IMP-09 开工方案，并明确目标运行环境为 Windows  
> 创建日期：2026-09-12；更新日期：2026-09-13

## 1. 当前结论

本 Feature 的目标是把已有生产排程需求、29 表数据库业务雏形和本地交互原型，转化为可部署的单站点生产排程 P0 产品。IMP-01～IMP-09 已完成。IMP-10 的技术项已收口：日冻结基线、每日生产表、人员去重累计产能/多席位峰值与技能有效期、订单/产品行 ETA、CSV/打印、权限审计与 OpenAPI 已形成纵切片；Worker 遗留求解回收、优雅停机、发布故障原子回滚/重试和 29 表恢复已有本地证据。PRD AC-01～24 与 21 项 P0 APS-AC 的技术回归均已通过。本机固定容量基准的 1k/5k/20k 独立单阶段形态均在 30 秒求解上限内得到经独立校验的可行候选，当前为有限 Go；结果不证明复杂业务形态的目标质量或未经验证的生产硬件 SLA。用户已明确 Windows 为目标运行环境，当前 Windows 11 x64、Microsoft JDK 17.0.18、OR-Tools JNI、API/Worker 启动和前后端制品均已通过隔离复验。真实角色/数据联调和业务 UAT 尚未完成，因此 IMP-10 仍为 `InProgress`，不能评为 `ReleaseReady`。

用户已批准[十阶段实施计划](任务清单.md)及其开工基线。计划使用 `IMP-01`～`IMP-10` 表示十个**实施里程碑**，与协作规范中的 Stage 1～10 **治理阶段**分开。Feature 处于 Stage 7 / InProgress：IMP-01～IMP-09 已完成，IMP-10 技术项已完成而 UAT 待执行。目标环境就是当前 Windows 本地环境，API 与 Worker 使用两个独立 Java 进程；Docker、WSL 和 Linux 仅可作为未来可移植性验证，不属于本 Feature 当前完成条件。

## 2. P0 最终闭环

```text
资源与工艺维护
  → 多产品订单与路线版本展开
  → 数据就绪和硬约束校验
  → 单 Worker 有限产能排程
  → 数量释放、转移批、拆批与共享加工批
  → 三类甘特、人工计划、锁定、版本比较与系统内发布
  → 开停工、实际占用、部分报工、质量处置、返工与数量账
  → 车间日报、人力产能、订单预计完成与原因解释
  → 安全、恢复、容量验证和用户验收
```

首版采用一套 APS 业务数据库服务对应一个生产站点，车间是组织、权限与报表根。首版不恢复 `aps_factory` 或 `factory_id`，技能只使用 `aps_resource_skill` 一张表。

## 3. 唯一事实源与入口

| 事实 | 唯一入口 | 当前状态 |
| --- | --- | --- |
| 产品需求、REQ-01～10、AC-01～24 | [产品需求文档](../../产品/产品需求文档.md) | Draft；产品正文，不在本 Feature 复制维护 |
| APS 规则、约束、技术设计与引擎 AC | [APS 排程引擎需求与技术设计](../../架构/APS排程引擎需求与技术设计.md) | Draft；Java 17、单 Worker、系统内发布和 P1 可靠外发口径已同步 |
| 29 表字段、关系与 DDL 候选 | [数据库设计](../../架构/数据库设计.md)、[DDL 草案](../../架构/生产排程数据库基线草案.sql) | Draft；已冻结为不可变 V001，并通过隔离 MySQL 实测，未在真实业务库执行 |
| 甘特组件决定 | [甘特图组件选型决策](../../决策/甘特图组件选型决策.md) | 精确版本与适配路线已锁定为受限 Go；正式工作台已实现，规模结果只记录基线，尚无批准阈值可判定性能达标 |
| 交互和业务行为样例 | [生产工作台 V2 原型](../../../../../prototype/production-workbench-v2/index.html) | 本地原型；不是生产代码 |
| 十阶段实现范围与任务 | [任务清单](任务清单.md) | 唯一实施任务事实源；IMP-01～IMP-09 Done，IMP-10 InProgress |
| 实现与验证证据 | [验证记录](验证记录.md) | IMP-10 报表纵切片、故障恢复、安全初筛、完整技术 AC 矩阵、固定容量形态有限 Go 及 Windows/JNI 目标运行时均已有证据；UAT 按真实状态保留 |
| 用户或业务验收决定 | [UAT 验收记录](验收记录.md) | Prepared / NotExecuted；执行环境、角色、数据和业务负责人待提供，决定不能由 Agent 代签 |

## 4. 拟修改的工程边界

用户批准后，P0 只在以下当前工程中新增或调整生产代码：

| 工程 | P0 职责 | 当前事实 |
| --- | --- | --- |
| `platform-backend/` | 新增 APS Maven 聚合模块、业务 API、MySQL 持久化、独立校验器、OR-Tools Worker；由 `ruoyi-admin` 装配 HTTP 与 RuoYi 权限 | M01～M29 已形成应用纵切片；执行 run、M26 实际段、报工、质量、数量流转、冲正、返工/补产、执行水位及输入事实已接入；阶段化执行和多阶段/可中断后续 carry 已通过求解器、独立校验和隔离 MySQL 回归 |
| `admin-web/` | 新增 APS 管理页面、三类甘特、版本比较、执行和报表页面；浏览器只访问 Java API | 资源/路线/订单、排程工作台、现场执行和 `views/aps/reports` 已接入；报表页覆盖日冻结对比、人员去重/峰值、UNKNOWN ETA、CSV 和打印 |
| `contracts/aps/` | 保存 REST OpenAPI、Solver 输入/结果/校验 JSON Schema 与黄金样例 | v1 Solver、计划、执行与 6 个报表查询/导出路径已通过统一门；外部下达不在 P0 |
| `scripts/aps/` | 保存隔离迁移检查、黄金回归、容量基准和恢复演练脚本 | 已有结构门、migration 静态门、本机隔离 MySQL 迁移/恢复演练、可重复容量基准及 Windows 目标运行时验证脚本；目标基线已锁定 |

`ruoyi-backend/`、`ruoyi-vue3-frontend/`、`ruoyi-app/` 是上游基线，不是本 Feature 的修改目标。原型只作为交互和验收输入，不直接搬运其 `localStorage` 状态或 `javascript-lp-solver` 实现。

## 5. 已批准开工基线

下列选择已经随十阶段计划获得用户批准，是当前开发基线：

1. P0 的 API 与 Worker 统一编译到 Java 17，沿用共享父工程，不把全仓升级 Java 21 作为 APS 开工前置；同步修订当前 APS 设计中的 Java 21 表述。
2. `admin-web` 只为新增 APS 文件增量接入 TypeScript、`vue-tsc`、Vitest 和 Playwright，不迁移既有 JavaScript 页面。
3. 使用 29 表、单 Worker、受控失败恢复和系统内发布；不把 `row_version` 冒充分布式租约。用户已批准把多 Worker 接管、Solver Attempt、Inbox/Outbox 和外部 MES 可靠回执转为 P1，PRD 与引擎设计已同步。
4. P0 日历事实为 M05 中的绝对可用/不可用时间窗，由人工、导入或受控 API 物化；循环班制模板和 HR 实时同步不在 29 表首版中暗建。
5. 单站点信息和 IANA 时区由部署配置承担；数据权限按 RuoYi 角色加车间/工作中心范围实现，不恢复工厂表。
6. MySQL 是 APS 唯一业务事实源；RuoYi 用户、部门只以稳定 ID 逻辑关联，不建立跨库外键、跨库 `JOIN` 或跨库事务。
7. OR-Tools CP-SAT 是 P0 求解 PoC 首选；任何候选必须再经过不依赖 OR-Tools 的独立硬约束校验器才能发布。
8. DHTMLX Gantt Community 与 `vis-timeline` 仅位于前端适配层；数据库、REST 契约和领域对象不保存组件原生对象。

## 6. 审核门与授权限制

用户批准十阶段计划后，已批准路径内的本地代码、文档、隔离数据库和自动化验证可连续实施；每个 IMP 完成后按真实证据汇报，不机械重复申请相同授权。以下事项不由路线批准自动授权：

- 在真实生产数据库执行 migration 或写入真实业务数据；
- 连接真实 ERP、MES、HR、WMS、QMS 账号或向外部系统写入；
- 安装或启用会产生费用的服务、正式发布或上线；
- Git 暂存、提交、建分支、推送、PR、标签和发布；
- 将 P1/P2 能力并入 P0，或新增多 Worker、消息中间件、Inbox/Outbox 等基础设施。

四个关键复核点是：IMP-01 技术与业务基线、IMP-06 OR-Tools PoC Go/No-Go、IMP-08 系统内发布语义、IMP-09 数量与执行账口径。IMP-10 的 UAT 决定只能由用户或业务负责人给出，且 `ReleaseReady` 不等于已发布。

当前按[任务清单](任务清单.md)完成至 IMP-09；IMP-10 的报表与 ETA、Worker 恢复、发布故障回滚/重试、完整 P0 技术 AC、固定独立单阶段容量形态及 Windows 目标运行时均已形成证据。复杂业务形态、真实试点与 UAT 前置仍未收口。真实生产迁移、外部账号、正式部署与 Git 写操作保持关闭。

## 7. IMP-01～IMP-07 完成结论与 IMP-08 技术收口

- 后端八模块、`ruoyi-admin` 装配、三重 Worker 运行门和 OR-Tools `9.15.6755` Windows x86_64 JNI smoke 已通过；当前共享后端全部 19 个 reactor 项目可编译。
- OpenAPI 3.1、JSON Schema 2020-12、当前 16 个正反例、JCS 基准与统一契约门已经建立并通过；这不代表 Java/TypeScript 代码生成或全部后续端点已实现。
- 前端锁定 DHTMLX Gantt `10.0.3` 与 `vis-timeline` `8.5.4`，500 资源/5,000 分段可在 Chrome 中渲染、销毁和重建；正式工作台已完成筛选、缩放、定位、状态恢复，当前 APS 全量 17 项浏览器用例通过。最近一次首屏为 16.085 秒，继续只判定适配路线“受限 Go”；在业务阈值获批并完成容量优化前，不宣称性能达标。
- V001 保持不可变；生产向前迁移 V002～V005 分别覆盖受控可中断、结构调整幂等、数量事件精确冲正和活动 run carry 身份，V006 为 M26 增加来源计划分配身份及复合外键。它们已在不启动系统 `MySQL80` 服务的临时 MySQL 8.0.46 中完成 migrate/validate/幂等、测试 V007 前进升级、29 表/当前 66 外键、约束反例、事务、乐观锁、换资源后可信输入、权限 SQL 幂等及逻辑备份恢复；临时实例和数据目录已清理。
- M01～M05 资源纵切片已在隔离 MySQL 和 Chrome 夹具验证；数据范围按车间负责人/范围管理员执行，工作中心继承所属车间范围，不新增范围表。
- M06～M18 产品、工序阶段、版本路线 DAG、多产品订单和冻结任务网络已在隔离 MySQL 验证；三类派生批、显式跨产品依赖、V1/V2 隔离、迁移确认和 `SAME_START` 阻断均有可重复证据。
- M01～M18 已能编译为稳定 hash 的 SolverInput；独立校验覆盖资源、日历、席位、容量、阶段、依赖、共享批、锁、真实占用和修订，并通过固定 20 任务及 O-100 就绪样例。
- 基础 CP-SAT 已生成并独立复验有限产能候选；M19 请求/状态/取消、单 Worker CAS、M20～M23 原子保存和 SSE 已实现，真实 MySQL 故障注入证明半套候选会整体回滚。Worker 默认仍关闭，未声称真实站点服务在线。
- IMP-07 已在求解器与独立校验器中接入数量/物料释放、共享批、跨窗口分段、续作准备、跨段资源固定/重选、独占资源暂停保留及作业/分段/分配三级锁；未经建模的组合显式失败关闭。当前正式基线由服务端从 M19～M24 可信编译，冻结作业精确保持，非冻结作业按低于延期的稳定性目标减少扰动，M24 细粒度锁可稳定继承；O-100 求解时间、共享炉和人员互斥已通过，因此 IMP-07 标记 `Done`。
- IMP-08 的工作台技术项已收口：三视图共用稳定身份和筛选集合，冲突/未排/过期原因可定位，人工锁与候选废弃保留历史，时间/资源及插单/拆批/固定合批均只创建版本化草稿；系统内发布重新复验并原子切换唯一 current。IMP-08 的系统 Chrome 9/9 功能用例通过，最新全量复验中的 500/5,000 首屏 16.085 秒只作性能基线。用户于 2026-09-13 接受发布语义，里程碑标记 `Done`。
- 用户于 2026-09-13 明确选择 Windows 为目标运行环境。`scripts/aps/verify-windows-runtime.ps1` 已在当前 Windows 11 x64 上使用固定 JDK 17 完成 JNI、前后端制品、默认关闭 Worker 和启用 APS API 壳的 RuoYi HTTP 启动；API 使用随机端口与临时 MySQL，系统 `MySQL80` 服务保持停止并在结束后清理。Docker、WSL 和 Linux 不再是当前门槛；现有数据库证据仍只来自隔离实例，不等于生产迁移。
