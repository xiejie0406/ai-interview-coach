# 生产排程数据库设计验证记录

> 历史状态：Superseded。本记录只证明旧 62 表 Draft 当时通过了文档静态检查，不能作为当前数据库设计或 SQL 的验证证据。当前 29 表方案尚未创建独立验证记录，其实际 migration 与验证安排以当前 Feature 任务源为准。

> 编号：VER-DB-APS-001；版本：1.0.0；文档状态：Draft；创建 / 更新：2026-09-11  
> owner：用户；执行与记录：Codex；范围：数据库设计文档，不是数据库实现验收  
> 事实源：[数据库设计](../数据库设计-62表草案.md)、[PRD V2](../../../产品/产品需求文档.md)  
> 状态说明：文档检查通过不表示DDL、运行事务、权限、性能或生产可用性通过

## 1. 本次范围

- 主文档包含62张候选表、公共字段、逐表字段类型 / 必填 / 默认值 / 主外键关系、3张Mermaid关系图、18项跨表约束、关键事务、报表口径和贯通样例。
- 沿用项目既有RuoYi身份库与PostgreSQL业务库边界；不复制身份凭据，不建立跨数据库物理FK。
- 仅新增设计及检查资料，在架构入口和PRD增加链接。没有连接、创建或迁移真实数据库，没有修改原型功能，没有安装依赖，没有Git写操作。

## 2. 文档级检查

| 检查 | 结果 | 证据及边界 |
| --- | --- | --- |
| 表编号、字段完整性、重复字段、FK目标、UQ字段 | Pass | [定向检查脚本](生产排产数据库设计检查-62表.cjs)：62张表、498个逐表字段（不含公共字段展开）、158个显式FK引用目标检查通过 |
| 关系图表名及关联方向的文本检查 | Pass / 文本级 | 3张图、61条边；只检查两端存在及至少一条FK关系，不等于Mermaid渲染或完整数据库ER校验 |
| REQ及约束编号、样例算术 | Pass / 文本与算术级 | REQ-01至10、C01至18；7组样例算术，不代表求解器、事务或SQL测试 |
| 仓库文档结构及本地链接 | Pass | `scripts/check-docs.ps1`实际输出：`PASS: documentation structure and links in 178 Markdown files.` |
| 独立只读领域复核 | Pass / 文档级 | 3轮复核，问题和修正见第3节；不代替用户或数据库验收 |

定向脚本使用Node内置标准库，不加载业务后端，也不访问数据库；[机器检查结果](数据库设计检查结果-62表.json)保存主文档SHA-256及逐项结果。当前命令：

最终定向结果：**10 Pass / 0 Fail**；运行环境Windows、Node v24.19.0，机器结果时间2026-09-11T13:24:11.571Z（北京时间21:24）。这些是10组文档检查，不是10组数据库集成测试。

```powershell
& 'C:/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe' 'scripts/check-aps-database-design.cjs'
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-docs.ps1
```

首次定向检查为9 Pass / 1 Fail：关系检查器只识别逐表显式字段，遗漏公共factory_id外键，将工厂→车间错误报为无关联。已修正检查器以应用第3.2节公共外键约定；不改变数据库设计来迎合该误报，修正后重新运行并保留最终机器结果。

## 3. 复核问题与修正

| 发现 | 已写入的修正 | 复核范围 |
| --- | --- | --- |
| 独占设备并发容量1不能表示炉装载200件 | T58–T59分别保存具体设备装载上限和工艺转换，多维同时检查 | 字段及业务关系 |
| TRANSFER展开丢失工作等待口径 | T28补lag_basis与lag_calendar_version_id | 字段与模板映射 |
| distinct规则误禁同一人顺序装卸 | T17限制同时席位，允许跨阶段复用；continuous身份规则另判 | 工艺语义 |
| 单位、路线和恢复数量可能误关联 | C02–C07、C16补父子 / 产品 / spec一致性，单位语义冻结，T52保留双单位换算 | 字段及约束设计 |
| 比例拆分产出可能舍入增减量 | T60按成员和释放批直接保存精确数量，T38引用具体释放批 | 数量模型 |
| 退预留被重复计为合格释放、预留退回无法重建 | T48区分事件类型并关联reservation_id，只认可真正合格释放 | 数量账及投影来源 |
| 冲正记录仍要求正向事件方向 | T50、T53明确反向事件、正值减计及单次冲正约束 | 账本方向 |
| carry运行可能重复启动或重复预测已报量 | T34部分唯一键、C13及10.2规定同一实际身份和有效余量 | 版本与执行关系 |
| 同版输入可能被覆盖；零问题行不能证明校验完成 | 新输入新版本，T32校验批号 / 时间 / 摘要，发布再次复验 | 发布事务设计 |
| 返工合格品不能满足限定原来源的下游 | 11.6定义批准恢复末端来源等价、return_task限制及去重 | 恢复归属 |
| 缺具名人机组合工时标准 | T61–T62保存标准、完整组合、有效期、依据及确认人，T36引用 | PRD 8.1覆盖 |
| 索引及样例沿用不存在的字段名 | expected_available_at、occurred_at、immediate_good_qty与字典统一 | 文本定向核对 |
| 发布描述误要求所有未来供给已实物放行 | 11.3区分冻结预测与现场实际放行，实际迟延 / 质量冻结不能忽略 | 发布与执行语义 |

最终独立复核确认T61–T62组合关系及REQ主要对象映射自洽；提出的最后3项字段名 / 发布措辞问题已按意见修正。该结论仅限文档中可见的确定矛盾，不证明真实数据库实现正确。

## 4. 数据库实施验收待办

以下项目全部为 **NotRun**，未通过文件存在、文字推演或原型测试代替：

| 编号 | 后续实施必须验证 | 预期断言 |
| --- | --- | --- |
| DB-01 | 从空库执行DDL及全部约束 | 循环FK可提交合法task/spec，跨工厂 / 跨路线 / 跨计划引用拒绝 |
| DB-02 | 图变更及消耗拓扑 | 循环、自依赖、错物料 / 单位拒绝；合法跨产品关系成功 |
| DB-03 | 人机并发与装载 | 同版独占冲突拒绝；异版可重叠；连续容量和多维装载上限有效 |
| DB-04 | 标准与日历版本 | 组合匹配、有效期、缺失标准阻断、跨班分段及多人不线性增速 |
| DB-05 | 发布与报工竞争 | 输入修订或实际水位变化时拒绝过期发布，正式指针 / outbox原子提交 |
| DB-06 | 同批并发预留与分批投入 | 100可用面对60+60不能超用；20件首批不能支撑60件下游产出 |
| DB-07 | 质量与恢复 | 待检不能领用；98+2返工计100；不重复原良品、处置量或恢复链中间产出 |
| DB-08 | 幂等与更正 | 重复请求不重复报工，反向事件守恒，投影可重建，已消费量不能直接撤销 |
| DB-09 | 三甘特及日报 | 共享批3机时 / 1人时，跨日分配正确，多技能工时不重复，日基线不漂移 |
| DB-10 | 交期与历史 | 任一必需项未知时整单UNKNOWN；历史计划恢复不回滚实际记录 |
| DB-11 | 故障与权限 | 事务中断无半账、外发重试可去重、跨工厂越权拒绝、审计不可随意修改 |
| DB-12 | 容量、迁移及恢复 | 用真实规模验证索引 / 锁吞吐；演练备份恢复、升级兼容及失败恢复 |

数据库、DDL、事务服务、外部接口均尚未实现，本次没有生产就绪或用户验收结论。Mermaid图本次只做文本关系检查，未进行独立渲染截图验收。
