# 生产排程数据库设计验证记录（46 表归档）

> 文档类型：技术验证记录；编号：VER-DB-APS-002；版本：1.0.0 Draft  
> 执行 / 更新：2026-09-11；owner：用户；执行与记录：Codex  
> 范围：[46 表数据库设计](../数据库设计-46表草案.md)与[MySQL 基线 DDL](../生产排程数据库基线草案-46表.sql)的静态一致性  
> 机器证据：[数据库设计检查结果-46表.json](数据库设计检查结果-46表.json)；检查器：[生产排产数据库设计检查脚本](生产排产数据库设计检查-46表.cjs)  
> 状态说明：静态检查通过不表示已连接 MySQL、执行 DDL、迁移数据、验证事务或达到生产可用

## 1. 实际执行

```powershell
& 'C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' scripts\check-aps-database-design.cjs
```

环境：Windows，Node.js v24.19.0。机器证据生成时间为 2026-09-11T15:23:39.566Z，即北京时间 2026-09-11 23:23:39。

## 2. 结果

**静态检查：19 Pass / 0 Fail。**

| 检查组 | 实际结果 |
| --- | --- |
| 表集合 | 设计 T01–T46 唯一；SQL 46 个 `CREATE TABLE`，集合完全一致 |
| 字段集合 | 设计展开公共字段后 780 个字段；SQL 780 个字段，类型和可空性一致 |
| 外键 | 设计 175 个外键；SQL 175 个外键，目标、组合唯一键和子表左前缀索引检查通过 |
| 数据库方言 | 46 表均为 InnoDB / utf8mb4；未发现 PostgreSQL 专用可执行语法 |
| 约束 | 150 个 CHECK 表达式通过保守词法检查；325 个命名约束全局唯一且不超过 64 字符 |
| 安全基线 | 未发现 DROP、TRUNCATE、DELETE、危险 ALTER、关闭外键、seed 或数据修改语句 |
| 文件卫生 | 未发现冲突标记或 NUL 字节；旧 62 表已退役物理表名未残留在 SQL |

本结果证明的是设计文档与 SQL 草案在静态结构上的一致性。CHECK 只做词法检查，未由 MySQL 服务端解析。

另有一轮独立只读审查基于相同 DDL SHA-256 完成，未发现未关闭的 P0/P1 静态问题；循环和前向引用均在目标表存在后补加，未发现高严重度 NULL 三值逻辑绕过。

## 3. 本次没有运行

以下均为 **NotRun**：

- MySQL 空库创建、`SHOW CREATE TABLE` 和实际方言解析；
- 外键、CHECK、唯一键对真实插入的拒绝行为；
- 任务 / spec 循环指针的同事务创建；
- 图无环、数量守恒、技能资格、资源时间重叠和累计容量的服务事务；
- 求解中报工、并发发布、Outbox 与正式指针原子切换；
- 查询计划、锁竞争、20,000 任务规模和容量性能；
- 备份、恢复、版本升级和失败回滚演练；
- 应用接入、外部 ERP / MES 联调和用户验收。

## 4. 与 APS 引擎设计的关系

[APS 排程引擎需求与技术设计](../../../架构/APS排程引擎需求与技术设计.md)识别了 APS-DG-01 至 APS-DG-07。它们是求解器实施前的数据契约差距或口径选择，不改变本记录对历史 46 表与 SQL 静态一致性的结论，也不应被本次 `19/19` 结果误写为已经解决。

下一次数据库验证必须在隔离 MySQL 8.0.16+ 环境执行空库 DDL、约束反例、并发事务和恢复演练，并保存可复跑命令与原始输出。

## 5. 文档链接检查

- 对本次设计、DDL 说明、PRD、验证、收敛记录、架构入口和两份 62 表归档共 8 个相关 Markdown 文件执行相对链接定向扫描：`Pass`，无缺失目标。
- 全仓执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-docs.ps1`：`Fail`，退出码 1。4 条范围外断链分别是 `AGENTS.md` 指向的 `文档/README.md`、`文档/规范/README.md`、`文档/规范/文档组织规范.md`，以及服装选品调研指向缺失的 `Python-Agent架构设计.md`。
- 上述 4 个文件不属于本次生产排程数据库授权范围，未擅自修改；该全仓失败不改写本节 8 个相关文件定向检查通过的事实。
