# 五类文档归类迁移记录

> 文档类型：迁移与恢复记录  
> 日期：2026-09-12  
> 文档状态：Completed  
> 风险等级：L1（文档路径和入口调整，不改变产品运行行为）  
> 授权依据：用户要求按若依、面试、AI 智能选品、Agent 桌面端和生产排产模块分类，并使用中文目录和文件名

## 1. 实际范围

本次只整理中央 `文档/`、根入口及用于文档校验的路径引用。业务源码、配置、数据库和可执行原型工程目录没有移动，也没有执行 Git 暂存、提交或推送。

最终分类为：

| 分类 | 当前入口 | 归属原则 |
|---|---|---|
| 若依框架 | [`../若依框架/README.md`](../若依框架/README.md) | 上游基线、共享工程和项目集成导航 |
| 面试项目 | [`../项目/面试项目/README.md`](../项目/面试项目/README.md) | 面试、题库、语音、评测、学习和项目专属若依集成 |
| 智能选品项目 | [`../项目/智能选品项目/README.md`](../项目/智能选品项目/README.md) | 选品、搭配、报价和项目内部 Python 智能体 |
| 智能体桌面端项目 | [`../项目/智能体桌面端项目/README.md`](../项目/智能体桌面端项目/README.md) | 桌面执行底座、采购询价和受控 Worker |
| 生产排产项目 | [`../项目/生产排产项目/README.md`](../项目/生产排产项目/README.md) | 有限产能排程、车间调度、APS 与执行反馈 |

目录不使用 `01`、`02`、`03` 等排序前缀，避免编号被误认为优先级或项目阶段。分类按事实所有权而不是关键词判断；因此面试项目的若依集成、智能选品项目的 Python 智能体仍保留在各自项目内。

## 2. 迁移和保护证据

迁移过程中曾保存只读恢复副本：

```text
D:\2025Ai\26-05-23\java-workspace\.runtime\文档备份-2026-09-12-五类归档前
```

该副本统计为 228 个文件、3,324,208 字节，包含迁移前的整个 `文档/`、根 `README.md`、根 `AGENTS.md`、检查脚本和前一轮迁移脚本。2026-09-12，用户明确说明该备份无用并授权删除；上述目录随后被完整删除，当前已不存在。此处路径和统计只保留为已发生动作的历史证据。

路径证据分两段保存：

- [`2026-09-12-项目文档归类映射.csv`](2026-09-12-项目文档归类映射.csv)：前一段从旧的顶层职责目录归入四个临时项目目录，共 214 行。
- [`2026-09-12-五类文档归类映射.csv`](2026-09-12-五类文档归类映射.csv)：从四个临时项目目录归入最终五类结构，共 214 行，并记录迁移前 SHA-256。

链接修复分两批执行：第一批脚本报告修改 25 个文件中的 97 个目标，第二批脚本报告修改 49 个文件中的 112 个目标；两批可能修改同一文件或链接，因此这些数字不相加作为唯一链接总数。之后还需以最终链接检查结果作为是否可用的证据。

三份原始 Word 只改路径和文件名，不改正文；对应 SHA-256 保存在映射表中。旧候选架构和被替代的数据库稿进入各项目 `归档/`，当前入口明确其历史性质。

## 3. 语义变化

- 根 `文档/README.md` 和 `文档/项目/README.md` 成为跨项目导航。
- `文档/若依框架/` 只维护共享入口；面试项目专属的 PostgreSQL、`ruoyi-interview` 和 Feature 事实没有上移为通用规则。
- 原先混在面试索引中的三个独立项目入口移到工作区总览。
- 生产排产、智能选品和智能体桌面端的产品、架构、原型、验证与归档资料分别回到对应项目。
- 被替代的 62 表排产设计、Python-first 桌面端候选架构和旧若依收敛方案保留原文，但不再作为当前事实源。

本次整理没有批准任何 Draft 产品、没有把静态检查写成联机验收，也没有改变已有 Feature 的用户验收结论。

## 4. 备份删除与现有恢复条件

本地迁移快照已经按用户授权删除，因此不能再通过原 `.runtime` 目录一键恢复。两张 CSV 仍保留旧路径、新路径和迁移前哈希，但它们不能独立重建已经删除的文件内容。

如以后需要回退，只能先检查版本库历史、外部备份或其他仍存在的原文来源，再基于两段映射制定新的恢复方案。当前工作区包含其他未提交修改，且本轮没有执行 Git 写操作；不得直接批量反向移动或覆盖当前文件。

## 5. 最终验证

| 检查 | 实际结果 | 限制 |
|---|---|---|
| 五类结构、中文命名和相对链接 | `scripts/check-docs.ps1` 通过；检查 225 个 Markdown、19 个 HTML | 只证明所扫描文件的结构与静态链接 |
| 中央文档基本格式 | 205 个 Markdown 均有一级标题；5 个 HTML 均有 `title` | 不评价正文产品结论 |
| JSON 语法 | 中央文档 13 个 JSON 加服装原型 2 个生成结果，共 15 个，全部可解析 | 不等于契约消费者已联调 |
| 最终迁移目标 | 五类映射 214 行，目标缺失 0 | 修改过正文链接的文本文件不再与迁移前哈希相同，这是预期变化 |
| 原始 Word 保护 | 3 份 Word 当前 SHA-256 均与五类映射中的迁移前值相同 | 只证明二进制内容未改 |
| 生产排产数据库静态检查 | 19/19 Pass；设计 46 表，SQL 46 个建表语句 | 未执行 MySQL 建库、迁移或业务联调 |
| 智能选品原型文档 | 检查 16 个事实源或派生文件、186 个本地链接，断链 0；17 项 REQ、32 项 AC 仍可识别 | 只验证文档生成和追溯，不等于生产 Feature 验收 |

本轮未启动业务服务、数据库、桌面程序或浏览器，也未执行真实账号、Provider、外部发送、发布或 Git 写操作。
