# 旧项目收敛记录

> 文档类型：迁移/删除记录
> 文档状态：Draft
> owner / 责任边界：执行人记录删除事实；业务 owner 负责后续用户验收结论
> 创建时间 / 更新时间：2026-08-30
> 产出阶段：8 审查验证 / 10 上线就绪准备
> 风险等级：L3（不可逆目录删除）

## 1. 范围与结论

用户已明确授权删除旧 React/Spring Boot 项目，仅保留 RuoYi 目标项目。已删除：

- `frontend/`：旧 React 19 前端（删除前 1,896 个文件，约 89.6 MB）
- `backend/`：旧独立 Spring Boot 多模块后端（删除前 1,313 个文件，约 68.6 MB）

删除当时保留：`apps/`（RuoYi backend、Admin、Portal、mobile）、`contracts/`、`docs/` 及根目录共享领域资料。随后同日已将 `apps/` 下工程提升到仓库根目录，见[根目录提升记录](2026-08-30-应用工程根目录收敛.md)。

## 2. 恢复证据

删除前在仓外生成并校验：

| 产物 | 路径 | SHA-256 | 说明 |
|---|---|---|---|
| 工作树归档 | `D:\2025Ai\26-05-23\ai-interview-coach-legacy-20260830.zip` | `534197E1E3D046B18EC7029652456BC91675188B6AC4A2338B47EA65EB4A11BD` | 包含 `frontend/`、`backend/` 当前文件（含未跟踪/忽略文件） |
| Git 历史 | `D:\2025Ai\26-05-23\ai-interview-coach-history-20260830-v2.bundle` | `B111FDEFCEFA9D73009C22E48D14FC95AA9E5B6B0CC8F697BC19EF713857DE90` | `git bundle verify` 报告 complete history |

恢复方式：从 ZIP 解压到仓库根目录可恢复删除前工作树；或从 bundle 恢复 Git 提交历史。恢复前需重新确认目标和影响，不自动覆盖当前 `apps/` 修改。

## 3. 链接与事实影响

历史规划、决策和阶段文档仍保留 `frontend/`、`backend/` 路径作为迁移来源记录，不代表目录仍存在。删除完成时的实现入口为 `apps/platform-backend/ruoyi-admin`、`apps/admin-web` 和 `apps/portal-web`；这些工程随后提升到仓库根目录。

未执行：Git `add/commit/push`、发布、真实 Provider/对象存储联机验证，以及由业务 owner 进行的最终 UAT 结论。这些不因目录删除自动变为通过。

## 4. 删除后检查

- `frontend/`：不存在
- `backend/`：不存在
- `contracts/`、`docs/`：存在；原 `apps/` 工程已提升到根目录后删除容器目录
- 运行服务：RuoYi backend `8081`、Admin `5173`、Portal `5174` 保持运行
