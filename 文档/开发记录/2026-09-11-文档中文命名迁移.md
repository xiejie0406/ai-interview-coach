# 文档中文命名迁移记录

> 任务 ID：DOC-NAME-20260911  
> 文档类型：文档迁移记录  
> 开始日期：2026-09-11；完成日期：2026-09-12  
> owner：Codex 执行；用户批准中文命名目标  
> 风险：L0，不改变业务运行行为；涉及批量文档及目录改名与入链更新，保留独立恢复基线  
> 当前结果：Completed

## 1. 目标与范围

根据用户在 2026-09-11 的明确要求，将文档根目录、全部文档子目录，以及其中项目自有、面向人阅读的文档文件名改为清晰中文，并同步修正项目内所有入链与生成脚本引用。日期、版本、Feature ID、项目编号和必要技术标识可保留，但目录名仍须包含中文语义。

文档根目录从 `docs/` 迁移为 `文档/`。本次不改动其他业务工程目录、源码行为、配置语义、产品状态或验证结论；不执行 Git 暂存、提交或发布。

## 2. 保留名称与映射

下列名称因工具、生态或机器契约保持不变：

- `AGENTS.md`、`README.md`、`LICENSE*`。
- OpenAPI / AsyncAPI / JSON 契约和证据文件。
- 代码、配置、脚本以及文档树以外的工程目录名。

完整逐文件旧路径→新路径见 [`2026-09-11-文档中文命名映射.csv`](2026-09-11-文档中文命名映射.csv)，逐目录旧路径→新路径见 [`2026-09-11-文档目录中文命名映射.csv`](2026-09-11-文档目录中文命名映射.csv)。映射在移动前生成，并校验源存在、目标未被占用、目标路径唯一且适用名称至少含有一个中文字符。

实际执行结果：

- 164 份项目自有人读文档按逐文件映射改为中文文件名；工具约定和机器契约文件保留原名。
- 47 个文档目录全部纳入逐目录映射，其中 46 个目录实际改名；`语音面试页/` 本身已是中文，只随父路径迁移。
- 文档根目录从 `docs/` 改为 `文档/`；旧根目录已不存在。
- 使用 [`../../scripts/migrate-document-directory-links.cjs`](../../scripts/migrate-document-directory-links.cjs) 按明确目录段和解析后目标范围更新 238 条 Markdown / HTML 本地链接；生成脚本和检查脚本中的路径另行定向更新。

## 3. 恢复基线

批量改名前的完整基线保存为：

- 文件：`D:\2025Ai\26-05-23\java-workspace-docs-before-chinese-naming-20260911.zip`
- 大小：41,832,956 bytes
- SHA-256：`6D7C8FBEB297FF0EBEA96232C3A2EC5F49DB4871F931712EE6DBBD2AD5D1B167`

目录迁移前、并行文档任务完成核心正文落盘后的即时基线为：

- 文件：`D:\2025Ai\26-05-23\java-workspace-docs-before-chinese-directories-20260911.zip`
- 大小：42,006,549 bytes
- SHA-256：`2DAF2752C389B93BF66E7A1E873D83692C1DB9F3DF05ACB7A0229DCDF04BF545`

只回退目录时，先执行 `node scripts/migrate-document-directory-links.cjs --reverse --apply`，再按逐目录 CSV 从最深层到根目录反向改名。需要同时回退中文文件名时，再按逐文件 CSV 反向改名。需要恢复正文时，将 ZIP 解压到独立临时目录后逐项比对，不直接覆盖当前工作区。

## 4. 实施保护与异常处置

- 迁移前确认另一个任务正在同一工作区写生产排程和 Python Agent 文档；等待核心正文稳定并取得“不再写旧目录”的交接后再移动，没有覆盖这些新增内容。
- 一次 PowerShell 链接预演暴露出正则转义歧义，因此在目录移动前停止。即时备份比对显示 5 个文件出现机械误改，已用定向补丁恢复；恢复后 SHA-256 与即时备份逐一一致。另 1 个原型 HTML 差异是正确的新目录链接，予以保留。
- 随后改用带目标范围校验、支持 dry-run / reverse 的 Node 脚本；先预览 78 个文件、238 条链接，再执行，并复跑得到 0 条待改。
- 全程未执行 Git 暂存、提交、推送、分支、标签或发布。

## 5. 验证记录

| 检查 | 预期 | 当前结果 |
|---|---|---|
| 映射完整性 | 所有适用文档和目录均有唯一中文目标名 | Pass：文件映射 164 行；目录映射 47/47，目标重名 0、占用 0、遗漏 0 |
| 目录执行 | 最终路径与映射一致，旧根目录消失 | Pass：46 次实际改名；47/47 最终目录存在；`docs/` 不存在 |
| Windows PowerShell 文档检查 | `powershell ... scripts/check-docs.ps1` 退出码为 0 | Pass：203 个 Markdown、5 个 HTML，本地链接 0 断链 |
| PowerShell 7 文档检查 | `pwsh ... scripts/check-docs.ps1` 退出码为 0 | Pass：203 个 Markdown、5 个 HTML，本地链接 0 断链 |
| 中文命名符合性 | 除明确文件例外外，人读文档文件名与全部文档目录名均包含中文 | Pass：人读文件 192、违规 0；目录 47、违规 0 |
| 生产排程检查器路径与设计 | 检查器能从中文目录读取基线且静态规则通过 | Pass：19 Pass / 0 Fail，46 表、780 字段、175 外键 |
| 服装文档派生链 | 中文目录可重新生成并验证阅读版 | Pass：8 个文件、88 条本地链接、0 断链、4 份派生副本 |

保留的旧英文路径只出现在本迁移映射、历史迁移/失败证据、归档原文及明确标注的“迁移前路径”中，不再作为当前可点击入口或运行脚本输入。
