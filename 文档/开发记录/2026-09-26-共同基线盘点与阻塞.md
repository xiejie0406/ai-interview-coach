# 并行开发前共同基线整理记录

> 文档类型：跨项目基线整理记录
> 日期：2026-09-26
> owner：共同基线整理任务
> 范围：共享若依、工程目录迁移、Aden、智能选品、生产排产、面试项目的当前未提交内容
> 授权：用户最初允许核对范围后的本地暂存和提交，随后明确要求“先直接提交吧，然后推到远程”；仍不含分支迁移、停服、数据库操作、清理或强制覆盖
> 风险边界：本记录不修改产品行为；登录安全行为修改不纳入本次基线整理
> 事实结果：提交候选已核对，按最新授权保全当前代码并推送；具体提交以包含本记录的 Git 提交为准，实际推送结果以任务交付回执为准
> 关联：[迁移记录](2026-09-25-工程按技术类型目录迁移.md)、[并行隔离方案](../若依框架/并行开发与验收隔离方案.md)

## 最新决定与交付范围

用户在获知若依内嵌私钥与“记住密码”关联后，明确要求暂不处理、直接提交并推送。依此保留既有登录行为、上游内嵌私钥及上游示例配置，不将其描述为已修复或无敏感材料。以下早期阻塞记录用于追溯，其“停止提交”决定已被本节最新授权替代。

本次采用一个完整提交，覆盖目录迁移、共享若依与统一密钥入口、四业务当前实现、配置模板、脚本、CI、契约和必要证据，避免迁移删除与新路径、共享模块与消费者之间形成不可用的中间版本。实际 `.env`、登录态、运行缓存、备份及 14 个 `.playwright-cli` 页面快照仍不提交。未修改任何业务源码以使检查通过。

10:46 复核时所有相关任务空闲，已取得的停写确认继续有效；远端 `origin/main` 为 `fe35a459ff92d913fe6bde293b2340add392e14f`，与本地原 HEAD 一致。推送目标为 `origin/main`，只允许普通推送，遇到远端前进不强推。

排除 14 个临时快照后的显式候选清单为 73 项修改、3882 项删除、3880 个未跟踪文件（包含本记录）。提交前按路径与 SHA-256 重核，暂存后检查 cached diff；Git 的重命名识别会使最终提交文件统计与此原始状态条目数不同。

## 初始 Git 现场

2026-09-26 10:26 北京时间重新执行 `git status --porcelain=v1 -z -uall`：73 项已跟踪修改、3882 项删除、3893 个未跟踪文件，共 7848 条。该统计早于本记录的创建；不能作为后续暂存时的最新事实。

- 分支：`main`。
- HEAD：`fe35a459ff92d913fe6bde293b2340add392e14f`。
- 暂存区为空，无合并冲突条目。
- 主工作目录：`D:/2025Ai/26-05-23/java-workspace`。
- 新采集任务工作树：`C:/Users/admin/.codex/worktrees/c235/java-workspace`，detached，仍位于上述旧 HEAD，不包含主目录未提交内容。
- 本轮没有执行 Git 暂存、提交、reset、clean、stash、切换分支、推送或数据库操作。

## 停写协调

已收到智能选品、面试、生产排产、Aden、统一密钥管理和新采集任务的主动源码、配置、文档、构建输出停写确认。各任务等待共同基线结果；其确认不代表所有运行进程静止。

Aden 报告人工验收脚本 PID 25056 仍在等待桌面窗口关闭。只读核对命令行与脚本后确认其使用 `InteractiveUat` 和 `InstalledDesktop`：运行数据位于已忽略且未被跟踪的 `frontend/desktop/aden-desktop/test-results/`；关闭窗口后的安装配置恢复目标在用户 LocalAppData 安装目录，位于仓库外。现有 API、Worker、前端、Python 和预览服务保持原状，本任务没有停止或重启它们。日志、缓存、测试库及以上运行目录不属于代码基线快照。

## 提交候选范围

| 分类 | 候选范围 |
|---|---|
| 共享若依与工程迁移 | 根入口、POM、ruoyi-backend 平台模块、上游参考目录、迁移删除与路径消费者 |
| Aden | ruoyi-aden、Electron 工程、两个 Python 工程、契约、必要测试、安装脚本与文档 |
| 智能选品 | ruoyi-fashion、fashion-ai-runtime、管理端页面、契约、必要测试与项目资料 |
| 生产排产 | APS Java 模块、管理端页面、契约、静态检查脚本和项目证据 |
| 面试 | ruoyi-interview、门户、小程序、共享前端契约、题库与语音资料 |
| 支持资料 | 核查后的配置模板、忽略规则、CI、脚本、文档与验收证据 |

不将目录迁移删除与对应新路径拆成明知不能工作的中间提交。共享密钥模块与各业务消费者也须结合依赖核对后确定提交边界。

## 删除核查

按迁移映射比较 HEAD 内容与当前目标文件，并允许 CRLF/LF 归一化：

- 3554 项迁移目标存在且内容一致。
- 209 项目标存在但内容变化，包含业务后续改动，不能仅凭映射判定全部删除正确。
- 1 项旧 `EnvironmentAdenRunnerPepperProvider.java` 映射目标不存在，当前存在 `ManagedAdenRunnerPepperProvider.java`；与统一密钥来源替换有关，须随该切片审查。
- 118 个根目录 Java 文件均在面试模块找到同名实现；115 个仅包名替换，另 3 个存在业务差异：`Question` 增加 Rubric 绑定，`QuestionVersion` 增加 category，`VoiceTurnExecution` 调整协议状态并增加 finishThinking。这些差异已只读比较，未自动恢复旧文件，也未据此批准全部删除。

迁移前备份 `.codex-tmp/legacy-paths-20260925/` 仍存在；本轮未复制、清理或证明该备份完整。新有效内容仍留在原工作区；没有另做包含未审核秘密的全量快照。

## 早期敏感内容阻塞与排除项

发现 4 处嵌入 RSA 私钥，均在对应文件第 8 行开始，文件整体与 HEAD 中旧路径原文件一致（忽略换行差异）：

1. `frontend/admin-web/src/utils/jsencrypt.js`。
2. `frontend/_reference/ruoyi-ui/src/utils/jsencrypt.js`。
3. `frontend/_reference/ruoyi-vue3-frontend/src/utils/jsencrypt.js`。
4. `ruoyi-backend/_reference/ruoyi-original/ruoyi-ui/src/utils/jsencrypt.js`。

活跃管理端 `src/views/login.vue` 第 70 行导入该工具，第 114 行写入加密密码 Cookie，第 159 行解密回填。不能将工具文件简单排除后仍声称基线可用；移除密码持久化或改变密钥机制属于登录行为变化。用户要求本次不提交私钥，也不为通过检查擅自重写业务，因此当前停止在此阻塞，不制作含私钥的复制快照或提交。

另外，上游参考 `ruoyi-admin/src/main/resources/application-druid.yml` 第 11、51 行，以及 `application.yml` 第 100 行存在非空数据库/管理登录密码和 Token secret 字段。文件与旧 HEAD 上游版本一致，但仍需明确模板化处理；本记录不包含其值。OpenAPI identity 的两处扫描命中已确认为 schema 定义，不是秘密。

扩展扫描中的 SQL 字符串、WebSocket 子协议前缀和证据文件哈希命中不是凭据；合成测试中的固定测试值不能作为实际业务凭据使用。文本规则初筛不等于完整安全审计，截图及二进制尚未全部完成敏感内容人工复核。

明确排除且保留本地现场：

- 实际 `.env`、登录态、私钥、实际密码/Token、数据库备份及业务隐私数据。
- `target`、`node_modules`、日志、临时 JAR、缓存、运行目录与历史迁移备份。
- `frontend/admin-web/.playwright-cli/` 下 14 个未跟踪页面快照；当前根级忽略规则没有覆盖此嵌套位置，后续须显式排除，不能直接按整个 frontend 目录暂存。

验收截图、报告保持原位，后续按仓库证据规则审核，不因目录命名检查失败而删除或自动改名。

## 本轮检查结果

工作目录均为主工作区，检查日期 2026-09-26。以下命令只读源码，没有执行 SQL、启动服务或覆盖 JAR。

| 命令/检查 | 实际结果 | 限制 |
|---|---|---|
| `node scripts/aps/check-imp01-structure.cjs` | Pass，退出码 0，32/32 | 静态结构检查，不证明业务运行 |
| `node scripts/aps/verify-migration.cjs` | Fail，退出码 1，9/10 | 权限码匹配出现 65 次、唯一值 29；脚本预期两者均为 29。未据此推断数据库有重复数据，未修改 SQL 或脚本 |
| `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-docs.ps1` | Fail，退出码 1 | APS 的 evidence、Aden 安装版证据的 screenshots 两处目录不符合中文命名规范 |
| `git diff --check` | Fail，退出码 2，3 处 | 选品验收记录第 76 行、APS 验收记录第 103 行、面试商业邀测验证记录第 4 行；未跟踪文件不在该命令覆盖范围 |
| Git 状态、迁移内容、敏感内容初筛、进程边界核查 | 已执行 | 结果为本轮采样，后续提交前必须重核 |
| 当前完整内容的隔离构建与定向测试 | NotRun | 发现私钥阻塞后未复制候选树或写入共享构建输出 |

历史报告不能直接作为本轮通过依据。例如 `ruoyi-system` 现有测试报告时间早于当前 `ManagedSecretService.java` 最近修改；面试现有 surefire 报告为混合历史，仍有一条 error。没有输入内容哈希和本轮隔离复验时，不将这些混合报告汇总为当前通过或当前失败的确定结论。

## 初次停止时的结论（已被最新决定替代）

共同基线尚未提交；没有推荐作为本次共同基线的哈希。旧 HEAD 只可作为历史起点，不能作为现有完整工程已保全的证据。

应先由用户明确登录凭据持久化的处理范围，完成活跃管理端内嵌私钥的移除及参考模板脱敏，保持既有私钥文件不被复制进额外快照。处理属于业务安全改动，应由相应任务按明确范围实施和验证，不能由基线整理任务静默改变。

阻塞解除后重新核对停写、索引、全部候选文件与排除项；冻结候选清单及内容哈希，采用隔离输出运行最小构建/测试，再按逐路径清单暂存并审查 cached diff。已有本地暂存与提交授权继续有效，不需要重复申请；仍不执行 push、数据库操作或服务停启。

## 最新授权后的最小验证

2026-09-26 10:47 起在当前源码上执行；不打包、不写入运行 JAR、不连接业务数据库，不启动或重启服务。TypeScript 明确关闭增量输出；Python 使用内存 AST 解析，不生成 pyc。

| 对象与命令 | 结果 | 证明范围 |
|---|---|---|
| `mvn -o -f ruoyi-backend/pom.xml -DskipTests validate --no-transfer-progress` | Pass，退出码 0，19 模块 SUCCESS | reactor、Maven/Java 基线与配置的依赖边界；不是 Java 编译或单元测试 |
| 管理端 `vue-tsc --noEmit --incremental false --project tsconfig.json` 与 `tsconfig.aps.json` | Pass，两项退出码 0 | 配置覆盖的选品与 APS TypeScript/Vue；不自动覆盖未纳入配置的 JS 密钥页面 |
| 门户 `vue-tsc --noEmit --incremental false` | Pass，退出码 0 | 门户类型检查 |
| 桌面 `vue-tsc` / `tsc --noEmit --composite false --incremental false`（web、node），以及 test 配置 noEmit 检查 | Pass，三项退出码 0 | 桌面 renderer、主进程、预加载和测试类型检查 |
| 候选 Python 文件 `ast.parse` | Pass，99 个文件，无语法错误 | 语法，不证明依赖导入或运行行为 |
| 候选 JSON 递归检查敏感键 | 未发现需新增排除的实际凭据字符串 | 规则检查不能代替完整安全审计 |

APS 静态检查、文档命名、空白差异的原始失败仍按上表保留，不改写为通过。截图抽查确认 APS 页面使用 UAT 样本，选品登录截图的密码和验证码已遮盖；未宣称完整截图安全审计。没有重跑完整业务验收、Java 编译、真实 Provider 或数据库迁移。

该提交用于保存当前工程并作为后续独立 worktree 的共同代码起点；不代表全部业务可运行、上线就绪或安全问题已经关闭。独立环境仍须配置隔离数据库、密钥自举、端口和运行目录，并执行所属任务的构建与验收；当前服务版本不能直接作为新基线验证证据。

首次按显式路径清单暂存时遇到 `.git/index.lock`，操作退出码 128，索引未改变。该锁为空、最后写入时间为 2026-09-25 11:44；核查没有 Git 进程，并成功独占打开锁文件。10:50 将其改名保留为 `.git/index.lock.stale-20260926-105035` 后重试，不删除锁、不清理源码、不终止进程。

重试暂存成功。`git diff --cached` 的 7835 个原始路径全部属于核对清单，没有清单外路径、实际 `.env`、运行产物或 `.playwright-cli` 快照；已跟踪工作区与索引一致。重命名识别结果为 3758 项迁移、122 项新增、124 项删除及 73 项修改。完整暂存差异的 `--check` 返回退出码 2、89 条空白诊断，范围包含此前未跟踪的新文档，故多于初始只检查已跟踪差异时的 3 条；保留原始结果，不为消除诊断批量改写他人文档。
