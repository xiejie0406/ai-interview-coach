# 文档结构收敛记录

> 文档类型：文档迁移记录  
> 开始：2026-09-10；完成：2026-09-11  
> owner：Codex 执行；用户批准目录整理目标  
> 风险：L0 运行行为不变；涉及批量文档移动，保留独立恢复基线  
> 范围：项目规范、文档目录、历史归档和 Markdown 入链  
> 证据结果：Pass

## 1. 目标与结果

按用户级文档治理规范建立项目级 `AGENTS.md` 和文档组织规范；当前事实按 product、features、architecture、research、reference、development-records、specs 分区，失效资料集中进入 `docs/archive/`。本次不删除文档正文、不修改业务代码、不变更产品批准或验证结论。

## 2. 迁移映射

| 原位置 | 新位置 | 语义 |
|---|---|---|
| `docs/phases/` | `docs/archive/phases/` | 旧阶段路线与窗口提示词 |
| `docs/prototypes/` | `docs/archive/prototypes/` | 旧全量产品原型审计 |
| `docs/review/` | `docs/archive/review/` | 旧 Canonical 恢复候选 |
| `docs/features/FEAT-RUOYI-LOGIN-001-homepage-login/` | `docs/archive/features/.../` | 面向已删除旧前端的 Feature |
| `docs/reference/{authentication,configuration,module-boundaries,migration-status-and-todo}.md` | `docs/archive/reference/` | 旧实现与迁移快照 |
| `docs/product/prd-v0.2-archive.md` | `docs/archive/product/产品需求文档-v0.2.md` | 被 v0.3 替代的产品稿 |
| 五份旧架构/契约/迁移候选 | `docs/archive/architecture/` | 不再作为当前架构入口 |
| `docs/setup/开发环境准备.md` | `docs/archive/setup/` | 旧环境规划 |
| `docs/specs/SPEC-*.md` | `docs/archive/specs/` | 迁移期四端 SPEC |
| `docs/project-research/` | `docs/research/projects/` | 当前独立项目调研归位，不归档 |

实际共移动 104 个文件。受移动影响的 Markdown 相对链接按新源位置和新目标位置机械重算，正文语义不参与批量改写。

## 3. 恢复

迁移前完整备份：`D:\2025Ai\26-05-23\java-workspace-docs-before-convergence-20260910.zip`（793,999 bytes；SHA-256 `92102F0B414038C982A53DB5E949F05CD582A7A3CA03545DA3015F3C3B071042`）。恢复时优先按上表反向移动；需要恢复未提交原文时，从 ZIP 提取到临时目录后逐项比对，不能直接覆盖当前工作区。

## 4. 验证

- 目录与文件数量：Pass。100 个原文文件进入 `docs/archive/`，另新增 1 个归档索引；4 个独立项目调研文件迁入 `docs/research/projects/`；迁移产生的空 `docs/setup/` 已在确认无内容后移除。
- 链接与目录规则：Pass。`scripts/check-docs.ps1` 检查根入口及 `docs/` 下 162 个 Markdown 文件，本地相对链接全部可解析，旧 `frontend/`、`backend/`、`apps/` 不再作为当前文档的可点击源码路径。
- 脚本兼容性：Pass。Windows PowerShell 5.1 与 PowerShell 7 均以退出码 0 完成检查。
- 第一次批量移动命令因同时包含计算路径与目录删除而被本机安全策略在进程启动前拒绝，没有发生部分移动；随后使用已核验的显式绝对路径完成迁移。
- Git / 构建 / 运行：未执行；本次不涉及业务运行行为，也未获得 Git 写操作授权。
