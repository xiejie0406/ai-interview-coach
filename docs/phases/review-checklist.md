# 十八期路线审查清单

> 文档类型：审查清单  
> 文档状态：Draft  
> owner / 责任边界：质量/架构复核者维护结果；用户形成批准结论  
> 创建时间：2026-08-02  
> 更新时间：2026-08-02  
> Roadmap ID：ROADMAP-INTERVIEW-001  
> 风险等级：L3  
> 产出/适用阶段：3–6  
> 阶段状态：WaitingForApproval  
> 证据结果：NotRun（以下为文档静态检查项，不是运行验证）

## 1. 当前静态审查结果

| 检查 | 结果 | 说明 |
|---|---|---|
| 推荐期数为 18，`phase-01` 至 `phase-18` 不重不漏 | 符合（只读） | 23 个文件中恰有 18 个 Phase 文件 |
| Phase ID 唯一、只依赖更早期次 | 符合（只读） | ID 无重复；第 7 节直接依赖未发现倒序 |
| 所有当前 P0 REQ/AC 有 owner/Phase | 符合（只读） | `REQ-01–12`、`AC-01–12` 在依赖矩阵逐项可检索；截断内容不猜测 |
| 每期具备用户/技术目标、范围、依赖、契约、文件树、验证、DoD | 符合（只读） | 18 期均有连续 1–26 节 |
| 11 个逻辑域、前端页面、schema、外部系统有 owner | 符合（只读） | 见架构审查与依赖矩阵；另含 `platform` 横切 owner |
| T0/T1/T2 顺序满足约束 | 符合（只读） | T0=01–04，T1=05–13，T2=14–18 |
| Realtime/B2B/K8s/微服务/SSO/数字人延后 | 符合（只读） | 各期非目标与 README 明确延后 |
| 没有 Draft→Approved、实现/验证/发布误报 | 符合（只读） | 18 期均为 Draft/NotRun；第 18 期 NotAssessed/NotReleased |
| 路线只有一个权威入口 | 符合（只读） | 唯一入口为 `docs/phases/`；重复审查路线已删除且全文无旧路径引用 |
| 没有源码、依赖、构建、测试、服务、部署或 Git 动作 | 符合（动作范围） | 仅执行文档读取、目标目录写入和只读静态检查 |

## 2. 用户批准前必须回答

- [ ] 是否接受 18 期而不是 19–22 期；若单人节奏需要更小切片，优先在 Feature `tasks.md` 拆 TASK，不增加 Phase 事实层。
- [ ] 是否接受 T0=01–04、T1=05–13、T2=14–18。
- [ ] 是否接受五个物理 Maven module 与 11 个逻辑域的组合。
- [ ] 是否接受 PostgreSQL 单库多 schema、personal tenant、Flyway 首期、Redis 第 14 期。
- [ ] 是否接受对象存储只在真实音频前进入，具体供应商/版本保持决策任务。
- [ ] 是否接受 REST/SSE/WebSocket/Webhook 的职责划分。
- [ ] 是否接受第 08 期前置 Outbox/Job/幂等/恢复，第 14 期只做多实例深化。
- [ ] 是否接受第 07 期测试权益/预留端口，第 16 期才接真实支付。
- [ ] 是否接受第 03/12/15 期分层完成同意、音频生命周期与完整删除/审计。

## 3. 每一期评审门

- [ ] “为什么存在”和用户可见成果能独立陈述，不只是技术组件名。
- [ ] 技术学习目标对 Java→AI Agent 转型有递进：领域建模→端口→结构化 Agent→评测→语音→可靠性→商业治理。
- [ ] 范围/非目标不会复制相邻期次；纯底层能力有明确必要性和可观察证据。
- [ ] 前置决策、直接依赖和停止条件足以阻止猜版本/供应商/隐私规则。
- [ ] `REQ/BR/AC/DES` 只做映射，不复制或改变上游正文。
- [ ] 正常、空、错误、拒绝、取消和恢复均有适用行为；不适用有理由。
- [ ] 后端 module/package/class、前端 route/component/hook/store、migration、契约和测试文件足够具体。
- [ ] “计划新增/计划修改”没有被写成实际创建/修改。
- [ ] 单元、集成、契约、UI、Golden Set、UAT 的适用性与不能证明什么清楚。
- [ ] DoD 是可观察标准；不能证明项、风险、下一期门和学习复盘完整。

## 4. 进入阶段 6 前门禁

- [ ] PRD `FUNC-VOICE-002` 后截断已由产品 owner 修复并复核。
- [ ] 技术架构数据模型截断已由架构 owner 修复并复核。
- [ ] Gate A 已逐项 Accepted/Rejected/Deferred 并回写事实源。
- [ ] 阶段 4 原型覆盖空、加载、错误、拒绝、取消、断线和恢复。
- [ ] Canonical Feature Spec 和技术设计为 Approved。
- [ ] 每个实施 Feature 建立完整 L3 资料包；唯一 `tasks.md` 有双向追溯。
- [ ] 执行包列明源码/测试/命令/外部调用/费用/停止条件及独立授权。

## 5. 上线前门禁

- [ ] P0 AC 有实际 `Pass/Fail/Blocked/NotApplicable`，未执行项不计通过。
- [ ] Golden Set、浏览器 UAT、tenant 越权、音频删除、支付幂等、故障恢复和成本上限有实际证据。
- [ ] 用户而非 Agent 形成 UAT 结论。
- [ ] 迁移、备份恢复、监控、停止与回滚准备完成。
- [ ] 若形成 `ReleaseReady`，发布事实仍明确为 `NotReleased`；发布、回滚和 Git 分别授权。
