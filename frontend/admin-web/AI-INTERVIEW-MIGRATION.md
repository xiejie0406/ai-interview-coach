# AI Interview Coach 后台迁移说明

本目录基于完整的 `RuoYi-Vue3` 上游创建，是可改造的后台应用；`../_reference/ruoyi-vue3-frontend/` 继续作为只读上游基线。

当前已增加 AI 面试后台总览、题库入口和独立 `/api/v1/*` 请求客户端。若依原生系统接口继续使用原请求客户端，两套鉴权与响应结构在后端统一方案确定前保持隔离。

当前未安装依赖、未构建、未启动、未联调。
